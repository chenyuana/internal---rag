# Internal RAG 改动记录（2026-08-24）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-21.md` 之后）的工作。
> 项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。
> 本文档按日期逐节追加改动/验证记录，沿用既有格式：问题 / 根因 / 修复 / 验证 / 改动文件 / 注意。
> 本会话为**新需求**（独立评测系统），记录结构改为：需求与评审 / 实施 / 验证 / 改动文件 / 注意。

---

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-21.md`（已覆盖：查询分解/转换架构完整实施、六格证据收紧、完整表格引用与 C 编号、成果验收语义、主体文档解析检索分数通道、跨文档句式覆盖、multi_hop 确定性矩阵优先、证据行质量修复；末次完整 pytest **484 passed, 3 failed**，3 项均为既有解析基线）。
- **本会话范围**：用户提出新需求——为内网知识库开发一个**独立的 RAG 自动化测评系统 `rag-evaluation`**（HTTP 调用 RAGFlow/Gateway，不修改 RAGFlow 核心源码，全部离线可用）。先对需求规格做**评审补充**，再按规格第十七条实施**第一阶段**（项目结构 + 数据模型 + JSONL 校验 + 测试样例与单元测试），完成后等待用户确认再进入第二阶段。
- **重要更正**：本会话实际发生于 **2026-08-24**；上一轮曾把"新建 rag-evaluation"误登记进 8.21 文档，本次已回滚（见 §2.2），正确记录在本文档。

---

## 1. 当前状态快照（2026-08-24）

以下为本次会话实际核对的快照（非引用旧文档）。

### 1.1 环境与代码

- 代码目录：`D:\internal-rag\source\internal-rag`（既有 Gateway 工程，本会话未改动其代码）
- 新项目目录：`D:\internal-rag\source\rag-evaluation`（独立评测系统，与 internal-rag 平级）
- 服务探测（2026-08-24 实测，HTTP 探活）：

| 服务 | 端口 | 状态 |
|---|---|---|
| Gateway | `127.0.0.1:8080` | ✅ `/health` 200 `{"status":"ok","service":"internal-rag","version":"0.1.0","environment":"development"}`，监听 PID **12640** |
| RAGFlow | `127.0.0.1:9380` | ✅ `/api/v1/system/healthz` 200 `{"db":"ok","doc_engine":"ok","redis":"ok","status":"ok","storage":"ok"}` |
| Ollama | `127.0.0.1:11434` | ✅ `/api/tags` 200：`qwen3:4b-instruct-2507-q4_K_M`、`qwen3.5:9b-q4_K_M`、`qwen3-embedding:0.6b` |
| Reranker | `127.0.0.1:8003` | ✅ `/health` 200 `{"status":"ok"}`（**8.21 快照中为关闭，当前已启动**） |
| MinerU | `127.0.0.1:8886` | 未探测（本会话无解析作业） |

- 知识库（`/api/v1/admin/datasets`）：`b5ecab6a8b2811f1b7c4d904b160df50`=法规11、`8837f5068b2a11f1b7c4d904b160df50`=知识库问答、`06b75da6899e11f1982a9328c14e1277`=法规、`b0681b64897c11f1bb84c3efa39c720c`=test。

### 1.2 新项目状态（rag-evaluation）

- 独立 venv：`D:\internal-rag\source\rag-evaluation\.venv`（Python 3.12.4，`D:\anaconda3\python.exe` 创建）。
- 已装依赖：httpx / pydantic 2.13 / PyYAML / typer / pandas / jinja2 + dev（pytest 9.1 / ruff 0.16 / mypy 1.10）；embedding extra（sentence-transformers）未装，按需安装。
- 单元测试 **46 passed**；ruff **All checks passed**；mypy **Success（7 source files）**。
- 本会话未初始化 Git 仓库（`rag-evaluation/` 尚无 `.git`；internal-rag 仓库仍为 3 个初始提交、工作区 99 项未提交/未跟踪）。

---

## 2. 改动记录（按日期逐节追加）

### 2.1 需求规格评审：20+ 条补充建议（2026-08-24）

#### 内容

用户给出完整需求规格（rag-evaluation：测试集管理、RAGFlow API 适配、检索/答案/Judge/忠实度/拒答/性能评测、版本回归、JSON/CSV/HTML 报告、7 个实施阶段）。评审发现并补充以下缺口（全部已落地或列入后续阶段设计）：

1. **与现有系统的衔接（最高优先级）**：
   - 真实答案链路应评测现有 Gateway（`/api/v1/chat/completions`，含证据判断、受约束生成、引用映射、确定性矩阵、diagnostics），直连 RAGFlow 只测检索层 → 适配层分 `RagflowClient` + `GatewayClient`；
   - 评测集模型需兼容现有 `tests/evaluation/*.jsonl`（8+8+26 道真实/合成用例），第二阶段提供导入器复用；
   - Gateway 答案四态（ANSWERABLE/PARTIALLY_ANSWERABLE/UNANSWERABLE/CONFLICTED）→ 测试集增加 `answerability` 枚举，避免"部分回答"误判。
2. **测试集**：`ExpectedSource.optional`/`min_required_sources`（知识库可能确实没有某文档，语料缺失不算检索失败）；`category` 受控枚举+合法扩展；`priority`（P0 回归门禁）；`expected_refusal_terms`（拒答规则匹配）。
3. **指标**：Hit@K 上限=min(K,候选数)、候选不足标记 `insufficient_candidates` 不虚构指标；nDCG 二元相关约定；文本规范化补充数字/单位/PDF 断行规则；检索噪音控制（contamination_rate）。
4. **Judge/忠实度/拒答**：Judge 提示词版本化 + temperature=0 + seed；可选校准集与一致性采样；原子事实先确定性句法拆分、LLM 抽取可选；拒答规则先行、LLM 复核。
5. **性能/回归/工程**：并发测试 warmup + 默认低压力 + 生产库高并发警告；元数据自动采集+配置覆盖+`metadata_unavailable`；回归阈值可配置（绝对值+百分比）；sentence-transformers 支持本地模型路径、无 Embedding 降级 `embedding_unavailable=true`；SQLite WAL+单写者；重试区分可重试/不可重试错误；配置指纹（config+dataset sha256）；日志不记录 API Key。
6. **规格本身写得好、保留**：模块化+类型标注+异常处理+结构化日志、响应适配层与评测逻辑解耦、保留全部原始子指标、严重失败一票否决 6 类、单题失败不影响其他题、原始请求响应不丢失。

#### 注意

- 评审建议按"是否影响第一阶段"即时落地：影响数据模型/加载器的（answerability/priority/optional 来源/拒答词/收集全部错误）已在第一阶段实现；涉及服务调用的（双客户端/适配层/导入器）列入第二、三阶段设计。

### 2.2 rag-evaluation 项目创建与第一阶段实施（2026-08-24）

> **阶段结束时快照**：本节为第一阶段（数据模型 + JSONL 校验）完成时的记录，
> "pytest 46 passed、客户端尚未实现"均为当时状态；第二阶段（客户端/适配/性能）
> 见 §2.3、GPT 评审处理见 §2.4。

#### 内容

按规格第十七条"先检查现有目录、不覆盖用户代码、先输出目录/数据模型/验收方式再实现"执行：

- **项目位置**：`D:\internal-rag\source\rag-evaluation`（与 internal-rag 平级；检查确认该目录不存在，未覆盖任何现有文件；internal-rag 代码零改动）。
- **目录结构**（25 个文件）：`pyproject.toml` / `README.md` / `.gitignore` / `.env.example` / `config/default.yaml` / `rag_eval/{models,datasets,cli.py}` / `datasets/{benchmark.jsonl, invalid_samples/}` / `tests/`。
- **数据模型**（`rag_eval/models/`）：
  - `enums.py`：`QuestionCategory`（11 个推荐题型 + 合法自定义扩展）、`Difficulty`、`Answerability`（对齐 Gateway 四态）、`Priority`（P0/P1/P2）；
  - `test_case.py`：`ExpectedSource`（document_name 必填 + page/section/clause_id 至少一个锚点 + `optional`）、`TestCase`（规格 14 字段 + 评审补充 answerability/priority/min_required_sources/expected_refusal_terms，`extra="forbid"`）、`Dataset`、`DatasetIssue`（line/case_id/field/message/severity）；
  - 一致性校验：must_refuse↔answerable 互斥、可答题必须有参考材料、拒答题必须有拒答词、must_include 与 must_not_include 禁重叠、min_required_sources 边界、answerability 推断与显式覆盖。
- **加载器**（`rag_eval/datasets/loader.py`）：逐行 JSONL 严格校验；坏 JSON / 字段错误 / 重复 id（含首次出现行号）/ 空行 warning / 非对象行 / BOM 兼容（`utf-8-sig`）；**收集全部问题而非首错即停**；`parsed_cases` 保留成功解析的题（单题失败不影响其他题）；sha256 指纹（复现用）；`DatasetLoadResult` 带 ok/error_count/warning_count/case_count。
- **CLI**（`rag_eval/cli.py`）：typer `rag-eval validate [path] [--strict]`，退出码 0/1/2。
- **数据**：`datasets/benchmark.jsonl` 演示集 12 题（覆盖 single_clause/comparison/cross_document/table_lookup/multi_hop/numeric/procedure/fact/unanswerable/prompt_injection/summary + 边界字段：P0、partially_answerable、min_required_sources、拒答词）；`datasets/invalid_samples/` 8 个坏样例（坏 JSON/缺字段/枚举非法/拒答冲突/重复 id/未知字段/无参考/无锚点）。
- **测试**：`tests/test_models.py`（16 项）+ `tests/test_dataset_loader.py`（30 项）。

#### 验证

- 独立 venv：pytest **46 passed**；ruff **All checks passed**；mypy **Success（7 files）**。
- CLI 实测：`rag-eval validate datasets/benchmark.jsonl` → `Valid: 12 cases (sha256=b90e78bdd2a5..., warnings=0)`，退出码 0；坏样例精确报错（如 `L2 id=dup-case-a field=id: duplicate id 'dup-case-a'; first seen at line 1`），退出码 1；`rag-eval --version` → `rag-eval 0.1.0`。
- 复现命令：`Set-Location D:\internal-rag\source\rag-evaluation; .\.venv\Scripts\python.exe -m pytest; .\.venv\Scripts\python.exe -m ruff check rag_eval tests; .\.venv\Scripts\python.exe -m mypy rag_eval`。

#### 改动文件

- **新增**整个项目 `D:\internal-rag\source\rag-evaluation\`：`pyproject.toml`、`README.md`、`.gitignore`、`.env.example`、`config/default.yaml`、`rag_eval/__init__.py`、`rag_eval/cli.py`、`rag_eval/models/{__init__,enums,test_case}.py`、`rag_eval/datasets/{__init__,loader}.py`、`datasets/benchmark.jsonl`、`datasets/invalid_samples/*.jsonl`（8 个）、`tests/{__init__,conftest,test_models,test_dataset_loader}.py`。
- **回滚**：`HANDOFF-2026-08-21.md`（删除误加的 §2.12，恢复至 8.21 定稿状态）。

#### 注意

- **尚未实现（等待确认后进入第二阶段）**：`RagflowClient` + `GatewayClient` + `response_adapter` + mock client；之后按规格顺序：检索/答案规则指标 → 本地 LLM Judge + 忠实度/拒答 → SQLite 存储 + JSON/CSV/HTML 报告 → 版本对比回归 → 独立并发性能测试。
- 第二阶段设计要点已定：双客户端分层（检索层走 RAGFlow 原生 API、答案链路走现有 8080 Gateway）；`response_adapter` 统一不同版本响应、未知字段保留原始数据、证据缺失时 `evidence_unavailable=true` 不伪造；mock client 保证无服务时单测可跑；评测集导入器复用现有 8+8+26 道用例。
- 遗留挂账（来自 8.18~8.21，未处理）：早熟中稻航高侧证据、沙地播种公式 OCR、生成端证据利用、RAGFlow 偶发僵死处置、检索对题面标点/空格敏感、`models.answer.timeout_seconds` 60→300、对比题确定性矩阵 Stage 2/3 后续项等，仍以 8.21 文档 §2 相应小节为准。

---

### 2.3 rag-evaluation 第二阶段：RAGFlow 客户端 + 响应适配 + Mock（2026-08-24 下午）

#### 内容

按用户确认的设计（双客户端 + 真实冒烟 + 本阶段实现配置加载）完成第二阶段：

- **配置与日志**：`rag_eval/config.py`（YAML + `${VAR:-default}` 环境变量占位符、Pydantic 类型化 AppSettings/RagflowSettings/GatewaySettings/RetrySettings、config_fingerprint 供复现）；`rag_eval/logging.py`（单行 JSON 结构化日志，不记录密钥）。
- **统一数据结构**：`rag_eval/schemas/evidence.py`（EvidenceItem：chunk_id/document_id/document_name/page/section/clause_id/chunk_text/similarity/vector_similarity/term_similarity + raw 保留未知字段，解析不到=None 不伪造）；`rag_eval/schemas/answer.py`（AnswerRecord：answer/status/citations/evidence_unavailable/http_status/延迟/retry_count/error_type/raw_request/raw_response/diagnostics）。
- **response_adapter**（核心，评测代码不直接依赖 RAGFlow JSON）：证据块候选路径多路尝试（data.reference/citations/chunks/records、顶层 reference/records、response.* 旧版包装等）；字段候选名映射（chunk_id↔id、docname↔document_name、score↔similarity、quote↔chunk_text 等）；数字安全转换（"0.75"→0.75）；无证据块 → `evidence_unavailable=true`；未知字段整体保留。
- **BaseClient**（httpx 封装）：`trust_env=False`（防企业代理把 127.0.0.1 送到外网）；重试仅限配置状态码（默认 429/5xx）/超时/网络错误，**4xx 不重试直接 ApiError**；指数退避+抖动；普通+流式（SSE）两种请求，流式记录 `time_to_first_token_ms`；原始请求/响应随 RequestOutcome 保存。
- **双客户端**：`RagflowClient`（原生 API：chat 普通/流式、retrieval 检索层、list_datasets 元数据；chat_id 或 assistant_id 至少其一）；`GatewayClient`（8080 网关 `/api/v1/chat/completions` + X-User-ID，额外解析 diagnostics）。
- **Mock**：测试用 httpx.MockTransport + `tests/fixtures/` 五份响应样本（典型 v0.26 / 旧版字段 / SSE 流 / 无证据 / Gateway），无真实服务时全量单测可跑。
- **CLI smoke**：`rag-eval smoke --config ... --knowledge-base 法规11 --max-cases N [--ragflow-chat-id <id>]`，真实调用并保存 raw 到 `<results_dir>/raw/<run_id>/`。

#### 验证

- 单元测试：pytest **76 passed**（新增 adapter 15 项 + clients 15 项：请求构造/解析/流式/重试/超时/SSE），ruff **All checks passed**，mypy **Success（16 files）**。
- **沙箱限制排查（重要经验）**：workspace-write 模式下 `tempfile.mkdtemp` 创建的目录不可枚举/不可删（写文件也 PermissionError），而 `os.mkdir` 创建的目录完全正常；pytest 内置 tmpdir 插件因此不可用，已改自定义 `tmp_path` fixture（os.mkdir + 工作区 tmp/ 下）+ 禁用 `tmpdir`/`cacheprovider` 插件 + `pytest_ignore_collect` 忽略探测残留目录。pip 在系统 Temp 解压也被拒（types-PyYAML 装不上，mypy 用 overrides 忽略 yaml 缺失标注）。
- **真实冒烟（2026-08-24，Gateway 在线）**：`smoke --knowledge-base 法规11 --max-cases 3`：
  - `demo-bridge-disaster-equipment`：`PARTIALLY_ANSWERABLE`，**9 citations**，`evidence_unavailable=False`，46.5s，model_calls=0 —— adapter 正确解析真实矩阵答案与引用；
  - `demo-sand-seeding-dual-requirement`：`ANSWERABLE`，**8 citations**，42.4s，model_calls=0；
  - `demo-ccar-25-981-fuel-tanks`：网关返回 **503**（重试 2 次后 ApiError http_5xx）——单题失败不影响其他题（验收要求 3 得到真实验证）；此题的 503 原因待查（可能是 CCAR 文档过滤或 RAGFlow 偶发僵死，见注意）。
  - 原始报文完整保存：`D:\internal-rag\temp\rag-eval-reports\raw\20260824T011712Z\`（每用例含统一结果 + 完整 raw_request/raw_response，raw_response 含 claims/citations/diagnostics 全部字段）。

#### 改动文件

- 新增：`rag_eval/config.py`、`rag_eval/logging.py`、`rag_eval/schemas/{__init__,evidence,answer}.py`、`rag_eval/adapters/{__init__,response_adapter}.py`、`rag_eval/clients/{__init__,base}.py`、`tests/fixtures/*.json/.txt`（5 个）、`tests/test_response_adapter.py`、`tests/test_clients.py`。
- 修改：`rag_eval/cli.py`（新增 smoke）、`pyproject.toml`（dev 依赖、pytest/ruff/mypy 配置调整）、`tests/conftest.py`（自定义 tmp_path + 收集忽略）、`README.md`（第二阶段说明）。

#### 注意

- 未进入第三阶段（检索/答案规则指标），等待用户确认。
- CCAR-25-R4 题 503 为真实环境问题（非评测系统缺陷）：后续可用 `GatewayClient` 复测，或先确认该题应过滤的文档/知识库；评测集该类用例可在第三阶段标记 `priority` 与允许文档后纳入回归。
- 冒烟/评测会真实消耗 API 额度与算力（单题 40s+），批量运行前注意限流（第三阶段并发评测会单独控制）。
- 沙箱残留：`tmp/` 与 `tests/probeA-*` 下有 mkdtemp 残留目录（沙箱内无法删除，已通过 .gitignore / pytest_ignore_collect / ruff exclude 规避，不影响功能）。

---

### 2.4 GPT 评审处理：契约修复 + 性能采集基础（2026-08-24 下午，第三阶段前置）

#### 背景

用户转来 GPT 对第二阶段的评审：代码运行开销优秀（数据集加载 1.24ms、适配 0.063ms/次），但**尚不能称为完整性能评测系统**；指出两个正确性风险、两个代码问题与性能采集缺口。本小节按 GPT 建议的优先顺序处理，属第三阶段的前置工作。

#### 修复 1：RAGFlow 适配器未读取 docnm_kwd / document_metadata（GPT P1 风险 1）

- `response_adapter._FIELD_CANDIDATES`：`document_name` 候选新增 `docnm_kwd`（RAGFlow retrieval chunk 标准字段）。
- 新增 `_NESTED_METADATA_CANDIDATES`：从嵌套 `document_metadata` 提取 page/section/clause_id（直接字段优先）。
- `document_keyword` 保留在 `EvidenceItem.raw`，供第三阶段检索指标使用。
- 新增单测：`test_ragflow_retrieval_block_docnm_kwd_and_nested_metadata`、`test_document_metadata_does_not_override_direct_field`。

#### 修复 2：HTTP 200 内 code != 0 被当作成功（GPT P1 风险 2）

- `RagflowClient._check_business_code()`：顶层 `code` 非 0 时抛 `ApiError(error_type="ragflow_business_error", status_code=200)`；chat 与 retrieval 均检查；原始响应由调用方保存。
- 新增单测：`test_ragflow_business_error_code_detected`。

#### 修复 3：每次检索调用独立耗时（GPT 建议 2）

- `schemas/evidence.py` 新增 `RetrievalResult`（items/raw/latency_ms/retry_count）；`RagflowClient.retrieval()` 改为返回 `RetrievalResult`，latency_ms 为独立检索耗时，用于慢查询定位。
- `ApiError` 增加 `retry_count` 字段（含流式/非流式抛错路径）；`perf._execute_one` 失败样本记录真实重试次数（此前硬编码 0）。

#### 新增：性能评测模块 `rag_eval/perf.py`（GPT 建议 3/4）

- `run_serial` / `run_concurrent`（ThreadPoolExecutor，默认 workers=2）；`summarize` 聚合；`save_report` 存 JSON。
- 指标：P50/P90/P95/P99（线性插值）、成功率、超时率、重试率、吞吐量（req/min）、模型/检索调用数聚合、分阶段耗时（从 Gateway diagnostics 尽力提取）、错误类型分布；**保留每题原始样本**。
- CLI `rag-eval perf --concurrency 1|N [--warmup] [--max-cases]`；报告存 `results_dir/perf/<run_id>-<mode>.json`。
- 修复 smoke 重复建连（GPT P2）：list_datasets 与逐题执行共用同一 GatewayClient。
- 单测：`tests/test_perf.py`（MockTransport + 可控延迟：串行样本/并发排序/分位数/超时分类/串行基线 vs 并发对比）。

#### 验证与真实性能数据

- 三件套：pytest **85 passed**（+9：adapter 2、client 业务 code 1、perf 6）、ruff All checks passed、mypy Success（17 files）。
- **真实 perf 冒烟（2026-08-24，法规11，4 题，报告在 `D:\internal-rag\temp\rag-eval-reports\perf\`）**：

| 模式 | 成功率 | 总耗时 | p50 | p95 | max | 重试 | 结论 |
|---|---|---|---|---|---|---|---|
| 串行 | 3/4 (75%) | 224.1s | 49.5s | 85.3s | 91.5s | 2 | **默认基线** |
| 并发 2 | 2/4 (50%) | 351.5s | 87.1s | 232.2s | 253.8s | 0* | **触发 503，反被拖慢** |

- 关键结论：**并发 2 即触发网关 503**（bridge 109.8s vs 串行 50.5s；table3 重试 2 次 253.8s），Gateway/RAGFlow 单 worker 对并发极敏感 → `perf` 默认并发改为 **1（串行）**，`--concurrency 2` 仅用于对比验证（GPT 建议的"并发 2 验证"实测结果即如此，比预估更保守）。
- CCAR-25-R4 题在 smoke/并发/串行**三次评测持续 503**（33~34s，重试 2 次后失败）——不是偶发，确定为稳定的网关侧失败（建议排查该题所需文档过滤/知识库归属，或先从基准集暂时移除并在第三阶段以 `allowed_documents` 门禁处理）。
- 分阶段耗时：本次 diagnostics 未暴露逐检索耗时（`retrieval_calls` 有计数、无耗时数组），慢查询定位依赖第三阶段接入 RAGFlow 原生 `retrieval()` 的 `RetrievalResult.latency_ms`。

#### 改动文件

- 修改：`rag_eval/adapters/response_adapter.py`、`rag_eval/clients/{__init__,base}.py`、`rag_eval/schemas/evidence.py`、`rag_eval/cli.py`（perf 子命令 + smoke 复用客户端）、`README.md`（perf 说明 + 实测数据）、`HANDOFF-2026-08-24.md` §2.2 标注阶段快照。
- 新增：`rag_eval/perf.py`、`tests/test_perf.py`。

#### 注意

- 性能评测仍待第三阶段完善：检索指标（Hit@K/MRR/nDCG 等）与答案规则指标、单题内多次检索的独立耗时链、版本回归比较（第六阶段）。
- 高并发（>2）在修复 Gateway/RAGFlow 链路前**禁止**；评测默认串行。
- 上一轮"CCAR 503 为真实环境问题、非评测系统缺陷"的表述过急：现确认它是**稳定复现的网关侧失败**，但根因（文档过滤/知识库归属/网关内部）尚未定位，保持"未解决的外部调用失败"表述。

---

### 2.5 第三阶段：检索指标 + 答案规则指标 + run 命令（2026-08-24 下午）

#### 内容

按 GPT 评审建议完成第三阶段核心（检索/答案规则指标与每指标单元测试），并用真实服务验证：

- **`rag_eval/metrics/normalization.py`**：评测用文本规范化（全角半角、中文标点、连续点线、空白折叠）+ `NumericSpec`/`extract_numeric_specs`（整数小数、百分数、数值范围 10m~50m/3m/s至8m/s、大于/小于/不低于/不超过、常用单位含中文别名"米/秒"→m/s、速率/时延/高度/距离/时间字段上下文识别）。
- **`rag_eval/metrics/retrieval_metrics.py`**：`EvidenceMatcher`（5 种匹配：文档名精确/归一化、页码、条款号、文本 bigram Jaccard 模糊、本地 Embedding 可替换接口，不可用时降级）+ 指标：Document/Page/Clause/Evidence Hit@K、Recall@K、Precision@K、MRR、nDCG@K（二元相关）；每题输出 `match_details`（期望来源命中排名与方式，不只总分）；K 超候选数时为 None（不虚构指标）。
- **`rag_eval/metrics/answer_rules.py`**：`evaluate_answer_rules`（key_points 覆盖率、must_include 通过率、must_not_include 违规、数值/单位一致性 ±epsilon、文档/页码/条款引用准确率（无期望页码/条款时视为不适用跳过）、拒答准确率（正确拒答 vs 编造：拒答词 + 仍给确定数值判定）、false_refusal（可答却空答）、格式合规）。
- **`rag_eval/evaluators.py` + `rag_eval/runner.py`**：编排（每题 Gateway 答案链路 + 可选 RAGFlow 检索层，单题失败不影响其他题）；`cli run` 子命令（`--with-retrieval`/`--top-k`/`--ks`），报告存 `results_dir/runs/<run_id>/`，原始请求/响应与检索原始报文落盘 `raw/`。
- **RagflowClient 构造放宽**：仅 `chat()` 需要 chat_id/assistant_id，`retrieval()` 可独立用于检索评测。
- 单测新增 51 个：normalization 16、retrieval_metrics 20、answer_rules 11、evaluators 8、runner 4（MockTransport 双客户端集成）。

#### 关键发现（真实评测，2026-08-24，法规11，3 题，`results_dir/runs/20260824T020936Z/`）

- **RAGFlow v0.26.4 retrieval chunk 真实结构**（实测）：文档名在 **`document_keyword`**（值形如 `20240124《规程》.pdf`，可能为字符串或列表），**没有 `docnm_kwd` 和 `document_metadata`**；page/section/clause 只以文本形式出现在 content（"章节：7.1.1 / 页码：14"），adapter 不做脆弱解析 → **page_hit/clause_hit 真实为 0（索引未存定位元数据），诚实报告不伪造**。
- **检索指标修复后**：document_hit@1=**1.0**、evidence_hit@1=1.0、**mrr=1.0**、recall@1=0.667、recall@20=0.8、precision@20=0.883；bridge 题两个桥梁文档来源只在 **rank 20** 命中（地灾 3 个在 rank 1）——真实检索对部分文档召回偏弱，可通过检索指标量化。
- **答案链路**：2/3 成功评测（CCAR 持续 503）；bridge `PARTIALLY_ANSWERABLE` key_points 1/4 + 数值缺口；sand `ANSWERABLE` key_points 2/7、must_include 4/5——**真实答案对 benchmark 期望要点覆盖约 27%**，既是 RAG 质量信号也提示 benchmark key_points 需校准。
- **环境故障记录**：评测过程中 RAGFlow 偶发僵死（9380 连接被关闭、Gateway /admin/datasets 503），用户重启后恢复；与 8.21 已知问题一致，评测系统正确捕获并隔离了该故障。

#### 改动文件

- 新增：`rag_eval/metrics/{__init__,normalization,retrieval_metrics,answer_rules}.py`、`rag_eval/evaluators.py`、`rag_eval/runner.py`、`tests/test_{normalization,retrieval_metrics,answer_rules,evaluators,runner}.py`。
- 修改：`rag_eval/adapters/response_adapter.py`（document_keyword 实测字段 + list 处理）、`rag_eval/clients/__init__.py`（构造放宽）、`rag_eval/cli.py`（run 子命令 + runner 化）、`tests/test_response_adapter.py`、`tests/test_clients.py`、`README.md`。

#### 注意

- 尚未实现：本地 LLM Judge（第四阶段）、SQLite/报告 HTML（第五阶段）、版本回归（第六阶段）、独立并发压测（第七阶段）；Embedding 语义匹配接口已备好（`TextSimilarity` protocol），默认规则匹配。
- 检索层 page/clause 指标受"RAGFlow 索引未存定位元数据"限制为 0；如需提升，应在入库阶段为 chunk 写入 `document_metadata`（属 internal-rag 接入台改造，非评测系统范围）。
- benchmark key_points 覆盖偏低：下一轮可用真实答案校准基准集（保持 P0 门禁语义），或接受低分作为持续改进信号。

---

### 2.6 GPT 评审修复：检索指标三个语义问题（2026-08-24 下午，第三阶段收尾）

#### 背景

GPT 评审确认当前为"规则型离线评测 MVP / 内部 Alpha（约 55–65%）"，并指出第三阶段三个指标问题会破坏"单题失败不影响整体"与指标语义：

1. 单题检索失败项无 `mrr`，`summarize_retrieval_results` 直接 `r["mrr"]` → **KeyError 使整个汇总崩溃**；
2. **Recall 被同一证据重复抬高**：一条证据可同时匹配多个期望来源（两个期望条款同文档、只召回一个条款 Recall 仍 1.0）；且文档+条款期望只匹配文档就算命中；
3. **nDCG 非标准实现**：`1/(rank+1)` 近似折损 + IDCG 假设前 K 全相关 → 仅 rank1 正确时 nDCG@10≈0.2475（应≈1.0）。

#### 修复

1. **汇总容错**（`evaluators.py`）：`mrr`/`ndcg@10` 改为 `isinstance(r.get(...), (int, float))` 过滤；新增 `failed_retrieval_cases` 计数；失败题计入 case_count 不参与指标均值。
2. **硬锚点组合匹配**（`retrieval_metrics.py` `_match_one`）：文档名必填锚点；期望给出页码 → 必须文档+页码同时；期望给出条款 → 必须文档+条款同时；文本/语义匹配降级为"命中方式补充标记"，不能绕过页码/条款硬锚点。条款匹配支持 **content 文本包含条款号**的回退（真实 RAGFlow chunk 无 clause_id、条款号在 content "章节：7.1.1" 中，第 4 条"文本规范化匹配"的实际应用）。
3. **Recall 一对一口径**（`recall_at_k`）：按证据 rank 贪心，**一条证据最多服务一个期望来源**，杜绝虚高。
4. **标准 nDCG**（`ndcg_at_k`）：`DCG=Σ rel/log2(i+1)`；`IDCG=把实际相关项排最前（前 |rel| 位全相关）`；仅 rank1 正确时 =1.0。

#### 验证

- 新增回归测试 11 个（`TestGptReviewFixes` + evaluators 容错用例）：Recall 单证据不虚高（0.5 而非 1.0）、条款/页码必须文档匹配、clause content 回退、nDCG rank1=1.0 / rank2=1/log2(3)、无相关=0、失败项汇总不崩。
- pytest **153 passed**（+11），ruff 0，mypy Success（23 files）。
- 说明：`TestEvidenceMatcher` 部分用例因新锚点规则调整了测试数据（证据补 clause_id/content），语义符合 GPT 规则。

#### 注意

- 修复后第三阶段"指标语义验收"完成；第四阶段（LLM Judge）按 GPT 评审修订后实施（六项输入隔离、null 不适用语义、JudgeBackend Protocol 预留 RAGAS、错误分类、4B/9B 模型分级）。
- 真实数据下 clause 锚点匹配依赖 content 回退；若入库侧补 `document_metadata`，结构化匹配优先生效（adapter 已支持）。

---

### 2.7 第四阶段：本地 LLM Judge + 原子事实拆分（2026-08-24 下午）

#### 内容

按 GPT 评审（六项输入隔离、null 不适用语义、错误分类、模型分级、RAGAS 预留）与用户确认（9B 正式默认 + 现有 12 题校准）完成第四阶段：

- **`rag_eval/judge/` 包**：
  - `schemas.py`：`JudgeSample` / `DimensionScore` / `JudgeResult` / `JudgeBackend`（Protocol，为 RAGAS 预留插拔点）；六项维度按两组隔离——组 A（correctness/completeness，含参考答案）、组 B（faithfulness/citation_quality/usability/refusal_quality，**不含参考答案**，只给实际证据+引用+原子声明+预期可答状态）；
  - `errors.py`：6 类错误（judge_timeout/http_error/invalid_json/schema_error/incomplete_scores/unavailable）；
  - `claims.py`：原子事实确定性拆分（句号/分号/列表项、"且/同时/分别"并列、条款号、数值、应/宜/可/不得强度；**补全共享主语与要求强度**——"设备应满足 A、B 和 C"拆成带"设备应"前缀的独立声明；主语继承截断为名词短语）；
  - `local_llm.py`：`LocalLLMJudgeBackend`（OpenAI 兼容；严格 JSON 解析；**只对调用/格式失败重试、不对低分重试**；空答案 correctness/completeness 确定性给 0；保留原始 prompt/输出；`apply_na_rules` 不适用维度置 null 不给默认分）。
- **runner 集成**：`judge.enabled`（默认 false，不影响现有规则评测）时每题拆 claims → JudgeSample → judge 结果与规则结果**并列**存入 `answer_results[].judge`；`judge_summary` 独立聚合（只统计适用维度、judge_errors 分类计数）。
- **CLI**：`run --judge/--no-judge`、`--judge-model`（4B 开发冒烟 / 9B 正式）。
- **config**：`judge.enabled: false`、`model: qwen3.5:9b-q4_K_M`、`timeout_seconds: 300`（9B 正式）。

#### 验证

- 单测 22 个（claims 8 + judge 14：正常/空答/坏 JSON/HTTP/超时/格式重试/refusal null/score 校验/输入隔离），pytest **177 passed**，ruff 0，mypy Success（28 files）。
- **真实 4B 冒烟**（`run --judge --judge-model qwen3:4b-instruct-2507-q4_K_M`，3 题，报告 `runs/20260824T025143Z/`）：
  - bridge 题 judge 成功（25.5s 两次调用）：correctness=3（理由逐环节指出缺失）、completeness=2、faithfulness=4（**supported_claim_ids 32 条 + unsupported=claim-12 逐条定位**）、citation_quality=5、usability=4、**refusal_quality=null（可答题不适用）**；
  - sand 题 `judge_invalid_json`（4B 组 A 输出非 JSON，重试后仍失败）——**如实记录 judge_error，不影响该题规则指标与其他题**；
  - CCAR 题网关 503 无 judge（无答案可评）。
- **真实 9B 冒烟**（`runs/20260824T024407Z/`）：9B 单次 ~79s 且不严格遵守"只输出 JSON"（输出叙述性英文），组 A+B 超过原 120s timeout → `judge_timeout`；已将 timeout 提至 300s 并保留 4B 冒烟通道（GPT 预见的模型分级问题得到实证）。

#### 改动文件

- 新增：`rag_eval/judge/{__init__,errors,schemas,claims,local_llm}.py`、`tests/test_claims.py`、`tests/test_judge.py`。
- 修改：`rag_eval/config.py`（JudgeSettings）、`config/default.yaml`（judge 默认 9B/timeout 300）、`rag_eval/runner.py`（judge 集成 + summarize_judge_results）、`rag_eval/cli.py`（--judge/--judge-model）、`tests/test_runner.py`（judge 集成用例）、`README.md`。

#### 注意

- 尚未实现：SQLite/CSV/HTML 报告（第五阶段）、版本回归（第六阶段）、独立并发压测（第七阶段）、RAGAS 对照后端（接口已预留）、20-30 题人工标注校准（当前用 benchmark 12 题，校准集扩充与一致性分析待后续）。
- Judge 稳定性：temperature=0 + seed 已设，但本地推理后端/模型版本仍可能产生差异；报告已保存 model/prompt_version/原始输出，多次重复的波动分析留待校准阶段。
- 4B 对复杂题（多环节对比矩阵）judge 理由基本可用；正式评测建议 9B（慢但语义更强），冒烟/CI 用 4B。

---

### 2.8 GPT 评审修复：Judge 协议与可审计性（2026-08-24 晚间）

#### 背景

GPT 评审第四阶段：方向正确、框架完整，但验收未完全通过（"本地 Judge 可运行原型"，运行可靠度 50-60%）。指出 4 个 P1/P2：

1. **P1 原始 Judge 输入/输出未写入报告**（`JudgeResult.to_dict()` 未序列化 raw 字段；且调用失败时原始输出在赋值前抛错而丢失）→ 无法审计非法 JSON 原因；
2. **P1 评分范围未校验**（99 保留、4.9 截断为 4，汇总可能出现 avg=99）；
3. **P1 缺失维度被静默视为成功**（自动补 null 而非 judge_incomplete_scores）；
4. **P2 citation_quality 不适用规则未实现**（只处理了 refusal_quality）；
5. **P2 RAGAS 插拔点只有 Protocol**（runner 硬编码 LocalLLMJudgeBackend）+ README 路径写错（backend.py vs schemas.py）。

#### 修复

1. **可审计性**：`JudgeResult.to_dict()` 序列化 raw_prompt_a/b + raw_output_a/b；`JudgeError` 携带 `raw_output`；evaluate 对组 A/B 各自 try，**失败也保存最后一次原始输出**（报告可审计模型为何输出非法 JSON）；空回答确定性 0 分保留。
2. **严格评分**：`_dimension_from_dict` 要求 **0-5 整数**（bool/非 int/越界/浮点 → `judge_schema_error`，不截断不保留）。
3. **缺失维度**：`parse_judge_json` 中维度 key 完全缺失 → `judge_incomplete_scores`（加入重试集合）；**模型显式 `null` = 合法不适用**（不再被当作缺失，也不再抛 schema_error——修复了 `_dimension_from_dict(None)` 崩溃）。
4. **citation_quality NA**：`apply_na_rules` 增加"must_refuse 且无引用 → citation_quality=null"。
5. **后端可注入**：`run_evaluation(judge_backend: JudgeBackend | None = None)`，注入后端时即使 `judge.enabled=False` 也生效；`JudgeBackend` Protocol 补 `close()`；README 路径修正为 `judge/schemas.py`。
6. **claims/ID 校验**：组 B prompt 的 `zip(..., strict=True)`；Judge 返回的 supported/unsupported claim ID **过滤到输入集合**（防伪造）。

#### 验证

- 单测 +9（TestStrictValidation 5 + judge 4：claim 过滤/raw 输出保留/incomplete 重试/注入后端），pytest **183 passed**，ruff 0，mypy Success（28 files）。
- **9B 复测被环境阻塞**：`run --judge --judge-model qwen3.5:9b-q4_K_M`（timeout 300）执行中 **RAGFlow 第二次僵死**（9380 连接被关闭，Gateway 存活但 503，CCAR 与 bridge 两题答案链路均失败）→ evaluated_count=0。9B judge 的端到端成功验证需在 RAGFlow 恢复后复测（用户重启 Docker/RAGFlow）。
- 4B judge 冒烟（上一轮）已验证完整链路；本轮修复（严格校验/raw 审计/claim 过滤）由 mock 单测覆盖。

#### 改动文件

- 修改：`rag_eval/judge/{schemas,errors,local_llm}.py`、`rag_eval/runner.py`、`tests/test_judge.py`、`tests/test_runner.py`、`README.md`。

#### 注意

- RAGFlow 稳定性仍是评测的主要外部风险（半小时内两次僵死）；建议后续处理 internal-rag 侧单 worker ReadTimeout 问题（8.21 HANDOFF 挂账）后再做批量评测。
- GPT 建议的"9B 真实运行 5-10 题、统计成功率/非法 JSON 率/耗时/重复波动、成功率≥90% 后再接 RAGAS/版本回归"列为第五阶段前置验收项，待环境稳定后执行。

---

### 2.9 模型分级实测：9B 不可批量、4B 达标（2026-08-24 晚间）

#### 背景

GPT 要求 9B 重新真实运行验证（timeout 300 后）。RAGFlow 重启后复测得到决定性结论。

#### 9B 实测（RAGFlow 重启后，`runs/20260824T032029Z/`）

- bridge 题：**judge_invalid_json，latency 552s（~9.2 分钟）**——排查原始输出：**组 A（correctness=1/completeness=2）JSON 完整合法**，失败发生在**组 B（含证据/引用的长 prompt）输出截断/非法**；单次调用 ~4.5 分钟，8GB 显存 + 单驻留下生成长输出不稳定；
- sand 题：**judge_timeout（600s = 组 A 两次各超 300s）**，无输出；
- 结论：**9B 在当前硬件不可作批量 judge**（单次 4.5 分钟、长 prompt 输出截断、超时频发）；仅保留为抽样校准选项（`--judge-model`），**默认模型改回 4B**（config/model 同步更新，timeout 300→120）。

#### 4B 全量校准集（benchmark 12 题，`runs/20260824T034257Z/`，GPT 要求的 5-10 题验收）

- 答案链路：8/12 成功（4 题 http_5xx：CCAR-25-981、5G 机巢协同、桥梁巡检流程、主体缺失拒答——RAGFlow 重启后部分题仍 503，环境不稳如实记录）；
- **judge：7/8 成功（87.5%）**，1 题 invalid_json（sand）；每题耗时 **avg 16.5s / min 9.4s / max 26.2s**——4B 批量可行（12 题 judge ≈ 3 分钟）；
- 维度均值：correctness=4.0、completeness=2.57、faithfulness=4.0、citation_quality=4.0、usability=3.86、refusal_quality=5.0；
- 观察：prompt_injection 题 correctness=1（4B 正确识别注入风险）、拒答题 refusal_quality=5（正确拒答）——judge 分数与预期吻合，具备校准基础。

#### 结论

- Judge 默认 4B 批量可用（成功率 87.5%，接近 GPT 建议的 90% 门槛），**定位仍为观察指标**（GPT 建议：达标前不参与发布阻断/综合总分）；
- 9B 仅在硬件升级或外部强模型接入后考虑；RAGAS 对照与版本回归门禁待 judge 稳定性进一步确认后接入；
- 下一轮若做第五阶段（SQLite/CSV/HTML 报告），将包含 judge 成功率的持续统计与重复评分波动分析。

#### 改动文件

- `config/default.yaml`（judge.model 默认 4B、timeout 120、注释 9B 实测结论）、`rag_eval/config.py`（JudgeSettings 默认同步）、`README.md`（模型分级实测说明）。

---

### 2.10 第五阶段：SQLite 存储 + JSON/CSV/HTML 报告（2026-08-24 晚间）

#### 内容

- **`rag_eval/store.py`**（`RunStore`）：SQLite 持久化（`results_dir/runs.db`，**WAL 模式** + synchronous=NORMAL）；`runs` 表（run 元数据 + summary_json）与 `question_results` 表（逐题：question/answer/status/answer_rules/judge/retrieval/record_summary JSON 列 + raw_path）；评测单进程串行写天然单写者；`load_run`/`list_runs` 供报告重新生成与历史查询。
- **`rag_eval/reporting.py`**：五类报告——`summary.json`（聚合摘要）、`question_results.jsonl`（逐题完整结果）、`metrics.csv`（聚合指标展平，utf-8-sig 便于 Excel）、`failures.csv`（失败题：答案调用失败/规则失败/Judge 失败，含原因）、`report.html`（jinja2 内联模板，**离线可开**：纯 CSS 无 JS 无外部引用、指标卡片/Judge 维度/检索表/`<details>` 逐题折叠/失败清单、jinja2 autoescape 防 XSS）。
- **CLI**：`run` 自动写 SQLite + 生成五类报告（每题补 raw_path）；新增 `report --run-id` 从 SQLite 重新生成报告。
- runner 的 answer_result 补充 case 元数据（category/priority/question/reference_answer）。

#### 验证

- 单测 11 个（store 5：roundtrip/缺 run/列表/WAL；reporting 6：五文件/各文件内容/HTML 关键区块与转义），pytest **195 passed**，ruff 0，mypy Success（30 files）。
- **真实 run**（4 题 + 4B judge，`runs/20260824T055335Z/`）：SQLite 写入（runs=1、question_results=4）、五类报告全部生成、`failures.csv` 正确列出 4 题失败明细（CCAR 503、bridge/sand/table3 规则缺口 + sand judge_invalid_json）、`report --run-id` 从 SQLite 重新生成成功。
- HTML 离线约束实测：无外部 http 引用、无 `<script>`、div/details 标签闭合平衡；Chrome headless 渲染成功。

#### 改动文件

- 新增：`rag_eval/store.py`、`rag_eval/reporting.py`、`tests/test_reporting.py`。
- 修改：`rag_eval/cli.py`（run 集成 SQLite+报告、report 子命令）、`rag_eval/runner.py`（case 元数据装饰）、`README.md`。

#### 注意

- 原始请求/响应不落 SQLite（体积大），存 run 目录 `raw/`，DB 只存路径引用。
- 版本回归（第六阶段）将基于 `runs.db` 的 `load_run`/`list_runs` 做 run 间对比；regression_report.json 在第六阶段生成。
- 遗留：RAGFlow 稳定性（评测期间仍见 503）、judge 成功率持续统计、9B 抽样校准、RAGAS 对照后端实现。

---

### 2.11 第六阶段：版本对比回归（2026-08-24 晚间）

#### 内容

- **`rag_eval/compare.py`**：`compare_runs(baseline, candidate)` 输出——新增通过/新增失败（`new_pass`/`new_fail`）、**回归题**（pass→fail，`kind=regression`，**P0 题回归 → `regression_failed=true`**）、修复题（`fixed`）、稳定/持续失败/移除（`stable`/`still_failed`/`removed`）、聚合指标提升/下降/持平（answer/judge/retrieval 各数值字段）、严重错误变化（numeric mismatch/must_not_include/call failed 等标记计数增减）、逐题延迟变化；`CaseDiff` 保留每题跨 run 状态与原因；`save_regression_report` 写 `regression_report.json`。
- **元数据采集**（规格十三）：`run` 自动记录 **git_commit**（`git rev-parse --short HEAD` 尽力采集，失败为 None）、**version 信息**（config 的 version 节：数据集/知识库/RAGFlow/模型/提示词版本）、dataset_sha256、config_fingerprint；SQLite `runs` 表新增 `git_commit`/`version_json` 列 + **轻量迁移**（旧库缺列自动 ALTER）。
- **CLI**：`rag-eval compare --baseline X --candidate Y`，打印摘要 + 写 `runs/<candidate>/regression_report.json`；`regression_failed` → 退出码 1。

#### 验证

- 单测 9 个（回归/P0 阻断/新增/移除/指标升降/严重错误/延迟/持续失败/序列化），pytest **204 passed**，ruff 0，mypy Success（31 files）。
- **真实对比**（baseline `20260824T055335Z` → candidate `20260824T060351Z`，两次 4 题 run）：`regression_failed=False`、无回归/新增/修复（两轮结果一致：CCAR 503、bridge/sand/table3 规则缺口）；`case_diffs` 4 题全部 `still_failed`（含原因），延迟变化正确记录（bridge +1877ms、sand -371ms、table3 -6814ms）；`metric_changes` 全部 unchanged；`regression_report.json` 生成。

#### 改动文件

- 新增：`rag_eval/compare.py`、`tests/test_compare.py`。
- 修改：`rag_eval/store.py`（新列 + 迁移 + save_run/load_run 扩展）、`rag_eval/cli.py`（compare 子命令 + git/version 采集）、`README.md`。

#### 注意

- 版本回归的价值依赖**可重复的评测环境**：RAGFlow 稳定性（503 频发）会污染对比结果（如 CCAR 持续失败掩盖真实回归）；建议先处理 internal-rag 单 worker ReadTimeout 再正式启用回归门禁。
- 剩余：第七阶段（独立并发压测）、RAGAS 对照后端、judge 稳定性/校准（成功率 90%+ 后接回归门禁）。

---

### 2.12 网关修复：单主体 comparison plan 不再 500（2026-08-24 晚间，internal-rag）

#### 问题

用户在智能问答（`/chat`）连接 DeepSeek API 后提问，返回 `500 The deterministic comparison matrix could not be built.`。

#### 根因

`rag_pipeline.py` 的 `is_matrix_first` 只检查 `query_type in {"comparison","multi_hop"}` 就进入确定性矩阵路径，而 `ComparisonMatrixBuilder.build()` 要求 **`subjects>=2` 且 `aspects` 非空** 才构建。当 legacy plan 的 query_type 是 comparison/multi_hop 但只识别出 1 个主体（或维度为空）时，build 返回 None → orchestrator 返回 None → pipeline 直接抛 `AppError(COMPARISON_MATRIX_BUILD_FAILED, 500)`，整次问答失败。与 DeepSeek 模型本身无关（任何对比/多跳题 + 不完整 plan 都会触发）。

#### 修复（`app/pipelines/rag_pipeline.py`）

1. **`is_matrix_first` 条件收紧**：增加 `len(plan.subjects) >= 2 and plan.aspects`——与 `build()` 的前置条件对齐，单主体题不再进入矩阵路径；
2. **矩阵构建失败改为降级而非 500**：`matrix_generation is None` 时记录 `stage_counts["matrix_build_failed"]`，回退到 extractive_answer / generator 路径（矩阵兜底 `_comparison_matrix_fallback` 失败时也优先提取式答案，不直接 500）。

#### 验证

- 新增回归测试 `test_single_subject_comparison_plan_does_not_500`（comparison plan 单主体 → 返回合法状态而非抛错）；pipeline 测试 **8 passed**，ruff 通过，mypy 仅剩既有 `model_client.py:81`。
- 注意：沙箱（workspace-write）下 python 创建目录不可枚举，internal-rag pytest 需用临时插件绕过（`D:\internal-rag\temp\pytest_override.py` + `-p no:tmpdir -p no:cacheprovider -p pytest_override`，`PYTHONPATH=D:\internal-rag\temp`），不改动工程配置。

#### 改动文件

- `app/pipelines/rag_pipeline.py`、`tests/unit/test_rag_pipeline.py`。

#### 注意

- **修复需重启网关生效**：`deploy\gateway\stop.ps1` + `deploy\gateway\start.ps1`（或重启 8080 进程）；用户当前连接的 DeepSeek 模型连接在网关重启后需重新接入（运行时 API Key 只在进程内存中）。
- 仍待定位：为何该题 plan 只识别出 1 个主体（planner/legacy 转换路径），后续可把这类题加入评测集门禁。

---

## 3. 工作原则（沿用，不重复展开）

1. 先读 `docs/agent-handoff.md`，再读目标模块和对应测试。
2. 任何解析修复都必须用真实失败文档回归；不写破坏全局的正则。
3. 用户只有一个上传入口，不重新要求用户选择解析器。
4. 物理页码、印刷页码、条款号分开存储；表格文本、二维结构、图片资产三者都保留。
5. `needs_review` 不能绕过门禁发布；发布前优先 dry-run；删除本地任务不删除 RAGFlow 内容。
6. 修改后至少运行目标测试、完整 pytest 和 ruff；页面改动必须浏览器实测。
7. 不输出或提交密钥；不清除 Docker 数据卷；不把 D 盘数据复制回 C 盘。
8. 新项目 rag-evaluation 同样遵守：D 盘约束、不依赖公网（Embedding/Judge 必须可配本地模型）、敏感值走环境变量、评测不修改生产 RAGFlow 数据。

---

## 4. 相关文档

- `README.md`
- `docs/agent-handoff.md`（最完整交接入口）
- `docs/query-translation-decomposition-refactor.md`（8.21 重构实施方案；配套评测集 `tests/evaluation/`）
- `HANDOFF-2026-08-21.md`（本会话起点）
- 新项目 `D:\internal-rag\source\rag-evaluation\README.md`（rag-evaluation 安装/配置/运行/结果解释）
