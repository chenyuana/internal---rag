# Internal RAG 改动记录（2026-08-19）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-18.md` 之后）的工作。
> 项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。
> 本文档按日期逐节追加改动/验证记录，沿用既有格式：问题 / 根因 / 修复 / 验证 / 改动文件 / 注意。

---

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-18.md`（已覆盖：跨规章语义混淆三层修复——规范性范围感知 scope validator、逐 Claim 安全降级、原文条款抽取 `regulation_answer_builder`、Reranker `--batch-size 2048` 修复、对比题句式规划与主体/维度扩展、证据提取层元数据行过滤与数值/表格块优先、前端 Markdown 表格渲染等）。
- **本会话范围**：自 2026-08-19 起。本会话开始以**熟悉工程、核对当前状态**为主，尚未产生代码改动；本交接文档自本次起按日期逐节追加后续改动/验证记录。
- **重要**：本会话的改动/验证记录在下方第 2 节中按日期逐节追加，不以计划项代替完成结果。

---

## 1. 当前状态快照（2026-08-19）

以下为本次会话实际核对的快照（非引用旧文档）。

### 1.1 环境与代码

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`、`http://127.0.0.1:8080/chat`
- 服务探测（2026-08-19 实测，HTTP 探活）：

| 服务 | 端口 | 状态 |
|---|---|---|
| Gateway | `127.0.0.1:8080` | ✅ `/health` 200 `{"status":"ok",...}`，development 环境 |
| RAGFlow | `127.0.0.1:9380` | ✅ `/api/v1/system/healthz` 200 `{"db":"ok","doc_engine":"ok","redis":"ok","storage":"ok"}` |
| Ollama | `127.0.0.1:11434` | ✅ 端口开放（今日 08:49 前后有启动/日志写入） |
| Reranker | `127.0.0.1:8003` | ✅ `/health` 200 `{"status":"ok"}`（今日 08:35–08:48 前后启动；08-18 已带 `--batch-size 2048` 运行） |
| MinerU | `127.0.0.1:8886` | ❌ 关闭（`deploy/mineru/status.ps1`：Installed=True / Configured=True / Ready=False，WebException） |

- 结论：宿主机主要服务在线；MinerU 关闭，涉及 OCR/远程解析的作业前需先启动 `.\deploy\mineru\start.ps1`。

### 1.2 Git 状态

- 仓库已有基线提交（`Initial commit` ×2 + `Merge GitHub repository initialization`）。
- 工作区仍有未提交改动（截至 2026-08-19，全部来自 08-17/08-18 会话，本会话未新增代码改动）：

| 类别 | 文件 |
|---|---|
| 已修改 | `app/core/config.py`、`app/ingestion/pipeline.py`、`app/pipelines/rag_pipeline.py`、`app/schemas/chat.py`、`app/services/answer_generator.py`、`app/services/coverage_selector.py`、`app/services/evidence_extractor.py`、`app/services/evidence_judge.py`、`app/services/evidence_text.py`、`app/services/query_planner.py`、`app/web/chat.html`、`app/web/static/chat.css`、`app/web/static/chat.js`、`config/default.yaml`、`prompts/answer_generation.txt`、`prompts/json_repair.txt`、`tests/unit/test_answer_generator.py`、`tests/unit/test_evidence_extractor.py`、`tests/unit/test_evidence_judge.py`、`tests/unit/test_pipeline.py`、`tests/unit/test_query_planner.py`、`tests/unit/test_rag_pipeline.py`、`README.md`、`HANDOFF-2026-08-17.md` |
| 未跟踪 | `HANDOFF-2026-08-18.md`、`app/services/evidence_scope_selector.py`、`app/services/grounding_validator.py`、`app/services/regulation_answer_builder.py`、`app/services/scope_validator.py`、`app/web/static/markdown.js`、`tests/unit/test_evidence_scope_selector.py`、`tests/unit/test_grounding_validator.py`、`tests/unit/test_regulation_answer_builder.py`、`tests/unit/test_scope_validator.py`、`tests/web/`、`.tmp-comparison-debug.json`（调试产物，莫乱码比对用，可清理） |

- **禁止** `git reset --hard` / `git clean` / `docker compose down -v`。

### 1.3 真实入库数据（D 盘实测统计，2026-08-19 重算）

- `D:\internal-rag\data\ingestion\jobs\`：96 份 `job.json`（UTF-8，读取需 `-Encoding UTF8`）。
- 任务状态分布：

| 状态 | 数量 |
|---|---|
| `warning` | 74 |
| `completed` | 21 |
| `needs_review` | 1 |
| `failed` | 0 |

- 发布状态：96/96 `published`（dataset `b5ecab6a8b2811f1b7c4d904b160df50` 为主要发布目标）。

### 1.4 测试基线

- 08-18 实测（宿主无沙箱环境）：完整 pytest `325 passed, 3 failed`；3 个失败均为既有解析基线（noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重，`test_regulation_preprocess.py` 相关）。
- 08-19 本会话重跑完整 pytest（DSH 沙箱内）：`262 passed, 3 failed` + **64 个环境性 ERROR**。经定位：
  - 3 个 failed 与 08-18 完全一致（`test_regulation_preprocess.py` 三个既有基线），非回归；
  - 64 个 ERROR 全部来自依赖 `tmp_path` fixture 的测试模块（`test_ingestion_api`、`test_pipeline`、`test_rag_pipeline`、`test_ragflow_publisher`、`test_remote_parsers`、`test_retrieval_api`、`test_retrieval_service`），根因是 **DSH 文件沙箱拒绝 pytest 临时目录访问**：默认 basetemp `C:\Users\Win11\AppData\Local\Temp\dsh-O2M9dv\pytest-of-Win11` `os.scandir` 被拒；项目 `pyproject.toml` 强制 `--basetemp=D:/internal-rag/temp/pytest`，该目录被运行中的 Gateway（`TEMP=D:/internal-rag/temp`）占用无法清理；即使用 `-o addopts= --basetemp=<工作区内目录>` 也在会话结束时 `cleanup_dead_symlinks` 被拒（WinError 5）。
  - **结论**：属沙箱环境限制，非代码问题；干净基线需在宿主（非沙箱）终端重跑 `python -m pytest` 确认（预期仍为 `325 passed, 3 failed`）。

---

## 2. 改动记录（按日期逐节追加）

### 2.1 会话初始化：熟悉工程与建立今日改动文档（2026-08-19）

#### 内容

- 通读 `README.md`、`docs/agent-handoff.md`（完整交接入口）、`HANDOFF-2026-08-18.md`（昨日全量改动记录）。
- 核对今日快照：服务探活（§1.1）、Git 状态（§1.2）、入库任务统计（§1.3）、测试基线（§1.4）。
- 新建本文档 `HANDOFF-2026-08-19.md`，沿用既有逐日逐节追加格式。

#### 改动文件

- `HANDOFF-2026-08-19.md`（新增）

#### 注意

- 本会话暂无代码改动；后续改动/验证将按日期追加到第 2 节。
- 遗留挂账（来自 08-18，尚未处理）：早熟中稻航高侧"单侧缺证据/适用对象约束"（`task_type`/适用对象过滤）、沙地播种量公式 OCR 乱码（需回解析层修公式图像）、生成端对已入证据内容利用不充分、RAGFlow 偶发僵死需按 08-11/08-18 记录处置。

---

### 2.2 验证：同一对比题多次询问答案不同——非确定性来源定位（2026-08-19）

> 用户报告：智能问答台同一问题（"对比地质灾害倾斜航测、河湖智能巡检无人机，在航向/旁向重叠度、地面分辨率要求上有什么差异？"）多次询问答案不同。本会话在宿主实测 3 次并定位根因（**未改代码**）。

#### 实测结果（网关 `/api/v1/chat/completions`，dataset `b5ecab6a…`，同 query 连问 3 次）

| 次数 | 耗时 | request_id | degraded | 答案摘要 |
|---|---|---|---|---|
| 1 | 91s | `6f58481a-b1be-4db3-8f33-dfdf3fa9ac61` | ✅ True | 仅"当前资料中未明确说明河湖智能巡检无人机对地面分辨率的具体数值要求。"，无表格、无引用列表 |
| 2 | 60s | `8d03b88b-02c1-4ed6-a125-74ae20d82192` | ❌ False | 完整 Markdown 表格：地面分辨率 1:500→5cm / 1:2000→20cm [C3]、河湖侧未明确；重叠度 丘陵山地 50%~80% [C1]、垂直影像旁向 40%~80% 最小 30%、倾斜摄影满足垂直影像后可不重设 [C1]；引用 C1+C3 |
| 3 | 78s | `2a768749-04a9-4b99-8d1c-d1dd654bc94c` | ✅ True | 仅"丘陵山地地区航向、旁向重叠度一般为 50%~80%，垂直影像旁向重叠 40%~80% [C1]"，缺地面分辨率行 |

（原始输出存 `tmp/chat-repeat-20260819.json`；`prompt_version` 均为 `answer-generation-v4`。）

#### 根因（代码 + 配置确认）

1. **主因——生成模型随机采样**：`config/default.yaml` `models.answer.temperature: 0.1`（`.env` 无覆盖），`app/services/model_client.py` `chat_completion` payload 仅带 `temperature: 0.1` + `reasoning_effort: none`，**未传 `seed`**。temperature 非 0 时对长结构化 JSON 答案逐 token 采样，同证据下措辞、详略、是否输出表格、保留哪些条款均会变化。
2. **放大因素——逐 Claim 校验门禁 + repair**：08-18 引入的 scope/grounding 校验是确定性规则，但作用于随机输出：某次 claim 通过、下次被拦 → 走一次随机 repair → 仍不过则删 claim 安全降级（实测 3 次中 2 次 `validation_degraded=True`），答案完整性随之波动。
3. **该题证据结构本身放大了波动**：河湖规程确无重叠度/地面分辨率规定（单侧缺证据，正确行为是写"未明确说明"）；地质灾害规程 6.3.2 同一 clause 含 a)垂直影像 / b)倾斜摄影 / c)机载激光雷达 三个适用对象，模型对 c 款"丘陵山地 50%~80%"是否当通用值、是否漏 65%~85% 航向值不稳定（与 08-18 §2.13 观察一致）。
4. **检索/证据链是确定的**：RAGFlow 混合检索 + llama.cpp reranker 对相同输入结果确定（`allow_rerank_fallback: false`），差异不来自"找到的证据"，而来自"模型如何组织输出"。

#### 稳定化候选方案（未实施，需用户确认）

- `models.answer.temperature` 0.1→0，并在 `model_client` payload 增加固定 `seed`（同证据下输出基本确定）；
- 对"单侧缺证据"对比题增加模板化兜底行（如"河湖侧资料未规定航向/旁向重叠度与地面分辨率"），避免模型漏行/误泛化；
- 说明：现有 grounding/scope 校验能拦"错误数值/跨规章归因"，但无法保证"完整性"（漏行）稳定。

#### 改动文件

- 无（仅验证记录）。

#### 注意

- 数据佐证已存 `tmp/chat-repeat-20260819.json`（控制台为 GBK 乱码属 PowerShell 5.1 显示问题，文件本身 UTF-8）。
- 若实施温度/seed 调整，需重启 Gateway 后重测该题并核对 §2.2 表格三类输出收敛为一致。

---

### 2.3 实施：答案生成确定性稳定化——temperature=0 + 固定 seed（2026-08-19）

> 用户确认实施 §2.2 候选方案一。目的：同一问题在证据不变时输出确定，消除"多次询问答案不同"。

#### 改动

1. **`config/default.yaml`**：`models.answer.temperature: 0.1 → 0`，新增 `models.answer.seed: 42`。
2. **`app/core/config.py`**：`ModelEndpointSettings` 新增 `seed: int | None = Field(default=None, ge=0)`。
3. **`app/services/model_client.py`**：`__init__` 保存 `self.seed = settings.seed`；`chat_completion` payload 在 seed 非 None 时追加 `"seed"` 字段（None 时保持不传，兼容未配置端点）。
4. **测试**：
   - `tests/unit/test_model_client.py`：`test_model_chat_completion_requests_strict_json_schema` 改为 `temperature=0, seed=42` 并断言 payload `temperature==0`、`seed==42`；新增 `test_model_chat_completion_omits_seed_when_unset`（seed 未配置时不传）。
   - `tests/unit/test_config.py`：新增 `test_answer_model_deterministic_sampling_defaults`（断言 `temperature==0.0`、`seed==42`）。

#### 前置验证（Ollama 兼容端点）

- 直接调 `http://127.0.0.1:11434/v1/chat/completions`，`temperature=0 + seed=42` 连发两次输出一致（API 接受 seed 参数，无报错）。

#### 验证

- 沙箱单测：`test_model_client.py` **5 passed**；ruff 对 4 个改动文件全过。
- 配置加载实测：`load_settings()` → `answer.temperature=0.0`、`answer.seed=42`。
- **宿主端到端复测**（Gateway 重启生效后，同题连问 3 次，request_id `7992dd43` / `e13e5f2c` / `4c44e287`）：
  - 3 次答案**字节级完全一致**（`run1==run2==run3` 均为 True），引用均为 [C1]，耗时 79~96s；
  - 输出存 `tmp/chat-repeat-20260819-after.json`。
- **内容层面观察（与确定性无关的既有遗留）**：稳定后的答案仍为纯文本（非表格）、degraded=True ×3；且把"丘陵山地地区航向/旁向重叠度 50%~80%"（地质灾害规程 6.3.2 c 款）错误套用到**河湖侧**（引用仍为 C1 地质灾害 chunk）——这是 08-18 §2.13 已挂账的"适用对象归因"问题，grounding/scope 校验均无法拦截（数值确实存在于被引 chunk 文本）。**确定性≠正确性**：温度 0 使错误答案也稳定复现；适用对象约束与单侧缺证据模板化兜底仍需后续处理（§2.2 候选方案二未实施）。

#### 注意（重要）

- **网关重启方式**：沙箱内 `Start-Process` 启动的网关子进程会随父 pwsh 会话退出被回收（实测第一次重启后进程消失）。当前网关以 DSH 托管后台任务运行（uvicorn 前台，job 存活期间服务在线）；**如需长期运行，请在用户终端执行**：
  ```powershell
  Set-Location D:\internal-rag\source\internal-rag
  .\deploy\gateway\stop.ps1
  .\deploy\gateway\start.ps1
  ```
- 沙箱内完整 pytest 仍受临时目录限制（见 §1.4）；`test_config.py` 新增用例需宿主运行确认。

---

### 2.4 实施：对比题"完整回答"三件套——表1 召回、主体章节入证据、跨主体归因拦截（2026-08-19）

> 用户确认实施（用户明确原则：**系统要求无对应内容就禁止输出，不能编造**——本组改动全部是收紧约束，不放松）。针对"对比地质灾害倾斜航测、河湖智能巡检无人机，在航向/旁向重叠度、地面分辨率要求上有什么差异？"。

#### 改动

1. **① 表1 地面分辨率召回（`app/services/query_planner.py`）**
   - `_ASPECT_SUFFIXES` 补充"上有什么差异/上有什么区别/方面有什么差异/方面有什么区别/有什么差异/有何区别"等，清掉实测中 aspect 残留尾巴（"地面分辨率要求上有什么差异"→"地面分辨率要求"，此前 q3/q4 子查询带尾巴）。
   - `_DIMENSION_EXPANSIONS` 新增 `"地面分辨率"/"地面分辨率要求" → "地面分辨率 表1 比例尺"`，把表格块检索词（表1/比例尺）带进子查询。

2. **② 对比矩阵主体文档优先（`app/services/coverage_selector.py`）**
   - `CoverageSelection` 新增 `chunk_subqueries: dict[chunk_id→subquery_id]`（round one/round two 均记录来源子查询）。
   - round two 在对比矩阵（`synthesis_mode=matrix` + `query_type=comparison`）下改为**两阶段填充**：先按分数填"round-one 命中文档"（即各对比主体对应文档）的候选，预算剩余才填其它文档——把主体自身章节（河湖规程 5.x 巡查实施、地质灾害 6.x）带进证据，且无关文档（激光雷达/桩位等）不再抢占名额；非对比路径保持原"新文档 +0.05"逻辑。

3. **③ 跨主体归因校验（新增 `app/services/subject_grounding_validator.py`）**
   - `SelectedChunk` 新增 `subquery_id: str | None = None`（`app/schemas/retrieval.py`）；`retrieval_service` 在 coverage 选择后用 `selection.chunk_subqueries` 回填。
   - `SubjectGroundingValidator`：对比题下，claim 点名某主体（`plan.subjects` 精确匹配）时，其引用 chunk 必须属于该主体（经 chunk.subquery_id→subquery.subject 映射）；跨主体引用仅在 claim 显式点名该主体时放行；未点名主体/无归属的 claim 保守跳过。直接拦截"河湖智能巡检无人机…50%~80%[C1(地质灾害)]"这类跨主体套用。
   - 接入 `rag_pipeline._validate_evidence_consistency`，开关 `generation.enforce_subject_grounding: bool = True`（`app/core/config.py` + `config/default.yaml`）。

4. **③ 强化提示词规则 22（`prompts/answer_generation.txt`）**：一侧缺同口径证据必须写"当前资料中未明确说明"，**不得把另一侧/其它文件的参数数值、适用对象套用到该侧**；各对比主体的事实只能来自属于该主体的证据（除非明确交叉引用）。`prompt_version` v4→**v5**（config.py + default.yaml 同步）。

5. **grounding 表格块支持（`app/services/grounding_validator.py`）**——① 的最后一环
   - 定位：表1 数值在 RAGFlow 中为**裸数字行**（`| 1:500 | 地裂缝 | 5 |`），单位在表头（"地面分辨率值（cm）"）；claim"5cm"与 chunk 无法字面匹配 → 地面分辨率 claim 被 `ClaimGroundingValidator` 误拦删除 → 答案缺数值。
   - 修复：`_table_encoded_marks`——仅对 Markdown 表格 chunk，claim 的"数字+单位"标记在"数字存在于表格行 且 单位存在于表头/正文"时判为有据；非表格正文块保持字面匹配（不放松）。

#### 测试

- `tests/unit/test_query_planner.py`：+2（地面分辨率 aspect 清理与扩展、方面…区别后缀）。
- `tests/unit/test_coverage_selector.py`：+2（归属记录+主体文档两阶段优先；非对比保持原逻辑）。
- 新增 `tests/unit/test_subject_grounding_validator.py`：6 用例（跨主体拦截/同主体放行/显式交叉引用/未点名跳过/无归属跳过/非对比跳过）。
- `tests/unit/test_grounding_validator.py`：+3（表格块数值放行/表格缺数拒绝/非表格不放松）。

#### 验证

- 沙箱单测：query_planner + coverage_selector + subject_grounding + grounding + model_client 等 **45 passed**；ruff 全过；定向 mypy 仅剩既有 `model_client.py:81`（08-18 已挂账）。
- 检索 debug 实测：q3/q4 aspect 已清为"地面分辨率要求"、query 含"表1 比例尺"；选中 8/120 全部来自两个主体文档（**无激光雷达/桩位等无关 chunk**），含 6.3.2 重叠度、**表1 表格块（RAGFlow 95f7f3a7）**、河湖 3.4/5.x/7.x 章节；`subquery_id` 已回填。
- **端到端 chat 实测**（两次，字节一致，`answer-generation-v5`，status=ANSWERABLE / degraded=False / repaired=False，引用 C1/C3/C6）：
  - 输出 Markdown 表格：地面分辨率侧"1:500 下地裂缝、崩塌需达到 **5cm** [C3]"；重叠度侧"航向 65%~85% 最小60%、旁向 40%~80% 最小30% [C1]，倾斜摄影可不重设 [C1]"；
  - 河湖侧两行均为"当前资料中未明确说明具体数值或计算方式"，**不再套用 50%~80%**；
  - 附注说明河湖证据仅涉设备参数（热红外焦距），未含重叠度/地面分辨率作业标准。
- 输出存档：`tmp/chat-answer2-20260819.txt`、`tmp/chat-answer3-20260819.txt`、`tmp/debug-chunks-20260819.json`。

#### 注意 / 遗留

- 检索 debug 的 RAGFlow chunk_id 与本地 chunks.jsonl 的 chunk_id **不一致**（RAGFlow 发布时重新编号），排查时需按"章节+页码+内容"匹配（本例：表1 块 = RAGFlow `95f7f3a7f7e47113`，6.1 正文块 = `f9f4b0ac9feeefaa`）。
- 内容微瑕（非本次范围）：答案把 a 款（垂直影像）数值前加了"丘陵山地地区"修饰（c 款才是丘陵山地 50%~80%），措辞易误导但数值正确；另 C6（6.3.1 航线敷设）被用于"复杂区加大重叠度"佐证，合理。
- 完整 pytest 与 `test_config.py` 新增用例需宿主（非沙箱）重跑确认（沙箱临时目录限制见 §1.4）。

---

### 2.5 复现确认与条款内适用对象区分强化（2026-08-19）

> 用户反馈其界面输出把 6.3.2 **c) 机载激光雷达** 的"丘陵山地地区航向、旁向重叠度 50%~80%"当成了地质灾害倾斜航测（摄影测量）的通用重叠度，且把 a) 垂直影像旁向"40%~80% 最小30%"转述成"30%-40%"；并贴出 6.3.2 原文与表1 要求系统按正确内容输出。

#### 复现与结论

- **当前代码/实例复现（chat-repro）输出为正确版本**：重叠度侧"航向 65%~85% 最小60%、旁向 40%~80% 最小30% [C1]、倾斜摄影可不重设 [C1]、复杂区加大 [C6]"；地面分辨率侧"1:500→5cm [C3]"；河湖侧两行"当前资料中未明确说明"；status=ANSWERABLE、degraded=False。
- 连续两次 retrieval debug 选中 chunk **完全一致**（检索确定）；temperature=0+seed=42 下生成确定 → 用户贴的 c 款版本并非当前代码+当前证据的输出，与 **08-18 §2.13** 记录的现象（模型把 c 款当通用值、漏 a 款数值）同源，属**条款内适用对象区分**的模型忠实性问题，历史上偶发。
- 根因：6.3.2 是"一条 clause 含 a/b/c 三款"，C1 证据 chunk 同时含三款数值与限定词；模型某次会丢失限定词归属（c 款"丘陵山地地区 50%~80%"被提升为航测通用值）。现有 grounding/scope/subject 校验均无法拦截（数值确实存在于被引 chunk，主体归属正确）——只能从提示词层面固定。

#### 改动

- `prompts/answer_generation.txt` 新增**规则 28**：同一法规条款内按适用对象分项（a) 垂直影像 / b) 倾斜摄影 / c) 机载激光雷达）规定数值时，必须逐项标明适用对象后再写数值，不得把某一分项的数值表述为整个作业类别或其它分项的通用要求；省略/丢失适用对象限定词（如"丘陵山地地区""垂直影像""倾斜摄影"）等同于编造，禁止。
- `app/core/config.py` + `config/default.yaml`：`prompt_version` v5→**v6**。

#### 验证（宿主重启后实测 v6）

- 同题输出：地面分辨率"1:500→5cm、1:2000→20cm [C3]"（更完整）；重叠度"**垂直影像**旁向40%~80%最小30%、航向65%~85%最小60% [C1]"（a 款已标注适用对象，规则 28 部分生效）；河湖侧两行"当前资料中未明确说明"+ 引用河湖 5.x 任务规划 [C7][C8]；ANSWERABLE/degraded=False。
- **残余瑕疵**：c 款"丘陵山地地区一般50%~80%，复杂区70%~85%"仍挂在航测主体下、未标注"机载激光雷达"，且与 a 款复杂区数值混排。
- **v6→v7**：规则 28 强化为"**逐项完整列出各分项及其数值与适用对象**"示例格式（垂直影像：…；倾斜摄影：…；机载激光雷达：…），禁止只摘取部分分项数值充当"一般/通用"要求；`prompt_version` v6→**v7**。需宿主再重启验证。
- **v7 复测仍残留**：c 款"丘陵山地地区一般50%~80%"仍挂在航测主体下、未标注"机载激光雷达"（v6/v7 两次稳定复现，temperature=0 使错误稳定）。两次 prompt 强化均未改变该行为。
- **根因定位（证据层）**：`EvidenceExtractor` 句子切分把 `"c) 机载激光雷达:旁向重叠度不小于30%;"` 与 `"在丘陵山地地区,航向旁向重叠度50%~80%。"` 切成两句，模型看到的"丘陵山地 50%~80%"是**无适用对象归属的孤立句**，被当作通用规定。
- **修复（`app/services/evidence_extractor.py`）**：新增 `_merge_subitem_follow_ons`——识别条款分项头（`_SUBITEM_HEAD_RE`：字母/汉字序号+短前缀+冒号），其后的承接句（`_FOLLOW_ON_RE`：在/当/其/此/如/即/指/若/则 开头）并入该分项，仅当紧邻前句是分项头时合并（避免"其他任务…"误并入）。`tests/unit/test_evidence_extractor.py` 新增用例（含真实 6.3.2 结构断言"机载激光雷达"在"丘陵山地"之前）。
- **验证（真实 chunk，还原 Unicode）**：合并后证据为 `"c) 机载激光雷达:旁向重叠度不小于30%;在丘陵山地地区,航向旁向重叠度50%~80%。"`，适用对象归属保留。单测 61 passed（evidence_extractor 5 + 相关模块）、ruff 通过。**需宿主再重启 Gateway 生效后复测**：重叠度侧应输出"垂直影像：…；倾斜摄影：…；机载激光雷达：…"逐项版本，c 款 50%~80% 不再作为航测通用值。

---

### 2.6 表1 以表格形式输出（2026-08-19）

> 用户问"表1 无人机航摄地面分辨率的内容能否正常召回并以表格形式召回？"

#### 现状核对

- **召回层 ✅**：表1 chunk（本地 `9183471e` = RAGFlow `95f7f3a7`，content_type=table）6 行数据完整在库（1:500→5cm×2 类、1:1000→10cm×2、1:2000→20cm×2），检索稳定选中（对比题 C3），证据提取整块保留（§2.12 `_merge_table_rows`），v7 答案已引用输出数值（5cm、20cm [C3]）。
- **表格形式输出层 ❌（本次修复）**：此前答案以文字叙述转述部分数值，未整表输出；且若模型把表1 内嵌进对比表单元格，前端 `markdown.js` 会把内嵌表格行误当外层表行解析导致展平错位（用户贴的乱序表格即此形态）。

#### 改动

- `prompts/answer_generation.txt` 新增**规则 29**：被引证据本身是 Markdown 表格且与问题直接相关时，必须在答案中以 Markdown 表格**原样完整输出**（放对比表之后的独立小节并写清表格标题，保留全部表头与数据行），不得只转述部分数值；对比表单元格内用文字概述并保留 [Cx] 引用（如"详见下方表1 [C3]"）；数字/单位/比例尺保持原文。
- `app/core/config.py` + `config/default.yaml`：`prompt_version` v7→**v8**。
- 前端无需改动（独立表格渲染 §2.11 已支持；刻意避免嵌套表格以规避渲染错乱）。

#### 验证

- 配置加载实测 `prompt_version=answer-generation-v8`。
- **需宿主重启 Gateway 后复测**：答案应在对比表之后输出完整的"表1 无人机航摄地面分辨率"独立表格（6 行数据齐全），对比表单元格概述 + [C3] 引用。

---

### 2.7 流程类问题：多规程综合 + 环节全覆盖（2026-08-19）

> 用户问"采用无人机开展松材线虫病监测，从航摄规划到成果归档完整操作流程包含哪些关键环节？"。库内有**两份**规程：茂名市 DB4409/T 60—2024（12页/38chunk）与广东省 DB44/T 2708—2025（16页/42chunk，省标、范围更大更新）。回答只引用了茂名标准且流程不完整（仅 3 环节，缺作业准备/地面验证/成果归档）。

#### 定位

- 证据里其实有省标准 chunk（C5/C6 = 4.1 无人机飞行器/4.2 相机性能指标），但它们是"航拍准备-设备指标"而非流程环节，模型没用是**合理的**；
- 真正问题：单查询路径 `final_limit = min(rerank_top_k=6, max_final_chunks=8) = 6`，30 候选 rerank 后只取前 6，被茂名标准的航摄规划/飞行控制/术语占满——**省标准流程章节（5 航拍作业/6 数据处理/7 现场检验/8 档案管理）与茂名后半部分（6 作业准备/8 数据处理/9 地面验证/10 档案管理）全部未进证据**，模型无从回答完整流程。

#### 改动

1. **① 证据覆盖（`app/services/retrieval_service.py` + `app/core/config.py` + `config/default.yaml`）**
   - 新增 `retrieval.procedure_rerank_top_k: 10`、`procedure_final_limit: 10`（流程/汇总类专用名额）；
   - `execute()` 把 plan 传入 `_execute_single`；procedure/summary 类（`query_type in {"procedure","summary"}`）放宽 rerank top_n 与最终名额；
   - 新增 `_chunk_section`（解析文本头部"章节："行取叶章节）与 `_diverse_by_section`（按"文档+章节"去重，同环节只留重排分最高 chunk；无章节头 chunk 不去重）——确保规划/作业/处理/验证/归档各环节及多份规程各自章节都有代表进证据。
2. **②③ prompt（`prompts/answer_generation.txt` + v9）**
   - 规则 30：证据含同一主题多份规程时，必须说明所依据的规程名称与出处并综合作答，差异分别注明出处，不得只采用其中一份；
   - 规则 31："完整操作流程/关键环节/从…到…"类问题必须按证据中出现的**全部环节逐环节**作答（如航摄规划、作业准备、影像获取、数据处理、地面验证、档案管理），不得遗漏证据中已有环节，也不得补充证据中没有的环节。

#### 测试

- `tests/unit/test_retrieval_service.py` 新增 4 个纯函数用例（章节头解析 ×2、同文档同章节去重、无头 chunk 保留）；4 passed、ruff 通过。
- 配置加载实测：`prompt_version=answer-generation-v9`、`procedure_rerank_top_k=10`、`procedure_final_limit=10`。
- **需宿主重启 Gateway 后复测**：松材线虫问题应覆盖完整流程链（含作业准备/地面验证/成果归档），并说明依据两份规程（省标 DB44/T 2708—2025 + 市标 DB4409/T 60—2024）及各自出处引用。

---

### 2.8 同名不同时间/地点规程：逐份分别输出（2026-08-19）

> 用户实测确认 §2.7 生效（流程 5 环节完整、两份规程均被引用），但回答仍以笼统"依据《无人机监测松材线虫病致死松树技术规程》"表述，未区分两份。用户提出：**同名但时间/地点不同的规程应逐份分别输出、各自标明来源**，这样后续遇到同名不同版本（如 2024 版 vs 2025 版）就不需要系统自行判断新旧取舍。

#### 改动

- `prompts/answer_generation.txt` 新增**规则 32**：证据包含名称相同/主题相同但发布时间或发布单位/适用范围不同的多份规程时，必须**逐份分别输出**——每份单独一节或一列，标明标准号、发布时间、发布单位/适用范围，并分别给出对应内容与引用；禁止合并为"依据《…》"单一表述，禁止自行按新旧/地域取舍。
- `app/core/config.py` + `config/default.yaml`：`prompt_version` v9→**v10**。

#### 验证

- 配置加载实测 `prompt_version=answer-generation-v10`。
- **需宿主重启 Gateway 后复测**：松材线虫问题应分列"广东省 DB44/T 2708—2025"与"茂名市 DB4409/T 60—2024"两份，各自标注来源与内容引用。

#### 实测 v10 仍合并 → 根因与二次修复

- 实测输出仍为"依据《无人机监测松材线虫病致死松树技术规程》"单一表述（v9/v10 两次 prompt 规则均未让模型分列）。验证发现：证据句文本被 `_META_PREFIX` 过滤掉"文档："行，模型仅能从 `EvidenceSentence.document_name` 字段（JSON 对象里）区分，9B 模型未利用 → **纯提示词约束不可靠**。
- **二次修复（确定性增强）**：
  1. `app/services/evidence_extractor.py`：`_with_source`——证据句文本内联来源前缀 `[来源：{document_name}] {句子}`，每条证据自带所属文档标识；
  2. `app/services/answer_generator.py`：user payload 新增 `evidence_documents`（本次证据涉及的全部文档去重列表），配合规则 32；
  3. 规则 32 补充："每条证据文本前的 [来源：…] 标识与 evidence_documents 列表列出了每条证据所属文档，必须据此区分各规程归属"。
- 测试：`test_evidence_extractor.py` +2（来源前缀、无文档名不加前缀），8 passed；ruff 通过。
- **需宿主再重启复测**：预期按"20241217（茂名市 DB4409/T 60—2024）"与"20250721（广东省 DB44/T 2708—2025）"分列输出，各自标注来源与引用。

#### 实测仍合并 → 后端确定性分列（v10 最终方案）

- 来源前缀 + evidence_documents 后仍合并：9B 模型连续 4 次（v8/v9/v10/来源前缀）都无法"同时区分多份相似文档并分列输出"，属模型能力硬限制，prompt 方案到顶。
- **最终方案（确定性）**：
  1. 新增 `app/services/multi_document.py`：`document_title`（书名号标题/去日期）、`titles_similar`（字符 Jaccard ≥0.7 判同名/同主题）、`multi_document_groups`（按 document_name 分组；仅当存在 ≥2 份同名/同主题规程且每组证据 ≥2 句时触发）；
  2. `app/pipelines/rag_pipeline.py`：`_build_validators`（validate 工厂化）+ `_generate_by_document`——触发时**每份规程单独生成一次回答**（各自只看自己的证据，引用编号全局唯一无需重映射），后端按"【文档名】"拼接分列；不涉及新旧/地域取舍；
  3. 触发条件精确：仅同名/同主题多规程才分列，普通多文档问题走原路径。
- 测试：新增 `tests/unit/test_multi_document.py` 7 用例（标题提取/相似判定/分组/不相关不触发/证据不足不触发）；合计 61 passed、ruff、mypy（仅既有挂账）。
- **注意**：分列模式每次回答生成次数 ×规程数（本场景 2 次，耗时约翻倍），属预期。
- **Bug 修复**：首版分列拼接报 500 `An internal error occurred`——两组生成的 `claim_id` 都从 c1/c2… 开始，合并后违反 `StructuredAnswer.claim_id 唯一` 校验（日志：`ValueError, claim_id values must be unique`）。修复：`_generate_by_document` 对第 2+ 组的 claim 加组后缀（`c1_g2` 等）后合并。
- **第二轮实测发现两个问题并修复**：
  1. **省标准流程章节未进证据**：30 候选中省标准 11 个 chunk，但只有 3 个（3.3/4.1/4.2）hybrid 分数高进入 rerank 输入（`rerank_input_k=12` 被茂名标准+其它文档占满），其 5 航拍作业/6 数据处理/7 现场检验/8 档案管理章节从未被重排 → 省标准分节答"未明确说明"。修复：`retrieval_service._balanced_rerank_input`——procedure 问题检测到同名/同主题多规程时，rerank 输入按**文档均衡轮转**（每文档内部章节多样性去重后逐轮取代表），保证各规程流程章节都有机会进入重排与证据。
  2. **无关文档混入分列**：`multi_document_groups` 原逻辑命中相似对后返回**全部**文档组（含常绿果树等无关文档）。修复：只返回**参与相似对**的文档组。
- 测试：`test_multi_document.py` +1（无关文档不进分列）、`test_retrieval_service.py` +1（均衡轮转含省标准章节）；合计 67 passed、ruff。
- **需宿主重启复测**：预期仅两个分节（20241217 茂名 + 20250721 省），且省标准分节应输出完整流程（含航拍作业/数据处理/现场检验/档案管理）。
- **v10 第三轮实测两个问题并修复**：
  1. **非对比问题被模型套用 Markdown 表格**（出现"# 表1/# 表2"且与证据"表1"混淆）：规则 26/29 的表格倾向被 9B 模型泛化。修复：新增**规则 33**——仅对比题用表格、仅表格证据原样输出；"完整流程/关键环节"类问题一律编号列表呈现，禁止 `|…|`/`# 表X`；`prompt_version` v10→v11。
  2. **省标准流程章节仍被裁掉**：均衡轮转把无关文档（常绿果树/河湖/输电线路）也带进 rerank 输入挤掉省标准章节，且 rerank 输出 top-N 裁剪再砍一刀。修复：`_balanced_rerank_input` 仅对**参与同名对**的文档轮转、其余按分数补足；新增 `_balanced_final_chunks`——最终证据基于 rerank 输入按**文档配额**选择（不受 rerank top-N 裁剪影响）；`_similar_document_names` 提取共用。
- **500 修复（claim_id 重复）**：模型两次输出（initial+repair）的 claims 含重复 claim_id → `_parse` 抛 ValidationError → first_answer/repaired_answer 全 None → sanitize 无对象兜底 → 报错。修复：`_parse` 对重复 claim_id **自动唯一化**（加 _2/_3 后缀）；`test_answer_generator.py` +1 用例。
- **完整 pytest（danger-full-access 沙箱放开后首次可跑）**：**360 passed, 3 failed**（3 失败仍为既有解析基线：noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重），无环境性 error；测试基线由 08-18 的 325 提升至 360（含本周全部新增用例）。§1.4 基线同步更新。
- **需宿主重启复测**：松材线虫问题应两个分节、编号列表（无表格）、省标准分节含完整流程；如遇 claim_id 重复报错即为已修复路径。
- **v11 复测：核心目标全部达成，最后一个完整性缺口并修复**
  - 实测：5:5 文档配额（C1/C3/C5/C7/C9=省，C2/C4/C6/C8/C10=茂名），引用归属完全正确（茂名分节 [C8][C10] 确属茂名文档，无跨文档污染）；分列、无表格、无编造。
  - **缺口**：省标准组 5 个 chunk 全是 3 术语/4 航拍准备/5 航拍作业，**6 数据处理/7 现场检验/8 档案管理未进证据**（rerank 输入 12 名额轮转只轮到省标准前 4-5 章），茂名组也缺 7 影像获取/8 数据处理/9 地面验证；且分列模式证据是全局提取后分组（每组证据句不足）。
  - **修复**：① `procedure_rerank_input_k: 16`、`procedure_rerank_top_k: 12`、`procedure_final_limit: 12`（config + default.yaml），rerank 输入按 16 名额均衡轮转；② pipeline 分列模式新增 `_evidence_by_document`——按文档对 generation_chunks 分组、**每组独立提取证据**（每组享有完整证据句预算），确保每份规程流程后半段（数据处理/验证/归档）也有证据。
  - 完整 pytest：**360 passed, 3 failed**（3 失败仍为既有解析基线）。
- **需宿主再重启复测**：省标准分节应出现"数据处理/现场检验/档案管理"等流程后半段环节。
- **v11 复测仍缺省标准流程后半段 → 放大名额**：12 选中（6:6）后省标准 6 个 chunk 全为 3 术语/4 航拍准备(4.1/4.2/4.3/4.4)/5 航拍作业，**6 数据处理/7 现场检验/8 档案管理在轮转顺序第 7-9 位，12 名额（每文档 6）轮不到**。修复：`procedure_rerank_input_k: 22`、`procedure_rerank_top_k: 16`、`procedure_final_limit: 18`（每文档约 9 个名额，覆盖省标准全部流程章节）。配置实测加载生效；ruff 通过。
- **需宿主再重启复测**：省标准分节预期含航拍准备/航拍作业/数据处理/现场检验/档案管理完整流程链。
- **v11 复测：省标准流程后半段仍缺 → 根因在候选池而非名额**：核实 RAGFlow 文档后确认省标准 6 数据处理(6.1/6.2)/7 现场检验(7.1/7.2)/8 档案管理 chunk **在库完整存在，但从未被 30 候选召回**——混合检索按相关度取前 30 时被设备/准备章节挤掉，**提高 rerank/final 名额无效**。修复：`retrieval_service._supplement_same_named_documents`——同名多规程时对每份参与文档用 **document_ids 过滤单独检索一次**（复用 `_filter_reason` 过滤与去重），流程后半段章节必然进入候选，再由均衡轮转选择。完整 pytest 360 passed。
- **需宿主再重启复测**：省标准分节应含数据处理/现场检验/档案管理环节。
- **新问题诊断：单主题流程问题证据碎片化（通用缺陷）**：用户问"无人机飞防作业从药剂配制到作业后废弃物处理完整流程有哪些关键操作要点？"，回答只有配制+废弃物处理两个端点。定位：`query_type=procedure` 正确、18 名额，但 `_balanced_final_chunks` 无同名对时回退 `_diverse_by_section` 按"文档+章节"去重——**18 个名额被 18 份不同文档各占 1 个**（马铃著/胡萝卜/常绿果树/河湖巡查/播种治沙全进来），相关规程（马铃著/飞防农作物/农业植保安全）流程章节占不满名额。修复：新增 `_single_topic_final_chunks`——按文档最高分排序，**与最高分文档分数 ≥70% 的文档视为同主题**，其流程章节优先轮转占满名额，明显无关文档仅在富余时进入；`_balanced_final_chunks` 无同名对分支改用它。
- 测试：`test_single_topic_final_chunks_prefers_relevant_documents`（高相关文档优先、无关不占名额；首版因无关文档分数偏高被误入 primary，加 70% 比例阈值修正）。完整 pytest **361 passed, 3 failed**（既有解析基线）。
- **需宿主再重启复测**：飞防问题应集中在高相关规程的流程章节（药剂配制→装载→飞防作业→清洗/废弃物处理），不再混入常绿果树/河湖等无关文档。
- **内业产出补全（用户确认）**：用户问"开展地质灾害无人机航测调查…内业解算阶段核心产出有哪些成果？"——系统输出流程六阶段正确，但内业产出缺**空三加密成果**（8.1，表2）与**三维点云数据**（8.5，las/ply/vtk），且"表面模型"术语混入。本地核查 8.1/8.5/10 成果格式章节均完整在库，属候选池未覆盖（流程后半段章节被前半段挤掉）。修复：`_supplement_same_named_documents` 扩展为 `_supplement_process_documents`——文档级补充检索的目标从"仅同名多规程"扩展到**单主题流程问题的最高相关主文档**（按 hybrid 最高分判定），确保其流程全程章节（含 8.1/8.5/10）进入候选。完整 pytest 361 passed。
- **注意**：复测前 RAGFlow 曾僵死（ReadTimeout，已知偶发），需先恢复 RAGFlow 再重启网关。
- **复测：证据已补全但生成端漏列**：文档级补充检索生效（18 选中含 8.1 空三 C12、8.5 点云解算 C13、8.5.1.4 C8、8.4 实景三维 C4、10 成果提交 C10），但模型输出仍只列 DOM/DEM/实景三维，**未用证据中的空三加密成果与三维点云**，且"表面模型"术语混入——问题在生成端组织。修复：prompt 新增**规则 34**——"核心产出/成果有哪些"类问题必须**逐项列出证据中出现的全部成果类型**（不得遗漏、不得用"等"带过），成果名称以证据原文为准（不得把"数字高程模型"写成"表面模型"）；`prompt_version` v11→**v12**。配置实测加载生效。
- **需宿主重启 Gateway 复测**：内业解算产出应逐项列出空三加密成果/DOM/DEM/实景三维/三维点云，术语与证据一致。
- **v12 复测：模型张冠李戴（非编造）**：输出把"影像资料、控制点、地形图、交通图、行政区划图、地质灾害分布图"列为内业产出——核对 C3 后确认这些词**真实存在于证据**，但来自 **5 作业准备/5.2 前期资料收集**（作业前收集的辅助数据），非内业产出；内业真实产出（空三/DOM/DEM/实景三维/点云）在证据里（C12/C13 等）却未用。属"环节归属错乱"。修复：
  1. `evidence_extractor._with_source` 增加**环节前缀** `[来源：…] [章节路径] 句子`（`_chunk_section_leaf` 解析"章节："行压缩空格）；
  2. 规则 34 强化：**成果/产出只能来自与问题所指环节对应的证据**（内业产出只来自"数据处理/成果提交"章节；"作业准备/前期资料收集"的地形图/交通图/行政区划图等辅助数据不是内业产出，禁止列入）；必须依据证据前的"[来源：…] [章节]"标识判断归属。
  - `prompt_version` v12→**v13**。测试 27 passed（含前缀格式更新）、完整 pytest 361 passed。
- **需宿主再重启复测**：内业产出应逐项列出且归属正确（空三/DOM/DEM/实景三维/点云），不再混入作业准备辅助资料。
- **新场景诊断与修复：跨文档多维度综合题**：用户问"同时使用 5G 低空基站与自动机巢开展河湖常态化无人机巡查，空域通信、机巢运维、数据归档三类要求如何协同匹配？"——回答结构对（三类分节）但**内容张冠李戴**：16 个选中 chunk 全部来自《低空5G通信基站建设要求》（30 候选 28/1/1，**河湖巡查 0 个**），"机巢运维/数据归档"节内容实为基站运维/基站规划内容，河湖内容缺失。根因：查询含"5G 低空基站"强关键词，且含"如何"被判 procedure 单查询，5G 基站文档垄断候选池。修复：
  1. `query_analyzer._classify`：在 procedure 判定前识别"…X、Y、Z 类要求 + 协同/联动/匹配/配合"（或"同时使用/结合"）→ **multi_hop**；
  2. `query_planner`：新增 `_CATEGORY_ENUM_RE`（"…、…、… N类要求/方面"）与 `_enumerated_dimensions`、`_multi_hop_context`——按方面拆子查询，**每个子查询携带完整主体上下文**（"同时使用 5G 低空基站与自动机巢开展河湖常态化无人机巡查，空域通信"），让机巢/河湖文档有机会被各方面子查询召回。
  - 测试：`test_query_planner.py` +2（三类要求拆方面、两类要求拆方面），16 passed；完整 pytest **363 passed**（3 个既有解析基线）。
- **需宿主重启 Gateway 复测**：该问题应拆 3 个子查询（空域通信/机巢运维/数据归档），机巢与河湖文档进入证据，"机巢运维"引用机巢管理要求（维护保养/运行数据）、"数据归档"引用河湖巡查档案管理/机巢运行数据管理。
- **复测：机巢归位、河湖仍缺 → 文档多样性修复**：v14 复测确认方面子查询生效（query_type=multi_hop、3 子查询、机巢文档 C8 进入、"机巢运维"引用机巢管理要求、空域通信归属正确），但"数据归档"答"未明确说明"——查证河湖巡查要求有 **9 资料归档** 章节且在库，q3 的 30 候选中河湖文档确有（hyb 0.298 排第 3），但 multi_hop 矩阵 round-two 按全局分数填 8 名额被 5G 基站文档垄断，河湖"进了候选没进最终"。修复：`coverage_selector` 对 **multi_hop 矩阵在分数填充前增加"文档多样性"阶段**——每个在任意子查询候选中出现过的文档至少进 1 个代表。测试 +1（5G 基站垄断场景下机巢/河湖文档必须进入），完整 pytest **364 passed**。
- **需宿主再重启复测**："数据归档"应引用河湖巡查 9 资料归档/机巢 7.3 运行数据管理内容。
- **复测：文档多样性生效但河湖代表章节错位 → 环节-章节归属规则**：v14 复测"数据归档"写成了河湖"巡查模式"内容（自动化模式/自动识别，引用 C6 河湖文档）——文档多样性保证"河湖文档至少 1 个代表"，但代表是分数最高的"巡查模式"章节而非"9 资料归档"，q3 方面无对应章节证据时模型用其它内容顶替。修复：prompt 新增**规则 35**——按方面/环节/类别组织的问题，每个方面的内容只能来自**章节与方面对应**的证据（"数据归档"只来自含"归档/档案/运行数据/存储"的章节；"空域通信"只来自含"通信/基站/信号/链路"的章节；"机巢运维"只来自含"维护/保养/运维/巡检/监控"的章节）；**无对应章节证据时必须写"当前资料中未明确说明"，禁止用其它方面内容顶替**。`prompt_version` v13→**v14**。完整 pytest 364 passed。
- **注意**：本会话 RAGFlow 第三次 ReadTimeout 僵死（已知偶发），复测前需恢复 RAGFlow；另"河湖 9 资料归档/机巢 7.3 运行数据管理"章节进证据属于检索精准问题，待 RAGFlow 恢复后按需评估（文档章节多样性补全）。
- **引用-内容脱钩（用户报告，方案 A 实施）**：用户问"结合林业有害生物飞防、松材线虫病遥感监测…如何搭配完成林地病害识别-药剂防治全链条作业？"，回答 18 个引用中 **8 处挂错**（如"空域批准"挂到喷洒无人机设备 C7、"电子围栏"挂到配药水 C13、"48h公布"挂到植保无人机一般要求 C12）——内容大多有据但引用挂错 chunk；数值 grounding 只查数值、非数值事实（空域/围栏/预警）是盲区。修复（用户确认方案 A）：新增 `app/services/claim_topic_validator.py` **`ClaimTopicValidator`**——提取 claim 的中文信息 bigram（过滤停用字/词、数字单位），**缺失比例 ≥50% 判定引用-内容脱钩并拒绝**（触发 repair/sanitize）；通用机制不特化领域；`enforce_claim_topic` 开关接入 pipeline（config + default.yaml）。测试 6 用例（含本案例"空域批准 挂 C7 拒 / 挂 C5 过"、多引用任一命中、短 claim 跳过、未知引用跳过），完整 pytest **370 passed**（3 个既有解析基线）。
- **需宿主重启 Gateway 复测**：该题引用应全部落到与内容匹配的 chunk（空域批准→5.1.1、桶混→6.3.3、电子围栏→7.2.4、48h公布→7.2.5 等），或经 repair 修正后再输出。

---

## 3. 工作原则（沿用，不重复展开）

1. 先读 `docs/agent-handoff.md`，再读目标模块和对应测试。
2. 任何解析修复都必须用真实失败文档回归；不写破坏全局的正则。
3. 用户只有一个上传入口，不重新要求用户选择解析器。
4. 物理页码、印刷页码、条款号分开存储；表格文本、二维结构、图片资产三者都保留。
5. `needs_review` 不能绕过门禁发布；发布前优先 dry-run；删除本地任务不删除 RAGFlow 内容。
6. 修改后至少运行目标测试、完整 pytest 和 ruff；页面改动必须浏览器实测。
7. 不输出或提交密钥；不清除 Docker 数据卷；不把 D 盘数据复制回 C 盘。

---

## 4. 相关文档

- `README.md`
- `docs/agent-handoff.md`（最完整交接入口）
- `docs/pdf-parsing.md`（解析方法讲解）
- `docs/ingestion.md`、`docs/milestone-2.md`、`docs/milestone-3.md`
- `HANDOFF-2026-08-18.md`（本会话起点）
