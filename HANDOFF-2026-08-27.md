# Internal RAG 改动记录（2026-08-27）

> 用途：记录 2026-08-27 文档编制工作台的产品决策、界面调整、实现范围与验证结果，供后续开发和验收使用。

## 1. 今日目标

- 将专业文档编制流程调整为“选择预设大纲 → 用户确认和编辑 → 按确认后的章节逐章生成”。
- 让生成后的大纲能够直接改名、新增和删除章节。
- 让“本节编制要求”可由用户按具体项目修改，并作为章节正文生成的实际输入。
- 支持为章节生成模型接入 OpenAI 兼容 API。
- 改善大纲栏视觉层次及各栏滚动到底后的可见性。

## 2. 产品决策

专业文档通常具有稳定格式，不再把完全自由的大纲生成作为默认路径。工作台采用以下流程：

1. 用户填写文档任务并选择文档类型；
2. 系统加载对应的确定性预设大纲；
3. 用户修改章节标题、编制要求，或新增、删除章节；
4. 章节生成模型严格使用用户最终确认的章节标题和编制要求生成正文。

该设计兼顾专业文档格式稳定性和不同项目的定制需要。重新加载预设大纲仍会提示将覆盖已有章节及正文，避免误覆盖。

## 3. 功能改动

### 3.1 预设大纲

- 新增“文档类型 / 预设大纲”选择项：
  - 智能推荐；
  - 适航符合性文档；
  - 试验大纲 / 验证计划；
  - 技术报告；
  - 通用专业文档。
- 保留原适航符合性 15 章模板和通用 8 章模板。
- 新增试验大纲 / 验证计划 10 章模板。
- 新增技术报告 8 章模板。
- “智能推荐”仅根据文档名称和编制目标选择预设模板，不自由改写模板结构。
- 原“生成编制大纲”按钮改为“加载预设大纲”。

### 3.2 大纲编辑

- 左侧大纲中的章节标题可直接编辑并自动保存。
- 支持新增章节。
- 每章提供悬浮删除操作；删除前确认，并明确该章正文会一并删除。
- 删除当前章后自动选择相邻章节。

### 3.3 本节编制要求

- 原只读说明改为可编辑多行文本框。
- 修改内容随当前文档项目保存在浏览器草稿中。
- 章节生成请求继续使用 `section_guidance` 字段，因此模型收到的是用户修改后的最终要求。
- 新增提示语，明确该字段用于约束专业内容、边界、证据和输出要求。

### 3.4 章节模型 API 接入

- 在“章节生成模型”旁新增“接入 API”入口。
- 支持填写连接名称、OpenAI 兼容 API Base URL 和 API Key。
- 连接成功后读取模型列表、刷新下拉框并自动选中新接入模型。
- API Key 仅保存在当前网关进程内存中。

## 4. 界面与滚动优化

- 大纲栏改为目录卡片风格：两位章节编号、状态副标题、当前章绿色强调和悬浮操作。
- 新增明确的“章节”按钮及“点击标题直接修改”提示。
- 调整大纲栏宽度、留白、阴影、背景与状态视觉层级。
- 修复网格子项被内容撑高导致标题被顶部导航遮挡的问题。
- 大纲、正文、项目列表和右侧检查栏改为独立滚动区域。
- 增加底部安全留白、稳定滚动条占位和滚动边界约束，确保最后一章、底部按钮和“下一步”内容可见。
- 优化滚动条宽度、颜色及悬浮样式；移动端继续使用自然页面滚动。

## 5. 改动文件

- `app/web/documents.html`
- `app/web/static/documents.js`
- `app/web/static/documents.css`
- `tests/unit/test_document_workbench.py`
- `HANDOFF-2026-08-27.md`

## 6. 验证

- 使用浏览器生成 15 章适航大纲，确认目录样式、选中态和内部滚动正常。
- 实测大纲最后一章可见，底部保留安全间距。
- 实测项目设置页、章节编辑页和右侧质量检查栏均可滚动到底。
- JavaScript 通过 `node --check`。
- 文档工作台单元测试通过：`2 passed in 3.49s`。
- 浏览器回归选择“试验大纲 / 验证计划”后生成 10 章，编制要求修改值与选择的预设类型均正确保留，控制台无错误。

## 7. 运行监控：RAGAS 校准评测重构

### 7.1 交互结构

- 将原有单张混合表单重构为“选择评测内容 → 确认评测配置 → 运行评测”三步式工作区。
- 新增“单题评测”和“批量校准”模式页签。
- 单题模式明确区分问题与可选标准答案，并实时提示可计算指标为 2/4 或 4/4。
- 批量模式读取内置基准题集，支持题目搜索、前 N 条评测或勾选多条指定题目。
- 为多题选择扩展 `RagasRunRequest.question_ids`，并在 `build_benchmark_cases()` 中支持按多个 ID 组装用例。

### 7.2 模型设置

- 将回答模型、判题模型、API 接入和连接移除收进独立“评测模型设置”弹窗。
- 在主操作区仅显示当前回答模型和判题模型摘要，降低配置噪声。
- 增加回答模型、判题模型职责说明。
- 按用户决定彻底移除界面中的“保存到 `.env`”选项。
- 所有界面发起的模型切换固定使用 `persist: false`；API Key 和模型连接仅在当前网关进程内生效。

### 7.3 运行状态与结果

- 增加启动前摘要：评测方式、用例数量和可计算指标数量。
- 进度区增加生成回答、整理上下文、RAGAS 判题、汇总结果四阶段提示。
- 统一成功、失败、取消和配置错误反馈样式，并提供可执行的排查提示。
- 四项指标卡增加中文解释、有效样本数以及“表现良好 / 需要关注 / 建议排查 / 无法计算”分级。
- 用例明细支持全部、低分、失败筛选以及题目/编号搜索。
- 用例可展开查看系统回答、标准答案、检索上下文和评测状态；为此评测结果增加 `answer`、`ground_truth` 和 `contexts` 字段。

### 7.4 改动与验证

- 修改：`app/web/monitor.html`、`app/web/static/monitor.css`、`app/web/static/monitor.js`、`app/api/v1/monitor.py`、`app/services/ragas_evaluator.py`、`tests/unit/test_ragas.py`、`tests/unit/test_monitor.py`。
- 浏览器确认批量模式加载 12 条基准题，可取消“前 N 条”并勾选指定题目；单题填写标准答案后摘要变为 4/4 项；模型设置弹窗正常；页面不存在 `ragasPersist`；控制台无错误。
- 自动化测试：`59 passed in 14.12s`。
- 根据界面复查反馈移除“最多评测”选择器：批量模式默认使用全部可评测用例，取消全选后可手动勾选多条题目。
- 修复回答模型/判题模型设置弹窗：消除 460px 容器与 540px 表单宽度冲突导致的横向溢出和内容裁切；弹窗现为 560px 自适应宽度，点击不同入口会聚焦并强调相应模型选择项。
- 修复后再次验证：弹窗 `clientWidth` 与 `scrollWidth` 均为 560px，回答模型和判题模型入口焦点正确，页面不存在“最多评测”和 `.env` 选项，控制台无错误；自动化测试 `59 passed in 14.11s`。

## 7. 字数硬门禁降级为软性建议（2026-08-27）

### 7.1 背景与决策

原章节质量门禁把“字数低于 350/550/800（brief/standard/detailed）”与必需要素缺失、范围不当收缩等同为硬性失败条件；门禁不过时前端直接丢弃生成的正文。这带来三个问题：

1. 与“证据不足时不得凑字，应明确列出缺口”的诚实原则自相矛盾——证据薄时如实写短并列出缺口是正确输出，却被字数线惩罚；
2. 把“长度”当成“质量”的代理指标，短而扎实的章节被整段丢弃；
3. 表格/矩阵类章节（如“符合性验证矩阵”）与叙述类章节共用同一字数线，口径不匹配。

决策：**字数从硬门禁降级为软性建议，门禁只拦内容实质问题**（必需要素缺失、范围不当收缩）；并按章节类型区分口径——表格类章节不设字数建议线，只看要素覆盖。

### 7.2 实现

- `app/services/document_section_generator.py`：
  - `_quality_gate` 拆分为两级返回 `(hard_passed, char_count, target_chars, hard_warnings, advisory_warnings)`；必需要素/范围收缩进入 `hard_warnings`（阻断），字数不足仅进入 `advisory_warnings`（建议）。
  - 新增 `TABLE_LIKE_SECTIONS`（当前含“符合性验证矩阵”）与 `LENGTH_BASE_CHARS`；新增 `_length_target(request, evidence_sentence_count)`：叙述类章节按证据量缩放建议篇幅（证据 ≥12 句为基准值，不足按比例下调、下限 200 字），表格类章节返回基准值仅作展示。
  - `_authoring_instruction` 提示词同步去矛盾：改为“证据充分时建议篇幅约 X 字；证据不足时如实列出缺口，不凑字、不虚构”；表格类章节提示“以要素完整覆盖为准，不以字数衡量篇幅”。
  - 状态语义不变：仅硬性门禁不过时才把 status 降为 `PARTIALLY_ANSWERABLE`。
- `app/schemas/documents.py`：响应新增 `advisory_warnings`；`coverage_warnings` 语义收敛为硬性警告；`target_min_chars` 注释改为“建议篇幅（软性）”。
- `app/web/static/documents.js`：生成正文只要有内容就写入草稿（不再因门禁失败丢弃）；软性建议以“建议：…”追加在状态提示后；硬性警告提示“正文已保留供人工核对”，由作者人工决定是否采纳。

### 7.3 验证

- 定向单测 `test_document_section_generator.py` + `test_document_workbench.py`：**12 passed**（含新增：短而要素完整的章节仅软性提示；表格类章节忽略字数；长度目标随证据量缩放）。
- Ruff 通过；`node --check` 通过。
- 全量 `tests/unit` 回归结果见下方（后台运行中或已完成）。

## 8. 结构文件生成初始大纲（2026-08-27）

### 8.1 背景与决策

用户反馈：文档编制中上传了结构文件（如 `1.范围；2.系统分级与配置基线；…10.交付文档`），但生成的大纲是"编制说明、依据文件与验证要求、试验对象与构型…"等预设章节名。诊断结论：

1. 选"智能推荐（auto）"时，`buildOutline` 按文档名称/目标正则推断类型，命中"测试/试验"类词即套用 `TEST_PLAN_OUTLINE`——章节名与预设完全对应，**确实是智能推荐导致**；
2. 但更根本的问题：`loadTemplate` 此前只把上传文件存为 `project.template = { name, truncated, preview }`（仅文件名/预览展示），**从不参与大纲生成**；`buildOutline` 只读 `OUTLINE_PRESETS`。结构文件这条链路从未接通，无论是否选智能推荐都不会按结构生成大纲。

决策：**打通"结构文件 → 初始大纲"链路**，预设大纲保留为兜底。上传结构文件后自动解析编号章节并（确认后）替换当前大纲。

### 8.2 实现

- 新增 `app/web/static/outline_parser.js`（UMD 纯函数，浏览器/Node 通用）：`parseOutlineFromText` 按 分号(全/半角)/换行 分段，识别 `数字 + [.、．)）]` 编号章节；标题去首尾空白与尾部句号；丢弃 `1.5 小节` 这类嵌套编号段；保留 `2024年清单` 这类数字开头标题；去重保序。
- `app/web/documents.html`：引入 `outline_parser.js`（在 `documents.js` 前）；格式模板卡片改名为"结构文件 / 格式模板"，文案说明"上传结构文件可直接生成初始大纲；未识别章节时仅保存为格式模板"；"加载预设大纲"按钮保留。
- `app/web/static/documents.js`：`loadTemplate` 解析出 ≥2 个章节时，当前大纲为空则直接替换、非空则弹窗确认后替换（每章 `guidance` 默认用统一文案，后由第 9 节升级为按章节主题自动生成）；识别失败回退为仅保存格式模板并提示。`buildOutline`/智能推荐逻辑不变，作为无结构文件时的兜底。
- 测试：新增 `tests/web/outline_parser.test.cjs`（7 个用例，node:test）；`tests/unit/test_document_workbench.py` 增加 outline_parser 静态资源、页面引入与提示文案断言，并更新 hero 文案断言。

### 8.3 验证

- `node --test` 前端 13 个用例全过（parser 7 + markdown 6）；`node --check` 通过。
- 用用户上传的真实结构文件（`1.范围；2.系统分级与配置基线；…10.交付文档`）端到端解析：**正好 10 章**，标题与文件一致。
- pytest 定向：`test_document_workbench.py + test_document_section_generator.py` **12 passed**；Ruff 通过。

## 9. 编制要求自动生成（2026-08-27）

### 9.1 背景与决策

结构文件生成大纲后，每章"本节编制要求"此前只有统一默认文案，逐章编写成本高。用户确认采用三层方案：

1. **提取层**：结构文件条目自带说明（`标题——说明` / `标题（说明）`）直接作为该章初始编制要求；
2. **规则层**：按章节标题关键词匹配主题模板（与预设大纲编制要求粒度对齐），纯前端确定性生成，即时生效；
3. **模型层**：用章节生成模型为全部章节批量生成或单章优化编制要求（结合文档目标、结构文件摘录），结果可编辑、可回退。

### 9.2 实现

- `app/web/static/outline_parser.js`：新增 `splitTitleAndGuidance`（拆破折号/括号说明）、`suggestGuidance`（15 组标题关键词→编制要求映射）、`parseOutlineSections`（编号段 → 每章附编制要求：说明优先 → 关键词映射 → 空）。
- `app/web/static/documents.js`：`loadTemplate` 改用 `parseOutlineSections`，每章 `guidance = 提取/映射结果 || DEFAULT_GUIDANCE`；新增 `generateAllGuidances`（大纲栏"编制要求"按钮，批量确认后应用）与 `optimizeSectionGuidance`（编辑页"优化本节"按钮，单章应用）；共用 `suggestGuidances` 调后端。
- `app/schemas/documents.py`：新增 `SectionGuidanceItem`、`GuidanceSuggestRequest`（含 `template_preview`、`sections`、可选 `model`）、`GuidanceSuggestResponse`。
- `app/services/document_section_generator.py`：`_resolve_model` 类型放宽到两类请求；新增 `suggest_guidances`（构造 JSON Schema 调模型 → 按输入标题映射回填 → 未生成章节保留原 guidance；JSON 无效抛 `GUIDANCE_SUGGEST_INVALID_JSON`，全空抛 `GUIDANCE_SUGGEST_EMPTY`）与 `_guidance_instruction`（要求模型不改章节标题、不新增章节、只输出 JSON）。
- `app/api/v1/documents.py`：新增 `POST /sections/guidance-suggest`。
- `app/web/documents.html`：大纲头新增"编制要求"按钮（`generateGuidance`）；本节编制要求区新增"优化本节"按钮（`optimizeGuidance`）。

### 9.3 验证

- `node --test` 前端 **18 个用例全过**（parser 12 + markdown 6），含用户真实 10 章结构全部命中关键词映射、破折号说明优先于映射等；`node --check` 通过。
- pytest 定向 **15 passed**（新增：模型输出按输入标题映射回填、模型缺章时保留原要求、非法 JSON 抛错）；Ruff 通过。
- 全量 `tests/unit` 回归结果见下方。
