# Internal RAG 解析质量改进交接（2026-08-10 之后）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-10.md` 之后）的工作。项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-10.md`（已覆盖：CCAR-23-R3 ToUnicode 修复生产验证、图片图名与正文混合修复、CCAR-27-R2 扫描件低质量 OCR 文本层检测、MinerU OCR 批大小配置化 + 进度回调、GB 42590-2023 PostScript 字形名页排除 vector 表格解析）
- **本会话范围**：自 2026-08-11 起的活动。会话开始时（08-11）尚无新的代码改动；本交接文档按日期逐节追加后续改动/验证记录
- **重要**：本会话的改动/验证记录在下方第 2 节中**按日期逐节追加**（沿用既有格式：问题/根因/修复/验证/改动文件/注意）

## 1. 当前状态快照（2026-08-11）

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`
- 单元测试基线（08-10 实测）：**176 passed, 3 failed**（3 个既有失败与近期改动无关：TOC 检测、跨页 caption 绑定、figure 去重；本会话若新增/修改测试，在此更新计数）
- Git：仓库仍无 commit，整个工程为未跟踪状态；**禁止** `git reset --hard` / `git clean`
- 服务状态：需在宿主机 PowerShell 运行 `deploy\gateway\status.ps1` 等脚本确认（Linux 沙箱无法执行 Windows 二进制）
- 源码最近改动：`2026-08-10`（见 HANDOFF-2026-08-10 第 2 节；08-11 会话开始前无新改动）
- 生产验证记录：08-10 已用最新代码重新解析 `CCAR-23-R3.pdf`（job `3afa24fd…`），route 分布、75 个资产与质量门禁与 08-07 一致，确认 ToUnicode 修复在正常文档上无误判

## 2. 改动记录（按日期逐节追加）

<!-- 后续改动在此追加：### 2.N 类型：标题（YYYY-MM-DD） -->

### 2.1 修复：重复 SAC 图标被 MinerU 识别为表格内容（2026-08-11）

- **问题**：`GB 42590-2023 民用无人驾驶航空器系统安全要求.pdf` 的解析页码 32（PDF 印刷页码 28）中，表 A.2 的“无人驾驶航空器紧急状态及触发方式”行、说明列被错误填入 `SAC`。同一误识别还出现在解析页 22、27。
- **根因**：`SAC` 并非 PDF 正文字形，而是一个小型浅蓝色标识。该标识由两组 220×220 的嵌入式图像对象组成，其中实际绘制对象在第 2–38 页重复出现；其落在表格空白区域时被 MinerU OCR 当作文字/单元格内容。
- **修复**：在 `HybridPdfParser` 调用 MinerU 前生成仅供 OCR 上传的临时 PDF。规则同时满足以下条件才处理：图像解码数据在至少 `max(3, ceil(总页数 × 50%))` 个页面重复，且 PDF 内容流中的实际显示边长不超过 36 pt。处理方式是删除该图像 XObject 的 `Do` 绘制指令，而不是替换像素；因此不会留下白块、破坏表格线，也不会按字符串删除合法的 `SAC`、`UTC`、`EIRP` 等正文缩写。原始 PDF、矢量表格解析和最终资产均不修改。MinerU 的 parser trace 追加记录被抑制的页面和绘制次数。
- **验证**：对原始文件生成的 OCR 临时副本验证，第 2–38 页共移除 37 次水印绘制；解析页 32 的表 A.2 已恢复为空白单元格，正文和表格线保持完整。`tests/unit/test_pipeline.py` 为 **41 passed**；完整 `tests/unit` 为 **243 passed, 3 failed**，3 个失败均为既有的 TOC 噪声、跨页表题绑定、figure 标题去重问题；Ruff 检查通过。
- **改动文件**：`app/ingestion/parsers/hybrid_pdf.py`、`tests/unit/test_pipeline.py`。
- **待做**：当前 job `3b31bd75b9a14230bc2aee0c3b9f007e` 仍保留旧产物（`warning`、`unreviewed`、未发布）。重启 API 服务加载代码后，调用 `POST /api/v1/ingestion/jobs/3b31bd75b9a14230bc2aee0c3b9f007e/retry` 重新解析，再复核第 22、27、32 页。

### 2.2 配置：允许受控处理 300 MiB 以内的 PDF（2026-08-11）

- **问题**：`HB 9103-2007关键特性的波动管理.pdf` 为 17 页、未加密的图像型 PDF，大小 257,293,928 字节（约 245.4 MiB），超过原 `INGESTION_MAX_UPLOAD_BYTES=209715200`（200 MiB）限制，因而在上传阶段被拒绝。
- **修复**：将本机 `.env` 中的 `INGESTION_MAX_UPLOAD_BYTES` 调整为 `314572800`（300 MiB），保留文件大小限制而非无限制放开；无排队或运行任务时重启 Gateway，并通过 `GET /api/v1/admin/config` 确认运行时实际值为 314,572,800。MinerU 保持单请求、4 页处理窗口。
- **验证**：已重新上传并完成 job `683189801bb9499bac31b83d28c73de3`：`completed`，17/17 页，77 chunks，`hybrid_remote`，文本覆盖率 1.0，0 warnings / 0 failures；队列已清空。原件位于 `data/ingestion/originals/cd/cde16803f4752d0f457ebd22cf506b836aaec265ee0ace43980473bb5236c9b8/source.pdf`，待人工审核，尚未发布。
- **改动文件**：`.env`、`HANDOFF-2026-08-11.md`。

### 2.3 修复：OCR 将合并表头展开为重复列（2026-08-11）

- **问题**：`HB 9103-2007关键特性的波动管理.pdf` 的解析页 15、16（附录 B 的过程控制文件样表）将“HB 9103 过程控制文件 共2页 第1/2页”分别重复为 15、19 个表头单元格。
- **根因**：原 PDF 中该文本各只出现一次，且横跨整张旋转表格的顶部。MinerU 未识别 `colspan`，把同一合并单元格错误展开为每列一个等值文本；错误在 `baseline.json` 中已出现，不是分块或前端渲染引入的。
- **修复**：新增通用的首行合并表头恢复规则：仅当首行至少 4 列、所有列非空且文本完全相同时，折叠为首列标题和其余空位；HTML 输出相应写为 `colspan`。正文行不处理，避免丢失实际的重复数据。远程 OCR 表格在合并到统一 IR 前即规范化。
- **验证**：重启 Gateway 并重跑 job `683189801bb9499bac31b83d28c73de3`（第 2 次）：完成状态，17/17 页，76 chunks，文本覆盖率 1.0，0 warnings / 0 failures。解析页 16 的标题在原始 OCR 文本和最终 HTML 中均只出现 1 次，最终 HTML 含 `colspan=19`，首行仅 1 个非空单元格；第 15 页同类标题也已去重。完整单测为 **244 passed, 3 failed**；3 个失败均为既有问题（TOC 噪声、跨页表题绑定、figure 标题去重），Ruff 通过。
- **改动文件**：`app/ingestion/pipeline.py`、`app/ingestion/parsers/hybrid_pdf.py`、`tests/unit/test_pipeline.py`、`HANDOFF-2026-08-11.md`。

### 2.4 改进：异形扫描表格可信度门禁与语义化检索文本（2026-08-11）

- **问题**：`HB 9103-2007关键特性的波动管理.pdf` 虽然在 MinerU 中完成 17/17 页解析，但旧质量门禁只检查“是否存在结构化表格”和 HTML 完整性，未检查表格结构是否可信。因此第 9 页流程图被归类为表格、第 15/16 页旋转宽表的行列/合并关系不可靠时，任务仍以 0 warnings / 0 failures 自动完成。RAGFlow 发布内容还会同时包含 Markdown 表格和完整 HTML，造成单元格文本重复、输入膨胀。
- **修复**：新增通用 `complex_table_fidelity` 诊断，不包含标准号、固定页码、标题白名单或固定列数。诊断基于扫描页 + 远程 OCR、宽表、纵向长边界（疑似旋转）、缺失有效列头、稀疏矩阵、重度 rowspan/colspan，以及“图 N …”标题落入 table block 的类型冲突。高风险页面进入 `table_schema_review`，产生 fail 门禁并阻止自动发布。表格 Chunk 改为检索优先的字段/值文本；只有表头唯一且可靠时才输出 `字段=值`，否则使用中性的 `第N列`，并把连续重复单元格压缩为 `第1-6列=…`，不虚构字段关系。HTML 和表格图片仍完整保存在 `document_ir.json` / assets 供追溯，但 RAGFlow `content` 不再重复附加完整 HTML。
- **验证**：新增定向测试后，`test_pipeline.py + test_ragflow_publisher.py` 为 **55 passed**，Ruff 通过；完整单测为 **250 passed, 3 failed**，仍是既有的 TOC 噪声、跨页 caption 绑定、figure 标题去重问题。Gateway 重启后重跑 job `683189801bb9499bac31b83d28c73de3`（第 3 次）：17/17 页，75 chunks，route=`hybrid_review`，状态=`needs_review`，0 warnings / 1 failure。`complex_table_review_pages=[9,15,16]`：第 9 页命中 `figure_caption_on_table_block`，第 15 页命中扫描宽表/疑似旋转/重度 span，第 16 页命中扫描宽表/疑似旋转/缺失列头结构。三页 HTML 与图片资产均仍保留，任务未发布。
- **改动文件**：`app/ingestion/pipeline.py`、`app/ingestion/parsers/hybrid_pdf.py`、`app/ingestion/publishers/ragflow.py`、`tests/unit/test_pipeline.py`、`tests/unit/test_ragflow_publisher.py`、`HANDOFF-2026-08-11.md`。
- **后续**：当前先解决“错误结构不得静默入库”和“减少 HTML 重复噪声”。若要自动修复第 15/16 页，还需实现表格裁剪图的 0/90/180/270 度候选解析、网格线/交点恢复以及候选评分；在完成跨文档评测集前不应自动放行此类高风险表格。

### 2.5 功能：原生支持 Word DOCX 摄取（2026-08-11）

- **目标**：资料接入台除 PDF 外支持 Word 文档，保持统一 Document IR、Chunk、质量门禁、人工复核与 RAGFlow 发布流程。
- **实现**：新增 `native-docx` 解析器并注册到 `ParserRegistry`。DOCX 直接读取 OOXML，不转 PDF、不调用 MinerU；按 Word Title/Heading/outline level 建立章节路径，保留普通段落、项目符号/编号列表、显式分页符与 `lastRenderedPageBreak` 逻辑页、表格标题、横向/纵向合并单元格、脚注/尾注和嵌入图片资产。表格继续使用检索优先的字段/值语义文本，同时在 IR 中保留 HTML、行列数据和图片。图片缺少替代文字、无法直接提取的 Word Drawing/Chart/SmartArt、嵌套表格会触发抽检警告。
- **安全与边界**：上传阶段校验 `[Content_Types].xml` 与 `word/document.xml`，并限制 OOXML 解压总量（1 GiB）和单个 part（256 MiB），避免仅凭 ZIP 签名放行。正式开放 `.docx`；旧二进制 `.doc` 和含宏 `.docm` 因本机无可靠无损转换链路而继续拒绝。保存和下载时强制使用规范 DOCX MIME，不信任客户端上传 MIME；RAGFlow 上传源文件也按扩展名发送正确 MIME。
- **前端**：文件与文件夹选择器允许 PDF/DOCX 混合批量上传，列表图标区分 PDF/DOCX。PDF 继续内嵌物理页；DOCX 页面详情明确说明逻辑分页并提供原件打开/下载入口。RAGFlow 实际内容预览同步移除不再发送的重复 HTML。
- **验证**：用含 Title、Heading、项目符号、合并表格、图片替代文字和显式分页符的两页 DOCX 做 Word 导出视觉对照，原生解析得到 2 个逻辑页、5 chunks、1 张结构化表、1 个 PNG 资产，0 warnings / 0 failures。Gateway 重启后实际上传 job `590a652a88174c41b0fbf7f860b79b66`，状态=`completed`、route=`docx_native`，解析器状态含 `native-docx`，运行配置允许 `.pdf,.docx`，表格 HTML 保留 `colspan=2`，源文件下载 MIME 正确。定向测试通过；完整单测为 **257 passed, 3 failed**，3 项仍是既有 TOC 噪声、跨页 caption 绑定和 figure 标题去重问题；Ruff 通过。
- **改动文件**：`app/ingestion/parsers/docx.py`、`app/ingestion/parsers/registry.py`、`app/ingestion/parsers/__init__.py`、`app/ingestion/jobs.py`、`app/ingestion/publishers/ragflow.py`、`app/core/config.py`、`config/default.yaml`、`app/web/ingestion.html`、`app/web/static/ingestion.js`、`pyproject.toml`、`tests/unit/test_docx_parser.py`、`tests/unit/test_ingestion_api.py`、`tests/unit/test_ragflow_publisher.py`、`docs/ingestion.md`、`HANDOFF-2026-08-11.md`。

### 2.6 功能：失败质量门禁支持人工确认放行（2026-08-11）

- **需求**：自动质量门禁仍应拦截不可信结果，但审核人员完成 Chunk 修订并核对原件后，需要有独立操作确认内容无误，而不是被 `needs_review` 永久阻止发布。
- **实现**：审核面板新增“人工确认无误”按钮，仅在未发布的 `needs_review` 任务上显示并启用。点击后先展示风险确认提示，再以 `quality_gate_override=true` 和 `approved` 状态提交。普通“审核通过”仍不能绕过失败门禁。后端保留任务的 `needs_review` 状态与原 QA fail，不篡改自动检测结论；审核记录新增 `quality_gate_override`、失败门禁完整快照及确认时间，并继续使用 `reviewer` 记录确认人。只有审核状态为 `approved` 且 override 快照有效时，发布与批量发布才接受该任务。
- **失效规则**：人工放行绑定当前解析与编辑版本。再次编辑任意 Chunk 或重新解析时，现有审核重置为 `unreviewed`，override 同步清空，必须重新检查和确认。失败门禁不存在、非 `approved` 状态滥用 override、处理/发布中的任务均由后端拒绝。
- **验证**：接口/UI 定向测试 **23 passed**；完整单测 **259 passed, 3 failed**，3 项仍为既有的目录 OCR 噪声、跨页表题绑定、figure 标题去重问题。Ruff 与前端 JavaScript 语法检查通过。Gateway 已在队列空闲时重启，`/health` 返回 `ok`，生产接入页已包含 `overrideReviewButton`。HB 9103 job `683189801bb9499bac31b83d28c73de3` 仍保持 `needs_review / unreviewed / not_requested`，没有代替用户执行人工确认或发布。
- **改动文件**：`app/schemas/ingestion.py`、`app/api/v1/ingestion.py`、`app/ingestion/jobs.py`、`app/web/ingestion.html`、`app/web/static/ingestion.js`、`app/web/static/ingestion.css`、`tests/unit/test_ingestion_api.py`、`docs/ingestion.md`、`HANDOFF-2026-08-11.md`。

### 2.7 性能与稳定性：RAGFlow 分块批量发布及事件循环解阻塞（2026-08-11）

- **问题**：资料接入台原先对每个 Chunk 单独调用一次 RAGFlow 新增分块接口。RAGFlow 又在 Quart 主事件循环中同步执行向量化和 Elasticsearch `refresh=wait_for` 写入，单块通常约 1 秒。连续发布会让同一事件循环长时间无法响应健康检查及其他 API；容器仍显示 `Up`，但 `/api/v1/system/healthz` 超时，Nginx 随后出现 499，表现为“RAGFlow 掉线”。现场 Elasticsearch、MySQL、Redis 均健康，未发现 OOM，故瓶颈不在基础存储或容器内存。
- **RAGFlow 修复**：新增 `POST /datasets/{dataset_id}/documents/{document_id}/chunks/batch`，单批上限 32。一个批次只初始化一次 embedding model，将文档名和全部 Chunk 一次性向量化，并用一次 Elasticsearch bulk 写入和一次文档计数更新完成持久化。CPU/同步 I/O 工作通过 `asyncio.to_thread` 移出 Quart 事件循环；原单块接口也复用该非阻塞实现，兼容已有客户端。批内重复正文会在写入前拒绝，避免相同内容哈希覆盖 Elasticsearch 文档却重复累计计数。
- **资料接入台修复**：发布器先按 RAGFlow 已有内容和当前任务内容去重，再按配置批量发送；默认 `RAGFLOW_PUBLISH_BATCH_SIZE=16`，允许范围 1–32。批次成功后仍逐 Chunk 回调进度，保持前端进度与断点恢复语义。失败重试仍依靠原有幂等检查，不会重复写入已经存在的 Chunk。
- **部署恢复**：旧 RAGFlow 进程已因事件循环永久阻塞而无法正常重启，最终重启 Docker Desktop 后使用项目的 `deploy/ragflow/start.ps1` 恢复服务。当前容器重新挂载 `internal-rag-ragflow-*` 原正式数据卷和自定义 API 文件，原 4 个知识库保留；没有将临时空卷替换为正式卷。Gateway 随后重启并加载批量发布配置。
- **验证**：真实 RAGFlow 临时知识库中一次发布 16 个唯一 Chunk 耗时 **2.482 秒**；发布期间以 50 ms 间隔执行 24 次健康探测，**24/24 成功**，最慢 0.615 秒。旧链路同等数量约需 16 秒且会饿死 API。临时知识库已删除。上线后再次连续探测 10 次，全部 HTTP 200（18–55 ms）；Gateway `/health` 为 200，RAGFlow、Elasticsearch、MySQL、Redis、MinIO 容器均运行正常。运行日志记录 16 块批次服务端耗时约 2.26 秒，未见新增 499、ERROR 或 Traceback。
- **测试**：发布器及接入 API 定向测试 **32 passed**；完整单测 **260 passed, 3 failed**，3 项仍为既有的目录 OCR 噪声、跨页表题绑定和 figure 标题去重问题。资料接入台 Ruff、RAGFlow 改动文件 Ruff 致命错误检查及 Python 编译检查均通过。
- **改动文件**：`runtime/ragflow-0.26.4/api/apps/restful_apis/chunk_api.py`、`app/ingestion/publishers/ragflow.py`、`app/core/config.py`、`config/default.yaml`、`.env.example`、`tests/unit/test_ragflow_publisher.py`、`HANDOFF-2026-08-11.md`。
