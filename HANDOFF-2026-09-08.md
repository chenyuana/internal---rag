# 2026-09-08 改动记录

## `23-46-finalRule26324.pdf` 第 10 页表格关联条款修复

### 问题与原因

第 10 页表格的关联信息错误显示为 `§23.813 Emergency exist access.`。核对原 PDF 版面，表格实际位于右栏 `§23.815 Width of aisle.` 标题之下，属于段落 (b)。

OCR 将该标题输出为 `§ 23.815Width of aisle.`，丢失了条号与标题之间的空格。扫描法规解析器的 `english_heading()` 要求条号后有空白，因此把该标题当作普通段落，未更新章节栈。后续表格及检索分块由此继续继承上一节 §23.813。此次错误的直接原因是标题漏识别，不是原文条款归属或多栏阅读顺序。

### 通用修复

修改文件：`app/ingestion/parsers/scan_regulatory.py`。

- 在 `english_heading()` 中对 OCR 粘连标题补充条号与标题之间的空格，再执行原有标题识别。
- 仅处理文本开头的明确 `§` 标记、数字条号与首字母大写且后接至少两个小写字母的标题单词；不依赖文件名、页码或具体条号。
- 不放宽任意数字与字母之间的边界，避免把行内引用、小写正文、段落后缀或单字母条号后缀误提升为标题。
- 修正该模块的导入排序，使 Ruff 检查通过。

新增测试文件：`tests/unit/test_glued_section_heading.py`。

- 3 组不同条号与标题的参数化测试，验证标题识别、表格章节继承及检索分块关联。
- 5 组反例，验证行内引用、小写正文、段落后缀、缺少明确标记和单字母后缀不会被此次修复提升为标题。

### 实际输出更新

- 任务 ID：`d619d1c4b420404a956ccde7693b73e1`。
- 原始 PDF：`D:\internal-rag\data\ingestion\originals\a4\a4f7ecc693790d20f36ee84911d3c3d52b8d08c12d914c85240bb7e91e7dbef5\source.pdf`。
- 输出目录：`D:\internal-rag\data\ingestion\outputs\d619d1c4b420404a956ccde7693b73e1\23-46-finalRule26324__a4f7ecc6`。
- 修复前备份：`D:\internal-rag\tmp\section-heading-26324-before`。

使用第 10 页原始 OCR 缓存执行 `reprocess_page(..., 10, "clean")`，重新构建文档章节和分块，再通过 `write_document()` 写回原输出目录。执行时将远程 OCR 方法替换为失败断言，确认此次重建没有调用远程 OCR。

第 10 页的 1 个表格块及其对应的 1 个检索分块，`section_path` 末级均已更新为 `§ 23.815 Width of aisle.`；写回后重新读取 `document_ir.json` 验证成功。段落 (b) 是原文归属判定，本次未宣称新增或修复 `article_id` 元数据。

任务仍处于待审核状态，未发布到 RAGFlow 或重建外部索引。本次处理范围是关联信息，不代表表格数据已通过人工审核。

### 验证结果

- `pytest tests/unit/test_glued_section_heading.py tests/unit/test_page_reprocess.py -q`：24 项通过。
- `pytest tests/unit/test_glued_section_heading.py tests/unit/test_scan_regulatory.py -q`：34 项通过、1 项失败。
- 失败项为 `test_long_tables_split_on_rows_without_losing_footnotes`，断言长表应产生多个块，但实际为 1 个块。在进程内恢复修改前的 `english_heading()` 后单独重跑，仍出现相同失败，确认不是此次标题修复引入。
- `ruff check tests/unit/test_glued_section_heading.py app/ingestion/parsers/scan_regulatory.py`：通过。

## 追加：第 10 页表格合并行、数字粘连与表头修复

### 原因与原文核对

用户指出表格数据仍显示为 `10 or fewer ....11 through 19 ... | 11212 | 1520`。这是独立于章节关联的 OCR 结构问题：两个数据行被压入一个 HTML 行，两层跨列表头也未正确用于语义化，导致字段退化为 `Column 1/2/3`。

核对原页像素，正确内容如下。宽度单位均为英寸，第一行的上标 `1` 是脚注标记，不属于数值。

| Number of passenger seats | Less than 25 inches from floor | 25 inches and more from floor |
| --- | --- | --- |
| 10 or fewer | ¹12 | 15 |
| 11 through 19 | 12 | 20 |

原表总表头为 `Minimum main passenger aisle width (inches)`。脚注说明在经试验论证并获得 Administrator 批准时，可以采用不小于 9 英寸的较窄宽度。

### 修复方式及泛化边界

- `app/ingestion/parsers/table_recovery.py`：增加 `merged_numeric_rows` 通用检测。当一个标签单元格出现多个点引导行标签，旁边又有至少三位连续数字时，标记结构歧义，不猜测数字切分。
- `app/ingestion/parsers/verified_tables.py`：按已目视核对的原页修复本表。校正受 PDF SHA-256、页码及原始单元格内容三重约束，其他文件不能继承这些数值。恢复两行数据，将两层表头展平为三个含完整含义及单位的列标题，并以 `¹12` 保留脚注标记。
- `app/ingestion/parsers/scan_regulatory.py`：人工来源核验方法标记改为 `source_visual_review`，避免新校正继续被错误标为 2026-08-31；同步更新既有测试断言。
- 对其他命中通用检测但缺少可验证恢复证据的表格，沿用结构复核流程：保留原始 HTML/文本及源图，暂不输出未经证实的单元格对应关系。

曾对原表裁剪图调用独立行 OCR，但返回空列表，没有可用坐标。因此本表使用明确限定来源的视觉核验校正，不宣称已实现任意数字粘连的自动拆分。

### 写回与验证

- 写回同一任务和输出目录，表格块与检索分块均恢复两个数据行，保留脚注正文和 `§ 23.815 Width of aisle.` 关联。
- 修复前备份：`D:\internal-rag\tmp\26324-before-table-row-repair`。
- 使用缓存清洗写回后重新读取 IR 验证；再次清洗仍保留正确数据，确认幂等。
- 新增 `tests/unit/test_merged_numeric_rows.py`，覆盖不同标签和数值的通用检测、正常表格反例、来源/页码/原单元格约束、正确语义输出及未知来源的待复核处理。
- `pytest tests/unit/test_merged_numeric_rows.py tests/unit/test_table_recovery.py tests/unit/test_verified_tables.py tests/unit/test_glued_section_heading.py -q`：54 项通过。
- 对上述三个解析模块及新增/更新的测试执行 Ruff：通过。
- 未发布到外部索引，仍保留人工审核流程。

## 核查：`23-49-FinalRule.pdf` 第 22–26 页跨页表与 chunk 大小

本次为只读核查，未修改该文档解析结果或分块规则。

- 任务：`4a5cf69e6fce4f5398fab4618f5197df`。
- 输出目录：`D:\internal-rag\data\ingestion\outputs\4a5cf69e6fce4f5398fab4618f5197df\23-49-FinalRule__77b25f8b`。
- 实际路由：`hybrid_remote`，由矢量解析处理第 22–26 页表格。

逐页查看原 PDF，确认五页是同一张 `Regulatory Evaluation Summary`，共三列。第 25 页末的 Section 91.205 行在第 26 页继续，随后 Section 91.209 为同表最后一行。

当前主表保留 44 个数据行和 1 个表头行，统一 table_id 为 `7ce46432ee0168f7a235`，拆为 6 个 chunk。第 26 页最后的 Section 91.209 却被另建为 table_id `13d4b02c50766c989cc2`，伪表名为 `Section 91.205 Powered civil`，唯一数据行被当成表头输出 `Fields:`。因此不能认定当前跨页处理完全正确；此错误不是字符阈值导致的正常分块。完整逻辑表应包含 45 个数据行并使用同一 table_id。

另外，跨第 25–26 页的 Section 91.205 行在 `table_row_pages` 中仅标记为 26，单行级来源范围不完整；当前末块覆盖 25–26 页，暂时掩盖了这一问题。

分块代码证据：`pipeline.py::build_chunks()`、`split_structured_table_block()`，以及 `regulations.py` 的字符常量。该路由采用 Python 字符数而非 token 数：最大值 1400，章节上下文预算为 30，传给表格分块器的预算为 1370。按完整行逐行累积语义化文本，下一行加入后超预算即新建 chunk，每块保留表头、表名和 table_id；单行超限时仍保留整行。常规正文目标 800 字符、重叠 100 字符不用于该结构化表格分支。单页表格可保留至 2400 字符的例外不适用于此跨页表。

当前主表块（数据行数不含重复表头）：

| 页码 | 数据行数 | 最终文本字符数 |
| --- | ---: | ---: |
| 22–23 | 9 | 1283 |
| 23 | 7 | 1277 |
| 23–24 | 7 | 1326 |
| 24–25 | 9 | 1295 |
| 25 | 8 | 1277 |
| 25–26 | 4 | 577 |

异常末行独立块为 122 字符。用当前 `build_chunks()` 对缓存主表重新计算，六块长度、数据行数和页码均与已保存结果一致。

### 追加修复：第 26 页末行并回主表

用户授权修复后，修改 `pipeline.py::merge_cross_page_tables()` 的同页相邻片段合并判断。标题恢复曾把上一行的 `Section 91.205 Powered civil` 提升为末行片段的伪表名，使原有“无标题续表尾部”规则失效。

新规则要求：前序已是跨页表、片段几何相邻且列数一致、片段标题是前一数据行标签的前缀、前后均有可识别且递增的条号，并且新片段不以新的列标题开头。满足这些证据时将片段并入原表，沿用原表名及表头。无文件名、页码或具体条号硬编码。

扩展 `test_merges_titleless_ordered_sections_and_open_tail`，覆盖无标题、前一行标签伪标题，以及独立标题不能合并的反例。

真实 PDF 通过本地原生/矢量解析重建，未调用远程 OCR。结果写回原输出目录：

- 一张跨第 22–26 页的表，表名 `Regulatory Evaluation Summary`。
- 45 个数据行加 1 个表头，六个 chunk 共用一个 table_id。
- Section 91.209 正确并入最后一个 chunk，恢复 Section / Incremental Cost / Benefit 的字段含义，不再输出 `Fields:`。
- 最后一个 chunk 包含 5 个数据行，695 字符，来源第 25–26 页；前五块不变。
- 非表格 chunk 文本逐一比较，全部不变。同步更新任务摘要的 chunk_count。
- 备份目录：`D:\internal-rag\tmp\23-49-before-tail-fix`，包含原输出和 job.json。
- 本次未扩展行级来源的数据模型；Section 91.205 的单行页码仍仅记录 26，完整块的页码范围为 25–26。这一既有局限保留记录。

验证：相关聚焦测试通过；两个模块共 143 项通过、3 项失败，禁用本次新增合并条件后，三个失败项仍全部复现（目录识别、表名引用覆盖及重复表名旧断言）。Ruff 通过。任务仍待审核，未发布外部索引。

## `23-62-finalRule-Docket.pdf` 第 30 页 Table RF3 表头与内容折叠

### 原因

原表具有 Table RF3 表名、两层表头、11 列及 7 个数据行。表号规则仅支持单个英文字母前缀，未识别 RF3；表名行被当成唯一表头，真正的分组表头及叶子列标题进入数据区。语义化因此降级为 `Column 1`，并把相邻相同值压缩为 `Columns 7-8=X`，无法清晰表达各机型适用关系。表名还错误使用了表前整段引导正文。

### 通用修复

修改 `app/ingestion/pipeline.py`：

- 表号及嵌入表名识别支持 1–8 个连续英文字母前缀，覆盖 RF3、AB12，并保持 A.1 等格式兼容。
- 新增 `_grouped_table_header_rows()`，对上层重复分组标签、下层各不相同且含文字的列标题、后续含数字数据行进行组合识别，恢复两层表头。
- 标题行被移出表体后，重新生成与结构化行一致的 HTML，避免旧 HTML 仍带错误表名行。
- 语义化使用“分组 / 列名=值”，各机型的 X 分别输出；真实单元格值不改写。

### 输出与边界

该文件存在两个任务。本次更新较新的待审核任务 `0bc4b9b5b327417292d7f516e2079735`；较早已审批任务 `5ce378d8af2d4b388a3a2852ac6fde42` 的历史版本未修改。

- 输出：`D:\internal-rag\data\ingestion\outputs\0bc4b9b5b327417292d7f516e2079735\23-62-finalRule-Docket__11a47265`。
- 备份：`D:\internal-rag\tmp\rf3-before-header-repair`。
- 仅重建 RF3 表格块、对应 chunk 及第 30 页预览，其他表格和正文块保留。
- 规范表名及关联为 `Table RF3`，11 列、2 个表头行、7 个数据行；完整单页表保持 1 个 chunk，1652 字符，适用单页表 2400 字符保留上限。
- 写回后检查全部 7 个数据行与修复前逐单元格一致，不丢行、不猜改数值。
- 原 OCR 的粘连条号 `23.6323.6723.77`、粘连词和原单元格文本仍保留；本次处理的是表头与列语义折叠，不代表这些转写问题已经校正。
- 未修改外部索引，任务仍待审核。

### 验证

新增 `tests/unit/test_grouped_table_headers.py`：5 项通过，覆盖多字母表号、旧格式兼容、分组字段、重复 X 不折叠、数据行不被识别为表头及正文反例。联合页面重处理、法规预处理与 pipeline 测试：164 项通过、3 项既有失败，与上一节记录的失败项相同。Ruff 通过。

---

## ⚠️ 事故记录：`ingestion.html` 被错误编码破坏（重要，请先读）

### 发生了什么（我的操作失误）
给 `app/web/ingestion.html` 做版本号替换时，用了 PowerShell 的
`(Get-Content -Raw) -replace ... | Set-Content -Encoding UTF8`。本机 PowerShell 的
`Get-Content` 默认编码是 **GB18030/系统 ANSI**，而该文件是 UTF-8：于是“以 GB18030 读回、
再以 UTF-8 写回”，整份 HTML 的中文被二次编码成乱码，且**有损、无法用统一反向还原**
（字节对齐错位，出现孤立字节）。这是本次事故的直接原因。

### 造成的损失范围（仅前端，后端零损失）
**仅前端三件套里“未提交的新版界面功能”丢失**，后端全部完好：

- 丢失（仅存在于未提交工作区，未提交、无 stash、无备份）：
  - `app/web/ingestion.html`、`app/web/static/ingestion.js`、`app/web/static/ingestion.css` 中较新的
    **页面详情 / 单页处理（clean·OCR 重跑）/ 编辑 Chunk** 三块 UI 与前端逻辑。
  - 相关 DOM id 大致包括：`pageDialog*`、`pageReprocess*`、`chunkEdit*`、`recleanPageButton`、
    `reocrPageButton`、`pageActionHint`、`pageSourceFrame`、`pageBlocks`、`pageChunks`、
    `pageCleanedText`、`pageRawText`、`pageDiagnostics`、`pageTrace`、`pageAssets`、
    `pageInspectorSummary`、`qualityGates`、`qualityGrid`、`routeSummary` 等（供重建参考）。
  - git 无法救回：`HEAD:app/web/ingestion.html` 只有 422 字节占位；`ingestion.js` 只有极老首版提交。

### 当前恢复状态
- 已从干净快照恢复三件套：
  `D:\internal-rag\backups\source\internal-rag-project-review-20260813\source\internal-rag\app\web\{ingestion.html, static\ingestion.js, static\ingestion.css}`
  （这是 **2026-08-13** 版本，上传/队列/审核/发布完整可用，但**不含**上述三块新功能）。
- 按用户要求，已把本次“LLM 插件前端开关/配置面板”从这三件套中移除，前端回到干净旧版；
  版本号已还原为 `?v=20260803-2`(css) / `?v=20260811-docx-1`(js)。
- 校验：三个文件 **0 乱码字符**；JS 引用的 **81 个 id 全部存在于 HTML**（不会因缺元素崩溃）。

### 后端（零损失，全部保留，功能可用）
- 新增 `app/ingestion/parsers/llm_structure.py`：`LlmStructureAnnotator` + `apply_events` + prompt。
  关键点：对 `deepseek-v4-flash` 必须传 `thinking:{"type":"disabled"}`，否则预算全花在 reasoning 返回空。
- 新增 `app/ingestion/llm_structure_store.py`：进程内存按用户存储 base_url/model/api_key（脱敏返回尾4位）。
- 改动：`parsers/registry.py`（解析后 annotator 钩子 + set/clear/close）、
  `ingestion/jobs.py`（`create_job(llm_structure_enabled)`、`_parse_with_structure`、`close`）、
  `api/v1/ingestion.py`（`llm_structure_enabled` 表单字段 + `GET/PUT /settings/llm-structure`）、
  `schemas/ingestion.py`（`IngestionJob.llm_structure_enabled`）、
  `core/config.py` + `config/default.yaml`（`LlmStructureSettings` + `ingestion.llm_structure`，env 键 `LLM_STRUCTURE_*`）。
- 另有（本轮更早、独立的）确定性修复，保留：`app/ingestion/pipeline.py` 的 `heading_kind` 增加
  **联邦公报附录边界**识别与 **“Section 23.NNN <动词>…”前言引用句**守卫；对应测试在
  `tests/unit/test_regulation_preprocess.py`。
- 测试：`tests/unit/test_llm_structure.py`（2 passed，纯单元）；后端导入正常；Ruff 通过（我改动的 py 文件）。
- 由于前端开关已移除，上传默认 `llm_structure_enabled=False`，后端插件不生效、不影响现有功能。

### 给后续接手者（GPT）的待办
1. **决定插件去留**：保留后端插件（已可用），或按需移除（删 `llm_structure.py`/`llm_structure_store.py`/
   `test_llm_structure.py`，并撤销 registry/jobs/ingestion/config/schemas 的接线；**不要 `git checkout`**，会误伤其它未提交改动）。
2. **重建丢失的前端三块功能**（若需要）：页面详情、单页处理、编辑 Chunk。旧版前端逻辑已丢，
   需按上面列的 DOM id 重建；参考 git 里 `app/web/static/ingestion.css` 的 `26c6a8f` 提交可拿到旧样式线索。
3. **若要重新加 LLM 前端开关**：后端已有 `GET/PUT /api/v1/ingestion/settings/llm-structure` 与
   上传字段 `llm_structure_enabled`，前端只需加一个 checkbox + 配置面板即可。
4. **教训**：禁止用 PowerShell `Get-Content/Set-Content` 读写 UTF-8 前端文件；一律用编辑工具，或用
   `[System.IO.File]::ReadAllText/WriteAllText(..., UTF8)` 显式指定编码。
