# Internal RAG 解析质量改进交接（2026-08-05 之后）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-05.md` 之后）的工作。项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-05.md`（已覆盖空白记录表、跨页误合并、图题误标、小麦 5 个问题、无编号子节、VLM 阻塞、2 字术语、跨页标题、纯矢量流程图、标准封面）
- **本会话范围**：自 2026-08-06 起的活动，随工作推进逐节追加
- **重要**：`CHANGELOG-rag-improvements.md` 是较早的累积记录（含 08-03 之前内容），本会话新增内容单独记录在本文档，避免混淆

## 1. 当前状态快照（2026-08-06）

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`
- 单元测试基线：**191 passed, 3 failed**（3 个既有失败与近期改动无关：TOC 检测、跨页 caption 绑定、figure 去重）
- Git：仓库仍无 commit，整个工程为未跟踪状态；**禁止** `git reset --hard` / `git clean`
- 服务状态：需在宿主机 PowerShell 运行 `deploy\gateway\status.ps1` 等脚本确认（Linux 沙箱无法执行 Windows 二进制）

## 2. 改动记录（按日期逐节追加）

### 2.1 验证：《应急通信空中基站》封面修复效果复查（2026-08-06）

> 背景：昨天 2.11 修复了《基于系留无人机的应急通信空中基站技术要求》封面页被误判为表格的问题。今天 08:39 用户在宿主机重新处理了该文档（job `f944bc887b844384a7e1f5893987d136`），此处记录复查结果。

#### 处理结果

| 项目 | 结果 |
|---|---|
| 文档 | 20231220：《基于系留无人机的应急通信空中基站技术要求》.pdf（17 页） |
| Job ID | `f944bc887b844384a7e1f5893987d136` |
| 状态 | `warning`（2 项 warn，0 项 fail） |
| 路由 | hybrid_review |
| 封面页（第 1 页） | `native_text` / `page_type=text` / `indexable=True`，**不再被拆成表格 chunk** ✅ |
| 目录页（第 3、4 页） | `toc_excluded`，不进索引 ✅ |
| 空页（第 2、6 页） | `blank_excluded` ✅ |
| 表格页 | 11、12、15 全部结构化；`unresolved_table_pages=[]`、`invalid_table_pages=[]`、`table_caption_mismatch_pages=[]`、`table_html_leak_pages=[]` ✅ |
| 门禁 | `table_merge_fidelity` warn（第 11、12 页合并单元格需人工核对）、`figure_semantics` warn（第 8 页 VLM 已描述） |

#### 结论

昨天 2.11 的封面页修复**已生效**：第 1 页封面标题不再是表格 chunk，`table_pages=[11,12,15]` 均正确。当前 warning 仅为需人工复核项，无 fail 门禁。

#### ⚠️ 遗留问题：封面 chunk 页码范围 1-5，粘连第 5 页公式占位符

复查中发现一个**昨天修复未完全覆盖**的残留问题：

- 第 1 个 chunk（`02a017f1285d02637cc3`，封面内容）的 `page_start=1`、`page_end=5`，文本尾部带 `第 5 页公式原版图像`。
- 根因：第 5 页是「前言」页，但因字体特征被 `_has_math_font` 判为 `formula` 页（`route=remote_ocr`、`page_type=formula`、`image_count=0`），生成整页 `p0005-page-01.png` 资产和 `第 5 页公式原版图像` 占位块（`vector_pdf.py:1408-1421`）。
- 该占位块没有 `section_path`，在 `split_blocks` 中与同样无章节路径的封面段落合并进同一 chunk，导致封面 chunk 页码范围被拉到 1-5。
- 昨天 2.11 修复只解决了封面不被判为表格页，**没有解决封面段落与后续无章节页（第 5 页公式占位块）的粘连**。
- 昨天的 HANDOFF-2026-08-05 第 2.11 节记录的现象「封面 chunk 的 page 范围变成 1-5」在今天重新处理时**仍然存在**（虽然封面已不再被拆成表格）。

**建议修复方向**（尚未实施）：
1. 封面页（`_looks_like_cover_page` 命中）应设 `indexable=False`，不进索引（与目录页一致），或至少在 `split_blocks` 中让封面段落单独成 chunk、不与后续页内容合并。
2. 空文本 / 纯占位的 formula 块（如 `第 N 页公式原版图像`）不应参与普通段落合并；公式占位块应只在有真实公式内容时生成，或标记为 `non_indexable`。
3. 进一步核验 `_has_math_font` 对第 5 页（前言页，无公式）的误判——该页含 CambriaMath 字体（可能是前言中少数数学符号），但页面主体是普通文字。

#### 验证命令（宿主机）

```powershell
Set-Location D:\internal-rag\source\internal-rag
.\.venv\Scripts\python.exe -B -m pytest tests\unit -q -p no:cacheprovider
.\.venv\Scripts\python.exe -B -m ruff check app tests\unit --no-cache
.\deploy\gateway\stop.ps1
.\deploy\gateway\start.ps1
```

### 2.2 修复：《飞行手册编制规范》第9页图1未识别（2026-08-06）

> 用户反馈《无人机系统飞行手册编制规范》第9页"图1 III 类手册的构成"未能正确识别。

#### 问题

- 文档：`20231229：《无人机系统飞行手册编制规范》.pdf`（33 页），job `3c711d606b484790b709448b5f789a37`
- 第 9 页是"图1 III 类手册的构成"——**带外框的文字结构图**（单个大外框 rect + 框内 11 个标签行 + 少量连接线）
- 解析结果：第 9 页被当作普通 `native_text`，`figure_review_pages=[]`、`preserved_figure_pages=[]`，未生成任何 figure 资产，图 1 完全缺失

#### 根因

第 9 页的图形态特征：

| 特征 | 值 |
|---|---|
| `image_count` | 0（无图片对象） |
| `rects` | 1（一个 287×192 的外框） |
| `curves` | 2 |
| `lines` | 8 |
| 图注 | 有（`图1 III 类手册的构成`，`FIGURE_CAPTION_RE` 命中） |

`preflight_pages` 的 figure 判定需要满足：
```python
(image_count and (...)) or (has_figure_caption and pure_vector_flow)
```
- `image_count=0` → 第一个分支不满足
- `has_figure_caption=True`，但 `_flowchart_regions(page)` 返回空 —— 该函数要求 `rects >= 6 and curves >= 4`（纯矢量流程图），第 9 页只有 1 rect + 2 curves，**达不到标准**
- 所以第 9 页未被加入 `figure_pages` → 不进入 vector 解析 → 图 1 未识别

这与昨天 2.10 修复的《外业规范》纯矢量流程图不同：纯矢量流程图是"大量框 + 箭头"，而带外框文字结构图是"单个大外框 + 框内文字"，两者形态不同。

#### 修复（`vector_pdf.py`）

新增**带外框文字结构图**识别，作为流程图之外的第三种无图片 figure 信号：

1. **新增 `_boxed_text_diagram_region(page)`**：识别"单个外框 rect + 框内标签文字 + 少量连接线"的结构图
   - 无图片对象、恰好 1 个 rect、`curves <= 3`、`lines <= 10`（超过则归 `_flowchart_regions` 管）
   - 外框尺寸 ≥60pt 且面积占比在 3%~90%（排除细下划线和整页边框）
   - 框内提取文本长度 ≥6 且至少 3 个标签行（排除孤立强调句）
   - 返回外框 bbox
2. **`preflight_pages`**：`has_figure_caption and boxed_text_diagram` 时加入 `figure_pages`
3. **`parse_pages` figure 资产生成分支**：`_flowchart_regions` 为空但 `_boxed_text_diagram_region` 命中时，按外框裁剪生成整图 figure 资产（`p0009-figure-01`），图注用 `_caption_near` 抓取

#### 验证

- **真实文档全 33 页扫描**：仅第 9 页被判定为带外框文字结构图，且确有图注；**无任何误报**
- 第 9 页外框 `(146.6, 260.1)-(433.7, 452.2)`，面积占比 0.11，框内 11 个标签行（封面/内封和内封背/更改记录/…/正文/附录），图注 `图1 III类手册的构成`
- 回归测试 3 个新增：
  - `test_boxed_text_diagram_region_detects_single_frame_structure_diagram`
  - `test_boxed_text_diagram_region_rejects_plain_text_page_without_frame`
  - `test_preflight_flags_boxed_text_diagram_with_caption`
- 相关测试套件（vector_pdf + pipeline + regulation_preprocess）：**107 passed, 3 failed**（3 个既有失败与本次无关：TOC 检测、跨页 caption 绑定、figure 去重）；`test_vector_pdf.py` 32 项全通过；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | 新增 `_boxed_text_diagram_region`；preflight figure 判定接入；parse_pages 生成外框整图资产 |
| `tests/unit/test_vector_pdf.py` | 新增 3 个回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物
- 新增代码在 Linux 沙箱（Python 3.10）无法跑完整 pytest（项目要求 3.11+）；相关测试用 `--noconftest --basetemp=/tmp/pytest-rag` 验证通过，宿主机需跑完整 `pytest tests/unit`

### 2.3 修复：《飞行手册》第9页 6.2.1~6.2.3.6 章节归属错误（2026-08-06）

> 用户在 2.2 修复后复查发现：第9页 6.2 开始的 6.2.1 到 6.2.3.6 内容"未能正确识别"——实质是**章节归属错误**：内容都在，但 section_path 全部挂在 `['附录']` 下，丢失了真正的父章节 `6 III类手册编制 → 6.2 构成`。

#### 问题

修复前（job `3c711d606b484790b709448b5f789a37` 的 document_ir）：

| block | 修复前 section_path | 应为 |
|---|---|---|
| `6 III类手册编制` | `['5.3 编写要求', '5.3.8 保养、维护及排故']`（上一页残留） | `['6 III类手册编制']` |
| `6.2.1 封面`（第9页） | `['附录', '6.2.1 封面']` | `['6 III类手册编制', '6.2 构成', '6.2.1 封面']` |
| `6.2.3.4 临时更改单记录`（第9页） | `['附录', '6.2.3 前置材料', '6.2.3.4 临时更改单记录']` | 同左但父级为 `6 III类手册编制` |
| `6.2.3.6 目次和图表清单`（第10页） | `['附录', '6.2.3 前置材料', '6.2.3.6 目次和图表清单']` | 同左但父级为 `6 III类手册编制` |
| `6.3 操作员手册(III-1)编制`（第10页） | `['附录', '6.3 操作员手册(III-1)编制']` | `['6 III类手册编制', '6.3 操作员手册(III-1)编制']` |

#### 根因（两个叠加）

**① `6 III类手册编制` 未被识别为顶层条款**
- `HEADING_PATTERNS` 顶层条款正则要求 `数字 + 空格 + 纯中文标题`（`[一-鿿]{2,12}$`）
- `6 III类手册编制` 含罗马数字 `III`，不匹配 → `heading_kind` 返回 None → 被当普通段落，section_path 残留上一页 `5.3.8`

**② 图1框内标签 `附录` 被误判为 annex，劫持章节栈**
- 图1 是带外框文字结构图，`_line_blocks` 把框内标签（封面/内封/更改记录/正文/**附录**）无差别提取为独立 paragraph 块
- `heading_kind("附录")` = `"annex"` → `annex="附录"`、`clause_stack=[]`
- 后续 6.2.1~6.2.3.7 全部挂到 `['附录']` 下
- **注意**：即便修复①让 `6 III类手册编制` 正确识别，只要 `附录` 标签仍在，它仍会清空章节栈（对照组已验证），两个修复缺一不可

#### 修复

**① 顶层条款正则支持罗马数字/括号/拉丁字母**（`pipeline.py`）

`HEADING_PATTERNS` 顶层条款正则从 `[一-鿿]{2,12}$` 放宽为：
```python
r"[0-9IVXLCMivxlcm()（）\-—–－．A-Za-z一-鿿]{2,20}$"
```
- 保留量词开头排除（`3 次重复`/`3 个并联的卷积神经网络` 仍不误判）
- 支持 `6 III类手册编制`、`6 操作员手册(III－1)编制`
- `80±5%`、`6.1 总则`（子条款走另一分支）、`23.2100` 均不误判

**② 图框内标签过滤**（`vector_pdf.py`）

figure 页 `_line_blocks` 提取正文后，若 `_boxed_text_diagram_region` 命中，过滤 bbox 完全落在外框内的 paragraph 块：
```python
if boxed_region:
    bx0, btop, bx1, bbottom = boxed_region
    result.blocks = [
        block for block in result.blocks
        if block.block_type != "paragraph"
        or not block.bbox
        or not (bbox within frame)
    ]
```
框内标签（封面/附录/正文…）是结构图的一部分，由 figure 资产承载，不再作为正文段落参与章节解析。

#### 验证

- **真实 PDF 第9、10页端到端**（模拟 vector 层输出）：6.2.1~6.2.3.4（第9页）、6.2.3.5~6.2.3.7（第10页）、6.3/6.3.1 全部正确嵌套在 `6 III类手册编制 → 6.2 构成 / 6.3 操作员手册(III-1)编制` 下；`blocks under 附录: 0`
- 新增回归测试 5 个：
  - `test_heading_kind_detects_top_level_clause_with_roman_numeral`（pipeline）
  - `test_roman_numeral_clause_is_parent_of_sub_clauses`（pipeline）
  - `test_frame_label_does_not_hijack_clause_stack`（pipeline）
  - `test_parse_pages_filters_boxed_diagram_frame_labels`（vector_pdf）
- 相关测试套件：**111 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | 顶层条款正则放宽（支持罗马数字/括号标题） |
| `app/ingestion/parsers/vector_pdf.py` | figure 页过滤带外框结构图框内标签 |
| `tests/unit/test_regulation_preprocess.py` | 新增 3 个章节归属回归测试 |
| `tests/unit/test_vector_pdf.py` | 新增框内标签过滤回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `3c711d606b484790b709448b5f789a37`）
- 重新解析后应确认：`6 III类手册编制` 成为顶层父章节、6.2.x 不再挂 `附录` 下、图1 仍作为 figure 资产

### 2.4 修复：《通信网智能巡检》第11页 5.6 下误生成 5.1（2026-08-06）

> 用户反馈《通信网智能维护技术要求 第6部分：基于无人机的通信网智能巡检》第11页，5.6 里面"为什么又生成了一个 5.1"。

#### 问题

- 文档：`20240329：《通信网智能维护技术要求 第6部分：基于无人机的通信网智能巡检》.pdf`（21 页），job `a12c658fe9c1426fa57b593d45a963a3`
- 第11页 `5.6 任务载荷` 下有**无编号子节** `可见光相机的指标要求如下`（实际应为 5.6.1）
- 解析结果：它被错误推断为 **`5.1 可见光相机的指标要求如下`**，与第10页已有的 `5.1 概述` 冲突

#### 根因

`split_blocks` 无编号子节推断的编号基准是**顶层条款** `clause_stack[0]`：

```python
base_number = _clause_number(clause_stack[0])   # 顶层 "5 巡检无人机系统" -> "5"
if base_number and "." not in base_number:      # "5" 无点号 -> 满足
    text = f"{base_number}.{sub_index} {text}"  # "5.1 可见光相机的指标要求如下"
```

- 第11页进入时 `clause_stack = ['5 巡检无人机系统', '5.6 任务载荷']`
- `clause_stack[0]` 是顶层 `5 巡检无人机系统`，`base_number = "5"` → 推断为 `5.1`
- 正确行为：应基于**当前活动条款** `5.6 任务载荷` 推断为 **`5.6.1`**

旧场景（昨天 2.5/2.8/2.9 修复）只处理了"顶层条款下的无编号子节"（`5 高水效小麦品种鉴定` → `5.1/5.2/5.3`），此时 `clause_stack[0] == clause_stack[-1]`，两者等价。新场景是"含点号子条款（5.6）下也有无编号子节"，需要区分。

#### 修复（`pipeline.py`）

引入 `unnumbered_parent_stack`：记录**最后一个真实编号条款**的完整章节前缀（`clause_stack[:-1] + [当前clause]`），无编号子节/术语项推断时以它为基准：

- 遇到真实编号 `clause`/`article`/`chapter`/`annex` 时重置 `unnumbered_parent_stack`
- 无编号子节推断：`base_clause = unnumbered_parent_stack[-1]`（当前活动条款）
  - 顶层场景：`['5 高水效小麦品种鉴定']` → base = `5` → `5.1/5.2/5.3`（保持不变）
  - 嵌套场景：`['5 巡检无人机系统', '5.6 任务载荷']` → base = `5.6` → `5.6.1`（修复点）
- 连续无编号子节：`clause_stack = unnumbered_parent_stack + [text]`，兄弟子节基于同一父级递增（`5.1 → 5.2`，而非 `5.1.1`）
- 术语项分支同步修改

#### 验证

- **新旧场景双验证**：
  - 旧场景（小麦）：`5.1 基本要求`/`5.2 种子要求`/`5.3 试验要求` 不变，`5.3.1 田间布置` 嵌套在 `5.3 试验要求` 下
  - 新场景（通信网）：`可见光相机的指标要求如下` → `5.6.1 可见光相机的指标要求如下`，section_path `['5 巡检无人机系统', '5.6 任务载荷', '5.6.1 可见光相机的指标要求如下']`，不再生成 `5.1`
  - 术语场景（大豆）：`3.1/3.2/3.3` 不变
- 新增回归测试：`test_unnumbered_subheading_under_nested_clause_gets_nested_number`
- 相关测试套件：**112 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | 新增 `unnumbered_parent_stack`，无编号子节/术语项推断基准改为当前活动条款 |
| `tests/unit/test_regulation_preprocess.py` | 新增嵌套子条款下无编号子节回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `a12c658fe9c1426fa57b593d45a963a3`）
- 重新解析后应确认：`5.6 任务载荷` 下 `可见光相机的指标要求如下` 变为 `5.6.1`，不再出现与 `5.1 概述` 冲突的 `5.1`

### 2.5 修复：《农业野生植物原生境保护点》第5页条款编号错乱（2026-08-06）

> 用户反馈《农业野生植物原生境保护点无人机监测技术规范》第5页解析内容有问题。

#### 问题

- 文档：`20250109：《农业野生植物原生境保护点无人机监测技术规范》.pdf`（7 页），job `9942fd0268fe4585959afa721921df14`
- 第5页条款编号严重错乱：`4.1.2 监测方法` 被解析成 `4 1 2 监测方法`，标题被错误切分
- 修复前 clauses：`4.1 化情况.`、`4.2 息储存.`、`6.1 能原因.` 等明显异常（标题被 PUA 字符破坏）

#### 根因

PDF 把**条款编号的点号**映射到 **PUA 私有区字符 `U+1001B0`**（全文档第 3-6 页共 66 处）：

```
4.1.2 → ４<U+1001B0>１<U+1001B0>２
```

该字符是合法 Unicode 私有区码位（category `Co`），`isprintable()` 返回 False，但：
- 不被 `repair_invalid_unicode` 处理（该函数只处理 UTF-16 代理对）
- 不在 `FAKE_GLYPH_TO_ASCII` 映射里（此前只覆盖 U+7280~U+72D8 的假字）
- 导致 `heading_kind`/`_clause_number` 无法识别编号 → 条款标题被错误切分（如 `4.1 设施监测` → `4.1 化情况.`）、层级丢失

#### 修复（`pipeline.py`）

在 `FAKE_GLYPH_TO_ASCII` 中加入 PUA 点号映射：

```python
chr(0x1001B0): ".",
```

`repair_fake_glyphs` 逐字符映射后自动还原；`clean_page` 对每行调用 `repair_fake_glyphs`，所以清洗后的文本（`cleaned_text`）和经 `_line_blocks`/`normalize_cell` 的块文本都还原。还原后 `4.1.2` 可被 `heading_kind`/`_clause_number` 正常识别。

#### 验证

- **真实流程模拟**（raw_text → clean_page → split_blocks，第4-6页跨页）：
  - `4.1 设施监测` / `4.1.1 监测内容` / `4.1.2 监测方法` / `4.1.2.1 初始信息获取` / `4.1.2.2 定期监测` / `4.2 资源监测` / `4.2.1 监测内容` 全部正确识别并建立完整层级
  - `6.1 监测信息分析` / `6.1.1 人工分析` / `7 档案管理` 正确
- 新增回归测试 2 个：
  - `test_repair_fake_glyphs_restores_pua_clause_dot`
  - `test_clean_page_restores_pua_clause_dot_in_body_text`
- 相关测试套件：**114 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `FAKE_GLYPH_TO_ASCII` 新增 PUA 点号 `U+1001B0` → `.` |
| `tests/unit/test_pipeline.py` | 新增 2 个 PUA 点号还原回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `9942fd0268fe4585959afa721921df14`）
- 重新解析后应确认：`4.1.2 监测方法` 等条款编号正确、`4.1 化情况.`/`4.2 息储存.` 等异常标题消失
- 其他文档若出现不同 PUA 码位代表不同符号，需继续补充映射表

### 2.6 修复：《电网设备无人机图像识别系统》第1页封面被解析为表格/图片（2026-08-06）

> 用户反馈《电网设备无人机图像识别系统技术要求》第1页怎么解析为图片了。

#### 问题

- 文档：`20250630：《电网设备无人机图像识别系统技术要求》.pdf`（16 页），job `e09c50d0bd5344dc91baaab90728d290`
- 第1页是标准封面（`ICS 29.020` / `CCS F29` / `DL/T 2908—2025` / `发布/实施`），但被解析为 `table_figure`：
  - 封面标题被识别为表格 chunk（`p0001-table-01`，把"中华人民共和国电力行业标准"当表格）
  - 页面 logo 被识别为 figure（`p0001-figure-01`，内容"力行业标准 DL/T 2908—2025"）

#### 根因

`_looks_like_cover_page` 的标准号前缀名单缺少 **`DL`（电力行业标准）**：

```python
_COVER_STD_NUMBER_RE = re.compile(
    r"(?:YD|DB|GB|GA|CY|JR|LD|LY|MH|NY|QX|SB|SL|TB|WB)\s*\d*/\s*T?\s*[×Xx0-9]"
)
```

`DL/T 2908—2025` 不匹配 → `_looks_like_cover_page` 返回 False → 封面页未被排除表格检测 → pdfplumber 把封面装饰框线识别为表格 → `table_pages` 命中 → page_type=`table_figure`（表格+页面 logo 图）。

这与昨天 2.11 修复的《应急通信空中基站》封面误判是同一类问题，只是那个文档的标准号前缀 `YD/T` 已在名单中。

#### 修复（`vector_pdf.py`）

`_COVER_STD_NUMBER_RE` 补充常见行业标准前缀：`DL|NB|HG|HJ|CJ|JG|JC|QB|FZ|JT|JTG`（电力/能源/化工/环保/城建/建材/轻工/纺织/交通）。

#### 验证

- 真实 PDF 第1页：`_looks_like_cover_page` 修复前 False → 修复后 **True**
- preflight 实测：修复后第1页不再进入 `table_pages`（`table_pages=[]`），`figure_pages=[12,13]`（只保留真实图片页）
- 既有封面（YD/T、DB41/T）不回归，正文页不误判
- 新增回归测试：`test_cover_page_recognizes_dl_power_industry_standard`（DL/T + NB/HG/HJ/CJ/JG/JC/QB/FZ/JT/JTG 前缀逐一验证）
- 相关测试套件：**115 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | `_COVER_STD_NUMBER_RE` 补充 DL/NB/HG/HJ/CJ/JG/JC/QB/FZ/JT/JTG 前缀 |
| `tests/unit/test_vector_pdf.py` | 新增 DL/T 及常见行业前缀封面识别回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `e09c50d0bd5344dc91baaab90728d290`）
- 重新解析后应确认：第1页封面走 `native_text`，不再生成 `p0001-table-01`/`p0001-figure-01` 资产
- 若后续遇到其他行业标准前缀（如 Q/GDW 企业标准、JJF 计量规程）仍可继续补充

### 2.7 修复：《民用无人驾驶航空器系统安全要求》第26页文本层损坏被解析为图片（2026-08-06）

> 用户反馈《民用无人驾驶航空器系统安全要求》（GB 42590-2023）第26页解析内容有误。

#### 问题

- 文档：`GB 42590-2023 民用无人驾驶航空器系统安全要求.pdf`（38 页），job `3b31bd75b9a14230bc2aee0c3b9f007e`
- 第26页被解析为 `figure_review`/`page_type=figure`，输出**全是乱码**（`!"!"#$%!"%"&`、`(cid:244)` 等），且生成 `p0026-figure-01` 图片资产
- 实际内容应为 `5.14 噪声试验` 的条款正文

#### 根因（两条判定路径打架）

第26页文本层损坏：pypdf 提取出 **PostScript 字形名**（`/G26`、`/G28`、`/G56/G22/...`），`is_glyph_name_garbage=True` → parse_pdf 路由为 `text_layer_review`（本应走 OCR）。

但 hybrid 层的 figure 判定把它拉回了图片路径：

1. `preflight_pages` 用 **pdfplumber** 提取文本，返回的是乱码 `!"!"#$%`（不是 `/G26`），`is_glyph_name_garbage=False`
2. 第26页有 2 张图片，`image_coverage=0.1213 ≥ 0.12` → preflight 判为 figure 页 → 加入 `visual_targets`
3. `figure_targets = (candidate_ocr_targets - text_layer_targets) | visual_targets` —— `visual_targets` **未排除** `text_layer_targets`
4. 第26页进入 figure 解析，route 变 `figure_review`，加入 `figure_review_pages`
5. `ocr_targets = (candidate_ocr_targets | formula_targets) - figure_review_pages - blank_pages` —— 因第26页已进 `figure_review_pages`，**被排除出 OCR**

结果：乱码文本页走了 figure 解析而非 OCR，输出全是乱码。

#### 修复（`hybrid_pdf.py`）

`visual_targets` 排除 `text_layer_targets`：

```python
visual_targets = preflight.figure_pages - text_layer_targets
```

文本层损坏页（PostScript 字形名 / 假字）无论图片覆盖率多高，都应走 OCR 而非 figure 解析——figure 解析只能输出乱码文本。

#### 验证

- 真实 PDF 第26页：修复前 `visual_targets` 含 26 → 修复后不含 26（`preflight.figure_pages=[26]`，减 `text_layer_targets={26}` 后为空），页面保持 `text_layer_review` 走 OCR
- 新增回归测试：`test_preflight_text_layer_corrupted_page_excluded_from_figure_targets`（验证 preflight 判为 figure 的页在 hybrid 层被 text_layer_targets 排除）
- 相关测试套件：**116 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/hybrid_pdf.py` | `visual_targets = preflight.figure_pages - text_layer_targets` |
| `tests/unit/test_vector_pdf.py` | 新增文本层损坏页排除 figure 回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `3b31bd75b9a14230bc2aee0c3b9f007e`）
- 重新解析后应确认：第26页走 `remote_ocr` 而非 `figure_review`，`5.14 噪声试验` 条款内容正确
- 若第26页 OCR 仍失败，需检查 MinerU 对该页的识别

### 2.8 修复：《民用无人驾驶航空器系统运行识别规范》第10页条款编号点号下沉（2026-08-06）

> 用户反馈《民用无人驾驶航空器系统运行识别规范》（GB 46750-2025）第10页解析有误。

#### 问题

- 文档：`GB 46750-2025 民用无人驾驶航空器系统运行识别规范.pdf`（31 页），job `4e28cd169df545b8a83c45ffd4a11ac7`
- 第10页条款编号点号完全丢失：`515` 应为 `5.1.5`、`516` 应为 `5.1.6`、`52` 应为 `5.2`、`521` 应为 `5.2.1`
- 全文档第7-12页普遍受影响：`31`=3.1、`32`=3.2、`35`=3.5、`511`=5.1.1、`513`=5.1.3 等

#### 根因

PDF 排版把条款编号的点号**下沉约 7pt**（独立成行）：

```
top=102.3  x0=60.8  515       ← 编号数字（实际是 5.1.5）
top=109.3  x0=68.7  ..        ← 下沉的点号
```

- pypdf（parse_pdf 阶段）提取正常：`5.1.5` ✓
- 但第10页是 `vector_layout`/`table_figure` 页，走 vector 解析
- `_line_blocks` 用 pdfplumber `extract_words` 按 top 分组，下沉的点号被拆成独立行 → 编号变成 `515`
- `heading_kind`/`_clause_number` 无法识别 `515` → 条款编号、章节层级全部错乱

这与 `_smart_cell_text` 修复的"表格单元格字间距大导致标点错位"是同类问题，但发生在**正文行**（非表格单元格）。

#### 修复（`vector_pdf.py`）

`_line_blocks` 增加两步处理：

1. **合并浮动点号行**：当一行是纯标点/数字（≤4 字符，如 `..`）且与上一行 top 差 ≤8pt 时，合并进上一行
2. **点号插入数字间隙**：块文本若以"纯数字串 + 点号串 + 正文"开头（如 `515 .. 民用…`），把点号插入数字间隙：`515` + `..` → `5.1.5`，恢复 `5.1.5 民用…`

#### 验证

- 真实 PDF 第10页：`515` → `5.1.5`、`516` → `5.1.6`、`517` → `5.1.7`、`52` → `5.2`、`521` → `5.2.1`，编号正确、在行首
- 第7页术语编号：`31` → `3.1`、`32` → `3.2`、`33` → `3.3`、`34` → `3.4`（术语定义编号也恢复）
- `heading_kind("5.1.5 民用…") == "clause"` ✓
- 新增回归测试 2 个：
  - `test_line_blocks_merges_floating_clause_dots`
  - `test_line_blocks_interleaves_dots_into_clause_number`
- 相关测试套件：**118 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | `_line_blocks` 合并下沉点号行 + 点号插入数字间隙 |
| `tests/unit/test_vector_pdf.py` | 新增 2 个点号下沉回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `4e28cd169df545b8a83c45ffd4a11ac7`）
- 重新解析后应确认：`5.1.5`/`5.1.6`/`5.2.1` 等条款编号正确、术语定义 `3.1`/`3.2` 正确
- 第9页 `513`（应为 `5.1.3`）点号与正文 `1s。` 混在同一行，本次未覆盖（边缘场景），后续可继续处理

### 2.9 修复：《民用无人驾驶航空器系统运行识别规范》第10页表格识别不全（2026-08-06）

> 用户在 2.8 修复后复查发现：第10页表格识别不全。

#### 问题

第10页实际有 2 个表格，但修复前只识别出 1 个残片：

| 表格 | 实际内容 | 修复前 | 修复后 |
|---|---|---|---|
| 数据包格式示意图 | 1 行 8 列（数据类型/版本号/数据长度/数据标识/数据内容项1/2/…/N） | **缺失** | ✓ 完整识别 |
| 表1 数据包内容 | 5 行 4 列（数据包内容/长度/说明/取值） | **仅 2 行 3 列**（丢 `取值` 列和 3 行） | ✓ 完整 5 行 4 列 |

#### 根因（两个叠加）

**① 数据包格式示意图（1 行 8 列）被 `_valid_table` 拒绝**
- `_valid_table` 要求 `len(rows) >= 2`，单行表头表格（数据包格式示意图）不满足 → 被过滤
- 导致 detected 只剩 1 个表格（表1）

**② 表1 被 expanded 残片覆盖**
- `_expanded_ruled_tables` 把"图3 数据包格式 + 表1 前 2 行"合并成一个 13 列错乱网格（bbox 420-564 覆盖两区域）
- `_choose_best_tables` 中 `len(detected)=1, len(expanded)=1`，比较 `_table_quality`：expanded = 13*10+populated 更高 → 选了 expanded 残片
- 表1 只剩前 2 行 3 列，`取值` 列和后 3 行丢失

#### 修复（`vector_pdf.py`）

`_valid_table` 支持**单行多列表头**（数据结构示意图）：

```python
# 新增分支：单行多列表格也是真实表格
return bool(
    len(rows) == 1
    and width >= 4
    and populated >= width
    and table_width >= 200
    and table_height >= 18
)
```

- 要求宽度 ≥4、每列都有内容、宽度 ≥200pt → 排除装饰框/单列附录标题
- 表0（数据包格式示意图）通过 → detected 有 2 个表格 → `_choose_best_tables` 因 `len(detected)=2 > len(expanded)=1` 返回 detected 两个完整表格

#### 验证

- 真实 PDF 第10页：
  - 表0：1 行 8 列完整识别
  - 表1：5 行 4 列完整识别（含 `取值` 列）
- 单行窄表格（如 `[["数据包内容", "长度"]]`）仍被拒绝，装饰框不误判
- 新增回归测试：`test_valid_table_accepts_single_row_multi_column_schematic`
- 相关测试套件：**119 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | `_valid_table` 支持单行多列表格 |
| `tests/unit/test_vector_pdf.py` | 新增单行多列表格识别回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `4e28cd169df545b8a83c45ffd4a11ac7`）
- 重新解析后应确认：第10页两个表格（数据包格式 + 表1）都完整

### 2.10 评估与修复：《民用无人驾驶航空器系统运行识别规范》跨页续表噪声 chunk（2026-08-06）

> 用户在 2.9 修复后询问：表1 是跨页表格，被分成两个 chunk 是否有影响。

#### 评估结论

**表1 跨页合并是正确的，不影响主内容**：

- 表1（数据包内容，5行4列）在第10页，最后1行（数据内容项）在第11页
- `merge_cross_page_tables` 已正确合并为 1 个表格块（`source_pages 10-11`，6 行），发布为单个 table chunk（`89a3836fad51`，ct=table），内容完整（含 `取值` 列和全部 6 行）

**但存在 1 个冗余噪声 chunk**：

- 第11页遗留 `表1 数据包内容续` 和 `( )` 两个 paragraph 块，被发布为 `5f100ace7f68`（ct=text，内容仅"表1 数据包内容续( )"）
- 原因：跨页合并把续表表格体并入了主表（第10页），但第11页的**续表标题**未被消费——`split_blocks` 的表格标题去重依赖页面内 `table_titles`，而第11页的 `table_titles` 只含 `表2`（不含已合并到第10页的 `表1`），所以 `表1 数据包内容续` 未被去重

#### 修复（`pipeline.py`）

`split_blocks` 过滤跨页续表标题残留（页面已有结构化表格时）：

```python
# "表N ... 续" 续表标题
is_continuation_title = bool(
    block_text and "续" in block_text and TABLE_TITLE_ROW_RE.match(block_text)
)
# "( )" 残留空括号
is_empty_residue = bool(
    block_text and len(block_text) <= 4 and re.fullmatch(r"[（(]?\s*[）)]?", block_text)
)
# 页面有结构化表格时过滤（续表体已并入跨页主表）
```

#### 验证

- 模拟第11页：`表1 数据包内容续` 和 `( )` 被过滤，`5.2.2` 正文和 `表2` 表格保留
- 真实文档跨页续表不影响：表1 主 chunk 仍完整包含全部 6 行
- 新增回归测试：`test_split_blocks_filters_continuation_title_residue`
- 相关测试套件：**120 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `split_blocks` 过滤跨页续表标题残留 |
| `tests/unit/test_regulation_preprocess.py` | 新增续表标题噪声过滤回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `4e28cd169df545b8a83c45ffd4a11ac7`）
- 重新解析后应确认：表1 仍为单个完整 chunk，`表1 数据包内容续( )` 噪声 chunk 消失

### 2.11 修复：《民用无人驾驶航空器系统运行识别规范》第10页 table_structure 门禁失败（2026-08-06）

> 用户在 2.9/2.10 修复后重新解析，报错：`table_structure: Table blocks could not be converted to complete rows: [10]`（fail 门禁，阻断发布）。

#### 问题

- 2.9 修复让单行多列表格（数据包格式示意图）通过 `_valid_table` 进入 `merge_cross_page_tables`
- 但 merge 内部的表格检查仍要求 `len(rows) >= 2`（或 `header_only_form`），单行 8 列示意图不满足 → 第10页被标记 invalid → `invalid_table_pages=[10]` → `table_structure` fail 门禁

#### 根因

`merge_cross_page_tables` 处理 rich_blocks 中的 table 块时：

```python
header_only_form = (len(rows) == 1 and len(rows[0]) >= 2 and raw_row_count >= 2)
if (len(rows) < 2 and not header_only_form) or len(rows[0]) < 2:
    invalid_pages.add(page.page_number)
```

- `header_only_form` 为"空白记录表"设计（表头 + 空行，raw_row_count ≥ 2）
- 单行多列表格（数据包格式示意图，1 行 8 列）`raw_row_count=1`，不满足 `>= 2` → 被判 invalid
- 与 2.9 的 `_valid_table` 支持不一致：vector 层接受单行表格，merge 层却拒绝

#### 修复（`pipeline.py`）

`merge_cross_page_tables` 的表格检查增加**单行多列表格**分支：

```python
single_row_schematic = (
    len(rows) == 1
    and len(rows[0]) >= 4
    and all(str(cell).strip() for cell in rows[0])
)
if (len(rows) < 2 and not header_only_form and not single_row_schematic) or (
    rows and len(rows[0]) < 2
):
    invalid_pages.add(page.page_number)
```

要求宽度 ≥4 且每列有内容 → 排除装饰框/单列标题。

#### 验证

- 真实 PDF 第10-11页：修复前 `invalid_table_pages=[10]` → 修复后 **`[]`**
- `structured_table_count=3`（数据包格式 + 表1 + 表2），表1 正确跨页合并（`cross_page_table_count=1`）
- 新增回归测试：`test_merge_accepts_single_row_multi_column_schematic`
- 相关测试套件：**120 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `merge_cross_page_tables` 支持单行多列表格，不再标记 invalid |
| `tests/unit/test_regulation_preprocess.py` | 新增单行表格 merge 回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `4e28cd169df545b8a83c45ffd4a11ac7`）
- 重新解析后应确认：`table_structure` 门禁通过，第10页 3 个表格（数据包格式 + 表1 + 表2）完整

### 2.12 修复：《民用无人驾驶航空器系统运行识别规范》数据包格式示意图标题错配（2026-08-06）

> 用户在 2.11 修复后复查发现：第10页"表一依然不算识别完整 少了最后一行"。

#### 排查结论

表1（数据包内容）**实际是完整的**：6 行（表头 + 数据类型 + 版本号 + 数据长度 + 数据标识 + 第11页的数据内容项），跨页合并正确，发布 chunk（`89a3836fad51`，pages 10-11）包含全部行。

用户看到"表1只有1行"的**真正原因**是**数据包格式示意图被错标为"表1 数据包内容"**：

| 表格 | 实际内容 | 修复前标题 | 修复后标题 |
|---|---|---|---|
| 数据包格式示意图 | 1 行 8 列（数据类型/版本号/…/数据内容项N，上方"图3 数据包格式"） | **"表1 数据包内容"（错误）** | **空** |
| 表1 数据包内容 | 6 行 4 列（跨页 10-11） | "表1 数据包内容" | "表1 数据包内容" |

用户看到第一个 chunk 标着"表1 数据包内容"却只有 1 行（表头），误以为表1不完整。实际是示意图冒充了表1。

#### 根因

`merge_cross_page_tables` 标题绑定逻辑：单行多列示意图无明确标题（`explicit_reference` 为空）时，从 `page.table_hints` 取未使用的"表1 数据包内容"绑定给它。

```python
elif hint_titles:
    title = next(hint for hint in hint_titles if _table_reference(hint) not in used_hint_refs)
```

#### 修复（`pipeline.py`）

单行多列无明确标题的表格（数据结构示意图）**不绑定 hint 表标题**：

```python
is_titleless_schematic = (
    len(rows) == 1 and len(rows[0]) >= 4 and all(str(cell).strip() for cell in rows[0])
)
if not is_titleless_schematic:
    # 绑定 hint 标题
```

真正的编号表格（多行）仍正常绑定 hint 标题。

#### 验证

- 真实 PDF 第10页：示意图标题从"表1 数据包内容"→**空**；真正的表1（6行4列）保留标题并跨页合并
- `table_titles.count("表1 数据包内容") == 1`（只有一个真表1）
- 回归测试更新：`test_merge_accepts_single_row_multi_column_schematic` 增加标题校验
- 相关测试套件：**120 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `merge_cross_page_tables` 单行多列无标题表格不绑定 hint 标题 |
| `tests/unit/test_regulation_preprocess.py` | 回归测试增加示意图标题校验 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `4e28cd169df545b8a83c45ffd4a11ac7`）
- 重新解析后应确认：数据包格式示意图不再显示"表1 数据包内容"标题；真正的表1 完整 6 行

### 2.13 修复：《民用无人驾驶航空器实名登记和激活要求》第12页内容错位 + 第17页门禁失败（2026-08-06）

> 用户反馈《民用无人驾驶航空器实名登记和激活要求》（GB 46761-2025）：12页 6 技术要求下面内容有些错位，17页未通过门禁。状态 `needs_review`（`table_caption_alignment` fail）。

#### 问题一：第17页 `table_caption_alignment` 门禁失败

- 门禁报错：`Detected table captions were not bound one-to-one to structured tables: [17]`
- 根因：页面 `table_hints` 含 **`表 A.6 的要求。`**（正文引用"应符合表A.5和表A.6的要求"被拆行误判为表题）
- `expected_caption_refs` 含 `表a6`，但实际没有 `表A6` 表格 → mismatch → 门禁 fail

**修复**（`pipeline.py`）：
- 新增 `TABLE_TITLE_PROSE_RE`：匹配 `表N 的要求/规定/参数/内容/格式/结构` 正文引用
- `detect_table_hints` 和 `_semantic_table_hints` 过滤这类 prose 引用，不再当作表题 hint

#### 问题二：第12页内容错位（列表项编号下沉）

- 第12页 `6 技术要求` 下列表项编号 `a)`/`b)` 下沉约 8pt，被 pdfplumber 拆成独立行：
  ```
  登记主体管理支持…进行实名登记   ← 正文
  a) : 、 、 ;                    ← 编号+标点碎片
  ```
- 与 2.8 修复的"条款编号点号下沉"同类，但涉及**字母列表项编号**

**修复**（`vector_pdf.py` `_line_blocks`）：
- 扩展点号合并正则为 `^([A-Za-z]?)(\d{1,6})\s+([.,，。]{1,4})\s*(.*)$`，支持**字母+数字条款编号**：
  - `A3` + `.` → `A.3`（多余点号插字母和数字之间）
  - `A31` + `..` → `A.3.1`
- 要求点号前**有空格**（下沉点号是独立 word），避免破坏正常编号 `6.2`

#### 验证

- 第17页：`A.3 激活状态上报接口要求` / `A.3.1 数据传输要求` / `A.3.2 激活状态上报接口请求参数要求` 正确合并；`表 A.6 的要求` 不再进入 expected refs
- 第12页：正常 `6.1`/`6.2` 不被破坏
- 新增回归测试 3 个：
  - `test_line_blocks_interleaves_dots_into_letter_prefix_clause_number`
  - `test_line_blocks_keeps_normal_dotted_number_intact`
  - `test_detect_table_hints_filters_prose_reference`
- 相关测试套件：**123 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | 新增 `TABLE_TITLE_PROSE_RE`；`detect_table_hints`/`_semantic_table_hints` 过滤正文引用 |
| `app/ingestion/parsers/vector_pdf.py` | `_line_blocks` 点号合并支持字母+数字编号 |
| `tests/unit/test_vector_pdf.py` | 新增字母编号点号合并、正常编号不被破坏测试 |
| `tests/unit/test_regulation_preprocess.py` | 新增表题 prose 引用过滤测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `d8ad0b50d90046bfb5b0e594186bfbcc`）
- 重新解析后应确认：第17页 `table_caption_alignment` 门禁通过（状态从 `needs_review` 变 `warning`）；第12页 `a)`/`b)` 列表项编号不再与正文分离
- 第12页列表项 `a)`/`b)` 编号下沉到正文下一行是 PDF 排版特征，`_line_blocks` 已尽量合并；若仍有残余碎片，后续可继续优化

### 2.14 修复：《GB/T 19000-2016 质量管理体系基础和术语》第20页术语标题号缺失（2026-08-06）

> 用户反馈该文档标题号缺失较多，比如第20页。

#### 问题

- 文档：`GB_T_19000-2016质量管理体系基础和术语.pdf`（56 页），job `8e0e1be8abf54a438c60f81b9733e4fd`
- 第20页是术语定义页（3.5 有关体系的术语 / 3.6 有关要求的术语），每个术语条目的**编号与术语名分离**：
  ```
  3.5.10        ← 编号独立
  愿景 vision   ← 术语名独立
  ```
- 正确应为 `3.5.10 愿景 vision`（编号 + 术语名 + 英文名）

#### 根因

PDF 排版把术语条目分成多行：
```
top=148  3510          ← 编号数字
top=155  ..            ← 点号下沉（3.5.10）
top=162  愿景 vision   ← 术语名+英文名（再下沉 ~14pt）
```
`_line_blocks` 按 top 分组（阈值 3pt），编号行和术语名行相距 14pt 被拆成两个 block。术语名虽然被 `_looks_like_term_heading` 识别，但编号 `3.5.10` 是独立 block，split_blocks 无法把它们关联成"编号术语标题"。

#### 修复（`vector_pdf.py` `_line_blocks`）

block 生成后合并相邻的"纯编号 block + 术语名 block"：

```python
# 3.5.10 + 愿景 vision -> 3.5.10 愿景 vision
if merged_blocks and re.fullmatch(r"\d+(?:\.\d+)+", merged_blocks[-1].text.strip())
   and _looks_like_term_line(block.text):
    merged_blocks[-1].text = merged_blocks[-1].text.strip() + " " + block.text
```

- 宽松术语名判断 `_looks_like_term_line`：`_looks_like_term_heading` 或"中文 + 英文词"（容忍英文名分号下沉成 `objectentityitem ; ;`）
- 双向合并（编号在前/术语名在前）以适配 bbox 排序差异

#### 验证

- 第20页术语条目全部正确合并：
  - `3.5.10 愿景 vision` / `3.5.11 使命 mission` / `3.5.12 战略 strategy`
  - `3.6.1 客体 objectentityitem` / `3.6.2 质量 quality` / `3.6.3 等级 grade` / `3.6.4 要求 requirement` / `3.6.5 质量要求 qualityrequirement`
- 术语条目正确嵌套在 `3.6 有关要求的术语` 下
- 新增回归测试 2 个：
  - `test_line_blocks_merges_term_number_with_term_name`
  - `test_line_blocks_merges_term_number_with_term_name_lenient`
- 相关测试套件：**125 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | `_line_blocks` 合并术语编号与术语名；导入 `_looks_like_term_heading` |
| `tests/unit/test_vector_pdf.py` | 新增术语编号合并回归测试 2 个 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `8e0e1be8abf54a438c60f81b9733e4fd`）
- 重新解析后应确认：第20页术语条目标题号（3.5.10/3.6.1 等）与术语名合并，不再缺失
- 已知次要问题：术语定义正文段会被误推断为更深层条款（如 `3.6.1.2 示例…`、`3.6.2.1 客体…`），内容不丢失，但层级略多；后续可优化

### 2.15 修复：《GB/T 19000-2016》第33页图片识别 VLM 误判（2026-08-06）

> 用户反馈该文档第33页图片识别有误。文档：`GB_T_19000-2016质量管理体系基础和术语.pdf`，job `8e0e1be8abf54a438c60f81b9733e4fd`。

#### 排查结论

第33页是附录A（概念关系图示），有 3 张概念关系图（图A1 属种关系、图A2 从属关系、图A3 关联关系）。**图片识别本身正确**：
- 3 张图都正确生成 figure 资产和 chunk
- 图题关联正确（`图A1 属种关系图` / `图A2 从属关系图` / `图A3 关联关系图`）

**但 VLM 描述质量差**：
- `图A2 从属关系图`：VLM 判为"图表标题可见，但无实际图形内容"（**错误**，实际是耙形结构图，399 水平段 + 1095 垂直段）
- `图A3 关联关系图`：VLM 判为"仅包含一个标题文本"（**错误**）

#### 根因

概念关系图是**扁平树形/耙形结构图**（图A2 高度仅 64pt、图A3 高度约 74pt）。figure 裁剪使用 160dpi 整页渲染，图A2 裁剪尺寸只有 **817×143px**——VLM 分辨率不足看不清树形连接线，误判为空图。

#### 修复（`vector_pdf.py`）

figure 裁剪改用**更高分辨率渲染**：

```python
_FIGURE_RENDER_RESOLUTION = 300

def _figure_crop_png(cls, page, *, bbox):
    page_png = cls._render_page(page, resolution=cls._FIGURE_RENDER_RESOLUTION)
    return cls._crop_png(page_png, ...)
```

- 仅 figure 资产裁剪用 300dpi；整页版面资产仍用 160dpi（不增加整页渲染开销）
- flow_regions / boxed_region / image_boxes 三处 figure 裁剪都改用 `_figure_crop_png`

#### 验证

- 图A2 裁剪从 817×143px → **1532×268px**（300dpi），树形连接线对 VLM 可读
- 新增回归测试：`test_figure_crop_uses_higher_resolution`（验证 `_figure_crop_png` 用 300dpi）
- 相关测试套件：**126 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | 新增 `_figure_crop_png`（300dpi figure 裁剪）；3 处 figure 裁剪改用 |
| `tests/unit/test_vector_pdf.py` | 新增 figure 高分辨率裁剪回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `8e0e1be8abf54a438c60f81b9733e4fd`）
- 重新解析后应确认：图A2/图A3 的 VLM 描述不再是"无实际图形内容"
- VLM 对稀疏概念关系图的描述仍可能不完美（模型能力局限），但更高分辨率会显著改善

## 3. 验证命令

```powershell
Set-Location D:\internal-rag\source\internal-rag
.\.venv\Scripts\python.exe -B -m pytest tests\unit -q -p no:cacheprovider
.\.venv\Scripts\python.exe -B -m ruff check app tests\unit --no-cache
.\deploy\gateway\stop.ps1
.\deploy\gateway\start.ps1
```

Linux 沙箱兼容验证（Python 3.10，跳过 conftest 的 app.main 导入链）：

```bash
python3 -c "
import datetime
if not hasattr(datetime, 'UTC'): datetime.UTC = datetime.timezone.utc
import pytest
raise SystemExit(pytest.main(['tests/unit/test_vector_pdf.py', 'tests/unit/test_pipeline.py', 'tests/unit/test_regulation_preprocess.py', '-q', '-p', 'no:cacheprovider', '--noconftest', '--basetemp=/tmp/pytest-rag']))
"
```

代码改动后必须重启 Gateway（无热重载）；前端 `Ctrl+F5` 刷新。

## 4. 已知遗留问题（来自 08-04/08-05 交接，可能作为本会话输入）

1. **封面 chunk 页码 1-5 粘连**（本会话发现，见 2.1）：封面段落与第 5 页公式占位块合并，需拆分或让封面 `indexable=False`
2. **表格+整页图冗余**：`vector_pdf.py` 需区分"纯表格页"vs"表格+插图页"（当页面有真实非表格图片 `image_coverage >= 0.05` 才生成 figure_page）
3. **OCR 英文术语假字**：MinerU 对小号拉丁字符识别精度不足，需 OCR 精度优化或视觉模型复核
4. **TOC 打分阈值未校准**：`toc_score >= 0.85` 目前目录页达不到，需积累真实分布或接入 VLM 复核
5. **8G 显存约束**：MinerU 与 Ollama 不能同时满载，否则 MemoryError
