# 2026-09-05 改动记录：23-54-DRS_98-19.pdf 栅格表格识别与数字恢复

## 1. 范围与任务

- 用户反馈文件：`23-54-DRS_98-19.pdf`（FAA NPRM，鸟撞适航标准，26 页）。
- 源文件（含栅格表格图版本）：SHA-256 `848359e52d7e39cee37f107f586c0f2517ecd7ebfed05ae3037e08334f046f95`。
- 原件：`D:\internal-rag\data\ingestion\originals\84\848359e52d7e39cee37f107f586c0f2517ecd7ebfed05ae3037e08334f046f95\source.pdf`。
- 注：同文档名下另有一个旧源 `5cc1cec6…`（无图版本）的产物目录 `…/1bcee49b…/23-54-DRS_98-19__5cc1cec6`，本次针对的是含栅格表格图的 `848359e5` 版本及其产物 `…/9891e5b3…/23-54-DRS_98-19__848359e5`。

用户分三阶段反馈的问题：①第 7 页表格整张被遗漏；②识别出来后第 7 页数字错位、第 25 页内容仍有误；③此前修正按单文件哈希兜底，要求对"一类文件"的通用改法。本文档记录最终落地方式：**通用路由 + 通用高分辨率裁切重识别（token 门控） + 受限哈希修正兜底（仅复杂大表）**。

## 2. 根因

该 PDF 正文是文字层，但若干表格被渲染为**栅格图片**（第 7、23、25 页），其单元格内容只存在于像素中：

- 原生文本抽取在表格处直接跳空 → 表格整体不可见；
- 向量解析器的"图片覆盖率"判定（≥12%）把第 7 页（覆盖率约 2.2%）当装饰图片丢弃；
- 整页 OCR 对**小表格**会把数字压糊、错位（第 7 页 `3 2` 融合、首行 `3` 丢失）；
- 整页 OCR 对**大表**（第 25 页 TABLE 2/3）存在行列错位（quantity/weight 填错格、"Plus N" 续行脱离）。

## 3. 三层修复（分层按错误类型，而非全按文件）

### 3.1 通用路由：栅格表格识别 → 远程 OCR

文件：`app/ingestion/parsers/vector_pdf.py`

- 新增 `VectorPreflightResult.raster_table_pages` 与 `raster_table_boxes`（页面 → 0..1000 归一化图片 bbox 列表）。
- 判定规则（不依赖文件名/哈希/页码）：
  - 页面图片在 PDF 结构中被标记为 `Figure`（pdfplumber `image.tag`）；
  - 且**无**明确图题（图N）、内联"following figure"引用、流程图、盒图等真实图形信号；
  - 则该栅格是文字层页上的**栅格表格**，路由到远程 OCR（`ocr_targets`），覆盖率高也不得判为 figure（第 25 页三张表合计覆盖率约 39%）。
- 覆盖范围：**一类文件**（正文为文字层、表格为栅格图片的法规 PDF）。

文件：`app/ingestion/parsers/hybrid_pdf.py`

- 预检结果中的 `raster_table_pages` 并入 `ocr_targets`，使这些页走 MinerU OCR。

### 3.2 通用修复：小表格高分辨率裁切重识别（token 门控）

文件：`app/ingestion/parsers/vector_pdf.py` + `app/ingestion/parsers/hybrid_pdf.py`

- `hybrid_pdf.py` 新增 `_table_token_multiset()` 与 `HybridPdfParser._recover_raster_tables()`。
- 流程（对 OCR 合并后的栅格表格页）：
  1. 用 `table_recovery.crop_table_pdf` 按 bbox 裁切表格区域，放大最多 6 倍、四周加白边；
  2. 把裁切 PDF 单独送 MinerU OCR；
  3. **采纳门控**：仅当裁切结果的词/数字多重集合与整页 OCR 结果**完全一致**才采纳，否则丢弃、保留整页结果。
- token 门控对分隔符（`-`/`–`/`—`/`−`/`_`）和单元格融合不敏感：裁切把 `3 2` 拆回 `3`、`2`，或把 `1.0-1.5` 读成 `1.0–1.5` 时多重集合不变 → 通过；一旦丢失/多出/重复某值（大表被裁切切碎时出现）→ 多重集合不匹配 → 回退整页结果。
- 实测边界（诚实记录）：
  - **有效**：小表格（第 7 页，源图约 215×92px）裁切后数字读对 `3 / 3 / 2`。
  - **无效甚至有害**：大表（第 25 页 TABLE 2 约 663×271px 等）裁切后单元格重复、多出空列 → token 门控拒绝。
  - 因此该机制只通用修复"源图太小导致整页 OCR 压糊"的一类错误，不触碰大表。

### 3.3 受限哈希修正兜底：复杂大表逐格校正（仅当通用机制无法修复）

文件：`app/ingestion/parsers/verified_tables.py`

- 第 25 页三张栅格表（TABLE 2、TABLE 3、33.77(e) 外来物表）的行列错位属于表格模型对齐问题，通用裁切无效，按既有"源哈希 + 页码 + 证据标记"机制逐格校正，数值对照《联邦公报》（63 FR 68645–68646）转录：
  - `DRS_98_19_PAGE_25_TABLE_2_ROWS`（18 行，含 Plus 3/4/5/6 续行复位）；
  - `DRS_98_19_PAGE_25_TABLE_3_ROWS`（6 行）；
  - `DRS_98_19_PAGE_25_FOREIGN_OBJECT_ROWS`（修复词粘连：`Foreignobject`→`Foreign object` 等）。
- **第 7 页不再做哈希修正**：其错误已由 3.2 的通用裁切机制修复（验证结果用 en-dash `1.0–1.5` 证明来自裁切而非哈希修正的 hyphen 版本），删除原 `DRS_98_19_PAGE_7_BIRD_QUANTITY_ROWS` 与其分支，并新增测试断言第 7 页不被哈希修正触碰。

## 4. 各页面最终结果（真实重解析回读）

| 页面 | 内容 | 修复来源 |
| --- | --- | --- |
| 7 | `Weight of bird / Number of birds` = 1.0–1.5→3、1.5–2.5→3、2.5+→2 | 3.2 通用裁切（`parser_trace` 显示 `table-crop-ocr recovered_tables: 1`） |
| 23 | `TABLE 1.—Large Bird Weight Requirements`（3 行） | 3.1 通用路由 + 整页 OCR |
| 25 | TABLE 2（18 行）、TABLE 3（6 行）、§33.77(e) 外来物表，共 4 个 table chunk | 3.1 路由 + 3.3 哈希逐表校正 |

代码验证：

- `tests/unit/test_vector_pdf.py`、`test_verified_tables.py`、`test_pipeline.py`、`test_scan_regulatory.py`、`test_remote_parsers.py`、`test_numeric_glyph_routing.py`：**218 passed**。
- 新增测试：
  - `test_preflight_routes_native_page_raster_table_to_ocr`（低覆盖率与高覆盖率栅格表均路由到 OCR，并断言 `raster_table_boxes` 输出）；
  - `test_table_token_multiset_is_separator_and_fusion_insensitive`（分隔符/融合不敏感、重复/丢失被拒）；
  - `test_drs_98_19_page7_not_hash_corrected_general_crop_handles_it`；
  - `test_drs_98_19_page25_table2_rows_are_realigned`、`test_drs_98_19_page25_table3_and_foreign_object_are_recovered`。
- `ruff check`（vector_pdf / hybrid_pdf / verified_tables 及对应测试）：**All checks passed**。

改动文件：

- `app/ingestion/parsers/vector_pdf.py`
- `app/ingestion/parsers/hybrid_pdf.py`
- `app/ingestion/parsers/verified_tables.py`
- `tests/unit/test_vector_pdf.py`
- `tests/unit/test_verified_tables.py`

## 5. 遗留事项

- 第 23 页 Table 1 存在轻微 OCR 空格瑕疵（如 `>A...`、`≤A<` 缺空格），属格式噪音、非内容错误，本次未处理；如需规范化可另加同型修正。
- 以上代码改动需重启网关加载后对目标文档重解析生成新 revision 才生效；产物尚未发布 RAGFlow。
- 既有全量用例中 `test_regulation_preprocess.py` 三个失败与 `test_section_ocr.py` 的 `test_hybrid_parser_version_is_updated` 等在本改动前即存在（与本次改动无关），未纳入本次处理范围。
