# Internal RAG 改动记录（2026-08-25）

> 用途：记录 2026-08-25 的工程接手、现状核对与后续改动，供后续智能体快速续接。
> 项目运行环境为 Windows，代码、运行时、模型与数据统一位于 `D:\internal-rag`；不得改为 WSL 部署。
> 本文沿用既有格式，按日期持续追加“问题 / 根因 / 修复 / 验证 / 改动文件 / 注意”。

---

## 0. 交接起点与本日范围

- **交接起点**：`HANDOFF-2026-08-24.md`。
- **本日初始任务**：阅读项目文档、熟悉工程结构，并创建今天的改动文档。
- **时间边界**：检查时没有 2026-08-25 的 Git 提交，也没有源码文件在今天被写入；当前工作区差异主要是 2026-08-17 至 2026-08-24 的累计未提交成果，不能视为今日新增改动。
- **本日实际变更**：仅新增本文档；尚未修改业务源码、配置、提示词或测试。

---

## 1. 工程理解

### 1.1 主工程：Internal RAG Gateway

- 代码目录：`D:\internal-rag\source\internal-rag`。
- 技术形态：FastAPI Gateway，统一承接资料接入、检索、问答、引用、模型管理和评测入口。
- 核心链路：
  1. 查询标准化与结构化规划；
  2. 主体文档解析、受限查询转换与 RAGFlow 混合检索；
  3. 候选融合、Reranker 与 `EvidenceRecord` 结构化证据抽取；
  4. 覆盖矩阵、证据范围/主题/归属/完整性校验；
  5. 确定性答案优先，模型生成可降级；
  6. 引用映射与安全 diagnostics 输出。
- 资料接入链路：单一上传入口，根据文件与页面质量自动选择原生解析、MinerU/OCR 等路径；经过质量门禁、预览和 dry-run 后向 RAGFlow 发布。
- 当前查询架构以 `docs/query-translation-decomposition-refactor.md` 为准：模型 planner、translator 和 comparison summary 路径保留，但受本机 8 GB 显存与模型稳定性限制，默认使用 deterministic/legacy adapter 与受控 taxonomy。
- 当前重要边界：ACL 和业务数据库仍不完整；RAGFlow Dataset ID 映射、用户/部门/角色/密级后台、文档版本工作流仍待建设。

### 1.2 独立工程：rag-evaluation

- 代码目录：`D:\internal-rag\source\rag-evaluation`，与 Gateway 平级，不修改 RAGFlow 核心源码。
- 作用：通过 HTTP 调用 Gateway/RAGFlow，对检索、答案、引用、拒答、Judge、性能和版本回归进行离线可复现评测。
- 已具备：JSONL 严格校验、双客户端与响应适配、检索/答案规则指标、本地 LLM Judge、SQLite WAL 存储、JSON/CSV/HTML 报告、run 间回归比较和基础性能采集。
- 当前批量 Judge 默认使用 `qwen3:4b-instruct-2507-q4_K_M`；9B 在本机过慢且易产生截断/非法 JSON，只适合抽样校准。
- 当前遗留：第七阶段独立并发压测、RAGAS 对照后端、Judge 稳定性与人工校准；Judge 成功率达到稳定门槛前不宜作为发布阻断项。

---

## 2. 当前状态快照（2026-08-25）

### 2.1 服务状态

本日通过项目自带 status 脚本实测：

| 服务 | 地址 | 状态 | 备注 |
|---|---|---|---|
| Gateway | `http://127.0.0.1:8080` | Ready | `/health` 返回 internal-rag 0.1.0 development |
| RAGFlow | `http://127.0.0.1:9380` | Ready | db/doc_engine/redis/storage 均为 ok |
| Ollama | `http://127.0.0.1:11434` | Ready | 版本 0.32.4，3 个本地模型可见 |
| Reranker | `http://127.0.0.1:8003` | Ready | `/v1/models` 正常 |
| MinerU | `http://127.0.0.1:8886` | Not Ready | 当前未启动；本日未执行解析作业 |

Ollama 当前模型：

- `qwen3:4b-instruct-2507-q4_K_M`
- `qwen3.5:9b-q4_K_M`
- `qwen3-embedding:0.6b`

### 2.2 Git 与工作区

- 主工程最后提交：`1343a5d`，时间为 2026-08-17 15:13:12 +08:00。
- 2026-08-25 无新提交。
- 主工程 `git status --porcelain`：共 109 项，其中 42 项 tracked modified、67 项 untracked。
- tracked 累计差异规模：42 个文件，约 6600 行新增、954 行删除；这是跨多日累计结果，不是本日增量。
- `D:\internal-rag\source\rag-evaluation` 当前没有独立 `.git` 仓库。
- 工作区存在 `.tmp-comparison-debug.json` 等临时/调试产物；在确认用途前不要提交或删除。
- 当前 Git 基线仍不干净，后续统计“今日改动”必须结合每日交接文档、文件时间和实际 diff，不能只依赖相对最后提交的总 diff。

### 2.3 测试基线

本日只进行了文档、代码结构、Git 状态和服务状态的只读核对，**未重新运行完整测试套件**。

最近有记录的验证结果：

- Gateway：`HANDOFF-2026-08-21.md` 记录完整 pytest 为 **484 passed, 3 failed**；3 项为既有规程解析基线。2026-08-24 的单主体 comparison 降级修复另有 pipeline **8 passed**、ruff 通过记录。
- rag-evaluation：第六阶段结束时 pytest **204 passed**、ruff 通过、mypy Success（31 files）。

后续修改代码后应重新执行目标测试；交付前按风险运行完整 pytest、ruff 和 mypy，不应把上述历史结果写成当日实测。

---

## 3. 今日改动记录

### 3.1 会话初始化：阅读文档并建立 2026-08-25 改动文档

#### 内容

- 阅读 `README.md`、`docs/agent-handoff.md`、`docs/query-translation-decomposition-refactor.md`、`HANDOFF-2026-08-21.md`、`HANDOFF-2026-08-24.md` 和 `D:\internal-rag\source\rag-evaluation\README.md`。
- 核对主工程目录、核心服务模块、Git 状态、最近提交及今日文件写入情况。
- 使用 Gateway、MinerU、模型和 RAGFlow 的 status 脚本核对实际运行状态。
- 新建本文档，建立今日增量记录基线。

#### 验证

- 确认主工程路径和 Git 仓库有效。
- 确认今天没有既有源码改动或提交需要追记。
- 确认 Gateway、RAGFlow、Ollama、Reranker 就绪；MinerU 未就绪。
- 未运行会产生业务数据的上传、解析、发布、真实问答或批量评测。

#### 改动文件

- 新增：`HANDOFF-2026-08-25.md`。

#### 注意

- 后续本日发生的代码、配置、测试、服务操作和真实评测结果，应继续追加在第 3 节，不覆盖初始快照。
- 不得把密钥、密码、完整模型原始输出或敏感 Chunk 正文写入交接文档。

### 3.2 修复：外部答案 API 空响应后的 recovery 失败

#### 问题

智能问答临时接入 DeepSeek OpenAI 兼容 API 后，请求最终返回 503：
`The local answer model recovery request failed.`；切换回本地 Ollama 模型可正常回答。

#### 根因

- 实际失败请求 ID：`e68a6350-a9a0-460c-9b84-821af4dd5b01`。
- DeepSeek 对严格 `json_schema` 请求返回 400；Gateway 正常兼容路径降级为
  `json_object` 后返回 200，但该次响应的最终 `content` 为空。
- 空响应 recovery 从原始严格 payload 重新构造，并直接调用 HTTP client，没有复用
  `_post_chat_payload()` 的 `json_schema → json_object` 兼容降级。
- recovery 添加了 Ollama 专用 `think=false`；DeepSeek 拒绝该参数。代码虽然随后删除
  `think`，但仍携带 DeepSeek 不支持的 `json_schema`，因此再次 400，最终返回 503。
- 错误文案固定写成 “local answer model”，对运行时接入的外部 API 有误导性。

#### 修复

- recovery 统一调用 `_post_chat_payload()`，与首次请求复用相同兼容策略。
- portable compatibility payload 统一移除 `reasoning_effort`、`seed` 和提供方专用
  `think`；本地 Ollama 仍可在 recovery 首次尝试中直接使用 `think=false`。
- 外部 API 不支持严格 Schema 时，recovery 可继续降级到 `json_object`，并在 system
  prompt 中保留完整 JSON Schema 约束。
- 错误文案改为 “selected answer model”，同时适用于本地模型与临时 API。
- 为 `flatten_json_schema()` 增加明确类型收窄，清除该文件既有 mypy `no-any-return`。

#### 验证

- 新增回归测试：模拟 DeepSeek 类接口（拒绝 `json_schema`/`think`、首次
  `json_object` 返回空内容、recovery 返回合法 JSON），确认最终恢复成功。
- `test_model_client.py`：**11 passed**。
- `test_model_client.py + test_answer_generator.py + test_rag_pipeline.py`：**28 passed**。
- ruff：**All checks passed**。
- mypy（`app/services/model_client.py`）：**Success: no issues found**。
- `git diff --check`：通过；仅有仓库既有 LF/CRLF 提示。
- 尚未重启 Gateway 或使用用户 API Key 做在线复测，避免清除仅保存在 Gateway
  进程内存中的临时模型连接。

#### 改动文件

- `app/services/model_client.py`
- `tests/unit/test_model_client.py`
- `HANDOFF-2026-08-25.md`

#### 注意

- 修复需要重启 Gateway 才能生效；重启后临时 API Key/模型连接会丢失，用户需重新
  接入 DeepSeek API 后复测同一问题。
- 如果外部服务在 recovery 后仍返回空内容，系统会返回通用的 selected answer model
  错误，不会再误报为本地模型故障。

### 3.3 修复：DeepSeek V4 默认思考耗尽输出额度导致 content 为空

#### 问题

完成 §3.2 并重启后，用户用 `deepseek-v4-flash/pro` 询问“采用无人机开展松材线虫病
监测，从航摄规划到成果归档完整操作流程”时，兼容请求和 recovery 均返回 HTTP 200，
但最终仍报 `The selected answer model returned empty content.`。

#### 根因

- 新请求 ID：`779022a6-bbe1-4654-a78a-14febd6afd19`，最终 502，约 69.1 秒。
- 当前 DeepSeek API 返回模型为 `deepseek-v4-flash`、`deepseek-v4-pro` 和 vision exp。
- DeepSeek V4 默认开启 thinking，官方 Chat Completions 关闭参数为
  `thinking: {"type":"disabled"}`；既有代码使用 Ollama 专用 `think=false`。
- 兼容重试会移除不支持的 `think`，随后 V4 恢复默认高强度 thinking；在当前
  `max_tokens=2048` 内只产生 `reasoning_content`，没有最终 `content`。recovery 重复了
  同一行为，因此即使 HTTP 200 也没有可验证的结构化答案。

#### 修复

- 按模型能力区分思考关闭协议：`deepseek-v4-*` 使用
  `thinking: {"type":"disabled"}`，其他 OpenAI 兼容模型继续使用
  `reasoning_effort=none`；Ollama 空响应 recovery 仍使用 `think=false`。
- DeepSeek V4 的严格 Schema、portable strict 和 `json_object` 三档兼容请求均保留
  `thinking=disabled`，避免降级时重新启用思考。
- 新增 DeepSeek V4 专项单测：模拟默认思考会耗尽额度、只有正确关闭思考才返回
  JSON，并断言全部兼容请求均携带官方参数。

#### 验证

- `test_model_client.py + test_answer_generator.py + test_rag_pipeline.py`：**29 passed**。
- ruff：通过；mypy（`model_client.py`）：通过。
- Gateway 已重启并 Ready，当前 PID **8484**。
- 重启清除了内存中的 DeepSeek API 连接；需用户重新接入后执行真实复测。

#### 改动文件

- `app/services/model_client.py`
- `tests/unit/test_model_client.py`
- `HANDOFF-2026-08-25.md`

### 3.4 修复：松材线虫病完整流程答案混文档、漏环节与归档误判

#### 问题

DeepSeek API 已能正常返回后，同题答案仍存在质量问题：

- 将 2024 茂名市标与 2025 广东省标拆开后分别输出不完整碎片；
- 把设备参数或飞行控制误当成完整准备/作业流程；
- 把“成果形式”误写成“成果归档”；
- 数据处理、地面/现场验证和档案管理等后半段大量缺失；
- 本地 4B 端到端复测还混入低空数字航空摄影测量内/外业规范。

#### 根因

1. 流程文档补检索仍使用全局相似度阈值 0.25，字面不接近问题的后半段章节会被过滤。
2. procedure plan 无显式 subject 时，泛化的“无人机/航摄”文档也参与最终文档轮转。
3. 旧 RAGFlow 发布 Chunk 没有 `chapter_path/section_title` 和 `章节：`头，章节多样性
   选择无法识别 `6.1` 属于第 6 章，封面/引用文件/附录反而占用证据名额。
4. 每章只保留一个 Chunk，使“6.1 组装调试/6.2 近场测试”可能被“6.3 记录检查”挤掉。
5. 完整流程仍交给模型逐文档生成；模型多轮 repair 后，大部分 claim 被一致性校验剔除，
   即使检索已有 36 条证据，最终仍只剩少数碎片。

#### 修复

- 已锁定文档的流程补检索使用 `similarity_threshold=0.0`，后续由章节多样性与 reranker
  选择，不在召回入口丢弃归档等章节。
- 增加流程题标题主题亲和度过滤：本题保留两份松材线虫病规程，排除泛化低空航摄规范；
  无清晰标题匹配时保持原候选，避免过度过滤。
- 章节识别兼容旧 Chunk：优先解析 `章节：`路径根节点，否则从正文首个 `N/N.x`
  标题归并到顶层章号。
- 同一顶层流程章最多保留两个子节代表，兼顾组装调试、测试与记录等同章核心工作。
- `RegulationAnswerBuilder` 增加 procedure 确定性路径：从“1 范围”的章节清单建立
  章号—标题映射，按文档章节顺序提取规划、准备、获取、处理、验证、档案环节并绑定
  引用；结构化章节足够时 0 次答案模型调用。

#### 验证

- 相关确定性构建、检索章节覆盖、主题过滤与 pipeline 测试：末轮组合 **15 passed**；
  另有组合 **11 passed / 8 passed / 5 passed**，ruff 与目标文件 mypy 均通过。
- `git diff --check` 通过（仅既有 LF/CRLF 提示）。
- 修复前端到端：4 个文档、12 次模型调用、PARTIALLY_ANSWERABLE、约 149 秒。
- 主题过滤后：仅两份松材线虫病文档、6 次模型调用、约 88 秒，但仍有 claim 缺失。
- 确定性流程初版：仅 2024 目标规程、ANSWERABLE、0 次模型调用、无 warning/missing、
  约 32.3 秒，已完整输出航摄规划、作业准备、影像获取、数据处理、地面验证、档案管理；
  档案内容明确为原始影像、处理影像、异常变色木位置数据和文档，保存 3 年。
- 最后增加“每章两个代表”后，单元/组合测试通过；在线复测连续两次被既有 RAGFlow
  `ReadTimeout` 阻断（请求 `c34f067f...`、`cf555fb1...`）。此时容器仍 Up，但
  RAGFlow health API 也超过 10 秒未响应，属服务稳定性问题，不是答案代码异常。
- Gateway 已重启并 Ready，当前 PID **11432**；RAGFlow 需恢复 Ready 后再做最终在线复测。

#### 改动文件

- `app/services/retrieval_service.py`
- `app/services/regulation_answer_builder.py`
- `tests/unit/test_retrieval_service.py`
- `tests/unit/test_regulation_answer_builder.py`
- `HANDOFF-2026-08-25.md`

#### 注意

- 多次 Gateway 重启已清除内存中的 DeepSeek 连接，用户需重新接入 API。
- 当前首要外部阻塞是 RAGFlow ReadTimeout；不要用增加生成重试掩盖检索服务无响应。

---

## 4. 当前风险与建议顺序

1. **先建立可靠 Git 基线**：检查 `.gitignore`，排除 `.env`、secrets、模型、缓存、日志、临时文件和运行数据，再将 8 月 17–24 日成果按功能拆分提交；未经用户确认不重写历史、不删除现有文件。
2. **验证 8 月 24 日 Gateway 修复已在线生效**：单主体 comparison 不应因确定性矩阵无法构建而返回 500；若需重启 Gateway，临时答案模型连接需要重新接入。
3. **定位稳定 503/ReadTimeout**：它会污染 rag-evaluation 的版本对比和并发指标，应先修复 Gateway/RAGFlow 单 worker 稳定性，再启用严格回归门禁。
4. **扩充并校准评测集**：Judge 成功率稳定达到目标后再参与发布阻断；9B 保持抽样校准定位。
5. **解析任务前启动 MinerU**：当前 MinerU 未就绪；真实解析修复必须使用失败 PDF 回归，并保留物理页码、印刷页码、条款号、表格结构与图片资产的边界。

---

## 5. 常用验证命令

主工程：

```powershell
Set-Location D:\internal-rag\source\internal-rag
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m ruff check app tests
.\.venv\Scripts\python.exe -m mypy app
```

独立评测工程：

```powershell
Set-Location D:\internal-rag\source\rag-evaluation
.\.venv\Scripts\python.exe -m pytest
.\.venv\Scripts\python.exe -m ruff check rag_eval tests
.\.venv\Scripts\python.exe -m mypy rag_eval
```

服务状态：

```powershell
Set-Location D:\internal-rag\source\internal-rag
.\deploy\gateway\status.ps1
.\deploy\mineru\status.ps1
.\deploy\models\status.ps1
.\deploy\ragflow\status.ps1
```

---

## 6. 相关文档

- `README.md`
- `docs/agent-handoff.md`
- `docs/query-translation-decomposition-refactor.md`
- `HANDOFF-2026-08-21.md`
- `HANDOFF-2026-08-24.md`
- `D:\internal-rag\source\rag-evaluation\README.md`
## 3.5 流程答案引用超出句子预算导致 500（已修复）

- 现象：同一无人机松材线虫病流程问题返回 `An internal error occurred.`。
- 根因：确定性流程答案覆盖全部已选章节并引用到后部章节 `C17`，但通用
  `EvidenceExtractor` 的全局句子预算裁掉了该引用；流水线仍用裁剪后的模型
  提示证据校验确定性答案，触发 `ValueError: answer references citations outside
  the current evidence: ['C17']`，最终被通用异常处理器转换为 HTTP 500。
- 修复：确定性规程答案改用其实际接收的全部 `generation_chunks` 构造引用校验
  集合；模型提示仍保持原句子预算，不增加模型上下文。
- 回归：新增“证据预算仅 1 句但流程答案包含最后档案章节”的流水线测试；
  `tests/unit/test_rag_pipeline.py` 9 项通过，Ruff 与 mypy 通过。
- 在线复测：知识库“知识库问答”，原问题完整返回 6 个环节，状态
  `ANSWERABLE`，无缺失、冲突或验证降级，模型调用 0，耗时 33.33 秒；请求 ID
  `8db8731d-685c-4b51-b896-3fb73ed835a2`。
## 3.6 流程完整性实验回退

- 曾尝试通过固定尾部查询、流程标题优先和重新发布结构化文档补齐两份同名
  规程；该方案可能对其它流程问题造成过度约束，且不应在问答修复中修改用户
  知识库，因此已全部回退。
- 本轮新建的两个摄取任务及其发布文档已删除；原知识库文档和其它知识库未动。
- 保留 3.5 的通用 500 修复，以及此前回答模型空内容兼容修复。

---

## 4. 8-25 下午：飞防流程题修复（同主题不同名规程 + 数据重发布）

### 4.1 问题

用户问"无人机飞防作业从药剂配制到作业后废弃物处理完整流程有哪些关键
操作要点？"。知识库有 8+ 份植保/飞防规程（马铃薯/露地蔬菜/胡萝卜/稻纵
卷叶螟/林业等），其中 20250122 马铃薯规程最贴题（含 4 植保无人机要求 /
5 飞防作业人员要求 / 6 农药选择及配制要求 / 7 作业要求 / 8 作业后处理 /
9 作业情况记录），但旧答案只输出它的 4 个零散环节，且缺少药剂配制→
废弃物处理的完整链条。

### 4.2 根因（三层）

1. **RAGFlow 中 20250122 是旧版解析**：44 个 chunk 全部无"章节："头、正文
   缺顶层标题行、声明与标题用字不一致（"配置"vs"配制"）、"4 植保无人机
   要求"标题丢失 → `_document_process_sections` 解析出 0 章节 → 整份跳过。
   本地 ingestion 产物（`document_ir.json`）有完整结构化 section_path，但
   未发布到 RAGFlow（或发布的是旧版）。
2. **`_balanced_rerank_input` 非同名分支无章节保底**：`similar_names` 为空
   （马铃薯/露地蔬菜/植保飞防标题 Jaccard 相似度不足 0.7，不构成"同名对"）
   时直接 `accepted[:limit]` 按分数截断，顶层章节被高分文档挤掉。
3. **`_supplement_process_documents` 只补最高分文档**：马铃薯不在补充目标
   （非同名、非最高分），顶层章节从未进入候选。

### 4.3 数据层修复（已执行）

- 用本地结构化产物（`document_ir.json`，43 chunks 带完整 section_path）
  通过 `RagflowPublisher.build_plan + publish` 重新发布 20250122 到
  "知识库问答"数据集（新文档 `3b72619ea03711f19df2b1068bb78a1b`）。
- 删除旧版无章节头文档（`d1339a228b2e11f1b7c4d904b160df50`，44 chunks）。
- 旧版 chunks 已备份至 `D:\internal-rag\temp\kb-backup-20260825\`。
- 发布后 chunk 带"章节：1 范围 / 2 规范性引用文件 / 3 术语和定义 /
  4 植保无人机要求…"完整头。

### 4.4 代码层修复（retrieval_service.py，通用、无关键词）

- `_process_section_floor`：流程环节保底改为**"能解析出 ≥2 个不同顶层
  流程章节"的文档**才参与保底（流程规程的结构特征，如"5 飞防作业人员
  要求/6 农药选择及配制要求/7 作业要求/8 作业后处理"）。不依赖"1 范围"
  声明 chunk 是否已在候选内（声明 chunk 可能因分数低未进候选，但其它
  流程章节已在——之前用声明门控导致鸡生蛋问题，马铃薯被误排除）。
  无流程结构的文档（巡检要求、作业手册）不参与保底，避免任意章节误当
  流程环节占用名额。非流程章节（范围/术语/基本要求/安全注意事项/附录）
  不参与保底。
- `_balanced_rerank_input` / `_balanced_final_chunks`：floor 保底对**所有**
  能解析出流程环节的文档生效，文档按相关性（文档内最高 rerank/hybrid
  分）降序填充名额——最相关文档（如最贴题的马铃薯规程）的完整流程环节
  优先进入，其余文档按相关性依次分配，避免多份同主题规程均摊名额导致
  每份都不完整。删除死代码 `_single_topic_final_chunks`。
- `_supplement_target_names`：保持原逻辑（同名对 + 分数最高主文档）；
  马铃薯等其它相关规程靠主检索自然进入（数据修复后带"章节："头的
  chunk 主检索即可返回）。
- 测试：`tests/unit/test_retrieval_service.py` 新增/更新 4 个用例（流程
  环节保底、非流程文档不保底、非同名最终证据、补充目标）；
  检索+构建器套件 28 passed，ruff/mypy 全过。

### 4.5 验证状态

- 单测全绿（28 passed，3 个既有失败与本次无关，已用 git stash 验证）。
- 在线复现：马铃薯规程**完整输出 4 个核心流程环节**——飞防作业人员
  要求（5）/ 农药选择及配制要求（6）/ 作业要求（7）/ 作业后处理（8，
  含补喷/废液处理/废弃包装处置/农药贮存），即"药剂配制→作业→废弃物
  处理"核心链条；露地蔬菜/植保飞防/林业规程作为补充。证据聚焦最相关
  规程，无无关文档污染。
- 修复过程中 RAGFlow 容器曾卡死（Docker daemon 挂起），重启 Docker
  Desktop + `deploy\ragflow\start.ps1` 后恢复。

### 4.6 注意

- 知识库中其余旧版植保规程（露地蔬菜/胡萝卜/稻纵卷叶螟/林业等）若同样
  无"章节："头，可参照 4.3 用本地 `document_ir.json` 重新发布，无需改代码。
  重新发布后它们也能被 `_process_section_floor` 完整解析、参与流程保底。
- 补充检索 page_size 已放大至 100（覆盖整文档），确保"10 档案管理"等
  低相关度章节不被 top-N 截断。

---

## 5. 8-25 晚：5G 机巢河湖巡查题"提到文档名但无引用"修复

### 5.1 问题

用户问"同时使用 5G 低空基站与自动机巢开展河湖常态化无人机巡查，空域
通信、机巢运维、数据归档三类要求如何协同匹配？"。答案正文提到"依据
《低空5G通信基站建设要求》《无人机河湖智能巡查要求》《微轻小型无人机
机巢通用管理要求》等资料"，但响应 citations 列表里没有《无人机河湖
智能巡查要求》的引用。

### 5.2 根因

- **数据层**：RAGFlow 中《无人机河湖智能巡查要求》是旧版解析（13 chunks、
  无"章节："头、缺"9 资料归档"章节正文——只有目录提及）。本地 ingestion
  产物（`document_ir.json`，78 chunks 带完整 section_path，含"9 资料归档"
  正文）未发布。模型无从引用该文档的归档内容，只能列文档名或写"当前
  资料中未明确说明"。
- 当前在线实测：修复前该问题走模型路径（multi_hop），`ClaimTopicValidator`
  拒绝模型 claim（话题词与引用 chunk 不匹配）→ 降级 UNANSWERABLE；
  用户贴的"提到文档名无引用"是更早一次模型输出的形态。

### 5.3 数据层修复（已执行）

- 用本地结构化产物重新发布《无人机河湖智能巡查要求》到"知识库问答"
  （新文档 `de0578faa04c11f1a0e2bfecce2469ee`，78 chunks 带完整"章节："头，
  含"9 资料归档"章节 b9314dda76）。
- 删除旧版文档（`d0878e128b2e11f1b7c4d904b160df50`，13 chunks）。

### 5.4 验证

- 在线复测：答案现在有《无人机河湖智能巡查要求》的真实引用 [C4]（"在
  巡查过程中（如河湖智能巡查）[C4]，无人机将采集到的图像和数据实时
  传输到地面控制中心…"），并出现在响应 citations 列表——"提到文档名
  但无引用"已解决。
- 检索+构建器套件 28 passed（3 个既有失败与本次无关）。

### 5.5 剩余问题（未改，需用户决策）

- "数据归档要求"仍显示"当前资料中未明确说明"：multi_hop 的 q3 子查询
  （"同时使用5G低空基站与自动机巢开展河湖常态化无人机巡查 数据归档"）
  太泛化，河湖"9 资料归档"chunk（含"空域使用批复文件/航摄原始数据…"）
  hybrid 分低、未进 top-12 候选。精准查询（"河湖智能巡查 数据归档 空域
  批复 航摄原始数据"）可将其排到最前（0.478），说明是子查询构造问题，
  非数据问题。当前以 PARTIALLY_ANSWERABLE + missing_information 诚实
  呈现，符合 prompt 规则 7/9（证据不足如实说明）。
- 若需让"数据归档"有内容，方向：multi_hop 子查询按 aspect 关键词精简
  或做章节补充检索（复用 procedure 的 `_supplement_process_documents`
  思路），但需评估对其它 multi_hop 题的影响（泛化性）。

### 5.6 第二轮：multi_hop 检索增强（已实施，部分有效）

用户反复确认"数据归档"缺失。深入定位：

- **根因链**：
  1. 单主体 multi_hop（synthesis_mode=matrix，但 subjects=1 合并主体）
     走 `_execute_plan` 普通分支，每个 subquery 只做**一次全库检索**，
     无 aspect 补充检索（comparison 路径有）→ q3 候选里河湖归档排不进；
  2. `_secondary_cell_retrieval_query` 只覆盖"成果验收"一个 aspect 词表，
     "数据归档"等无词表 → 无补充检索；
  3. `ASPECT_RETRIEVAL_TERMS` 无"数据归档"条目，且"数据"在
     GENERIC_BIGRAMS 中 → `dimension_keywords("数据归档")` 只剩"归档"
     一个弱 bigram → `cell_has_substantive_evidence` 判定失败 →
     coverage 选择器给 q3 退选无关"引言"chunk。

- **已实施**：
  1. `_secondary_cell_retrieval_query`：词表未覆盖的 aspect 回退为
     `subject + aspect`（保留主体上下文，如"…河湖常态化无人机巡查 数据
     归档"）——通用，不新增词表；
  2. `_execute_plan` 普通分支：每个 subquery 增加 aspect 补充检索 +
     RRF fusion（与 comparison 路径一致）；
  3. `complex_candidates_per_subquery` 8→12（pydantic 上限），让
     coverage 选择器有更多候选。
  效果：q3 从"完全无候选"变为"有河湖文档候选"，河湖文档进入最终证据
  （q2 的 C8）；"9 资料归档"具体章节仍因 coverage 判定（弱 aspect 词）
  未入选。

- **未实施（需用户决策）**：让"数据归档"精确命中河湖"9 资料归档"
  章节，需处理 aspect 词表缺口（如 `ASPECT_RETRIEVAL_TERMS` 增补或
  GENERIC_BIGRAMS 对复合词的放行）——这属于词表机制改动，影响所有
  多维度题，风险需评估。
- 测试：检索+构建器+流水线 37 passed（3 个既有失败与本次无关）；
  ruff/mypy 全过。
