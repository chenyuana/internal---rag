# Internal RAG 改动记录（2026-08-17 起）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-13.md` 之后）的工作。
> 项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。
> 本文档按日期逐节追加改动/验证记录，沿用既有格式：问题 / 根因 / 修复 / 验证 / 改动文件 / 注意。

---

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-13.md`（已覆盖：跨文档综合题误判 fact 修复、跨文档技术参数侧漏召回修复、`_DIMENSION_EXPANSIONS` 维度词表扩展、`complex_candidates_per_subquery` 2→6 / `complex_rerank_input_k` 12→36）。
- **本会话范围**：自 2026-08-17 起的活动。会话开始时以熟悉工程、评估完成度为主，尚未产生代码改动；本交接文档自本次起按日期逐节追加后续改动/验证记录。
- **重要**：本会话的改动/验证记录在下方第 2 节中按日期逐节追加，不以计划项代替完成结果。

---

## 1. 当前状态快照（2026-08-17）

以下为本次会话实际核对的快照（非引用旧文档）。

### 1.1 环境与代码

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`、`http://127.0.0.1:8080/chat`
- 服务探测（2026-08-17 09:09 实测，HTTP 探活）：
  - Gateway `127.0.0.1:8080/health`：不可达（未运行）
  - Ollama `127.0.0.1:11434`：不可达（未运行）
  - RAGFlow `127.0.0.1:9380`：不可达（未运行）
- 结论：当前宿主机服务全部处于停止状态，执行任何解析/发布/问答前需先跑 `deploy\*` 启动脚本。

### 1.2 Git 状态

- 仓库仍无任何 commit：`main` 分支 `git log` 为空，整个工程为未跟踪状态（`git status --short` 全为 `??`）。
- **禁止** `git reset --hard` / `git clean` / `docker compose down -v`。
- 首要非功能任务仍是建立只含源码/文档/测试的基线提交（见 `docs/agent-handoff.md` 第 21 节）。

### 1.3 真实入库数据（D 盘实测统计）

- `data\ingestion\jobs\`：95 份 `job.json`（另有 190 个文件含 `review_history.jsonl`）。
- `data\ingestion\outputs\`：2481 个产物文件。
- `data\ingestion\originals\`：95 份原件。
- 任务状态分布：

| 状态 | 数量 |
|---|---|
| `warning` | 73 |
| `completed` | 21 |
| `needs_review` | 1 |
| `failed` | 0 |

- 发布状态：95 份全部 `published`。
- 路由分布：

| route | 数量 |
|---|---|
| `hybrid_review` | 60 |
| `hybrid_remote` | 27 |
| `native_text` | 8 |

- 交接文档 P1 中列出的"失败文档"当前状态（已在本次会话核实为 `warning`，非 `failed`）：
  - `…化学中毒现场采样检测技术规范…` → `warning / hybrid_remote`
  - `…常绿果树养分诊断无人机多光谱遥感监测…` → `warning / hybrid_review`
  - `…低空数字航摄与数据处理规范…` → `warning / hybrid_remote`
  - `…地面站通用要求…` / `…试飞风险科目实施要求…`（水印样例）→ `warning / hybrid_review`

### 1.4 测试基线

- 本会话（2026-08-17）已实际运行：
  - 完整 pytest：`285 passed, 3 failed`；3 个失败仍为既有解析问题（noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重），与本次改动无关；
  - ruff：通过（含本次改动的 `app/ingestion/pipeline.py`、`tests/unit/test_pipeline.py`）；
  - mypy：本会话未运行。
- 命令：
  ```powershell
  Set-Location D:\internal-rag\source\internal-rag
  .\.venv\Scripts\python.exe -m pytest
  .\.venv\Scripts\python.exe -m ruff check app tests
  .\.venv\Scripts\python.exe -m mypy app
  ```

---

## 2. 改动记录（按日期逐节追加）

### 2.1 修复：修订决定/修订说明页混入检索索引（2026-08-17）

#### 问题

智能问答实测 CCAR-27-R2 的"对复合材料旋翼航空器结构进行损伤容限评定和疲劳评定的选用优先顺序是如何规定的？"时，回答的引用证据里出现了 `[C6]`：条号=`None`、页码 8-10，展示片段是"起落架收起 / 自转纵向静稳定性"等完全无关内容，只有被截断的尾部才碰巧复述了第 27.573 条全文。用户据此发现"引用了一个无关 chunk"。

#### 根因

CCAR-27-R2 第 2–23 页是"交通运输部关于修改《正常类旋翼航空器适航规定》的决定"整块修订决定：用"一、将第X条修改为 / 十六、增加一条，作为第X条 / 二十、删去第X条"等动作句式列出三十四条修订，并用全角引号整页复述修订后的条款全文。这些复述与正文条款重复、没有独立条号，此前未被识别，被当成正文切块进入检索索引，成为噪声 chunk（第 8–10 页、第 10–11 页的无条号 chunk 即由此而来）。

#### 修复

`app/ingestion/pipeline.py` 参照目录页 `toc_excluded` 的做法，新增修订页识别：

- 新增常量 `REVISION_TITLE_RE`（页首"修订决定/修订记录/修订说明/修改记录"等标题）、`REVISION_ACTION_RE`（"将…第X条…修改为/更改为/顺延为"、"增加一条/一款/一目/一项，作为第X条"、"删去第X条"；其中"将"分支强制要求动作词，避免误伤正文"应将第X条确定的重量…移到…"这类引用）、`_REVISION_QUOTE_CHARS`（全角引号集合）。
- 新增 `is_probable_revision_page(text)`：三信号 OR——页首修订标题 / 修订动作句式 / 引号复述续页（≥3 行引号且比例 ≥0.3，覆盖跨页续行拉低引号比例的场景）。引号行若"引号后紧跟 ASCII 字母"（如色度坐标 `"X"`、`"Y"`、`"Z"`）则不计入，避免误伤正文里用引号标记数学变量的页。
- `parse_pdf` 第二层路由循环里（紧跟 TOC 检测之后）命中则 `indexable=False`、`route="revision_excluded"`。`split_blocks` 跳过 `indexable=False` 的页，且 `hybrid_pdf.parse` 先调 `parse_pdf` 再复用这些页，故原生与混合两条解析路径均生效。
- QA 输出新增 `revision_pages` 字段，便于诊断。

#### 验证

- CCAR-27-R2 真实文档回归：第 2–19、22 页全部标为 `revision_excluded` 且 `indexable=False`，第 8–10 页（C6 来源）不再进入 chunks；正文第 201 页"应将第X条…移到"不再被误标；修订范围内无条号 chunk 由 5 降至 2（仅剩封面/附件D表格的"文档前缀"块）。
- 全库泛化扫描（97 份 baseline.json）：仅 5 份被命中、共 30 页，且全部是 CCAR 适航规章的"修订决定/修订说明/修改记录"；技术标准/技术手册/测试报告均未命中。修正"引号后紧跟 ASCII 字母不计入"约束后，色度坐标页（CCAR-27 p189，`"X"`/`"Y"`/`"Z"` 变量）不再被误标。
- `tests/unit/test_pipeline.py` 新增 9 个用例（动作句式、附件条款、引号续页、低比例续页、正文不误判、少量引号不误判、引号变量不误判等），全部通过。
- 完整 pytest：`286 passed, 3 failed`；3 个失败均为既有问题（noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重），与本次改动无关。ruff 通过。

#### 改动文件

- `app/ingestion/pipeline.py`
- `tests/unit/test_pipeline.py`

#### 注意

- 该检测是通用的：任何含"修订决定/修订说明"的法规、标准（适航规章、GB 标准的修改单等）都会受益，不是 CCAR-27 特例。
- 泛化边界：全库 97 份中仅 5 份适航规章命中，误伤面窄。CCAR-23-R3 第 188/191/196 页属"页内修订说明"（正文夹带"本条删除/本条修订"标注），被动作句式命中——排除它们语义正确（是修订说明而非条款正文）；但若未来遇到"修订标注与正式条款正文混排在同一页"的文档，整页排除会连带丢失正文，届时应评估改为块级而非页级排除。
- 本次改动只作用于解析层，**不影响已发布数据**；要让修订页真正从检索里消失，需重启 Gateway → 重新解析受影响文档 → 重新发布到 RAGFlow（会消耗解析/发布资源，未擅自执行）。
- 残留（次要，暂未处理）：第 23 页（"本决定自…施行"结尾，引号比例 0.27 低于阈值）与第 20–21 页附件D HIRF 结构化表格（有 `table_id`，非文本噪声）未纳入排除。
- 附带发现（与本次无关，需另处理）：本次实测中 Reranker 始终 `fallback=True`、未生效，直连 `127.0.0.1:8003/rerank` 报 `input (684 tokens) is too large … increase the physical batch size`——llama.cpp 启动 `ubatch` 过小，容纳不下单个 chunk（约 806 字）。

---

## 3. 工作原则（沿用，不重复展开）

1. 先读 `docs/agent-handoff.md`，再读目标模块和对应测试。
2. 任何解析修复都必须用真实失败文档回归；不写破坏全局的正则。
3. 用户只有一个上传入口，不重新要求用户选择解析器。
4. 物理页码、印刷页码、条款号分开存储；表格文本、二维结构、图片资产三者都保留。
5. `needs_review` 不能绕过门禁发布；发布前优先 dry-run；删除本地任务不删除 RAGFlow 内容。
6. 修改后至少运行目标测试、完整 pytest 和 ruff；页面改动必须浏览器实测。
7. 不输出或提交密钥；不清除 Docker 数据卷；不把 D 盘数据复制回 C 盘。

---

## 4. 相关文档

- `README.md`
- `docs/agent-handoff.md`（最完整交接入口）
- `docs/pdf-parsing.md`（解析方法讲解）
- `docs/ingestion.md`、`docs/milestone-2.md`、`docs/milestone-3.md`
- `HANDOFF-2026-08-13.md`（本会话起点）
