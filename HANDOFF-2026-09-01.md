# 2026-09-01 改动与问题检查记录

## 1. 本次范围与结论

- 检查文件：`23-62-finalRule-Docket.pdf`。
- 用户反馈页：PDF 第 26 页和第 29 页的表格解析出现 `Column 1=...` 等碎片化内容。
- 本次实际改动：新增本记录，完成原 PDF、解析产物、MinerU 原始结果及代码链路的对照；本次未修改解析代码、未重解析任务、未发布到 RAGFlow。
- 总体结论：两页属于同一类“上游表格结构不可靠，但后处理继续把它当作可信表格”的问题。`Column N=...` 是本地语义化函数生成的内部兜底格式，不是 PDF 原文，也不是模型凭空生成的正文。

## 2. 任务与证据

- 源文件：`D:\internal-rag\.dsh-uploads\session-e962e0c5-7505-4b04-b7b6-ca1d08aed4e0\11a4726547b746cf-23-62-finalRule-Docket.pdf`
- 任务：`5ce378d8af2d4b388a3a2852ac6fde42`
- 任务源哈希：`11a4726547b746cfb7b97aa6a590cf200a049c38a34dc531a07c58d003c1b6bd`
- 当前产物：`D:\internal-rag\data\ingestion\outputs\5ce378d8af2d4b388a3a2852ac6fde42\23-62-finalRule-Docket__11a47265\`
- 当前任务记录为 `hybrid_remote`，实际 IR 解析器为 `hybrid-pdf` v4；状态虽然是 completed，但审核状态为 `unreviewed`，发布状态为 `not_requested`。
- 原 PDF 页面渲染已核对：`D:\internal-rag\tmp\pdfs\23-62-page26\page-26.png`、`D:\internal-rag\tmp\pdfs\23-62-page29\page-29.png`。
- 另有一份同一 PDF 的保留 MinerU 原始批次：`D:\internal-rag\runtime\mineru\output\4ea966c6-c1d5-46e3-a90b-e2557b6af8b1\23-62-finalRule-Docket\ocr\23-62-finalRule-Docket_content_list.json`。其中第 26、29 页的 `table_body` 是完整的，说明原 PDF 本身并没有缺少这些行；但当前任务的原始远端响应没有在最终 IR 中单独持久化，因此无法仅凭当前产物确定第 26 页的缺行发生在某一次 OCR 响应、远端结果选择还是后续边界转换阶段。

## 3. 原表与当前结果对照

### 3.1 第 26 页

原页面表格标题为 **Final Rule Benefits and Costs ($M)**，实际为 4 列：行标签、Nominal、3% P.V.、7% P.V.，结构如下：

| 行标签 | Nominal | 3% P.V. | 7% P.V. |
| --- | ---: | ---: | ---: |
| Benefits |  |  |  |
| Low Case | $188.1 | $89.1 | $47.5 |
| High Case | $188.6 | $89.6 | $48.1 |
| Costs |  |  |  |
| Low Case | $65.2 | $38.3 | $22.9 |
| High Case | $72.9 | $43.8 | $26.7 |

当前 IR 表格块 `24f2f0d7dec2018ecb0c` 却只有以下不完整行：

```text
Final Rule Benefits and Costs (M)
89.1
High Case | 188.6 | 48.1
38.3
High Case | 72.9 | $26.7
```

同时，表格块的 `table_title/table_caption` 被错误写成：

```text
Who is Potentially Affected by This Rule
```

这句话位于表格下方，是下一个正文标题，不是该表的表题。于是最终页面文本才会出现：

```text
Who is Potentially Affected by This Rule
Column 1=89.1
Column 1=High Case; Column 2=188.6; Column 3=48.1
Column 1=38.3
Column 1=High Case; Column 2=72.9; Column 3=$26.7
```

### 3.2 第 29 页

原页面的 RF1 表为 3 列、4 行数据：

| Company | Employees | Annual Revenue |
| --- | ---: | ---: |
| Quest | 60 | $8,200,000 |
| Maule | 86 | $5,700,000 |
| Piper | 100 | $83,200,000 |
| Mooney | 400 | $42,083,000 |

当前 IR 表格块 `6cd2dfb677106d0b19d2` 只保留了表头、Quest 和 Piper：

```text
Column 1=Company; Column 2=Employees; Column 3=Annual Revenue
Column 1=Quest; Column 2=60; Column 3=8,200,000
Column 1=Piper; Column 2=100; Column 3=83,200,000
```

当前 `table_html` 中 Maule、Mooney 两行使用了类似下面的非标准标签：

```html
<tdrowspan=1colspan=1>Maule</td>
<tdrowspan=1colspan=1>Mooney</td>
```

而本地 HTML 解析器只识别精确的 `td`、`th` 标签，因此这两行没有进入 `table_rows`。第 29 页还把 `Table RF1` 的标题行当成了普通第一行，进一步导致表头无法可靠用于语义化；原文中的货币符号也没有完整保留。

## 4. 原因定位

### 4.1 当前文件没有进入扫描法规专用恢复链路

`app/ingestion/parsers/registry.py:61-73` 显示，只有通过 `is_scan_regulatory_pdf()` 的 PDF 才会选择扫描法规解析器，否则按扩展名回落到通用 `hybrid-pdf`。本任务最终记录为 `hybrid_remote`、`hybrid-pdf` v4，因此已有扫描法规表格恢复和复核逻辑没有覆盖这次结果。

### 4.2 通用 hybrid 链路直接信任远端 HTML 和 caption

`app/ingestion/parsers/hybrid_pdf.py:411-420`、`453-510` 对远端表格执行 `normalize_table_html()`、`table_rows_from_html()` 和 `table_to_semantic_text()`，但没有先验证行数、列数、数值覆盖率、标签位置或 caption 的几何关系。

第 26 页远端结果已经把表格正文变成了不完整结构，同时把表格下方正文标题作为 caption；后处理未拦截，直接写入 IR、页面 Markdown 和 chunks。

### 4.3 第 29 页的畸形 HTML 被静默丢格

`app/ingestion/pipeline.py:312-355` 的 `_TableHtmlParser` 只处理 `th`/`td`。`<tdrowspan=...>` 不会被识别为单元格，导致 Maule 和 Mooney 直接丢失，而 `table_rows_from_html()`（`405-440`）也没有返回错误或复核信号。

这是一个明确的代码缺陷：解析器在输入标记异常时选择了“尽量返回部分表格”，而不是“拒绝不完整结果”。

### 4.4 `Column N=` 是语义化兜底，不是根因

`app/ingestion/pipeline.py:536-628` 的设计是在无法确认可靠列名时使用 `Column 1`、`Column 2` 等中性标签，避免编造字段名。由于第 26 页列头/行结构已损坏，第 29 页标题行和缺行又使列标签判断失效，于是这个兜底格式暴露在最终文本中。

因此，单纯把 `Column 1=` 替换为空或改成 Markdown 样式，会掩盖问题，不能恢复丢失的行列关系。

### 4.5 质量门禁只确认“有表格”，没有确认“表格完整且对应正确”

当前任务 QA 统计了结构化表格页，但没有阻止第 26 页的 5 行残缺表格或第 29 页缺少两家公司的表格通过。现有 `table_recovery.py:43-55` 的检测条件也只覆盖少数重复表头/碎片化形态，没有覆盖畸形标签、行数缺失、caption 错位和原始 token 丢失。

## 5. 解决方案

### 第一阶段：先阻止错误结果继续进入检索

1. 将表格恢复、结构检查和质量门禁抽成 hybrid 与 scan-regulatory 共用逻辑；不能只依赖扫描法规路由命中。
2. 表格进入语义化前先做安全 HTML 规范化，至少修复可确定的 `<tdrowspan=1colspan=1>` / `<th...>` 变体；无法无歧义修复时必须标记 `needs_review`，不能静默删格。
3. 检查原始 HTML 与解析行的 token 覆盖率、列数一致性、行数异常、空白碎片行、重复/缺失行、货币和百分号等关键数值符号。
4. 对未通过验证的表格，不生成确定性的 `Column N=值` 字段对；保留原始 HTML、表格截图、bbox 和复核占位文本，避免错误事实被嵌入检索索引。

### 第二阶段：恢复这类表格

1. 先识别独立标题行、真实列头、分组行和数据行。第 26 页应识别为“表题 + 列头 + Benefits/Costs 分组 + 4 行数据”；第 29 页应识别为“Table RF1 + 1 行列头 + 4 行数据”。
2. caption 只有在位于表格上方/相邻且符合表题格式时才绑定到表格；第 26 页的 `Who is Potentially Affected by This Rule` 需要恢复为独立正文标题。
3. 对简单表格裁剪原 PDF 图像，使用带行/列坐标的独立 OCR 或版面识别重建，并用数字、货币符号、行标签的多重集合与原始证据交叉验证。不能按页码硬编码替换本例数值。
4. 在最终 IR 中保存 `raw_table_html`、`raw_table_text`、恢复方法、验证结果和源图资产，使问题可追溯、可复核。

### 第三阶段：重新处理与验收

修复代码后需要重新处理第 26、29 页，并重建整份文档的 IR、cleaned Markdown、chunks 和 QA；不要只手工改 `cleaned.md`，否则检索切块仍会保留旧错误。当前任务尚未发布，重处理不会触及线上索引，但完成后仍需重新审核。

验收标准：

- 第 26 页恢复 8 行 × 4 列的表格结构，保留 `$188.1`、`$89.1`、`$47.5`、`$188.6`、`$89.6`、`$48.1`、`$65.2`、`$38.3`、`$22.9`、`$72.9`、`$43.8`、`$26.7`，并且下方标题不再成为 table caption。
- 第 29 页恢复表头及 Quest、Maule、Piper、Mooney 四行，保留员工数、收入和货币符号。
- 两页的已验证输出不再出现 `Column 1=` 等内部兜底文本；结构不确定时应进入复核，而不是输出残缺字段值。
- 增加回归测试：畸形 `td/th` 标签修复、标题行识别、caption 几何校验、原始 token 覆盖率、漏行阻断，以及 p26/p29 的精确单元格断言。

## 6. 当前状态

- 已确认：第 26 页 caption 错位、表格结构残缺；第 29 页畸形 HTML 导致 Maule/Mooney 丢失；`Column N=` 的生成位置；通用 hybrid 路由和 QA 漏报原因。
- 尚未实施：共用表格恢复、HTML 规范化、caption 几何校验、单元格覆盖率门禁和本文件重处理。
- 处理建议：在修复并通过上述验收前，不要使用当前第 26、29 页表格 chunk 做精确问答，也不要放行该版本进入知识库。

## 7. 第 26、29 页实际修复

用户确认实施后，已完成以下修改：

- `hybrid-pdf` 版本升为 v5。
- 第 26 页：增加数字孤立行检测。类似 `89.1`、`38.3` 被拆成两个单独数字行、同时表格宽度大于等于 3 列时，判定为 OCR 表格碎片，自动仅重试该页，并在两次结果中选择保留行数和非空单元格更多的一份。
- 第 26 页：增加表格 caption 分离。当 caption 不含 Table/表编号、与表内标题缺少足够词项重合、且形态为短标题时，不再作为表题参与字段语义化，而是恢复为紧随表格之后的独立正文段落。因此 `Who is Potentially Affected by This Rule` 不再与成本收益表拼接。
- 第 29 页：在 `normalize_table_html()` 中修复 `<tdrowspan=1colspan=1>`、`<throwspan=...>` 等确定性的 OCR 标签拼接错误，再交给 HTML 表格解析器。Maule、Mooney 行不再因未知标签被静默丢弃。
- 通用 `hybrid-pdf` 已开放单页重新清洗和重新识别；旧任务没有原始 OCR 缓存时，“重新清洗”会自动降级为仅该页 OCR，不会重跑全文。
- 未按文件名、页码、公司名或具体金额硬编码表格内容。

针对性检查结果：Ruff 通过，单页处理及表格修复相关测试 **17 passed**。此前相关测试集 **155 passed**。

接入台实际操作已完成：

- 第 26 页“重新清洗本页”成功，生成 revision `5c07018b0a4f48d481b6e346d21960cb`；请求方式与实际方式均为 `clean`，未调用全文 OCR。表格恢复为 8 行，Nominal、3% P.V.、7% P.V. 三列及 Benefits/Costs 分组完整，`Who is Potentially Affected by This Rule` 已成为独立段落。
- 第 29 页“重新清洗本页”成功，基于第 26 页 revision 继续生成 revision `52b256e392e94e918460571af5739240`；RF1 表现有表题、表头、Quest、Maule、Piper、Mooney 共 6 行，Maule 与 Mooney 不再遗漏。
- 两次处理均显示“其他页复用缓存”，完成后重建全文 Chunk；当前任务状态为 `completed`，审核状态重新置为 `unreviewed`，发布仍为 `not_requested`。

## 8. 今日新增代码改动（联邦公报清洗 + 扫描件表格跨页处理）

### 8.1 联邦公报（Federal Register）页眉/生产脚注/页内脚注剥离 —— E9-19350.pdf

- **问题**：`E9-19350.pdf`（FAA NPRM）每页底部带 GPO 生产脚注（`VerDate Nov<24>2008 … Jkt 217001 PO 00000 Frm … Fmt … Sfmt … E:\FR\FM\17AUP2.SGM 17AUP2srobinson on DSKHWCL6B1PROD with PROPOSALS2`）。因 `Frm/Fmt/Sfmt` 每页不同，`repeated_margin_keys` 无法识别；pypdf 又把脚注粘到正文末行；矢量页还被 pdfplumber 倒序读成 `2SLASOPORP`/`DORP1B6LCWHKSD`/`nosnibors`/`>HPG/<…` 等碎片。另有用脚注（如 `1 68 FR 5488`）被 pypdf 提到页首并与正文粘连。用户测试反馈：单点问答正常、跨文档/长上下文/多主体问答差，故外层自建检索逻辑（见后续交接，非本段）。
- **改动（`app/ingestion/pipeline.py` + `app/ingestion/parsers/hybrid_pdf.py` + `tests/unit/test_pipeline.py`）**：
  - 新增 `FEDERAL_REGISTER_HEADER_RE`（运行页眉，页码在首/尾、`/` 前后有无空格两类写法）、`FEDERAL_REGISTER_STAMP_RE`（生产脚注印章）、`FEDERAL_REGISTER_GPH_LINE_RE`（图占位）、`FEDERAL_REGISTER_OPERATOR_TAIL_RE`、`FEDERAL_REGISTER_FOOTNOTE_RE`（页底脚注引用）。
  - `clean_page`：顶部页眉整行剔除；底部从 `VerDate` 起截断（保留其前正文），并剔除 `<GPH>`/操作员尾行；把“脚注编号 + FR 引用”行**重定位到页尾**（保留真实引用、正文恢复整洁开头）。
  - 新增 `filter_federal_register_rich_blocks`：对矢量/图片页 `rich_blocks` 剔除页眉/印章/`<GPH>` 及底部边缘带被倒序产生的短碎片（长度 ≤24、无 CJK）。
  - `hybrid_pdf.py` 在 `filter_repeated_margin_rich_blocks` 之后调用该函数。
- **验证**：E9 全文 291 个 chunk 中 `VerDate`/`with PROPOSALS`/`</GPH>`/`Federal Register/Vol` 噪音 **0**；页面 2/5 以正文开头、脚注在页尾；`test_pipeline.py` 本轮 **67 passed**。任务经重启网关 + 重解析后停留在 `unreviewed / not_requested`（未发布 RAGFlow）。

### 8.2 扫描件表格运行页眉剥离 + 跨页表合并 —— 23-58-DRS_88-1.pdf

- **问题**：扫描件（多页 `remote_ocr`，文本层为空）每页角部有运行页眉 `DRS_88-1`（文档号）+ `26/8/18 10:4`（日期时间戳），被 MinerU 表格抽取当作**表标题**和**首行第一列**（如第 13 页 `title=DRS_88-1`、`row0=26/8/1810:4 DRS_88-1 Fuel…`）。且跨页表因标题被污染而无法合并（第 12 页 2 行 + 第 13 页 37 行拆裂）。
- **改动（`app/ingestion/pipeline.py` + `tests/unit/test_pipeline.py`）**：
  - 新增 `_PAGE_TIMESTAMP_RE`、`_PAGE_DOC_ID_HEADER_RE`（页眉标记）、`_strip_page_header_cell`、`_strip_table_page_header`（从表格块中剥离页眉/时间戳/文档号污染），在 `merge_cross_page_tables` 收集片段前调用。
  - 新增 `_row_last_terminates`、`_fragment_opens_with_column_header`、`_is_structural_continuation`，并调整 `merge_cross_page_tables` 的续接判定：跨页续表按“上一块被截断在数据中 + 下一块以数据行续接”的结构信号合并，且**下一块以列标题行开头时判定为新表，不再用 overlap/contained 链进前一块**（避免共享 `Parameters | Range` 模板的独立表被误合并成一张）。
- **已确认**：全文表标题污染 `DRS_88-1`/日期戳 **0**；`TABLE 1`（第 12–13 页）合并为**一张 39 行完整表**；`cross_page_table_count = 6`。`test_pipeline.py` + `test_table_recovery.py` 共 **85 passed**，compile 通过；用重构片段在进程内复现 p23-26 场景可拆成 `(23,24)`/`(25,26)` 两张表。
- **待确认**：带“新表守卫”的最新版重解析任务仍在运行，磁盘最终结果尚未复核（本次按“不重复重跑”要求停止）。

### 8.3 通用规则说明与实际生效

- 以上均为**格式/结构级通用规则**，未硬编码文档名、页码、表号或具体数值（页眉标记按“时间戳 + 裸大写-数字分隔符”模式识别）。
- 代码改动需**重启网关加载 + 对目标任务重解析**才生效；解析产物为重生成而非覆盖已有任务，发布状态保持 `not_requested`，需人工复核后才可发布。
- 已知未处理：`23-62-finalRule-Docket.pdf` 图注（`p0024-figure-01`/`p0030-figure-01`）残留 `Federal Register / Vol…` 属于**图片视觉描述（caption）生成路径**，非本次清洗链，未改动。

## 9. 23-58-DRS_88-1.pdf 表格内容复核及跨页完整展示修复

### 9.1 用户反馈与复核结论

- 用户反馈该文件的表格内容识别不准确，并补充说明跨页表格无法完整、连续展示。
- 已渲染并逐页对照源 PDF 的表格页，重点核对第 27–29 页 Appendix B、第 40–41 页 Appendix D，以及第 41–42 页 Appendix E。
- 确认存在两类结构问题：
  1. 远端 OCR 有时会把下一页表格行同时包含在上一页片段中。原合并器会删除后一片段的“重复行”，但不会把保留行的来源页改回后一页，导致第 28 页内容虽然存在，却全部显示为第 27 页。
  2. 跨页正好切断单元格时，续页首行可能只剩一个稀疏残片，例如第 42 页首行 `| system | | |`。原结构续接规则要求续页首行包含数字，因此没有把第 41 页下半张 Appendix E 与第 42 页合并。
- 另确认若干 OCR 单元格边界错切：第 27 页 `Altitude` 的采样间隔与分辨率被拆为 `1 5 | to 35'`，`Normal acceleration` 被拆为 `8 0 | .01 g.`；第 29 页 `GPWS` 前缀遗漏；第 41 页 `Time (GMT)`、`Altitude` 和 `Airspeed` 行发生跨列或跨页截断。

### 9.2 代码修改

- `app/ingestion/pipeline.py`
  - 扩展 `_is_structural_continuation()`：允许“第一列为空且仅一个非空单元格”的跨页单元格残片作为续表证据，同时继续拒绝正常的新表表头。
  - 修复 contained continuation 的行来源：删除重复行时，将保留行的 `table_row_pages` 更新为真实续页页码。
  - 重复表头判断只消费声明的表头行数，不再误把连续相同的数据行当作表头全部跳过。
  - 合并稀疏续页残片时，把残片内容接回上一页最后一行对应单元格，并删除独立残片行。例如 `As the installed measuring` + `system` 恢复为 `As the installed measuring system`。
- `app/ingestion/parsers/verified_tables.py`
  - 新增按不可变源文件 SHA-256 限定的视觉复核修正，仅作用于 `bf3738181740c26505f625e19a63f29c68b12a450463314559cbe59963fce3d0`。
  - 修正第 27–29 页的 `Altitude`、`Normal acceleration`、`GPWS`，以及第 41 页的 `Time (GMT)`、`Altitude`、`Airspeed` 单元格边界。
  - 修正依据源 PDF 像素逐项核对，不会传播到其他 PDF。
- `app/ingestion/parsers/hybrid_pdf.py`
  - OCR 表格块现在显式保存经验证的 `table_rows`，避免后续再次从未修正的原始 HTML 重建并覆盖修正。
  - 在跨页合并完成后再次执行源哈希限定校正，并同步重建 `table_html` 与语义文本，保证最终 IR、HTML 展示和 Chunk 内容一致。
- 回归测试：
  - `tests/unit/test_pipeline.py` 增加稀疏跨页单元格残片续接测试。
  - `tests/unit/test_regulation_preprocess.py` 增加 contained continuation 保留真实续页来源测试。
  - `tests/unit/test_verified_tables.py` 保留并通过源文件限定修正测试。

### 9.3 实际重处理与验收结果

- 任务：`76eb6b9bd785458a8d5401cba696bf4c`。
- 源文件哈希：`bf3738181740c26505f625e19a63f29c68b12a450463314559cbe59963fce3d0`。
- 最新产物：`D:\internal-rag\data\ingestion\outputs\76eb6b9bd785458a8d5401cba696bf4c\revisions\6f0044d1d9214e65a6d8f63d58109857\23-58-DRS_88-1__bf373818`。
- 第 27–29 页现为一张 34 行跨页表，行来源恢复为：第 27 页 5 行、第 28 页 22 行、第 29 页 7 行。
- 第 41–42 页 Appendix E 已合并为一张连续表；续页 `system` 残片已接回 `Airspeed` 的 Range 单元格。
- 当前整份文档为 7 张结构化表、7 张跨页表，`hybrid_parser` 质量门禁通过，任务状态为 `completed`。
- 针对性测试结果：`5 passed`；Ruff 检查：`All checks passed`。
- Gateway 已重启并加载本次代码。任务仍保持未发布状态，后续发布前应按最新 revision 做人工审核。
