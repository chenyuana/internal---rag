# 2026-09-02 改动记录：23-58-finalRule-2005-20245.pdf 表格修复

## 1. 范围与任务

- 用户反馈文件：`23-58-finalRule-2005-20245.pdf`。
- 任务 ID：`1ecf7086ec1544a888679c81ffdeb8e5`。
- 源文件 SHA-256：`43418b5c19a3341816c3c8df4a5d32eb35eb6b55c2e2a24a71fa5b47bd598ebc`。
- 原件：`D:\internal-rag\data\ingestion\originals\43\43418b5c19a3341816c3c8df4a5d32eb35eb6b55c2e2a24a71fa5b47bd598ebc\source.pdf`。

用户报告的“第 29 页表格”实际位于原 PDF 页脚标记为 **28/48** 的页面；该页包含 `Table 1.--Significant Differences in Assumptions and Parameters Used for the Rule and for the Proposal`。本次同时核对并修复了第 37、39 页表格。

## 2. 原因与原件核验结论

### 2.1 第 28 页 Table 1（用户所指第 29 页表格）

远端 OCR 将 3 列、23 行的表格压缩为 6 行，多个行标签、数值和分组标题被粘连，导致以下项目遗漏或无法对应：Part 121 Airplanes、Large Production Helicopters、Business Jets、Miscellaneous 等分组，以及其下的 CVR、RIPS、CPDLC、DFR/DFDAU 参数。

按原件恢复为 3 列：`Assumption/parameter`、`Final rule`、`Proposal`，共 23 行，保留了货币符号、百分比、逗号分隔数值和重复表头。例如：

- `Number of 30-Minute Memory Solid State CVRs to be replaced | 4,634 | 3,741`
- `Cost of RIPS | $4,180 | $2,820`
- `Price of Aviation Fuel | $1.60 | $0.75`

### 2.2 第 37 页 Appendix E / Appendix F

Appendix E（表 1）原件只有一个数据行：

`Stabilizer Trim Position or Pitch Control Position⁵ | Full Range | ±3% unless higher uniquely required | 1 | ³1`

旧 OCR 结果把 `Resolution⁴` 表头中的脚注标记及表格空白/边界区域拆成两个独立、错误的 `4` 单元格。原始 MinerU 响应只保留整张表的 bbox，未保存单元格级坐标，因此无法反推出每个错误 `4` 的精确像素位置；对照原件后可以确认它们都不是有效数据。表头中的 `⁴` 是脚注标记，最后一格的 `³1` 是“脚注 3 + 数值 1”，两者均已按原件保留。

Appendix F（表 2）旧结果误形成 6 列，且把采样间隔的五个 `2` 串到 `22222`。已恢复为 5 列、5 个数据行；Collective、Pedal Position、Lat. Cyclic、Long. Cyclic、Controllable Stabilator Position 的采样间隔均为 `2`，分辨率均为 `²1`。

### 2.3 第 39 页 Appendix M

旧 OCR 把 `Time or relative times counts` 的 Range、Accuracy 和采样间隔向右错移，并把 12a/12b、13a/13b 的 Remarks 跨行截断。已恢复为 6 列：Parameters、Range、Accuracy (sensor input)、Seconds per sampling interval、Resolution、Remarks；恢复了参数 1、12a、12b、13a、13b 共 5 个数据行。

## 3. 代码改动

### 3.1 受限视觉核验表格修正

文件：`app/ingestion/parsers/verified_tables.py`

- 新增 `FINAL_RULE_2005_20245_SHA256`。
- 增加第 28、37（两张表）、39 页的原件逐格核验行数据。
- 修正函数仅在以下条件同时成立时生效：
  1. 源文件 SHA-256 完全匹配；
  2. 页码匹配；
  3. OCR 行中仍包含表格特征词（例如 `Stabilizer Trim Position`、`Collective`、`Time or relative times counts`）。

因此修正不会将本次的具体数值传播到其他 PDF 或不匹配的表格。

### 3.2 HTML、页面文本和 Chunk 同步

文件：`app/ingestion/parsers/hybrid_pdf.py`

- 在远端 OCR 合并、单页 clean 重处理两条路径均应用已核验行数据。
- 当行数据被修正时，立即使用修正后的行重建 `table_html`，而不是只修改 `table_rows`。
- 这样 `document_ir.json` 的表格块、页面结构化预览、`cleaned.md` 的语义文本以及最终检索 Chunk 均使用同一份行列数据，避免“检索正确、页面预览仍错位”。

## 4. 实际重处理

网关重启以加载上述代码后，对同一任务顺序执行了单页 `clean` 重处理；每次均创建新的不可变 revision，未覆盖旧产物：

1. 第 28 页：`ec0d1690244344fbb6eb1e37cbc84768`
2. 第 37 页：`0a2dd05148694906a74416ec27c53383`
3. 第 39 页：`c2f0ffad7cc7427b8a981e567a952a81`

第 39 页完成后的活跃产物目录为：

```text
D:\internal-rag\data\ingestion\outputs\1ecf7086ec1544a888679c81ffdeb8e5\revisions\c2f0ffad7cc7427b8a981e567a952a81\23-58-finalRule-2005-20245__43418b5c
```

## 5. 验收结果

通过活跃任务 API 与最终 `document_ir.json` 回读确认：

| 页面 | 结构化表格结果 | HTML 与行数据一致性 |
| --- | --- | --- |
| 28 | 1 张表，23 行 × 3 列 | HTML 23 行，和 `table_rows` 一致 |
| 37 | 2 张表，分别为 2 行 × 5 列、6 行 × 5 列 | 无独立错误 `4` 单元格 |
| 39 | 1 张表，6 行 × 6 列 | HTML 6 行，和 `table_rows` 一致 |

代码验证：

- `pytest tests/unit/test_verified_tables.py -q`：**5 passed**。
- `ruff check app/ingestion/parsers/verified_tables.py app/ingestion/parsers/hybrid_pdf.py tests/unit/test_verified_tables.py`：**All checks passed**。
- 额外运行 `tests/unit/test_page_reprocess.py` 时，既有的 `test_hybrid_page_ocr_retries_fragmented_table_and_rebuilds_chunks` 失败。该测试使用不同的源文件和 SHA-256，不会命中本次受限修正规则；本次目标任务的端到端重处理和上述表格回读均通过。后续应单独排查该既有重试用例。

## 6. 当前状态与后续事项

- 本次修复产物未发布到 RAGFlow，审核状态仍为 `unreviewed`。
- 记录写入时（2026-09-02 01:43），该任务已再次进入全文处理：`status=running`、`attempts=7`、`page_reprocess=null`。待该运行完成后，应再次核对第 28、37、39 页，确认其新活跃 revision 仍保留本记录中的受限修正，再进行人工审核或发布。
