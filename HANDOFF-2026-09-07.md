# 2026-09-07 改动记录：`23-54-FINAL RULES_1998-4815.pdf` 表格完整性与跨页表名绑定

## 范围

- 处理文件：`23-54-FINAL RULES_1998-4815.pdf`
- 原始文件：`D:\internal-rag\data\ingestion\originals\86\862568bd8476189190a72129f7fa5382c00c6a3b971798468766a8d8f043f803\source.pdf`
- 重解析任务：`0cd565faa2834291b4739d1ecef63fa0`
- 输出目录：`D:\internal-rag\data\ingestion\outputs\0cd565faa2834291b4739d1ecef63fa0\23-54-FINAL RULES_1998-4815__862568bd`

本次处理第 22 页的 Table 2 和第 23 页的 Table 3：前者不应被切成多个检索块，后者应继承上一页印刷的表名。

## 问题与原因

### 第 22 页表格被分割

原 PDF 的第 22 页中，`Table 2 of §33.76 Medium Flocking Bird Weight and Quantity Requirements` 是一张完整的单页表格，共 18 行。其语义化文本约 2,106 个字符，超过了常规正文分块上限（约 1,400 字符）。`split_structured_table_block` 因而按行拆为两个块，并在后一个块中重复表头。数据未丢失，但单张表的行内容被分散，不利于完整检索和问答。

### 第 23 页表名未正确表达

`Table 3 to Sec. 33.76.--Additional Integrity Assessment` 的表名印在第 22 页底部，表格网格从第 23 页顶部开始。原有矢量表格标题绑定只在同一页内按几何位置匹配，无法把相邻页的页底标题绑定到下一页页首的无标题表格，因此第 23 页 Table 3 的 `table_title` 为空，标题保留为第 22 页的普通段落。

## 修复内容

修改文件：`app/ingestion/pipeline.py`

- 扩展表格标题与表格引用识别规则，支持英文 `Table ...` 格式。
- 新增 `_bind_trailing_table_captions(pages)`：仅在上一页下方 18% 区域发现编号表名、下一页上方 25% 区域发现无标题表格，且上一页表名前已有表格时，才执行跨页绑定。
- 绑定后将表名写入目标表格的 `table_title`，重新生成带标题的语义文本，并删除上一页原始标题段落，避免重复检索。
- 在 `merge_cross_page_tables()` 和 `split_blocks()` 中调用该逻辑；后者为幂等兜底，覆盖混合解析中矢量表格在后续阶段才加入的时序。
- 调整结构化表格分块：单页表格在不超过 2,400 字符时保持为一个完整块；跨页表格和超大表格仍沿用按行拆分策略。

新增测试：`tests/unit/test_regulation_preprocess.py`

- `test_keeps_moderate_single_page_table_as_one_chunk`
- `test_binds_bottom_page_english_caption_to_next_page_table`

## 重解析结果

当前重解析输出共 74 个块，状态为 `needs_review`（仍受既有人工表格审阅门禁控制，未发布）。

- 第 22 页 Table 2：仅保留 1 个表格块，含完整 18 行。
- 第 23 页 Table 3：表名为 `Table 3 to Sec. 33.76.--Additional Integrity Assessment`，并已写入该表格块的语义文本。
- 第 22 页：不再残留该 Table 3 表名的普通段落。

## 验证

已完成聚焦单元测试和静态检查：

```powershell
.\.venv\Scripts\python.exe -m pytest tests/unit/test_regulation_preprocess.py -q -k 'binds_bottom_page_english_caption or keeps_moderate_single_page_table'
# 2 passed, 65 deselected

.\.venv\Scripts\python.exe -m ruff check app/ingestion/pipeline.py tests/unit/test_regulation_preprocess.py
# All checks passed
```

同时用 PDF 页面渲染复核原件第 22、23 页的实际排版：Table 2 为完整单页表；Table 3 的标题确实位于前一页页底，表体开始于下一页页首。

## 追加改动：`23-53-finalrule28652.pdf` 第 13–15 页跨页表格

### 原因

该 PDF 的目标表格没有显式表名，表头位于第 13 页底部，主体行位于第 14 页，第 14 页底部的 `Ice` 行没有闭合底边并在第 15 页顶部继续。原有网格检测只得到第 13、14 页的封闭区域：第 13 页表头与第 14 页主体未稳定合并，第 14 页的 `Ice` 行被拆成普通段落，第 15 页续文也未被识别为表格。

### 修复

修改 `app/ingestion/parsers/vector_pdf.py` 和 `app/ingestion/pipeline.py`：

- 根据连续的竖直边界、列坐标和页首/页尾横线识别“未闭合的表格尾行”和下一页的续行网格。
- 将第 14 页尾部的 `Ice` 行按列提取，再与第 15 页的 `anti-icing system...` 续文拼接。
- 将匹配的下一页提升为表格候选页，并生成表格覆盖标记，避免续页内容留在普通正文中。
- 放宽带测量值的首行识别（如 `Birds: 3-ounce size...`），同时保留标题行、稀疏续行和独立表格之间的边界。
- 为跨页尾行更新来源页归属，最后一行标记为第 15 页。

### 真实 PDF 回归结果

使用当前代码直接解析原始 PDF（未依赖模拟数据）得到：

- 表格范围：第 13–15 页；
- 结构化表格行数：5 行（1 行表头 + 4 行数据）；
- 行来源页：`[13, 14, 14, 14, 15]`；
- `Ice` 行的 `anti-icing system, or a slab of ice ...` 已拼接回第二列；
- 文档块数由原结果的 55 降为 54，续页不再作为独立普通表格块。

新增跨页稀疏续行测试；`test_split_table_header.py` 与 `test_vector_pdf.py` 共 67 项通过，Ruff 检查通过。质量门禁仍要求第 13–15 页人工核验，未执行发布。

## 后续

该版本未执行发布；在人工复核队列确认后，再按现有流程发布解析结果。

## 追加改动：`23-49-FinalRule.pdf` 第 22–26 页跨页表格

### 原因

原 PDF 的 `Regulatory Evaluation Summary` 是一张连续表格：第 22 页给出表头，
第 23–25 页继续列出 Section 行，第 25 页的 `Section 91.205` 第一列在第 26 页
继续，第 26 页随后还有 `Section 91.209`。旧结果把各页拆成独立表格，将续页首行
误标为表头，并把 91.205 的续文留在普通段落中。

### 修复

修改 `app/ingestion/pipeline.py` 和 `app/ingestion/parsers/vector_pdf.py`：

- 以连续递增的 `Section` 编号作为无重复表头时的跨页续表证据；
- 保留只有一个非空单元格的页首续行，并拼接回上一页对应单元格；
- 识别按单元格分段绘制的横线，续行裁剪在第一条行边界处停止，避免吞入下一行；
- 将跨页表最后一页紧接的无标题单行表格继续并入原表；
- 从表格前的短标题段落恢复 `Regulatory Evaluation Summary` 为表名。

### 真实 PDF 回归结果

重解析后写回：
`D:\internal-rag\data\ingestion\outputs\4a5cf69e6fce4f5398fab4618f5197df\23-49-FinalRule__77b25f8b`

- 表格范围：第 22–26 页；
- 结构化表格：1 张，46 行（1 行表头 + 45 行数据）；
- `table_header_rows = 1`，续页不再重复计为表头；
- `Section 91.205` 续文与 `Section 91.209` 已归入同一表格；
- 表名为 `Regulatory Evaluation Summary`；
- `cross_page_table_count = 1`，`table_fragment_count = 6`；
- 质量门禁仍保留人工复核要求，未自动发布。

### 验证

新增有序 Section 续表、页首分段横线和跨页尾行回归测试；相关测试共 13 项通过，
Ruff 检查通过。原始 PDF 第 22–26 页已渲染并完成版式核对。

## 追加改动：`23-49_DRS_94-21.pdf` 第 25–29 页跨页表格

### 判定与原因

原 PDF 的 `Regulatory Evaluation Summary` 从第 25 页连续到第 29 页。第 28 页以
`Appendix F to part 23` 结束，第 29 页页首的 `Section 91.205` 和 `Section 91.209`
仍保持相同四列结构，且紧接其后才进入 `Evaluation of Provisions With Non-Negligible
Projected Costs` 正文，因此第 29 页两行属于同一张跨页表格。

旧逻辑只检查相邻末行和首行的递增 Section 编号；由于第 28 页末行是 Appendix，
递增编号证据中断，导致第 29 页被另建为表。两个无标题表又都沿用了此前被误判为
章节标题的正文残句 `23.1587 as proposed new paragraph (d)(10). Because ...`，造成
看似一致但语义错误的关联信息。该残句与表格内容无关。

### 修复

- 当同宽法规汇总表此前已有多个 Section 行、页末为 Appendix、下一页仍从 Section
  行开始时，继续判定为同一跨页表格；
- 从前一页紧邻的标题恢复 `Regulatory Evaluation Summary`，支持原生文本块无坐标时
  从页尾原始文本进行保守识别；
- 对表格检索元数据使用规范表名作为 `section_path`，不再继承陈旧正文章节；
- 在标题识别入口拒绝 `23.1587 as proposed ...` 这类“编号开头的正文句”被提升为标题。

### 真实 PDF 回归结果

修复结果已写回：
`D:\internal-rag\data\ingestion\outputs\8286f254d0a84f8bb9ec7e0f1d2af1cb\23-49_DRS_94-21__94ab664e`

- 表格范围：第 25–29 页；
- 结构化表格：1 张，46 行；
- 表名和 `section_path` 均为 `Regulatory Evaluation Summary`；
- 第 29 页 `Section 91.205`、`Section 91.209` 已并入同一 `table_id`；
- `table_series = [[25, 26, 27, 28, 29]]`；
- `structured_table_count = 1`，`cross_page_table_count = 1`；
- 全文已无 `23.1587 as proposed ...` 的错误 `section_path`；
- 人工表格复核门禁仍保留，未自动发布。

新增 Appendix→Section 跨页衔接、前页标题恢复及正文残句拒绝测试；聚焦测试 3 项
通过，Ruff 检查通过。

## 追加修复：`23-53-finalrule28652.pdf` 第 13–15 页关联信息

### 原因与判定

原页第 13 页明确位于 `Sec. 33.77 Foreign object ingestion.` 下，段落 (e) 随后写明
“under the following ingestion conditions”，表格从该处开始并延续到第 15 页。
`Engine operation` 与 `Ingestion` 实际是表头第 4、5 列，与表名或章节名无关。
旧解析把相邻表头单元格拼成 `Engine operation Ingestion` 并当作表名；由于表名优先于
章节上下文，该错误字符串又进入 `section_path`，造成错误关联。

### 修复与结果

- 新增表名与首行连续表头单元格比对，命中时判定为表头碎片并清空伪表名；
- 无有效表名时，从表前最近的法规标题恢复关联，允许 `Sec. 33.77 ...` 这类以句点
  结尾的法规标题；
- 第 13–15 页仍保持一张跨页表格，共 5 行、统一 `table_id`；
- `table_title` 和 `section_path` 均修正为
  `Sec. 33.77 Foreign object ingestion`；
- 全部输出中不再存在 `Engine operation Ingestion` 的错误表名或关联路径；
- 结果已写回原解析输出目录，人工复核门禁仍保留，未自动发布。

新增表头碎片伪标题回归测试；相关聚焦测试 3 项通过，Ruff 检查通过。

## 追加修复：`23-53-finalrule28652.pdf` 表格条号继承

### 原因

第 13–15 页表格属于 `Sec. 33.77 Foreign object ingestion` 的 paragraph `(e)`。
旧规则只完整识别 `§ N.N`，没有统一识别 `Sec. N.N` / `Section N.N`；因此当前条号
没有在第 13 页切换到 33.77，并错误沿用了更早正文中的 `33.78(a)(2)`。此外，表格
语义文本以章节标题开头，若再次按普通标题处理，会把已经识别出的 `(e)` 层级重置
为裸 `33.77`。

### 泛化修复

- 条号解析统一支持 `§`、`Sec.`、`Sec`、`Section`，不依赖具体文件名；
- 在当前法规条号下识别独立或复合段落标记，如 `(e)`、`(a)(2)`；字母标记切换
  同级 paragraph，数字标记可继承当前字母层级；
- 结构化表格的语义文本不再触发新的标题状态，避免覆盖紧邻段落层级；
- 表名中解析出的法规条号与当前条号一致时保留当前细分层级，不一致时才切换基准条号；
- 按用户确认，原文 `1\1/2\\-pound` 的字符形式不做规范化替换。

### 实际结果

- `article_id_raw = 33.77(e)`；
- `article_id_normalized = 33.77(E)`（系统规范化形式）；
- `article_parent_id = 33.77`；
- 章节仍为 `Sec. 33.77 Foreign object ingestion`；
- 页码仍为 13–15，跨页表格结构和原文字段保持不变；
- 结果已写回原解析输出目录，尚未自动发布或重建外部索引。

新增 `Sec.`/`Section` 识别和表格局部 paragraph 继承测试；4 项聚焦测试通过，Ruff
检查通过。更宽的既有测试集为 83 项通过、3 项既有失败（两个 OCR 表格一致性基线
和一个旧解析器版本断言），与本次条号修复无关。

### 追加：第 13–15 页表格 Chunk 文本去重

修复 `chunk_from_blocks()` 对结构化表格的上下文拼接：当表格语义文本首行已经等于
`section_path` 中的规范标题时，删除正文中的重复首行，仅由 chunk 上下文输出一次。
该处理只删除与上下文完全相同的前导整行，不会删除行内的 `Foreign object`、
`Speed of foreign object`、`Engine operation`、`Ingestion` 等真实字段标签。

重解析后的目标 chunk：

- 标题只出现一次：`Sec. 33.77 Foreign object ingestion`；
- 不再出现独立的 `Speed of foreign` 伪标题；
- 每个数据行继续采用 `字段=值` 表达，并保留统一 `table_id`、第 13–15 页来源范围
  以及结构化 `table_rows`/HTML；
- 新增表格标题与上下文去重回归测试，相关测试 3 项通过，Ruff 检查通过；
- 输出已再次写回原目录，仍保留人工复核门禁，未自动发布或重建外部索引。
