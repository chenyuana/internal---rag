# 2026-09-10 改动记录

本轮全部围绕「块级关联信息」（chunk 正文 / section_path / 关键词锚点）的**通用规则修复**。起因是用户逐份指出的四个具体页面，但每次落地都实现「这一类」，而不是「这一条」。文末「六、遵循的原则」记录了本轮用户明确提出的设计准则。

---

## 一、表格没有带上自己的说明句（`23-26-DRS_75-10.pdf` 第 24 页）

### 问题与根因
第 24 页 `Sec. 25.397 Control system loads.` 下的表格，其说明句 `(c) Limit pilot forces and torques. The limit pilot forces and torques are as follows:` 被作为**独立兄弟块**发布，表格块正文直接从 `Control=Aileron:` 开始；表格块关键词只有标题词（`PART 25/AIRWORTHINESS/…/Control/system/loads` + 6 个同引用中文变体），`Limit/pilot/forces/torques` 一个都没有，中文主题锚点也为零。

根因两处：
1. `build_chunks()` 遇到表格块时先把 `pending` 全部 flush，说明句必然留在上一个 chunk；
2. `extract_keywords()` 只从「结构标签 + 45 条术语表」取词，正文词汇除非命中术语表否则进不了锚点。

### 通用修复
- `pipeline.py`：新增 `_table_lead_in_block()`（紧邻表格、以 `as follows` / `following apply` / `the following table` / `如下` 收尾的块判为 caption，随表格第一个分块发布）与 `_attach_caption_sections()`（向量解析器把表标题当 section 时，前导句与表不在同一分组，按「条号一致」条件把前导句并入表所在小节）。
- 同规则应用到 OCR 路由 `scan_regulatory._build_chunks()`（并要求同小节，防止跨节误贴）。
- `regulations.py`：`extract_keywords()` 改为 ①caption 术语优先 ②去掉 Part/Chapter 横幅样板词 ③折叠同引用变体 ④只补**对侧语言**术语（`missing_concept_aliases()`）⑤上限 16→20。

### 证据
- 可比文档（关掉改动后能逐块复现存库 chunk_id 的 56 份）：悬空前导句 **21 → 2**，表格块数 205 → 205（无膨胀），19 个表块获得 caption。
- 该类文档：Part 横幅关键词槽位 23,751 → 992；重复引用槽位 21,410 → 15,346；跨语言槽位 21,358 → 24,570。
- Docket 75-10 p24：表格块正文与关键词均含 caption 内容；`limit pilot forces and torques` 命中 3/5 → 5/5。
- 新增 `tests/unit/test_table_lead_in_attachment.py`（7 例）。

---

## 二、附录横幅被粘进正文，整段附录归属错误（`23-26-DRS_75-19.pdf` 第 29 页）

### 问题与根因
第 29 页（标题页 + 延期提案表，共 12 个块）的 `section_path` 全部是 `PART 35 - … / Sec. 35.35 Blade retention test.`，应为 `APPENDIX I COMMITTEE IV (POWERPLANT) PROPOSALS DEFERRED GROUP 1`。

根因三层：
1. 原始 PDF 里第 28 页 `APPENDIX I …` 本是独立行；
2. `clean_page()` 的段落拼接启发式把它与上一句无缝粘连（前一行以 `.` 结尾 → 分隔符取空串）；
3. `heading_kind()` 只认 `Appendix X to Part N—…`（带破折号）与中文 `附录`，不认这种全大写 FR 横幅，于是标题事件丢失。

### 通用修复（`pipeline.py`）
- `_APPENDIX_BANNER_RE` + `split_appendix_banner()`：全大写横幅识别，并带**首尾边界守卫**（必须收尾上一句、开启下一句），句中引用 `See APPENDIX I for details` 与整段全大写正文都不会被提升；
- `heading_kind()` 认其为 `annex`；`clean_page()` 与块级 `_split_inline_heading_prefixes()` 两处拆出横幅；
- 表格 caption 只带横幅时（横幅只存在于 `table_title`）由 `_page_appendix_banner()` 作为该页开头的附录标题发出。

### 证据（`tmp/verify_appendix_fix.py`，用 baseline 原文 + 存储块文本重放）
| | 修复前 | 修复后 |
|---|---|---|
| p28 | 段落内粘着 `…P-84.APPENDIX I COMMITTEE IV…` | 独立 `[annex]` 横幅；被粘前句仍属 PART 35/Sec 35.35 |
| p29（11 段 + 表） | 全部 PART 35/Sec 35.35 | 全部 `APPENDIX I …` |
| p30 / p31 | 段落为 PART 35/Sec 35.35 | `APPENDIX II …` / `APPENDIX III …` |

全库扫描：只有 3 份文档、9 处横幅（75-19 四处、75-31 三处、75-26 两处），全部为真附录标题。
新增 `tests/unit/test_appendix_banner_attribution.py`（5 例）。

---

## 三、FR 前言标签未识别 + 句子碎片被当成表标题（`23-26-DRS_75-26.pdf` 第 1 页）

### 问题与根因
第 1 页表格的归属是 `["Proposed Rule Making have been issued:"]` —— 那是**上一句话的折行尾巴**，不是标题（原文第 46/48 行：`…Airworthiness Review Program Notices of` / `Proposed Rule Making have been issued:`）；而 `SUPPLEMENTARY INFORMATION:` 等 FR 前言标签根本不被识别为标题，整页前言段落 `section_path` 为空、标题为「文档前言」。

### 通用修复
- 表标题合理性：`_is_fragment_table_title()` ——标题以 `:` 结尾且上一段无句末标点 ⇒ 判为折行尾巴，仍留在表格正文，但不再提升为 `section_path`；
- 前言标签：见第五节 5.2（已从 6 个标签的枚举改为形态 + 位置规则）。

### 证据（`tmp/verify_preamble_fix.py`）
| | 修复前 | 修复后 |
|---|---|---|
| 表格 block/ chunk 的 section | `["Proposed Rule Making have been issued:"]` | `["SUPPLEMENTARY INFORMATION:"]` |
| 表格关键词 | `["Proposed","Rule","Making","have","issued"]` | `["SUPPLEMENTARY"]`（随后由表锚点补足，见第四节） |
| 该页前言段落 | `[]` | `["SUPPLEMENTARY INFORMATION:"]` |

---

## 四、表格锚点：用表结构做检索词

表格的 caption 往往很薄（`SUPPLEMENTARY INFORMATION:`），但表本身很具体。`regulations.table_anchor_terms()` 从表结构取三类锚点：

1. **表头字段名**（首行，≤4 个）：`Control` / `Maximum forces or torques` / `FAR Citation`；
2. **首列行标识**（≤2 个，含字母或汉字、≤24 字符）：`Aileron: Stick Wheel` / `Rudder`；
3. **标识符型取值**（≤3 个，`23.33`|`25.1305(a)`|`75-10`|`Pt. 23`，首段不以 0 开头，故 `0.403` 这类测量值不入围）：`75-10 / 75-19 / 75-20`。

标签归一化（都是形态规则）：脚注标记剥离（`torques2`、`Wheel*`）；OCR 把表头与值粘一起时在 `=` 处截断；过长限定语先砍括号/逗号再按词截到 40 字符；续表横幅行（`DISTRIBUTION TABLE—Continued` 被 OCR 切成 `…—COn` + `tinued`）整行不取字段名。

### 证据
- 全库严格对照（同一 chunk 开/关 `table_rows`，`tmp/anchor_strict_effect.py`）：286 个表块中 285 个获得锚点，共 **+1,383**（4.9/块），因 20 槽预算挤出 197（0.7/块）；**非表格 chunk 逐字不变**。
- Docket 75-26 p1：关键词 `["SUPPLEMENTARY"]` → `["Airworthiness Review Program Notice No.","Notice No.","Federal Register Citation","75-10","75-19","75-20","SUPPLEMENTARY"]`；查询 `75-19` 命中 **0/1 → 1/1**、`Notice No.` 0/2 → 2/2。
- **发布时重算**：`chunk_publish_facets()` 读 `raw_chunk["table_rows"]`，所以已解析过的文档**不必重解析，重新 publish 即可生效**。
- 新增 `tests/unit/test_table_anchor_keywords.py`（8 例）。

---

## 五、两项「去枚举」重构（用户要求：通用规则优先，不针对某类细节加料）

判据：**形态 / 位置 / 频率 / 关系** 类规则可泛化；**枚举内容词汇**（主题词表、文档类型标签名、语言样板词）不可泛化。

### 5.1 删除 47 词「横幅词表」（净删代码）

上一轮我曾据「关键词统计」判断表头混入了 Part 横幅，据此加了 `_TABLE_BANNER_WORDS`。本轮关掉该过滤器做全库实测（`tmp/table_anchors_raw.py`、`tmp/compare_banner_rules.py`）：

- 46 份有表格的文档、420 个表锚点中，**横幅词 0 个**；全大写 ≥8 字母的单元格只有 8 个，且全是真表头（`LIMIT FLIGHT LOAD FACTORS`、`AVERAGE LIMIT CONTROL SURFACE LOADING`、`DISTRIBUTION TABLE`、`SUBJECT GROUP:`）。
- 而该表在**误杀真表头**：`Normal and utility categories`、`Acrobatic category`、`2002 No.`（其规则「剥离结构词后为空即为横幅」会连带丢掉纯数字单元格）。

结论：上一轮的「污染」结论来自**旧产物的混合统计**（把标题路径的词算到表格路径上），是误读。**动作：删除 `_TABLE_BANNER_WORDS` 与 `_is_banner_field`**，测试改为记录实测依据并断言真表头不再被误杀。

### 5.2 标签行：形态识别 + 位置判定（取代 6 个标签名枚举）

- **形态**（通用）：`_LABEL_LINE_RE = ^[A-Z][A-Z0-9 ,.&'’()/\-]{1,48}:` —— 全大写标签 + 冒号（值可在同行或下一行）。全库 269 个标签行中 268 个是真标签（仅 1 个 caption 类），故形态识别可靠。`clean_page()` 增加「标签行独立成行」（纯形态，不改语义）。
- **位置**（状态机，零枚举）：文档开头的报头块由三件事结束——第一个**裸标签**（`SUMMARY:` / `SUPPLEMENTARY INFORMATION:`）、第一个**叙述段**（≥12 词）、或**行内标签的值本身已是整句**（`SUMMARY: This final rule amends …`）；此后每个标签开新节。合订本（一个 PDF 里多份通知）用数据驱动的重开规则处理：**文档的第一个标签名再次出现 ⇒ 新报头开始**（`first_label`）。
- 判定顺序：先跑既有 `heading_kind()`，已识别为标题的行（如 `PART 23 -- AIRWORTHINESS STANDARDS: NORMAL, UTILITY, …`）**不拆**，保持整行作为节标题。

### 已知代价与下一步（用户已确认：保留现状）
72 份文档 A/B（`tmp/verify_label_rule_ab.py`，legacy 6 名字 vs 新规则）：**66 份文档、2,947 处块归属变化**。

- 收益：`NPRM ACTIONS:`、`REGULATORY TEXT:`、`TERMINATION DATE:`、`FINAL RULE ACTIONS:` 等**从未枚举过**的真分节现在正确开节（此前其正文挂着陈旧小节）；
- 代价：合订本中 `AGENCY:` / `ACTION:` / `FOOTER:` 等报头标签会被当成节标题；个别 `SUMMARY:` 正文退回「文档前言」。
- 根因：报头标签与分节标签**排版同形**（`DATES:` 带行内值、`ADDRESSES:` 裸标签、`SUPPLEMENTARY INFORMATION:` 两者皆可），「元数据 vs 分节」是语义判断，位置只能近似。
- **已确认决策（选项 1）**：保留现状（形态 + 位置），不回退到枚举。
- **下一步（选项 3）**：把该判定交给摄入期 LLM 结构化通道（`llm_structure.py`，job 已有 `llm_structure_enabled`），按 schema 标注每个标签 `masthead` / `division`；届时位置规则退化为「无模型时的兜底」。代码中已在 `metadata_open` 处写明该计划。

---

## 六、附带修复

1. **浏览器打印页脚 `about:blank N/M`**：全库 57 份文档存在（最多 88 处/文档），此前会被写进 chunk 正文。`clean_page()` 剥离 + 纯页脚 block 丢弃。**页码/跳页不受影响**：引用页码来自 `positions[0][0]` 或 `published_page_number()` 解析的 `页码：` 头（`citation_location.py`），实测去页脚前后恢复值一致（1→1、2→2、3→3）；原始 PDF 未改动，`#page=N` 跳转照旧。
2. **「查看发送到 RAGFlow 的实际内容」面板与实际发布内容不一致**：面板由前端 `ragflowContentPreview()` 自行拼装，漏掉了 `检索锚点（非规范译文）：…` 行与真实 `important_keywords`。抽出单一渲染源 `ragflow.chunk_publish_facets()`（publisher 与预览共用），预览接口返回 `ragflow_content` / `ragflow_important_keywords`，前端优先渲染服务端值。新增 parity 回归测试；实测对 769-chunk 真实文档 821 次 preview↔plan 对比 **0 处不一致**。
3. `app/web/ingestion.html` 资源版本号更新（`20260910-publish-preview-1`），并修正 `test_ingestion_api.py` 中过期的 `?v=` 断言（该测试原本就是失败的）。

---

## 六之二、表格「caption」实为表内分组标签（`23-26-DRS_75-31.pdf` 第 65 页，用户第二轮报告）

### 问题与根因
第 65 页的表格 `section_path` 为 `["General Aviation Manufacturers"]`，应为 `APPENDIX I - MISCELLANEOUS PROPOSALS DEFERRED.`（附录横幅本身已正确识别：第 64 页 `[annex]` 块 + 第 65 页正文段都在该小节下）。

根因：该附录表**按提交方（proponent）分组**，组标签行印在表格顶部，被向量抽取器当成表格 caption；而 `build_chunks` 的「表标题比正文标题更权威」规则把 `table_title` 提升为 `section_path`，于是真实小节被顶掉。核对表内数据：`Proponent=General Aviation Manufacturers Association.`——所谓标题其实是**表内某个值的截断**（0.72）。

### 通用修复（`pipeline.py`）
- `_document_table_value_keys(pages)`：收集**本文档所有表格单元格**的紧凑形式（文档级上下文，`split_blocks` 本来就有全部 pages）；
- `_is_data_derived_table_title(title, value_keys)`：标题必须是某个表内值的**前导部分且覆盖率 ≥ 0.6**（即"标题是表内数据的截断"），才判为非标题。它仍留在表格正文里（证据不丢），但不再提升为 section。
- 与既有的 `_is_fragment_table_title()`（折行尾巴）并列生效。

### 判据的实测边际（重要）
全库 90 个表标题中，该判据只命中 **3 处**，且三处全是同一类真实缺陷；**87 个真 caption 一个不动**（`Table RF2`、`Table 1--Calibration Table`、`Sec. 25.101 General`、`Table B1 …` 等），最高命中比值 0.77，是真 caption 与数据标签之间的干净分界。

| 命中的表格标题 | 实为 |
|---|---|
| `General Aviation Manufacturers` | `General Aviation Manufacturers Association.` 的截断（75-31 p65，本次报告） |
| `Joint Airworthiness Requirements` | `Joint Airworthiness Requirements Committee.` 的截断（75-31 p66，同型） |
| `Airworthiness Review Program:` | `Airworthiness Review Program Notice No.` 的截断（75-26） |

### 证据（`tmp/verify_group_label_caption.py`，真数据重放）
- 75-31 p65（3 个表块）与 p66 表块：`section` 均为 `["APPENDIX I - MISCELLANEOUS PROPOSALS DEFERRED."]` ✓；
- 75-26 p1 表格：仍为 `["SUPPLEMENTARY INFORMATION:"]` ✓（无回归）；
- 新增 `tests/unit/test_table_title_plausibility.py`（4 例：分组标签不顶替小节、兄弟表的截断值同样被拒、真 caption 仍然提升、折行尾巴被拒）。

---

## 七、遵循的原则（用户本轮明确要求）

1. **案例驱动发现、规则驱动实现**：从具体页面出发，但每次都要问「它属于哪一类」，实现这一类。
2. **优先形态/位置/频率/关系类判据**，避免枚举内容词汇；已有枚举要标注为「待模型替换的兜底」。
3. **不为了一个不存在的现象加规则**：加规则前先量（5.1 就是反例，已删）。
4. **必须重解析 vs 只需重新发布要分清**：
   - 只需重启网关 + 重新 publish：关键词/锚点（发布时用 `extract_keywords` 重算）、发布内容前缀、questions；
   - 必须重解析：chunk 正文与边界（caption 合并）、section_path/标题（附录横幅、标签行规则、碎片表标题）、页脚剥离。
5. **不实证不声称**：所有结论都带全库扫描或 A/B 数字，并标注不可比的部分（OCR 路由用独立切块器，离线不可比，已排除）。

---

## 八、验证与状态

- 全量单元测试：**908 passed**；**10 项失败为既有失败**（`hybrid_pdf` 版本号 5≠4、CSS 缓存版本号、OCR 路由切块等，均在本轮之前就失败，已用「把本轮改动临时置空」的对照跑确认失败集合完全一致）。
- `ruff check app tests`：本轮改动文件全部通过；`mypy`：`regulations.py`、`domain_terms.py` 干净，`pipeline.py`/`scan_regulatory.py` 新增行范围内无报错（两文件其他位置为既有报错）。
- 三处已报告案例在本轮新规则下复验通过（`tmp/verify_reported_cases.py`）：75-26 p1 表格 = `SUPPLEMENTARY INFORMATION:`；75-19 p29/p30 = `APPENDIX I`/`APPENDIX II`；75-10 p24 = `PART 25 / Sec. 25.397`。
- 网关：运行中的进程（2026-09-10 10:02:50 启动）**未加载本轮改动**（uvicorn 无 `--reload`；吃改动需重启）。`23-26-DRS_75-10` 已于 10:04 用当时代码重解析（769 chunk，p24 已合并 caption），但其后的改动（预览一致性、`following` 停用词、附录/前言归属、表锚点、词表删除）尚未生效。
- 未发布：本轮涉及文档均为 `publication: not_requested`，未写入 RAGFlow 线上数据集。

### 相关脚本（`D:\internal-rag\tmp\`）
`verify_leadin_fix.py`（表格 caption 前后）、`corpus_leadin_ab.py`（全库 A/B）、`verify_stored_p24.py`（真实产物复验）、`verify_preview_parity.py`（预览一致性）、`inspect_publish_payload.py`（真实 payload）、`verify_appendix_fix.py`、`verify_preamble_fix.py`、`verify_table_anchors.py`、`anchor_strict_effect.py`、`compare_banner_rules.py`、`table_anchors_raw.py`、`probe_label_lines.py`、`verify_metadata_boundary.py`、`verify_label_rule_ab.py`、`verify_reported_cases.py`。

---

## 九、折行标题（`23-26-DRS_75-31.pdf` 第 68 页）+ 一次人为事故的处置（2026-09-10 下午）

### 9.1 用户报告的缺陷

p68 打印为两行的附录标题：

```
APPENDIX III - MISCELLANEOUS PROPOSALS REMOVED FROM CONSIDERATION FROM THE
FIRST BIENNIAL AIRWORTHINESS REVIEW
```

已发布数据里 ① section 停在 `... CONSIDERATION FROM THE`（`tmp/verify_7531_p68_replay.py` 打印的存储块可见），② 该页表格 `72a5b00d72bc3e27315f`（41 行，p68–70）的 section 变成了尾行 `FIRST BIENNIAL AIRWORTHINESS REVIEW`。

### 9.2 根因（两个，缺一不可）

1. **折行未合并**：`clean_page` / 富文本块都没有把「以连接词结尾的全大写行 + 紧随其后的全大写行」当成一个标题。
2. **合并被表标题去重抢先吃掉**（关键）：`split_blocks` 在过滤富文本块时，会把「文本等于某个表 `table_title` 的段落」当作重复 caption 丢弃。而尾行常常正是该表的 caption，于是合并永远看不到这一行——这是本页真正难找的地方。

### 9.3 通用修复（`app/ingestion/pipeline.py`，全部为形态/位置判据）

- `_HEADING_CONNECTOR_RE`（`THE|OF|AND|FOR|TO|IN|ON|FROM|BY|WITH|AS|A|AN` 结尾）+ `_WRAPPED_HEADING_MAX_CHARS=200` + `_is_all_caps_line()` → `_continues_heading(line, following)`：**两行都全大写**、首行以连接词结尾、合并后 ≤200 字符。
- `_join_wrapped_heading_lines()`（`clean_page` 的 kept 行）、`_join_wrapped_heading_parts()`（富文本块）。
- **去重顺序修复**：富文本块在做 caption 去重**之前**先跑一次 `_join_wrapped_heading_parts`（`rich_blocks = _join_wrapped_heading_parts(list(page.rich_blocks))`）；合并后的标题不再等于表 caption，于是既保住标题，也保住去重本身。
- `_title_repeats_section(title, section_path)`：表标题若已经是其上 section 标签的一部分（归一化后子串包含、长度 ≥6），不提升为 section——避免「用半截标题替换完整标题」。

### 9.4 证据

- 单测：`tests/unit/test_wrapped_heading_join.py` 5 项（`pytest` 全绿）。
- 真实数据回放：`tmp/verify_7531_p68_replay.py`（用存储 IR 里 p68–70 的真实标题/表格行重建富文本块）→ 合并为一条 `annex`、表格 section = 完整 APPENDIX III 标题、无 tail-only 分块。
- 全库量化：`tmp/corpus_wrapped_heading_scan.py`：75 份文档 / 34,237 块中，**59 块**的已发布 section 带截断全大写标题，分布在 **3 份文档**（75-31 36 块、75-10 22 块、23-29-finalRule16919 1 块）。
- 修复覆盖度：`tmp/coverage_wrapped_heading_scan.py` 显示**真正独立的截断标题只有 2 处**：75-31 p68（已覆盖）与 75-10 p73 `PART 127 - CERTIFICATION AND OPERATIONS OF SCHEDULED AIR CARRIERS WITH`。

### 9.5 已知未覆盖（诚实记录）

75-10 p73 的续行不是整行全大写，而是「首词全大写 + 紧接正文」：

```
PART 127 - CERTIFICATION AND OPERATIONS OF SCHEDULED AIR CARRIERS WITH
HELICOPTERS 2-214. By revising the title and introductory language of Sec. 127.105 to read as follows:
```

当前规则要求续行整行全大写，故**这一处仍不会被合并**。若要覆盖，需要一个同样属于形态类的推广：续行只取**行首连续的全大写词**（其余留给正文）。全库受影响文档数为 1，尚未实施，等用户确认。

### 9.6 事故与处置（必须记录）

- **事故**：为做一个 A/B（临时关掉一行看测试差异），用了 Windows PowerShell 5.1 的 `Get-Content -Raw` + `Set-Content` 回写源码。PS 5.1 默认按 ANSI(GBK) 读写，UTF-8 字节流按 GBK 两两配对时，非法配对（续字节 + 紧跟 `"`/`)`/空格等）被替换成一个 `?`，**一次吃掉 1–2 字节**：`app/ingestion/pipeline.py` 共 **322 处 / 207 行**受损（187 处在中文注释，135 处在代码或字符串字面量，部分收尾引号被吞导致语法失效）。ASCII 代码结构完好。
- **处置（用户决定：放弃字节级恢复，改用旧副本继续）**：`tmp/backups/pipeline.damaged-20260910-144111.py`（244,394 B，sha `a99c57e0`，与事故前逐字节一致）完整归档；`app/ingestion/pipeline.py` 用 09:42 的旧副本（sha `9bee4d62`）替换，然后把今天已验收的改动**逐项重放**回来。
- **重放方法与保真度**：从归档件按区域抽取代码（`tmp/extract_region.py`），文档字符串/正则等字面量一律取自损坏前编译的 `tmp/pipeline.pristine.cpython-312.pyc`（`tmp/pristine_text.py`），每项重放后用 `tmp/verify_port.py` 逐个函数核对：**所有字面量都等于 pristine 常量**（唯一例外 `document_heading` 是 set 字面量被编译成 frozenset 的误报）。
- **完备性**：`tmp/check_symbol_deps.py` 显示「只存在于归档版的符号」**0 个**。
- **重放清单**：① 值班页脚剥离 `about:blank N/M`；② 附录横幅归因（`split_appendix_banner`/`_page_appendix_banner`/`_split_inline_heading_prefixes`/`heading_kind` 分支）；③ label 形态规则 + `metadata_open` 边界机（含 `clean_page` 保留 label 行）；④ 标题可信度（`_is_fragment_table_title`/`_document_table_value_keys`/`_is_data_derived_table_title`/`_title_repeats_section`）；⑤ 折行标题（本节）；另补 `_attach_caption_sections`（caption 与表同 section）。

### 9.7 验证与状态（本轮最新）

- 全量单元测试：**920 passed / 11 failed**，失败集合与「事故前最新版」**完全一致**（10 项历史失败 + `test_regulation_preprocess.py::test_split_blocks_filters_fullpage_figure_and_duplicate_table_title`，后者在旧副本基线上同样失败，非本轮引入）。
- `ruff check app/ingestion/pipeline.py` 与五个特性测试文件：**全部通过**。
- `mypy app/ingestion/pipeline.py`：38 个错误，与旧副本基线**数量完全相同**（全部位于 600–2100 行既有区域，重放区域 3000 行之后零报错）。
- 五项特性测试合计 **28 passed**（折行 5 + 标题可信度 4 + 附录横幅 5 + FR 前言 6 + 表格承接 8）。
- **操作约束（用户要求）**：禁止用 PowerShell 文本命令读写源码；改动任何源码前先用 `tmp/backup_source.py <path>` 落时间戳备份到 `tmp/backups/`。
- 仍需用户确认：是否重启网关并重解析 75-31/75-10 等文档，让存储产物与修复后的代码一致（chunk 正文/`section_path` 类改动必须重解析）。

### 9.8 评估后不修改：表格标识符锚点里的 `Part 1`（用户第二轮追问）

**来源（已复现）**：`23-26-DRS_75-26` p48 表 `6af90b20eeb9f89fd1f2`（标题 `APPENDIX II COMMITTEE III (AIRFRAME) PROPOSALS WITHDRAWN BY PROPONENT`，14 行）的 `keywords` 含 `Part 1`。它是该表**第 3 列 `Comm. III` 的真实单元格值**（委员会 III 的第 1 分组，见 row1 = `25.613 | 214 | Part 1 | H-28 | Joint Airworthiness Requirements Committee.`），由 `regulations.table_anchor_terms()` 的标识符分支抓取：

- `_IDENTIFIER_VALUE_RE`（`regulations.py:394`）的 `(?:Pt|Part|Sec|§)\.?\s*[1-9]\d{0,2}` 命中裸写的 `Part 1`；
- 取用处 `regulations.py:473-481` 扫描第 1–12 行的**每一列**，形状匹配即收，取满 `_TABLE_IDENTIFIER_LIMIT = 3` 为止；
- 该表 anchors = `['14 CFR (FAR Sec.)', 'Proposal No.', 'Comm. III', 'Agenda Item', '25.613', 'Part 1', '25.615']`。同文档 p43 表 `cb389a8f01fcac0f6e12` 的 `Comm. III` 列同样产出 `Part 2`。

**全库实测（75 份文档）**：已发布标识符锚点 **136** 个 —— 43 来自法规引用列（列头 `14 CFR (FAR Sec.)` 等），93 来自其它列。93 个中：裸 `Part/Pt/Sec N` **4** 个（`Part 1`、`Part 2`、`Part 27`×2）、百分数/量纲列数值 **42** 个（`1.0`、`1.2`、`17.00`、`28.33`、`1.7`…）、其它列 **47** 个（含 `Notice No.` 列的 `75-10`/`75-19` 等真有用值）。裸 `Part N` 全库共 7 处：4 处来自 `14 CFR (FAR Sec.)` 列（`Pt. 23`/`Pt. 1`/`Pt. 25`/`Part 25`，真引用）、2 处是你报的委员会分组、2 处无表头列。

**派生性检验**（该代号是否在**本文档正文**里被当作条款引用，如 `§ 25.613`）：全库去重后被引用 **19** 个（`25.613`、`23.415`、`23.703`、`33.67`、`25.1305`…，整数部分全 ≥ 21），从未被引用 **80** 个（`1.0`、`20.0`、`15.2`、`17.00`、拼接垃圾 `23.6323.6723.77`、`Part 27`…）。注意该检验**不能单独当规则**：43 个法规引用列锚点中有 27 个未被正文引用（它们只是表格里列出的条款，属合理锚点）。

**决定（用户 2026-09-10）：不修改。** 理由：这类锚点只在查询里真的出现 `Part 1` 时才起作用，而法规类提问不会这么问（问的是 `25.613`、`Part 25`），留在那里无害；两种修法都比问题本身重——F1 需要列头结构词表（CFR/FAR/Sec/§/Part），F2 需要「整数部分 ≥ 10」这类领域形状阈值，属为不存在的现象加规则（与 5.1 删除 47 词横幅表同一原则）。

**数据可复用**：`tmp/measure_identifier_columns.py`、`tmp/breakdown_published_identifiers.py`、`tmp/measure_identifier_cited.py`、`tmp/show_bare_part_anchors.py`、`tmp/probe_7526_p48_part1.py`。将来若真出现误召回，可直接用它们给出改动前后的数字。

