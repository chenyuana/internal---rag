# 2026-08-31 改动与问题检查记录

## 1. 本次范围与状态

- 用户要求：按日期建立今天的改动文档，检查 `Docket4080__v0001__1918F11D.pdf` 第 4 页表格解析格式，并给出修改方案。
- 初次检查仅新增本记录；用户随后明确要求“修改并测试结果”，现已完成下述代码实现及样本验收。
- 第 2 至 6 节保留初次诊断与方案背景；第 7 节为最终实现和测试状态。未重启 gateway、未覆盖接入台旧任务、未发布至 RAGFlow、未推送 GitHub。
- 检查基线：`main` 分支，已有提交 `74eff14e663db560f4e8a633dd848b3cff4afc5b`。已有未跟踪文件 `.tmp-comparison-debug.json` 未改动。

## 2. 检查样本与证据

- 原件：`D:/scriptsFAA/faa_test/final_rules/1964/1964-09-28__Amdt_23-0__Airworthiness_Standards__Normal,_Utility,_and_Acrobatic_Category_Airplanes_(New)__1918F11D/Docket4080__v0001__1918F11D.pdf`
- 位置：PDF 物理第 4 页，印刷页码 17958，§ 23.21 Proof of compliance，段落 (b)。
- 对照方式：渲染原 PDF 第 4 页，并查看接入结果保留的表格截图，逐项核对单元格关系。
- 本次检查的接入任务：`3d8298ea1fe44a6aad36877b10f72bb2`。
- 接入结果目录：`D:/internal-rag/data/ingestion/outputs/3d8298ea1fe44a6aad36877b10f72bb2/Docket4080__v0001__1918F11D__584f6dda/`。
- 结果中的解析器：`scan-regulatory-pdf`，版本 `2`。本问题不能归因于仍使用 v1 或未重启 gateway。
- 第 4 页表格块：`0463af072ba4ed46de97`；截图资产：`assets/823a45c9ca6db6ac128c.jpg`；表格资产确实存在。
- MinerU 原始结果：`D:/internal-rag/runtime/mineru/output/d572ca9e-f905-4182-9a0a-5b32bac62e8e/Docket4080__v0001__1918F11D/ocr/Docket4080__v0001__1918F11D_content_list.json` 中的 `page_idx=3` 表格。
- 临时原页渲染：`D:/internal-rag/temp/pdf-review-0831/page4.png`，不属于仓库交付文件。

## 3. 原表的正确结构

原表为 **2 列、1 行表头、3 行数据，无合并单元格**：

| Item | Tolerance |
| --- | --- |
| Weight | +5%, -10% |
| Critical items affected by weight | +5%, -1% |
| C.G. | ±7% total travel |

以上省略单元格末尾句点，但保留数值、正负号、百分号和原文标签。`Critical items affected by weight` 在图中折成两行，仍属于同一个单元格；标签后的点线是引导线，不是额外列或单元格边界。上述内容仅为历史文档转录核对。

## 4. 当前错误及来源

### 4.1 上游结构识别错误

MinerU 原始 HTML 与接入结果中的 HTML 一致，主要内容如下：

```html
<table>
  <tr><td colspan="2">Item</td><td rowspan="2">Tolerance +5%，-10%.</td></tr>
  <tr><td>Weight</td><td></td></tr>
  <tr><td></td><td>Critical items affected</td><td></td></tr>
  <tr><td>by weight__ C.G.</td><td></td><td>+5%， -1%. ±7% total travel.</td></tr>
</table>
```

具体错误：

1. 两列被识别为三列，`Item` 被错误添加 `colspan=2`。
2. `Tolerance` 表头与 Weight 的数值被合并，并错误添加 `rowspan=2`。
3. 折行标签 `Critical items affected by weight` 被拆成不同记录。
4. `by weight` 与下一行的 `C.G.` 合并，两个不同项目的容差也被挤进一个单元格。

这是表格结构和数值对应关系错误，不只是排版美观问题；仅修改 CSS 不会纠正数据。

### 4.2 后处理沿用了错误结构

`app/ingestion/parsers/scan_regulatory.py` 将 `table_html` 交给 `table_rows_from_html`，随后直接生成 `table_to_semantic_text`，缺少结构有效性检查。

当前展开后的首行含重复的 `Item`，导致 `app/ingestion/pipeline.py` 中的语义转换退回“第 1 列/第 3 列”等中性列名。输出包括：

```text
记录1：第1列=Weight；第3列=Tolerance +5%，-10%.
记录2：第2列=Critical items affected
记录3：第1列=by weight__ C.G.；第3列=+5%， -1%. ±7% total travel.
```

前端 `app/web/static/ingestion.js` 的 `structuredTableMarkup` 对 HTML 做安全过滤后展示，无法自行恢复丢失的行列关系。错误结构已经进入 IR 和检索用文本，修复不能仅局限于展示层。

### 4.3 现有质量检查漏报

当前 `table_row_alignment` 检查主要针对“行数不超过 2，且出现多个 `(a)/(b)` 标签”的合并行现象。第 4 页有 4 行、不符合此特征，因此没有被标记；现有 QA 提示的是第 6 页行对齐问题，以及其他页的图像页和批注问题。

先前的结构性验收通过不能证明每个单元格的对应关系正确。本例说明验收应补充第 4 页逐单元格断言。

## 5. 修改方案（待审核，尚未实施）

### 第一阶段：先拦截不可靠结构

在扫描法规解析链路的表格语义化之前增加结构检查，综合判断以下可疑信号：表头混入数据值、异常重复列头、大量空白碎片行、跨表头与数据区的合并、标签续行与下一条项目混杂、同一数据格出现多组疑似独立值。

不能仅凭存在 `rowspan/colspan`、空白格或百分数就认定错误；合法复杂表格也可能具备这些特征。需要组合证据，避免误伤。

对无法验证的表格明确标记待复核，保留截图和原始 HTML；不要继续把错误配对作为已确认的字段事实用于检索。发布流程应区分“证据已保留”和“结构已确认”，必要时阻止未复核表格发布。

### 第二阶段：局部重识别与结构恢复

- 仅处理可疑表格区域，优先使用原 PDF 的高清局部渲染，无须默认重跑整个 46 页文档。
- 实施前确认 MinerU 中间产物是否含足够的词/行坐标；可用时复用，否则进行局部 OCR 获取几何信息。不能假设现有 HTML 已包含这些信息。
- 按文本横向位置恢复列，结合纵向位置与右侧数值行恢复记录；只在几何证据支持时合并左侧折行标签。
- 引导点线仅作为版式装饰处理，保留真实小数点、正负号、百分号及 `±`，避免全局字符清洗破坏数值。
- 原始 HTML 和图像保持可追溯；修复结构另存，并记录来源、复核状态。只有能验证各项目和值的对应关系时才自动接受，否则保留待人工复核状态。
- 不按文件名、页码或本例三个容差值硬编码替换；上面的人工核对结果用于测试基准，不作为生产规则。

### 第三阶段：统一展示与检索并补足验收

UI 表格、IR 行列数据和检索用字段文本应统一来自已验证的结构。主要修改范围预计为扫描法规表格处理、质量门禁及对应测试；若需要保留 OCR 几何信息，再调整上游适配接口。前端仅在必要时补充“待复核/已修复”状态展示。

验收至少覆盖：

1. 本例第 4 页精确恢复两列、一行表头、三行数据；逐项断言 Weight、完整折行标签和 C.G. 对应的容差。
2. `+5%`、`-10%`、`-1%`、`±7%` 的符号及单位不丢失，不互串。
3. 原始错误 HTML 必须触发复核标记，不能生成无提示的确定性错误配对。
4. 图像资产与原始 HTML 仍可回查，展示和检索文本一致。
5. 第 6 页已知合并行、合法跨行跨列表头、正常多行单元格、表下注释等回归用例不受误伤。
6. 重放本样本后检查新的 IR、QA 与接入台展示；若旧结果已发布，另行确认重新发布/替换范围，代码更新本身不会修复旧索引。

## 6. 本次验证与后续

- 已完成：PDF 原页与截图目视核对、MinerU 原始 HTML 与当前 IR 对照、解析后处理及前端展示路径检查、质量门禁漏报条件检查。
- 本次未运行新的自动化测试，未声称方案已通过测试。
- 下一步：用户审核后实施结构门禁与局部恢复方案，并以本页逐单元格结果验收。仅重启 gateway 不会纠正当前已生成的错误结构。

## 7. 用户确认后的实际实现与验收

### 7.1 实现范围

- `app/ingestion/parsers/table_recovery.py`（新增）：联合检测重复表头、表头混入带符号百分数及碎片行；沿用多标签合并行的检测信号。不会因为单独存在跨行、跨列或空白格就判错。
- 对支持的碎片化两列表格，从原 PDF 渲染高清局部图像，调用现有 MinerU 的纯文字 OCR（关闭表格和公式模型），获得文字行坐标、文字和置信度。
- 按列间分隔和纵向对齐恢复表格，允许项目名称向上折行；以原始 HTML 和独立局部 OCR 的词项及带符号数值多重集合一致性检查防止丢字、改值。坐标异常、列交叉、低置信度、漏字或正负号冲突均拒绝自动接受。
- 原 PDF 隐藏文字层存在错字、符号错误和重叠文字，未将其作为可靠恢复来源。初次局部截图 OCR 漏掉 C.G.，也未接受；改用原 PDF 高清裁剪后完整恢复。
- `app/ingestion/parsers/remote.py`：新增 `ocr_table_lines` 请求及行坐标读取，限制超时和 ZIP 展开规模。复用既有 MinerU 服务，无须新增 OCR 服务或修改 MinerU 安装目录。
- `app/ingestion/parsers/scan_regulatory.py`：解析器升为 **v3**；修复后 HTML、IR 行列和检索语义使用同一份结构；明确记录一行表头。保留 `raw_table_html`、`raw_table_text`、`table_recovery_lines`、恢复方法与状态，截图资产继续保留。
- 无法可靠恢复时，只输出待复核占位说明及证据引用，不输出未经确认的字段/数值配对，并产生失败质量门禁。每文档最多尝试 4 个支持形态的表格，每次局部请求最多 120 秒，超限或失败均进入复核。
- 不按文件名、页码、标签或本例容差值硬编码修复。当前自动恢复器有意限定为支持的两列形态，不宣称能自动修复任意复杂表格。

### 7.2 第 4 页结果

最终 IR 表格为一行表头加三行数据，与第 3 节原表一致：

```text
Item | Tolerance
Weight | +5%,-10%.
Critical items affected by weight | +5%,-1%.
C.G. | ±7% total travel.
```

检索语义中明确生成 `Item=C.G.；Tolerance=±7% total travel.`，不再出现错误的第 3 列、跨条目合并或表头混入容差。页面 Markdown 同步包含修复后的 HTML 和原截图。

### 7.3 验收方式与产物

本次采用 **46 页既有真实 OCR 批次的来源哈希校验回放 + 第 4 页真实局部 OCR 请求**。不是重新跑完整 46 页 OCR，也不是用预写正确表格替代模型输出。

- 输出目录：`D:/internal-rag/deliverables/2026-08-31/docket4080-table-recovery/`
- 验收报告：上述目录的 `acceptance.json`。
- 文档产物子目录：`Docket4080__v0001__1918F11D__584f6dda/`，包括 `cleaned.md`、`document_ir.json`、`qa.json` 及截图资产。
- 15 项样本验收检查全部通过，新增第 4 页精确单元格、原始证据保留及检索配对断言。
- 总计仍为 46 页、485 个切块、53 个资产；内容覆盖率 100%，文字页覆盖率约 93.48%（38、39、40 页仍为图像页）。
- **结构验收通过不等于全文质量门禁全部通过**：第 4 页恢复成功；第 6 页旧有合并行问题仍为 `needs_review`，不猜测其数值对应关系。接入台正常流程将要求人工复核，不能把这份文件描述为全部表格已修好。

复现命令（仓库根目录，批次路径沿用 8 月 30 日验收报告）：

```powershell
$report = Get-Content D:/internal-rag/deliverables/2026-08-30/docket4080-live/acceptance.json -Raw | ConvertFrom-Json
$sample = Get-ChildItem 'D:/scriptsFAA/faa_test/final_rules/1964/1964-09-28*/Docket4080*.pdf' | Select-Object -First 1
.venv/Scripts/python.exe -m tests.acceptance_scan_regulatory $sample.FullName --output D:/internal-rag/deliverables/2026-08-31/docket4080-table-recovery --live-table-recovery --replay-batches $report.replay_inputs
```

### 7.4 自动化回归

- 相关测试：`test_table_recovery.py`、`test_scan_regulatory.py`、`test_pipeline.py`、`test_remote_parsers.py`，最终 **120 passed**。
- 新增 17 个测试用例，覆盖精确单元格、数值/符号冲突、漏行、低置信度、坐标异常、合法合并单元格和空白格、脚注与原始证据保留、修复失败回退，以及更换标签/容差后的通用恢复。
- 修改范围 Ruff 检查通过，`git diff --check` 通过。
- 首次测试因既有默认临时目录权限失败，随后使用独立专用临时目录复测通过，未删除原临时目录或改动其权限。
- 最终完整单元测试：**630 passed，6 failed**。失败名称与 8 月 30 日记录一致，均不在本次修改模块内：
  - `test_regulation_preprocess::test_detects_noisy_ocr_table_of_contents`
  - `test_regulation_preprocess::test_cross_page_table_continuation_satisfies_caption_reference`
  - `test_regulation_preprocess::test_split_blocks_filters_fullpage_figure_and_duplicate_table_title`
  - `test_retrieval_service::test_comparison_runs_subqueries_and_reports_missing_coverage`
  - `test_retrieval_service::test_section_family_expansion_does_not_enable_llm_keyword_extraction`
  - `test_retrieval_service::test_complex_reranker_uses_cell_query_and_rejects_low_scores`

### 7.5 使用与部署注意

需重启 gateway 加载 v3，并在接入台对该文件重新处理；仅重启不会替换旧任务的解析产物。此次产物保存在独立验收目录，没有覆盖已有任务或索引。由于第 6 页仍需复核，不应未经核对就人工放行整份文件。

## 8. 第 5 页行内公式转换修复（后续追加，当前版本 v4）

用户指出截图中的速度和爬升率公式被输出为 `±bV_s0`、`0.02 ±bV_s0^2`，并明确要求修改。

### 原因与修改

- 对照 MinerU 原始结果：对应段落中的公式为 `\pmb { V } _ { { s } _ { 0 } }` 及其平方；这两处原始 OCR 未包含正负号。
- 错误发生在 `remote.py` 的 LaTeX 转纯文本阶段：符号替换没有命令边界，把加粗命令 `\pmb` 的前缀 `\pm` 替换为 `±`，留下 `b`。
- `_GREEK_OR_SYMBOL_RE` 增加完整命令边界，避免将长命令的前缀替换成数学符号。
- 显式移除完整的 `\pmb` 样式命令，保留参数、嵌套括号、上下标，并支持无括号形式；不全局删除正负号，不强行更改 OCR 的字母大小写。
- 扫描法规解析器版本更新为 **4**，包含此前第 4 页表格修复。

### 实际结果与测试

截图所对应的段落现输出：

```text
(1) Each airplane with a V_s0 of more than 70 miles per hour must be able
to maintain a steady rate of climb of at least 0.02 V_s0^2 (that is, ...)
```

- 新增 9 个回归用例，覆盖截图中的嵌套公式、嵌套及无括号加粗、真实 `\pm`/`\mp` 保留、长命令前缀不误替换、完整段落转换。
- 相关测试：**129 passed**；Ruff 检查通过。
- 完整单元测试：**639 passed，6 failed**，失败名称与第 7.4 节既有六项一致；另有既有 docstring 转义警告，未将其计为测试失败。
- 46 页来源校验回放 + 第 4 页真实局部 OCR：**17 项验收检查全部通过**。新增第 5 页指定公式精确检查与 `±b` 残留检查；第 4 页表格检查仍通过。
- 最新验收报告：`D:/internal-rag/deliverables/2026-08-31/docket4080-formula-fix/acceptance.json`。
- 最新产物目录：`D:/internal-rag/deliverables/2026-08-31/docket4080-formula-fix/Docket4080__v0001__1918F11D__584f6dda/`。
- 当前 46 页、484 个切块、53 个资产；正文修复后字符数改变导致切块边界调整，块类型数量和内容覆盖率未下降。

### 范围与剩余问题

本次修好的是加粗命令误转正负号及用户截图中的两处表达式，不等于全篇公式已逐一通过语义验收。同页另一段 `(a) Each multiengine airplane...` 的原始 OCR 自带 `\lambda` 及重复上标，当前仍为 `0.02 v_s0λ^v^2`，属于另一类上游识别问题，未猜测性替换。第 6 页表格仍需复核。

需重启 gateway 加载 **v4**，然后重新处理文件；本轮未重启服务、覆盖接入台旧结果、发布索引或推送 GitHub。

## 9. 单页重新清洗 / 重新识别（当前版本 v6）

### 功能及入口

用户确认实现单页重处理后，已在接入台“页面级诊断”弹窗增加两个按钮：

- **重新清洗本页**：使用保存的转换前 MinerU 原始条目，重新执行本页清洗。不会将已清洗文本冒充原始 OCR；旧任务缺少原始缓存时，自动回退到仅该页 OCR，确认框提前说明。
- **重新识别本页**：仅向 MinerU 请求选中页，再执行清洗和必要的局部表格恢复。其他页面复用已有内容与资产。
- 接口：`POST /api/v1/ingestion/jobs/{job_id}/pages/{page_number}/reprocess`，请求体为 `{"mode":"clean"}` 或 `{"mode":"ocr"}`，成功返回 202 后进入既有串行队列。
- 范围：目前仅开放 `scan-regulatory-pdf` 扫描法规 PDF；其他解析器不显示可用操作，后端也会拒绝。

### Chunk 与结果一致性

- 页面合并后，在本地重建整份文档的标题层级、Chunk、资产引用与质量检查，而不是将单页 Chunk 直接追加到旧集合。跨页 Chunk 和后续章节归属允许随标题变化重新计算。
- 重建结果写入 `outputs/{job_id}/revisions/{revision_id}/...` 独立目录；写入成功后才原子切换任务的当前输出路径。旧结果不删除，QA 记录上一版本输出路径、目标页、请求方式和实际方式。
- 出错时恢复旧任务结果、审核与发布信息，显示“旧结果已保留”，不会把半成品当作当前版本。
- 成功后清除此前审核结论和发布计划，必须重新审核、生成整份文档的新发布计划；本次没有发送任何内容到知识库。
- 含任何人工 Chunk 修订的文档会拒绝单页处理并提示冲突，避免重建时静默丢失人工内容。重复点击、运行中任务、无效页码/方式均有校验。
- 新解析任务开始保留 `raw_ocr_item`；旧任务无需整份重跑即可通过一次单页 OCR 为该页补齐缓存。

### 已发布任务限制

检查发现现有 RAGFlow 发布器先删除过时 Chunk 再写入新 Chunk，不能提供事务式替换或失败保留完整旧版的保证。本次未扩大修改范围去重写发布事务。

因此 **已发布、发布中、发布失败或已取得远端 document_id 的任务均暂不开放单页处理**。这样不把局部重处理结果误当新文档追加，也不破坏线上旧版本。该限制在前端和后端均有检查；未发布任务使用最新完整 Chunk 集正常走已有审核发布流程。

### 修改文件

- `app/ingestion/parsers/remote.py`：保留转换前 OCR 条目。
- `app/ingestion/parsers/scan_regulatory.py`：v6，单页清洗/识别、缓存合并、源文件哈希检查、资产安全读取、全文结构重建及阅读顺序修复。
- `app/ingestion/jobs.py`：单页队列请求、冲突拦截、独立版本写入、失败回退、审核/计划失效。
- `app/schemas/ingestion.py`、`app/api/v1/ingestion.py`：请求模式和接口。
- `app/web/ingestion.html`、`app/web/static/ingestion.js`：按钮、确认说明、禁用条件、队列提示；避免切换任务时对错误文档执行页操作。
- `tests/unit/test_page_reprocess.py`、`tests/unit/test_ingestion_api.py`、`tests/acceptance_page_reprocess.py`：单元、接口和真实样本验收。

### 实测结果

使用前一轮 v4 的 46 页文档产物，选择第 5 页：

| 操作 | 实测耗时 | 远端请求 |
| --- | --- | --- |
| 旧缓存不足，自动单页 OCR | 39.60 秒 | 仅 `[5]` |
| 使用新缓存重新清洗第 5 页 | 0.69 秒 | 0 次 |

耗时包含解析、合并及全文 Chunk 重建，不含产物写盘和排队时间，不是所有文档的固定时延承诺。

- 7 项真实验收全部通过：仅目标页 OCR、缓存清洗零远端请求、其他 45 页清洗内容不变、页数完整、Chunk 重建、资产保留、第 5 页指定公式正确。
- 报告：`D:/internal-rag/deliverables/2026-08-31/docket4080-single-page/acceptance.json`。
- 产物：报告同级的 `single-ocr/`、`cached-clean/` 子目录；未覆盖接入台已有任务。
- 新增单页功能测试 13 项；相关解析、接口、发布回归合计 **117 passed**。
- Ruff 检查和 JavaScript `node --check` 通过。前端入口通过静态页面接口测试；未在运行中的旧 gateway 上点击提交或替用户重启服务。
- 最终全量单元测试：**652 passed，6 failed**，失败名称仍与第 7.4 节记录一致。既有第 6 页表格复核要求和其他上游公式问题未由本功能自动解决。

### 使用方式

重启 gateway 并刷新接入台，打开已有任务的指定页，点击相应按钮即可；无需先重新处理整份文档。旧页缓存不足时第一次会做单页 OCR，此后同页纯清洗可复用缓存。现阶段需使用尚未发布、没有人工 Chunk 修订的扫描法规 PDF 任务。

## 10. “点击没有反应”与按键美化修复

- 根因一：接入台每 3 秒轮询时，即使任务未变化也调用 `renderJobs()`，浏览器中已显示的任务按钮被替换成新 DOM 节点，点击可能落在已脱离文档的节点上。
- 根因二：原生 `window.confirm()` 在当前内嵌浏览器环境没有稳定显示，用户看不到下一步提示。
- 修复：轮询结果与现有任务 JSON 相同时跳过列表和详情重绘；仅任务真实变化时刷新。新增页面内确认弹窗，点击后展示处理页码、模式及 Chunk/审核影响，取消不会发送请求。
- 按键采用主次配色、图标、`缓存优先`/`OCR` 标签、hover/focus 状态和明确的禁用提示；弹窗标题区改为自适应高度，避免新增操作挤压 PDF 预览。
- 静态资源版本号更新为 `20260831-page-actions-2`，避免浏览器继续使用旧 JavaScript/CSS。

### 浏览器实测

- 使用 `http://127.0.0.1:8080/ingestion` 刷新页面并打开样本任务第 4 页。
- 两个按钮均为 enabled；提示为“仅处理当前页 · 其他页复用缓存 · 完成后重建全文 Chunk 并重新审核”。
- 点击“重新清洗本页”后出现页面内“重新清洗第 4 页”确认框；点击“取消”后确认框关闭，未发送重处理请求，任务状态仍为 `warning`、`page_reprocess=null`。
- `node --check app/web/static/ingestion.js`、Ruff 和相关 API/解析测试均通过。

## 11. 单页按钮提交无反馈修复

- 原因：`submitPageReprocess()` 的 JSON POST 未设置 `Content-Type: application/json`。部分网关会按非 JSON 请求处理并返回 422；原实现的错误提示也不够显眼。
- 修复：补充 `Content-Type` 和操作用户标识请求头；提交失败时同时在任务详情区显示红色错误消息；静态资源版本更新为 `20260831-page-actions-3`，刷新后不会继续使用旧脚本。
- 浏览器实测：第 4 页点击“重新清洗本页”并确认后，任务队列显示“正在解析 / 自动解析 / 15%”，顶部“正在处理”变为 `1`；约 2 秒后恢复为 `需要复核 / 100%`，第 4 页文本和 Chunk 重新加载。说明 202 排队、处理中轮询和完成状态均可见。

## 19. 23-48.pdf 英文条款结构、表格与公式修复（hybrid v3）

- 用户审核诊断后要求“开始修改”，随后要求提速、不做过多测试。本节为本轮最终状态。
- 样本：`D:/23部历年修订/23部历年修订/2017年修订/23-48.pdf`，SHA-256 为 `cca74790718346dde44462b4ee5792a0bbbc3b89a64327200a2efb9e71d8a4ed`。已逐页对照原件渲染。
- 修复 `regulations.py` 对 `§ 23.415 Ground gust conditions.` 的条号识别；OCR 段落提升为条款标题，正文 Chunk 归属 `23.415`，不再全部归为“文档前言”。首页标题前的元数据表仍作为独立前言信息保留。
- 修复 `pipeline.py`：英文表格不再添加中文“记录N”及中性中文列名；标签/值型元数据表输出全部四项，不再误用首行数据作列头。中文普通数据表保留现有格式。
- 表格注释判定增加“只能有一个不同的非空单元格”限制，防止将 `(b)` 至 `(f)` 数据行剥离为表下注释。第二页保留表头与六行数据，(d)/(f) 的 K 单元格保持原件空白，HTML 与检索行数据一致。
- 新增 `parsers/section_ocr.py`：只对含英文 § 标题的 remote_ocr 文档进行结构恢复；将简单公式、紧随的 `where` 和完整变量定义绑定成一个公式块。仅在全部公式变量均有连续、明确说明时合并；不猜测缺失说明或吞并后文。
- 文本纠错限定为上述已核对的文件哈希及精确误识别片段，修复元数据字段名、`ft.-Ibs.`、引用后多余句点和 `momenton` 等；不根据其他版本修改原件内容。源 OCR 文本、HTML、坐标及纠错记录保存于 baseline/IR，文件校验值不匹配时不套用样本文字纠错。
- 移除被误作表格标题的同条号 `Sec. 23.415` 页眉。清洗文本保留标题和段落边界，修订来源行与 (c) 正文分段。
- 增加 `ocr_structure_consistency` 门禁，检查条款身份、HTML/检索行不一致及公式说明被拆散。接入台清洗区按安全转义后的结构块显示标题、公式与表格；旧结果仍回退到纯文本展示，静态资源版本更新为 `20260831-structured-text-6`。
- 实际 MinerU OCR 验收通过：两页均 `remote_ocr`，产出 5 个 Chunk；元数据表 4 行，正文表 7 行（含表头），五个公式变量说明处于同一 Chunk，未出现结构失败门禁。仅保留原 PDF 字形乱码转 OCR 的来源警告。
- 验收脚本：`tests/acceptance_section_ocr.py`。隔离输出：`D:/internal-rag/temp/23-48-structure-v3/23-48__cca74790/`。
- 已执行的相关测试合计 **267 passed、3 failed**；3 个失败均为本轮之前已知的 `test_regulation_preprocess.py` 用例：`test_detects_noisy_ocr_table_of_contents`、`test_cross_page_table_continuation_satisfies_caption_reference`、`test_split_blocks_filters_fullpage_figure_and_duplicate_table_title`。本轮新增 15 个用例全部通过，实际 PDF 验收通过。按用户要求不继续扩大测试。
- JS 语法检查通过；浏览器仅完成连接准备，未进行新的页面交互验收，不能据此宣称 UI 实测通过。
- 全程未启用 VLM、未覆盖接入台旧任务、未发布知识库、未推送 GitHub、未重启 gateway。使用方式：重启 gateway，强制刷新接入台，对这份两页文档重新解析；旧任务不会因重启自动变成新结果。

## 18. 23-48.pdf 数字字形乱码强制 OCR

- 样本为接入任务 `2d346ebe76884a229333a587168f0033` 的两页 `23-48.pdf`，SHA256 `cca74790718346dde44462b4ee5792a0bbbc3b89a64327200a2efb9e71d8a4ed`。两页均为 Type3 字体、无 ToUnicode，原结果分别误走 native_text/vector_layout。
- 当前接入台调用 ParserRegistry / HybridPdfParser，不调用工作区外层的 `doc_router.py`。修复实际位于 `pipeline.is_glyph_name_garbage()`：补充连续高密度 `/0/1/2…/i255` 数字字形检测，要求至少 40 个编号、至少 10 个不同编号、连续长串占比超过 50%，避免普通日期、路径、分数、数字表格误判。
- 复用现有文字层损坏路由，命中页面进入 MinerU `parse_method=ocr`，排除矢量文字/表格恢复；OCR 返回仍包含字形乱码时不标记成功，保留未解决的质量门禁。HybridPdfParser 版本升为 2。
- 未采用单一 `isalnum()<30%`：当前 pypdf 提取结果字母数字占比约 68.2%，数字乱码会被算作正常内容；也未把图多单独当作整份文件 OCR 条件。
- 真实端到端验证两页均为 `remote_ocr`，清洗字符计数 1398/963；恢复 §23.415 Ground gust conditions 等正文，轨迹仅 native 预检 + MinerU OCR，无 vector 解析。输出 `D:/internal-rag/temp/23-48-ocr-v2/source__cca74790`。未覆盖线上任务、未发布知识库，VLM 仍关闭。
- 回归 **100 passed**：覆盖数字字体乱码、零图片坏字层、好坏混合页按页路由、普通中英文/日期/路径/公式不误判，以及坏 OCR 响应不伪装成功；Ruff 通过。
- 使用：重启 gateway 后重新解析该文件。旧任务为 hybrid-pdf，不要将扫描法规专用的“仅重新清洗”误当成重新运行混合解析器路由；需走整份重新解析/重新提交入口。

## 17. Docket1186 第四页整页误删修复（v10）

- 根因：MinerU 将本页三张表格及右栏法规正文均标为 `header`；原始文字已识别，但扫描清洗器全部按页眉排除，导致第 4 页 searchable text = 0。并非空白页。
- 通用纠错仅在整页没有保留正文、至少 4 个有合理坐标的长页眉块、总文字至少 600 字符、存在明确 § 条款标题和至少两段 (a)/(b) 正文、纵向跨度及长段落满足条件时触发。正常有正文页面、缺条款证据、无效几何不恢复；页码和短印刷编号继续排除。保留原始 OCR 标签并新增质量警告。
- Table II/III/IV 的行列采用源图核对修订，严格匹配 Docket1186 的完整 PDF 哈希、第 4 页及各块原始 OCR 文本哈希。分别恢复 4/12/8 个数据行，保留 ±100、±70、负数及 +974 等源文符号；不是对任意表格的通用数字拆分规则。
- 真实旧缓存执行 `reprocess_page(..., 4, 'clean')`：恢复 10 个正文/表格块，三张表含表头分别 5/13/9 行，§91.170 及生效日期正文保留，第 1—3 页清洗文本不变。输出 `D:/internal-rag/temp/docket1186-v10/source__052b3a89`，未覆盖运行任务。
- 第二次单页清洗结果一致；clean 模式保留原始 baseline 文本，不再用修订表格 HTML 覆盖原始文本。最终清洗测试没有调用 OCR/VLM；诊断时尝试一次局部 MinerU OCR，但其表格结构仍有缺行，未采用该结果。VLM 开关仍为 false。
- 相关回归 **92 passed**，Ruff 通过，diff 检查仅有 CRLF 提示。重启 gateway 后重新清洗第 4 页即可应用。

## 16. 附录内表图引用关联（v9）

- 不调用 OCR/VLM。使用现有图题；本样本缺失图题通过已核对的元数据补齐 Figure 1—6，匹配 PDF SHA256、图片字节 SHA256 及物理页，不按图片排列顺序猜图号。
- 新增同文档同附录的显式 Figure/Fig. 引用解析，支持 `figures 5 and 6`。图号缺失、重复或明确引用其他附录时保守不匹配。
- 图片继续独立 Chunk。引用块追加目标图名与来源页；图块追加明确归属的引用行；双向 `related_chunks` 保存目标 Chunk、页码、资产等元数据。对附加文本重新计算 Chunk ID，再生成关联，避免悬空链接。
- 接入台 Chunk 增加“查看关联图 / 查看引用来源”跳转按钮；RAGFlow 发布内容包含引用上下文，tag 保存关联 ID。此处没有改写检索服务为递归扩展整块，双向上下文已包含在对应可检索文本中。
- 真实缓存测试：第 37 页 Table 1 对接第 38 页 Figure 1/2；控制面载荷表对接第 40 页 Figure 5/6。六张图仍各为单页单资产，53 个原始资产保留。测试结果 `D:/internal-rag/temp/figure-links-v9/source__584f6dda`，没有覆盖运行任务或自动发布到知识库。
- 回归：关联、分块、扫描法规、单页重处理、发布及接入接口测试通过；覆盖同附录重复图号拒配、跨附录不串联、复数引用及发布内容保留关联。
- 使用：重启 gateway、刷新页面，在任一相关页执行一次重新清洗，按新逻辑重建全文 Chunk；已发布的知识库内容需重新审核发布才能更新。

## 15. 无 VLM 的图片 Chunk 隔离与附录边界（v8）

- 按用户要求保持 `FIGURE_VLM_ENABLED=false`；撤回本轮尝试的 VLM 接入，没有执行模型识图。
- 扫描法规图片块独立生成 `figure` Chunk，每块保留图片资产引用和来源页，不再合并到相邻正文。现有图题可用作标题；缺失图题仍显示来源页，不编造名称或图内信息。
- 对 `APPENDIX BCONTROL SURFACE LÒADINGS` 这类缺分隔符的全大写标题，仅在紧随的非排除块以同字母章节编号（如 `B23.1`）开头时恢复附录边界；原始 OCR 保留。
- 用真实旧 IR 执行第 38 页 `clean`，重建全文 Chunk：38—40 页得到 6 个独立图片 Chunk，均为单页、单资产；第 41 页 `B23.1` 正确归到附录 B，且不带前六张图。53 个资产保留，全文 507 个 Chunk（所有图片均独立分块）。没有调用 OCR 或 VLM。
- 测试结果输出：`D:/internal-rag/temp/figure-chunks-v8/source__584f6dda`。相关回归 42 项通过。没有直接修改线上任务输出；重启 gateway 后单页清洗会按新规则重建全文 Chunk。
- 未完成项：缺失图名的图片仍需补录名称，不能仅凭哈希文件名实现按图意检索。

## 14. 第六页表格实际恢复（v7，后续修复）

- 修复了第 13 节尚未解决的表格内容恢复问题。`verified_tables.py` 保存经源图逐格核对的修订，严格匹配 PDF SHA256、页码 6 和原始表格单元格内容。它是指定文件的修订记录，不是通用 OCR 恢复算法；不同文件、页码、数字或表头不会套用该修订。
- `clean` 和完整解析均应用修订，无需额外 OCR；保留 `raw_table_html` / `raw_table_text`，恢复方法标记为 `source_visual_review_2026-08-31`，质量门禁继续给出来源修订提示。解析器版本为 7。
- 恢复结果：Stick = 60/30/空，Wheel = 75/60/空，Rudder pedal = 空/空/150，prolonged application = 10/5/20（Pitch/Roll/Yaw）。
- 真实旧 IR 实测调用 `reprocess_page(..., 6, 'clean')`：占位内容消失、表格进入 Chunk、其他 45 页清洗文本不变；46 页、53 资产、483 Chunk。不调用远端 OCR。测试输出位于 `D:/internal-rag/temp/page6-verified-v7/source__584f6dda`，没有直接覆盖运行中的任务。
- 回归：表格修订、已有表格恢复、扫描法规、单页重处理共 57 项通过。第 38 页本轮仅检查原图并提出图表检索策略，没有修改处理逻辑。

## 13. 第六页待复核表格与页面门禁颜色

- 第六页原始 MinerU 表格将多个逻辑行压成一行，数字列为 `607510`、`30605`、`15020`。现有占位文字表示结构未通过校验，不表示没有检测到表格。裁剪 OCR 未获得完整、可信的逐行结果，本轮没有加入猜测拆数规则，也没有宣称已自动恢复该表。
- 页面资产区增加待复核表格说明和可展开的原始 OCR 表格/HTML，仅供对照原图，不改变入库内容或放宽质量门禁。
- 页码卡片关联质量门禁：失败显示红色，警告显示黄色；支持 `Page N`、`pages [N, ...]` 和显式页码字段。“首个问题页”使用同一判断。无页码的文档级提示不猜测关联页。
- 静态资源版本更新为 `20260831-quality-pages-4`。本轮展示改动可直接用于旧解析结果，无需重新清洗全文。
- 验证：Node 语法检查通过，9 个页码/门禁映射检查通过；接入接口及表格恢复测试 **40 passed**；`git diff --check` 无空白错误（仅 CRLF 提示）。未完成浏览器端视觉复验。

## 12. 第 10 页多栏与图片阅读顺序修复（v6）

- 现象：第 10 页的 MinerU 内容条目顺序发生跨栏倒置；`0.0 at V_D`、`§ 23.335`、宽幅飞行包线图片等内容在清洗文本中被插入到错误位置。
- 范围：仅 `scan-regulatory-pdf` 页面，且只在存在有效 `bbox` 与宽幅图片锚点、并满足高置信度局部几何条件时修复；普通 PDF、DOCX、无几何证据或无宽图页面不改变原顺序。跨栏续句还必须落在下一栏顶部或宽幅图片下方的紧邻位置，避免把普通低栏文本误接到上一栏。
- 修复：保留 `source_ordinal`、`raw_ocr_item` 和原始 `raw_text`；清洗/Chunk 使用修正后的顺序。处理跨栏续行、同栏垂直倒序、宽幅图片锚点；页码、页眉、页脚和边缘批注不参与重排。
- 质量门禁：发生重排时新增 `reading_order_repair` 警告，提示人工对照原页，不静默掩盖布局异常。
- 实测第 10 页顺序：`(3)` → `0.0 at V_D` → `(c) Gust envelope` → gust 两项 → `(d) Flight envelope` → 图片 → `NOTE` → `§ 23.335` → `The selected design airspeeds`；第 10 页重建后页数仍为 46、资产仍为 53，全文 Chunk 按新顺序重建（样本为 483 个，原结果为 484 个，属于边界变化而非丢失）。
- 回归测试：扫描法规、单页重处理、接入接口共 **60 passed**；另有“低栏文本不误接跨栏”的防回归用例。Ruff、`git diff --check` 和 Python 编译检查通过。真实样本缓存清洗耗时约 0.69 秒；使用旧缓存时重新识别约 27.23 秒，仅请求第 10 页。
- 当前完整 `pytest` 结果为 **655 passed、6 failed**；6 项失败均为本轮之前已记录的预处理/检索测试，不涉及本次阅读顺序修复，相关目标测试无新增失败。
