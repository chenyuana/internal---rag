# 2026-08-12 改动记录

## 1. 多阶段 RAG 第一阶段改造

### 1.1 背景与判断

当前资料接入链路对文字型 PDF、法规条款和常规矢量表格已经具备较好的可用性。现场问答表现显示，现阶段主要瓶颈不再是“文字是否被解析出来”，而是复杂问题仍统一采用一次召回、一次截断、一次生成的单跳 RAG：

- 简单事实和精确条款问题通常可以直接命中关键 Chunk；
- 多文档对比容易由单一文档占满候选，无法保证对象和维度同时覆盖；
- 长流程问题只召回离散高相似片段，不能恢复连续章节和步骤顺序；
- 总结问题按相关度截取少量片段，不能保证文档覆盖率；
- 生成模型会在证据单侧缺失或适用范围不同的情况下强行补齐比较结论。

因此，从本日起将文档解析优化降为“由真实失败样例驱动的维护项”，主要研发方向转向多阶段检索编排和证据覆盖控制。

### 1.2 本阶段目标

第一阶段只改造复杂问题，不改变当前简单事实问答的快速路径：

1. 增加确定性的复杂问题规划结构；
2. 对比问题提取比较对象和比较维度，生成“对象 × 维度”子查询；
3. 多跳问题按独立要求生成子查询；
4. 每个子查询独立召回，再统一去重；
5. 最终证据选择从“全局分数截断”改为“必需单元覆盖优先，剩余预算再按相关度补充”；
6. 输出机器可检查的覆盖矩阵，区分 `covered` 与 `missing`；
7. 简单事实、精确条款和无法可靠规划的问题继续使用原单次检索路径；
8. 规划或复杂检索异常时安全回退，不让新能力降低已有问答可用性。

### 1.3 明确边界

本阶段不做以下事项：

- 不继续增加 PDF/OCR 全局解析启发式规则；
- 不引入可自由调用工具的自治 Agent；
- 不让模型自行决定是否扩大权限、知识库或文档范围；
- 不把特定作物、项目或任务的要求自动提升为设备类别通用要求；
- 不在第一阶段实现完整 Map–Reduce 总结和跨文档冲突裁决；
- 不在第一阶段依赖重新解析或重新发布全部现有文档。

### 1.4 计划中的代码结构

- `app/schemas/retrieval.py`
  - 增加 `QueryPlan`、`SubQuery`、`CoverageCell` 等结构；
- `app/services/query_planner.py`
  - 确定性识别比较对象、维度和独立要求；
- `app/services/coverage_selector.py`
  - 按子查询覆盖情况选择最终 Chunk，并生成覆盖矩阵；
- `app/services/retrieval_service.py`
  - 保留简单问题原路径；复杂问题执行多子查询召回、合并和覆盖优先选择；
- `app/services/evidence_judge.py`
  - 使用覆盖矩阵阻止证据缺失单元被判定为完整可回答；
- `tests/unit/`
  - 增加规划、覆盖选择、复杂检索回退和简单路径不回归测试。

### 1.5 初始运行约束

- 每个复杂问题最多生成 12 个子查询；
- 每个子查询独立召回，但复用原 ACL、文档范围和元数据过滤；
- 子查询候选在进入最终上下文前按 Chunk ID 去重；
- 最终证据预算仍受现有 `max_final_chunks` 限制；
- 第一轮优先为每个必需子查询保留一个证据；
- 第二轮再按重排分数、混合分数和文档多样性补充；
- 任一子查询无可靠证据时在覆盖矩阵中明确标记缺失，不要求模型强行生成差异。

### 1.6 验收标准

至少覆盖以下定向用例：

1. 简单事实题只调用一次检索；
2. 两对象、三维度对比题生成六个子查询；
3. 某一对象或维度无证据时覆盖矩阵标记 `missing`；
4. 同一文档不能仅凭大量相似 Chunk 挤掉其他必需单元；
5. 重复 Chunk 不重复进入最终上下文；
6. 复杂规划失败时回退到原单次检索；
7. 现有精确条号匹配、ACL、版本过滤、引用映射和 Reranker 行为不回归。

### 1.7 实施状态

- [x] 方案先写入当日改动记录；
- [x] 查询规划结构与规则；
- [x] 多子查询检索；
- [x] 覆盖优先选择与覆盖矩阵；
- [x] 接入证据判断和生成上下文；
- [x] 定向测试；
- [x] 完整回归与实际检索验证。

后续将在本文件继续记录实际实现、测试结果、运行参数和遗留问题，不以计划项代替完成结果。

## 2. 第一阶段实际实现结果

### 2.1 新增能力

已新增以下实现：

- `app/services/query_planner.py`
  - 简单事实题生成单查询计划；
  - 对比题确定性提取比较对象与比较维度；
  - 按“维度优先、对象展开”的顺序生成最多 12 个子查询；
  - 流程和总结题先记录对应扩展/综合模式，但尚未在本阶段执行完整章节扩展和 Map–Reduce。
- `app/services/coverage_selector.py`
  - 第一轮为每个必需单元保留一个候选；
  - 第二轮按重排/混合分数和文档多样性补足最终预算；
  - Chunk ID 全局去重；
  - 生成 `covered/missing` 覆盖矩阵。
- `app/services/retrieval_service.py`
  - 简单问题继续使用原单次检索；
  - 复杂对比问题执行多次轻量召回；
  - 每个子查询先保留 2 个混合检索候选；
  - 去重后最多对 12 个候选执行受控重排；
  - 每个覆盖单元使用自己的短查询计算 rerank 分数；
  - 低于 `complex_min_rerank_score` 的候选不计为覆盖；
  - 复杂检索出现 `AppError` 时回退原单次检索，并记录 `multi_query_fallback`。
- `app/services/evidence_judge.py`
  - 接收覆盖矩阵；
  - 缺失任一必需单元时降为 `PARTIALLY_ANSWERABLE` 或 `UNANSWERABLE`；
  - 不再只按原问题标点拆分判断复杂问题是否完整覆盖。
- `prompts/answer_generation.txt`
  - 允许列出共同要求；
  - 明确一侧无同口径证据时不得反推差异；
  - 禁止把特定作物、项目、任务要求泛化为设备类别通用要求；
  - 不再要求所有维度必须存在差异。

### 2.2 运行参数

新增默认配置：

```yaml
retrieval:
  max_complex_subqueries: 12
  complex_candidates_per_subquery: 2
  complex_rerank_input_k: 12
  complex_similarity_threshold: 0.15
  complex_min_rerank_score: 0.1
```

本机 reranker 改用标准 `RERANKER_PATH=/rerank`。复杂问题不再让每个子查询分别重排 30 个候选，避免 CPU 推理成倍放大；目前是每个单元保留少量候选，并按该单元的短查询进行受控重排，总重排预算不超过 12 个候选。

### 2.3 兼容性修复

真实调试发现：旧发布链路没有把隐式 `status=effective` 写入 RAGFlow 的 `document_metadata`。网关此前把该条件前置下推给 RAGFlow，导致网页聊天可召回、网关 `/retrieval/debug` 却返回 0 个结果。

现已改为：

- 不再把隐式 `status=effective` 前置下推给 RAGFlow；
- RAGFlow 返回候选后仍由网关执行本地状态过滤；
- 明确标记 `superseded` 等非有效状态的 Chunk 继续排除；
- 旧 Chunk 缺少 status 字段时按有效资料兼容处理。

### 2.4 测试结果

- 新增规划、覆盖选择、复杂检索、低分证据排除、旧状态兼容和异常回退测试；
- 本次相关定向测试：`13 passed`；
- Ruff：`app` 与 `tests` 全部通过；
- 完整单元测试：`265 passed, 3 failed`；
- 3 个失败均为 2026-08-11 已记录的既有解析测试：
  - noisy OCR TOC；
  - 跨页表格 continuation caption；
  - full-page figure/table title 去重。

本次改动没有新增完整回归失败。

### 2.5 真实知识库验证

使用问题：

> 对比植被多光谱遥感无人机、普通可见光巡检无人机，在成像波段、作业最佳时段、精度评价指标上有什么不同？

对正式知识库执行 `/api/v1/retrieval/debug`：

- 正确识别为 `comparison`；
- 生成 2 个对象 × 3 个维度 = 6 个子查询；
- RAGFlow 共返回 180 个原始候选；
- 每个单元保留少量候选后执行重排；
- 最终选择 8 个证据 Chunk；
- `reranker_used=true`；
- 总耗时约 33 秒；
- 覆盖矩阵和引用 ID 已随 debug 响应返回。

Gateway 已在确认无解析/发布任务运行后重启，并加载本次代码。

### 2.6 当前仍未解决的核心问题

第一阶段解决的是“对象 × 维度有没有候选证据”，尚未完整解决“候选证据的适用范围是否同口径”。Reranker 可以判断“普通巡检 + 作业时段”是否语义相关，但不能可靠判断该条款究竟是：

- 设备类别通用要求；
- 特定行业巡检要求；
- 特定作物/物候要求；
- 单一项目或示例参数。

因此，`covered` 当前表示存在高相关候选，不等于已经证明可以形成通用比较结论。生成提示已经禁止错误泛化，但仅靠提示仍不足以形成确定性保障。

下一阶段 P0 是实现证据适用范围判定：

1. 为证据增加 `task_type / applicable_subject / sensor_type / requirement_scope / normative_level`；
2. 覆盖单元从 `covered/missing` 扩展为 `covered/missing/scope_mismatch/conflicted`；
3. 对比双方只有在同一适用层级时才允许形成差异；
4. 特定任务证据仍可引用，但只能以“该标准针对该任务规定……”的形式回答；
5. 建立多文档对比评测集后再调整 `complex_min_rerank_score`，当前 `0.1` 只是初始保守值。
# Reranker 30 秒超时修复（2026-08-12）

## 故障现象

- RAGFlow 报错：`HTTPConnectionPool(host='host.docker.internal', port=8003): Read timed out. (read timeout=30)`。
- 8003 端口和 `llama-server` 进程均正常，故障不是网络不可达或服务退出。

## 根因

- 当前 reranker 使用 CPU-only 的 llama.cpp，启动参数为 `--parallel 1`，每个约 230～400 token 的候选片段耗时约 1.0～1.9 秒。
- 新建且启用 reranker 的 RAGFlow 助手仍保留默认 `top_k=1024`；实际一次请求提交了约 64 个候选片段。
- RAGFlow 的 LocalAI 客户端读取超时固定为 30 秒，服务尚未处理完全部候选时客户端取消请求，因此日志出现大量 `cancel task`。

## 本次修复

- 将所有已绑定 `qwen3-reranker-0.6b-q8_0@local-reranker@LocalAI` 的有效助手统一设置为 `top_k=12`，保留 `top_n=8`。
- 修复后复核数据库配置，并使用 12 个候选进行端到端耗时验证；目标是在 30 秒内稳定完成。
- 暂不单纯增大客户端超时：那只能隐藏候选规模失控，且会使聊天响应长期卡住。

## 验证结果与运维约束

- 相同长度测试片段下，8 个候选耗时约 5.99 秒，12 个候选耗时约 9.02 秒，均低于 30 秒读取超时。
- 修复后两个启用本地 reranker 的有效助手均为 `top_k=12`、`top_n=8`。
- RAGFlow 0.26.4 新建聊天助手的默认 `top_k` 仍为 1024。以后新建助手或重新启用 reranker 时，必须同时把 `top_k` 调为 12；`top_n` 可保持 8。这里的 `top_k` 是送入重排前的候选规模，不应与最终引用数量混淆。
- 若再次出现同类 30 秒错误，优先检查助手的 `top_k` 是否被界面保存回 1024，以及 8003 日志中是否出现成批的 `cancel task`，不要首先增加超时。

# 选项1：网关多阶段链路作为正式问答入口（2026-08-12）

## 决策背景

- 用 RAGFlow API 复现"外部1"聊天助手（id `224ab5b0…`，绑定知识库"法规11" `b5ecab6a…`）对对比问题
  `对比植被多光谱遥感无人机、普通可见光巡检无人机，在成像波段、作业最佳时段、精度评价指标上有什么不同？`
  的回答，发现**原回答不正确**：
  1. 对比对象缺失：三个维度均没有"普通可见光巡检无人机"侧的任何描述；
  2. 范围混淆：把大豆/野大豆/水稻等不同作物的多光谱要求（波段、时段、NDVI/NNI）堆砌成"差异"，
     属于"特定任务要求泛化为设备类别差异"；
  3. 检索漏召回：RAGFlow 原生单次检索上下文全被多光谱文档占满，可见光巡检文档
     （《变电站无人机巡检系统》3.2、《架空输电线路无人机巡检作业技术导则》3.1）未进入上下文。
- 根因是检索阶段单次召回被单侧文档占满 + 生成阶段无"缺证据不编造"约束。
- **用户决策：采用选项1**——网关多阶段链路（覆盖矩阵 + 覆盖优先选择 + PARTIALLY_ANSWERABLE 降级 + 修正对比提示词）
  作为正式问答入口；同时先用 RAGFlow 提示词约束止血。

## 止血改动（RAGFlow 外部1 提示词）

- 更新 `外部1` 的 `prompt_config.system`，追加 5 条对比类约束：
  逐维度列出双方证据、单侧缺失须明写"知识库中未找到【对象】在该维度的资料"、特定作物/任务要求不得泛化为设备类别通用区别、
  同一条款同适用两侧须列为共同要求、不要求每个维度必须存在差异。
- 备份：`D:\internal-rag\backups\ragflow-chat\external1-chat-20260812.json`（改动前完整配置）。
- 验证：改动后重跑同一对比问题，回答从"编造差异"变为逐维度先列多光谱证据、再明写"普通可见光……知识库中未找到对应的具体技术参数资料"，
  并明确标注"特定要求"、结尾说明"无法对比出通用区别"。**效果：不再编造差异，但可见光侧仍无实质对比内容（检索漏召回未由提示词解决）。**

## 网关 POC 验证（选项1 主链路实测）

- 用网关 `POST /api/v1/retrieval/debug`（知识库 `b5ecab6a…`）执行同一对比问题：
  - `query_type=comparison`，6 个子查询（2 对象 × 3 维度）；
  - 覆盖矩阵 **6/6 covered**——含可见光巡检无人机三维度；
  - 选中 8 个 Chunk，`reranker_used=true`，RAGFlow 原始候选 180 个。
- 可见光侧实际命中的证据（网关能检索到，RAGFlow 原生漏掉）：
  - q2 成像波段：《水稻群体长势无人机监测技术规程》5.2 传感器相机（可见光 RGB 成像）；
  - q4 作业时段：《架空输电线路无人机巡检作业技术导则》3.1（可见光相机/摄像机载荷定义——注意：这是设备定义，不是"时段"证据，存在单元与证据语义错配）；
  - q6 精度指标：《通信网智能维护技术要求 第6部分》5.6.1（可见光相机指标：像素 2400 万以上、功耗≤3W、重量≤300g）。
- **结论：网关多阶段链路能解决"可见光侧完全缺失"的检索问题，这是选项1的关键支撑证据。**

## 选项1 当前阻塞点（重要）

- 网关 `POST /api/v1/chat/completions` 生成环节返回 **502 `ANSWER_MODEL_INVALID_JSON`（ValidationError）**：
  answer 模型 `qwen3.5:9b-q4_K_M`（Ollama）对 `StructuredAnswer` 严格 JSON Schema 的生成不稳定，
  即便 `json_repair_attempts=1` 也未能修复。
- 检索端（debug）正常、覆盖矩阵正确，但生成端不可用，导致选项1 目前无法直接上线。

## 502 根因定位与修复（2026-08-12，已完成代码修复）

### 根因（三层）

用宿主 Ollama 手动复现网关生成调用，定位到 **三层独立问题**（前两层代码修复后，真实网关 502 的
错误类型从 `ValidationError` 变为 `ValueError`，暴露了第三层）：

1. **`$ref` 嵌套 schema 导致模型丢弃 `claims` 字段（主因）**
   `AnswerGenerator` 传给模型的 `StructuredAnswer.model_json_schema()` 含 pydantic 生成的
   `$defs`/`$ref` 嵌套（`claims.items` 引用 `#/$defs/CandidateClaim`）。手动复现对比：
   - 用原始 `$ref` schema → qwen3.5:9b **100% 输出不含 `claims` 的 JSON**（只含 answerability/answer/missing_information/conflicts），
     触发 `StructuredAnswer` 校验器 `at least one claim is required` → ValidationError；
   - 用**内联展开**（去掉 `$ref`/`$defs`、把 `claims`/`citation_ids` 提升为 `required`）→ 连续 3 次**稳定输出 2 个 claims**，校验全通过。
   这也解释了网关 502 的 67 秒耗时（首次生成 + repair 均用 `$ref` schema，两次都丢 claims）。

2. **"资料缺失"类 claim 无引用被 `CitationValidator` 拒绝（次因，schema 修复后暴露）**
   修复 schema 后，模型对"作业最佳时段未明确说明"这类**诚实缺失声明**会生成空 `citation_ids=[]`，
   而 `CitationValidator(require_citations=True)` 拒绝一切空引用 claim → 仍可能 502。

3. **旧发布 chunk 无 `status` 字段被 `CitationValidator` 误判为无效（网关重启实测暴露）**
   网关重启加载 flatten schema 后，真实 chat 请求错误从 `ValidationError` 变为 `ValueError`。
   检查选中 chunk 元数据发现：**旧发布链路未写 `status` 字段（`metadata.status=None`）**，
   而 `CitationValidator` 原逻辑 `if not include_historical and chunk.metadata.status != "effective"`
   把 `None != "effective"` 判为无效 → ValueError。
   注意 `retrieval_service._filter_reason` 已有兼容（`status is None` 视为 effective），但 `CitationValidator` 漏了。

### 修复内容

**`app/services/model_client.py`** — 新增 `flatten_json_schema()`，在发送前把 response_schema
递归展开 `$ref`/`$defs`、去除 `title`、把嵌套对象字段全部提升为 `required`、设置 `additionalProperties=false`。
`chat_completion()` 发送前调用它。对 qwen3.5:9b 有效（见上），对 verifier 等其他本地模型同样生效。

**`app/services/citation_validator.py`** — 两处改动：
- 允许"缺失/未说明"语义的 claim 无引用通过：
  新增 `MISSING_SEMANTIC_MARKERS`（未明确/未说明/未找到/未提供/无资料/无法对比等），
  空引用 claim 只要含这些标记即视为诚实缺失声明，否则照旧拒绝。防止无引用 claim 夹带编造事实。
- 兼容旧发布 chunk 无 `status` 字段：`chunk.metadata.status is None` 视为 effective，
  与 `retrieval_service._filter_reason` 的兼容策略一致；`superseded` 等明确非有效状态仍拒绝。

### 验证结果

- `flatten_json_schema` 对真实 `StructuredAnswer` schema：`$ref`/`$defs`/`title` 全部移除，
  顶层 `required` = [answerability, answer, claims, missing_information, conflicts]，
  `claims.items.required` = [claim_id, claim, citation_ids]。
- 端到端（Ollama，真实 evidence + flatten schema）：连续 3 次 HTTP 200，均输出含 `claims` 的合法 JSON，
  耗时 9~24 秒（原 502 为 67 秒）。
- `CitationValidator` 逻辑验证（ast 模拟）：正常引用通过；"未明确说明"空引用通过；编造事实空引用拒绝；
  `status=None` 通过、`superseded` 拒绝、`include_historical=True` 时 superseded 通过。
- **完整链路模拟**（flatten schema + answerability + CitationValidator 含 status=None 兼容 + 缺失声明宽容）全通过。
- 新增/更新单测：`tests/unit/test_model_client.py`（flatten_json_schema）、
  `tests/unit/test_citation_validator.py`（缺失声明宽容 + status=None 兼容）。
- 沙箱缺 httpx/pytest，无法完整跑单测；已在沙箱用 ast 提取纯函数验证逻辑，宿主机需重启后跑完整单测。
- 宿主单测已跑：`tests/unit/test_model_client.py tests/unit/test_citation_validator.py` → **6 passed, 3 skipped**
  （3 skipped 为 async 测试，pytest 环境缺 pytest-asyncio 插件，非代码问题）。

### 真实网关验证（2026-08-12，宿主机重启后）

- 重启 Gateway 后跑 `python -m pytest tests/unit/test_model_client.py tests/unit/test_citation_validator.py -q --noconftest` → **6 passed, 3 skipped**。
- `POST /api/v1/chat/completions`（对比问题，知识库 `b5ecab6a…`）→ **HTTP 200，不再 502**。
  响应：`status=ANSWERABLE`，`json_repaired=False`（一次生成直接通过），耗时约 69 秒。
- **网关回答（修复后）**：
  - 成像波段：普通可见光巡检搭载可见光相机/摄像机 [C4]；植被多光谱用于蓝 434~456nm 等多光谱成像 [C2]；
  - 作业最佳时段：当前资料中未明确说明两种无人机的具体作业时间要求（缺失声明，空引用被宽容放行）；
  - 精度评价：可见光侧重定性巡视（无特定量化指标）；多光谱通过遥感指数参数分析 [C7]；
  - 结论：成像波段存在差异（单色 vs 多谱段），作业时段资料未明确对比，精度评价前者定性、后者定量。
- **关键改进对比**：
  - RAGFlow 原生（止血前）：可见光侧完全缺失，把不同作物多光谱要求堆成"差异"；
  - RAGFlow 原生（止血后）：可见光侧仍无实质内容，仅明写"未找到"；
  - **网关（选项1）：可见光侧与多光谱侧均有证据，双对象对比完整，缺失如实声明，不编造。**
- 遗留（已知，非本次阻塞）：q4 可见光"作业时段"命中的是《架空输电线路无人机巡检作业技术导则》3.1 设备定义，
  属"单元与证据语义错配"，覆盖矩阵虽标 covered 但证据未直接回答时段——对应后续"适用范围判定"P0。

### 待办（宿主机执行）

- 无阻塞项。可继续推进：前端接入 / 覆盖选择语义完善 / 建立对比评测集。

## 网关 vs RAGFlow 回答评测（2026-08-12，字段级并排）

对同一对比问题，对网关（选项1 多阶段链路）与 RAGFlow 外部1（止血后）回答做 7 维字段级并排评测。

### 评测结论

| 维度 | 网关 | RAGFlow(止血后) | 说明 |
|---|---|---|---|
| 对象覆盖 | 5/5 | 3/5 | 双对象都有内容，但 RAGFlow 可见光侧用荒漠植被调查顶替 |
| 证据来源正确性 | 5/5 | 2/5 | 网关可见光=C4《架空输电线路巡检导则》(真巡检)；RAGFlow 可见光=荒漠调查 |
| 范围混淆 | 5/5 | 1/5 | RAGFlow 把荒漠植被调查的可见光要求描述成巡检无人机特点 |
| 编造 | 5/5 | 3/5 | RAGFlow 把多光谱规程的 0.1m/5cm 塞给可见光侧(软性编造) |
| 缺失声明 | 5/5 | 3/5 | 网关诚实声明"作业时段未明确"；RAGFlow 仍用荒漠调查凑数 |
| 引用质量 | 5/5 | 2/5 | RAGFlow 引用无一条可见光巡检文档 |
| 精度指标差异 | 4/5 | 2/5 | 网关正确区分"定性巡视 vs 定量遥感指数" |
| **总计** | **34/35 (97%)** | **16/35 (46%)** | |

### 核心结论

- **网关回答显著优于 RAGFlow 止血后回答**（97% vs 46%），差距集中在证据来源正确性、范围混淆、引用质量三项——
  这些正是多阶段检索 + 覆盖矩阵能解决、而 RAGFlow 单次检索无法解决的问题。
- RAGFlow 止血只消除了"硬编造差异"，但可见光侧仍无真实巡检证据，只能拿植被调查顶替。
- **结论：继续使用 RAGFlow 外部1 对话框无法获得正确对比回答；建议将问答入口切换到网关多阶段链路。**

## 聊天页实现（2026-08-12，网关问答入口）

基于评测结论，为网关新增聊天页 `/chat`，作为多阶段检索问答的正式入口（替代 RAGFlow 外部1 对话框）。

### 实现内容

- **`app/web/chat.html`** — 聊天页主体：知识库选择、问题输入、示例问题、回答/结论要点/覆盖矩阵/资料缺失/引用证据展示区。
- **`app/web/static/chat.css`** — 与资料接入台统一的视觉风格（复用同一 CSS 变量与配色）。
- **`app/web/static/chat.js`** — 前端逻辑：
  - 加载 `GET /api/v1/admin/datasets` 填充知识库下拉；
  - 调用 `POST /api/v1/chat/completions` 问答；
  - 渲染 `status` 徽章、`answer`（高亮 `[Cx]` 引用）、`claims`、`coverage_matrix`（covered/missing）、`missing_information`、`citations`（含 quote 折叠预览）。
- **`app/main.py`** — 新增 `/chat` 路由，返回 `chat.html`。
- **`app/api/v1/admin.py`** — 新增 `GET /api/v1/admin/datasets`，返回 RAGFlow 数据集 `[{id, name}]` 供知识库选择。
- **`app/services/ragflow_client.py`** — 新增 `list_datasets()`，兼容 RAGFlow `/api/v1/datasets` 的两种响应结构
  （`data` 为数组，或 `data.docs` 包装）。
- **`tests/unit/test_ragflow_client.py`** — 新增 `list_datasets` 两结构测试。

### 验证状态

- 沙箱语法检查：`ragflow_client.py`、`admin.py`、`main.py`、`test_ragflow_client.py` 全部通过；`chat.js` 通过 `node --check`。
- 沙箱用真实 RAGFlow `/api/v1/datasets` 响应验证 `list_datasets` 解析逻辑：正确解析 4 个数据集（知识库问答/法规11/法规/test）。
- **网关重启后真实验证（全部通过）**：
  - `GET /api/v1/admin/datasets` → 返回 4 个数据集；
  - `GET /chat` → HTTP 200，页面加载正常；
  - `GET /assets/chat.js` / `chat.css` → HTTP 200；
  - 问答流程（法规11 知识库，对比问题）→ HTTP 200，`status=ANSWERABLE`，2 claims / 5 citations / 58.8s；
  - `/ingestion` 顶栏已含"智能问答"入口链接（`.nav-link`），点击跳转 `/chat`，两页互通。

### 使用方式

- 浏览器访问 `http://127.0.0.1:8080/chat`，选择知识库（默认第一个），输入问题，等待多阶段检索回答。
- 或从 `http://127.0.0.1:8080/ingestion` 顶栏"智能问答"按钮进入。

## 后续计划

1. **（已完成）网关生成端 502**：三层修复（flatten schema + 缺失声明宽容 + status=None 兼容）已实现并真实网关验证通过。
2. 网关覆盖选择完善：q4 命中"设备定义"而非"时段"证据，说明需要把"单元与证据语义错配"纳入覆盖质量检查
   （对应 2.6 适用范围判定 P0）。
3. **（已实现，待验证）前端接入：网关 `/chat` 聊天页已实现**，调用 `/api/v1/chat/completions`，展示引用/覆盖矩阵。
4. 建立对比问题评测集（含本问题作为"范围混淆"失败样例，评测数据见上文），跟踪止血/切网关后的改进。

## 改动文件

- `RAGFlow 外部1 聊天助手 prompt_config.system`（经 `PUT /api/v1/chats/{id}` 更新，无代码改动）；
- `D:\internal-rag\backups\ragflow-chat\external1-chat-20260812.json`（新增备份）；
- `app/services/model_client.py`（新增 `flatten_json_schema`，chat_completion 发送前内联 schema）；
- `app/services/citation_validator.py`（缺失声明 claim 无引用宽容 + 旧 chunk `status=None` 兼容）；
- `tests/unit/test_model_client.py`（flatten 测试）；
- `tests/unit/test_citation_validator.py`（缺失声明/编造拒绝/status=None 兼容测试）；
- `app/web/chat.html`、`app/web/static/chat.css`、`app/web/static/chat.js`（新增聊天页）；
- `app/main.py`（新增 `/chat` 路由）；
- `app/api/v1/admin.py`（新增 `GET /api/v1/admin/datasets`）；
- `app/services/ragflow_client.py`（新增 `list_datasets()`）；
- `tests/unit/test_ragflow_client.py`（list_datasets 测试）。
