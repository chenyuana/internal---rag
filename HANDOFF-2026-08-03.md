# Internal RAG 项目开发交接（2026-08-03）

> 用途：供后续智能体快速接手今天完成的开发工作。项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。

## 1. 当前状态

- 项目目录：`D:\internal-rag\source\internal-rag`
- 资料接入台：`http://127.0.0.1:8080/ingestion`
- Gateway：已重启，`/health` 返回 `ok`，`/ready` 返回 `ready`
- 数据目录：`D:\internal-rag\data\ingestion`
- Python：`D:\internal-rag\source\internal-rag\.venv\Scripts\python.exe`
- RAGFlow：本地 Docker Desktop 部署，宿主端口通常为 `18000`（Web）和 `9380`（API）
- 解析队列并发：固定为 1，避免本机内存和显存争用
- 现有知识库和已发布内容没有被今天的改动自动删除、修改或重新发布

重要：当前仓库执行 `git status --short` 时项目文件整体显示为未跟踪状态。后续智能体不得使用 `git reset --hard`、`git clean` 或类似命令清理工作区。

## 2. 今天完成的工作概览

今天主要完成四组改进：

1. 处理队列资料查询与人工 Chunk 修订。
2. 同页多表、表题绑定和合并单元格保真。
3. 单行法规条款的 Chunk 完整性修复。
4. 大批量资料的顺序化人工审核队列。

详细历史仍记录在 `CHANGELOG-rag-improvements.md` 的第九至第十二节；本文是面向接手者的独立摘要。

---

## 3. 资料查询与人工 Chunk 修订

### 3.1 队列资料查询

- `GET /api/v1/ingestion/jobs` 支持 `query`、`limit`、`offset` 和 `review_status`。
- 查询字段包括文件名、上传文件夹相对路径、任务 ID、SHA256、目标知识库、解析状态、发布状态、审核状态、审核人、审核备注和错误信息。
- 前端会分页读取全部任务，每页 200 条，因此资料数量超过 200 后仍可查找。
- 搜索在浏览器已加载的完整任务集合上即时过滤。

### 3.2 人工修改关联 Chunk

页面路径：

`处理结果 → 页面解析检查 → 打开具体页 → 该页关联 Chunk → 编辑`

接口：

`PATCH /api/v1/ingestion/jobs/{job_id}/chunks/{chunk_id}`

行为：

- 修改的是最终发布给 RAGFlow 的 Chunk 文本。
- 不改写原 PDF、页面原始文本层、原始内容块或图片资产。
- 同步更新 `document_ir.json`、`chunks.jsonl` 和 `qa.json`。
- 每次修改追加写入 `manual_edits.jsonl`，记录操作者、原因、版本、时间和修改前后哈希。
- 已生成但未发布的旧计划会失效，需要重新生成。
- 人工修改表格 Chunk 时，旧 `table_html`、`table_rows` 等结构化字段会被清除，Chunk 类型转为 `manual_text`，避免“新文本 + 旧表格”冲突。
- 已发布完成的版本禁止继续在接入台修改，避免本地内容与 RAGFlow 不一致。
- 人工修改后审核状态自动重置为 `unreviewed`，必须重新审核。

主要实现：

- `app/api/v1/ingestion.py`
- `app/ingestion/jobs.py`
- `app/schemas/ingestion.py`
- `app/web/ingestion.html`
- `app/web/static/ingestion.js`
- `app/web/static/ingestion.css`

---

## 4. 同页多表与表格格式保真

主要回归样本：

`《水利水电工程无人机机载激光雷达地形测量技术导则》`

已解决的问题：

- 同一页面存在多个表格时，不再全部继承第一个表题。
- 一个大检测网格覆盖多个“表 N”标题时，按表题与水平边界切分为独立表格。
- 表格 ID 加入页内顺序和边界，避免同页表头相同导致 ID 冲突。
- 支持 `表A.1` 等带字母编号的表题。
- 附录标题和表格附近说明文字不再被错误吞入表格。
- 优先保留 pdfplumber 检出的真实合并单元格。
- `None` 合并占位会转换为 HTML `colspan`。
- “备注”“注”等原本跨列的一整格不会再被拆成多个空列。
- 单页表格的原始 span HTML 会保留到最终 Chunk；跨页合并或分块重建时才回退为矩形 HTML。
- 新增表题错配、重复表格 ID、合并单元格丢失等诊断和质量门禁。

真实文件复测摘要：

- 第 7～8 页：表1～表6表题和章节正确，ID 唯一。
- 第 9 页：表7、表8被拆成两个独立表格，正文不再混入。
- 第 22 页：表A.1从真实网格起点提取，共18行；备注和注释保持跨列结构。

主要实现：

- `app/ingestion/parsers/vector_pdf.py`
- `app/ingestion/parsers/hybrid_pdf.py`
- `app/ingestion/pipeline.py`
- `tests/unit/test_vector_pdf.py`
- `tests/unit/test_regulation_preprocess.py`

注意：接入台的“表格与图片资产”用于人工核对；最终发给 RAGFlow 的主要检索载体仍是结构化表格对应的 Chunk 文本及相关元数据，并非只发送截图。

---

## 5. 法规条款 Chunk 完整性

主要回归样本：

`《低空航空器通信接入要求》`

根因：旧分块器将所有 `clause` 内容块都当作标题排除。当条号与完整正文处于同一行且没有后续段落时，该条款会在 `blocks → chunks` 阶段消失，导致页面清洗文本完整但最终发布 Chunk 缺少 `6.1.2`、`7.1` 等条款。

修复：

- 增加“实质性单行条款”判定。
- 带规范性谓词、完整句末标点或足够正文长度的单行条款会保留。
- `4 通信设备`、`4.1 基本要求` 等纯结构标题仍不会单独生成无意义 Chunk。
- 单行条款作为标题与正文时只写入一次。
- 原生和混合解析均增加 `clause_chunk_coverage` 质量门禁。
- QA 输出新增 `unpublished_clause_count` 与 `unpublished_clause_ids`。

复测结果：

- 最终 Chunk 数由 22 增至 35。
- `6.1.2`、`7.1` 均生成独立 Chunk，页码为第 9 页。
- `unpublished_clause_count = 0`。

---

## 6. 顺序化人工审核队列

### 6.1 数据模型

新增四种审核状态：

| 值 | 界面名称 | 含义 |
|---|---|---|
| `unreviewed` | 待审核 | 尚未人工检查，旧任务默认进入此状态 |
| `in_review` | 审核中 | 已开始检查，可暂存备注 |
| `approved` | 已通过 | 可实际发布到 RAGFlow |
| `rework` | 需返工 | 发现问题，需要重新处理或人工修订 |

`IngestionJob` 新增：

- `batch_id`
- `sequence_in_batch`
- `review.status`
- `review.note`
- `review.reviewer`
- `review.started_at`
- `review.reviewed_at`
- `review.updated_at`

旧 `job.json` 没有 `review` 字段时，Pydantic 自动兼容为 `unreviewed`；不会自动批准，也不会自动发布。

### 6.2 审核接口

`PATCH /api/v1/ingestion/jobs/{job_id}/review`

示例请求：

```json
{
  "status": "approved",
  "note": "条款、表格与页码均已核对"
}
```

规则：

- 处理中或排队中的任务不能提交审核结论。
- 已发送或正在发送的任务不能修改审核结论。
- 只有解析状态为 `completed` 或 `warning` 的资料才能标记为 `approved`。
- 标记为 `rework` 时必须填写备注。
- 每次变化追加到任务目录的 `review_history.jsonl`。
- 重新解析或人工修订 Chunk 会把审核状态重置为 `unreviewed`。

### 6.3 前端审核流程

02 处理队列现在分为：

- 待审核
- 审核中
- 已通过
- 需返工
- 已发送

处理结果页新增：

- 当前审核序号和整体审核进度。
- 审核备注。
- 上一份、下一份。
- 暂存并下一份。
- 需返工并下一份。
- 审核通过并下一份。

顺序规则：

- 默认按首次上传时间升序排列。
- 同批文件带 `batch_id` 和 `sequence_in_batch`。
- 文件夹上传仍按自然文件名顺序提交。
- 重试、人工编辑、审核状态更新不会依据 `job.json` 修改时间重新排序。

发布规则：

- 审核前允许生成 dry-run 发布计划，供人工核对。
- 实际发布必须为 `approved`，否则后端返回 `INGESTION_REVIEW_REQUIRED`。
- 批量发送工具只在“已通过”页签显示。
- 已发送资料仍与审核队列分开，并可仅清理接入台本地记录；该操作不会删除 RAGFlow 内容。

---

## 7. 今天涉及的核心文件

```text
app/schemas/ingestion.py
app/api/v1/ingestion.py
app/ingestion/jobs.py
app/ingestion/pipeline.py
app/ingestion/parsers/vector_pdf.py
app/ingestion/parsers/hybrid_pdf.py
app/web/ingestion.html
app/web/static/ingestion.js
app/web/static/ingestion.css
tests/unit/test_ingestion_api.py
tests/unit/test_regulation_preprocess.py
tests/unit/test_vector_pdf.py
CHANGELOG-rag-improvements.md
```

## 8. 验证结果

最终验证结果：

- 接入台接口专项测试：19 项通过。
- 完整单元测试：120 项通过。
- Ruff：本次 Python 修改全部通过。
- JavaScript：Node `--check` 通过。
- Gateway：页面、JS、CSS、`/health`、`/ready` 均在线。

完整单元测试仍有 2 个与今天审核队列无关的既有失败：

1. `tests/unit/test_regulation_preprocess.py::test_clean_page_removes_annex_decor_letter`
   - 期望删除计数为 2，实际为 3。
2. `tests/unit/test_remote_parsers.py::test_clean_inline_latex_cleans_table_html_cells`
   - 期望 `(KH_2PO_4)`，实际为 `(KH2PO4)`。

不要把这两个既有失败误判成今天新增功能造成的回归。

## 9. 常用验证与运行命令

在 PowerShell 中执行：

```powershell
Set-Location D:\internal-rag\source\internal-rag

# 接入台专项测试
.\.venv\Scripts\python.exe -B -m pytest tests\unit\test_ingestion_api.py -q -p no:cacheprovider

# 完整单元测试
.\.venv\Scripts\python.exe -B -m pytest tests\unit -q -p no:cacheprovider

# Python 静态检查
.\.venv\Scripts\python.exe -B -m ruff check app tests\unit --no-cache

# Gateway 状态
.\deploy\gateway\status.ps1

# 代码修改后重启 Gateway（它不使用热重载）
.\deploy\gateway\stop.ps1
.\deploy\gateway\start.ps1
```

如页面仍显示旧界面，使用 `Ctrl+F5` 强制刷新。HTML 当前通过 `?v=20260803-2` 刷新 JS/CSS 缓存。

## 10. 后续智能体接手建议

1. 修改解析或 Chunk 逻辑后，必须同时检查 `document_ir.json`、`chunks.jsonl`、页面关联 Chunk 和最终发布计划，不能只看“清洗后文本”。
2. 表格修复必须同时验证：表题、行列、合并单元格、跨页连续性、备注整格、最终 Chunk 文本和 RAGFlow 发布载荷。
3. 新的解析结果不得沿用旧的人工审核通过状态。
4. 不要绕过服务端 `approved` 发布门禁；前端禁用按钮不是安全边界。
5. 不要自动批准旧任务，不要在测试时自动发布真实资料。
6. 本机资源有限，RAGFlow DeepDoc、MinerU、Ollama 和接入台大文档解析应尽量错峰运行。
7. 修改代码后必须重启 Gateway；仅刷新浏览器不会加载新的 Python 后端代码。

## 11. 当前可直接继续的方向

- 使用真实大批量资料验证审核序号、跨页导航和审核备注体验。
- 给审核队列增加“仅看问题页”“按文件夹/批次过滤”“审核人筛选”等能力。
- 增加任务总数接口，避免前端通过分页直到空页计算总数。
- 修复上面两项既有单元测试差异。
- 继续优化复杂跨页表格、公式和纯扫描 PDF；这些仍是解析质量的主要风险区域。
