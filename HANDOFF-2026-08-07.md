# Internal RAG 解析质量改进交接（2026-08-06 之后）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-06.md` 之后）的工作。项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-06.md`（已覆盖封面修复复查、带外框文字结构图、罗马数字顶层条款与图框内标签过滤、嵌套无编号子节、PUA 点号还原、DL 行业标准封面、文本层损坏页排除 figure、条款编号点号下沉、单行多列表格识别与合并、跨页续表噪声、表题 prose 引用过滤、术语编号合并、figure 300dpi 高分辨率裁剪）
- **本会话范围**：自 2026-08-07 起的活动，随工作推进逐节追加
- **重要**：`CHANGELOG-rag-improvements.md` 是较早的累积记录（含 08-03 之前内容），本会话新增内容单独记录在本文档，避免混淆

## 1. 当前状态快照（2026-08-07）

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`
- 单元测试基线：**144 passed, 3 failed**（3 个既有失败与近期改动无关：TOC 检测、跨页 caption 绑定、figure 去重；本会话 2.1~2.10 共新增 15 个回归测试，08-06 结束时为 126 passed）；宿主机以实际运行结果为准
- Git：仓库仍无 commit，整个工程为未跟踪状态；**禁止** `git reset --hard` / `git clean`
- 服务状态：需在宿主机 PowerShell 运行 `deploy\gateway\status.ps1` 等脚本确认（Linux 沙箱无法执行 Windows 二进制）

## 2. 改动记录（按日期逐节追加）

> 沿用既有格式，每节结构为：**验证**：{文档}修复效果复查 / **修复**：{文档}{问题}——含问题、根因、修复、验证、改动文件、注意。

### 2.1 修复：《GB/T 19000-2016》第33页起图片截取不全（2026-08-07）

> 用户反馈《GB/T 19000-2016 质量管理体系基础和术语》从第33页开始图片截取不全。

#### 问题

- 文档：`GB_T_19000-2016质量管理体系基础和术语.pdf`（56 页），job `8e0e1be8abf54a438c60f81b9733e4fd`
- 第33-46页是附录A的概念关系图示（图A1~A16），figure 裁剪图内容**贴边截断**：图A1（属种关系）右侧约 170px 的树形分支丢失、上下贴边；大图（图A5 等）同样顶部/右侧内容被截
- VLM 描述因此误判：图A2 报"无图表内容，仅有标题文本"，图A1 报"左下角可见文字'从属关系'的部分内容"

#### 根因

**页面 MediaBox 原点偏移导致裁剪坐标错位**：

- 该 PDF 全部页面的 MediaBox 原点不在 (0,0)，而在 **`(-57.24, 53.91)`**（`/MediaBox [-57.237 53.914 538.038 895.804]`）
- pdfplumber 报告的对象坐标（`page.images` 的 bbox、`extract_words` 等）**相对 MediaBox 原点**
- 而 `page.to_image()` 渲染的 PNG 被 `PageImage._reproject()` 归一化到 **cropbox 原点**（可见区域原点）
- `_crop_png` 直接用 `bbox * scale` 把 bbox 映射到渲染图，**未减去 MediaBox 原点偏移**，导致裁剪窗口整体错位（Δx≈-57pt, Δy≈+54pt）
- 错位结果：裁剪窗口左移/上移，实际图内容被顶到窗口边缘甚至裁掉

**为什么从33页开始**：第33-46页是附录A的概念关系图（唯一被 figure 解析路径裁剪的 imagemask 图）；前32页无此类大图走 figure 裁剪，故不受影响。

#### 修复（`vector_pdf.py`）

1. **新增 `_page_origin(page)`**：返回页面可见原点偏移 `(cropbox[0], cropbox[1])`；无 cropbox 时返回 `(0.0, 0.0)`（常规页面不改变行为）
2. **`_crop_png` 增加 `origin` 参数**：裁剪前先将 bbox 平移 `(left-origin_x, top-origin_y)` 再乘渲染比例；原点为 (0,0) 时与旧逻辑完全一致
3. **`_figure_crop_png`**：自动传入 `origin=self._page_origin(page)`
4. **table 资产裁剪**（`parse_pages` 内 `_crop_png` 调用）：同样传入 `origin=self._page_origin(page)`

#### 验证

- **真实 PDF 全 14 页 figure 复扫**（33-46 页，16 个 figure）：修复后所有裁剪图四周留白正常（如 A1 `T26 B19 L12 R11`、A5 `T11 B19 L18 R28`），无贴边/截断；修复前 A1 `T0 B0 L2 R180`（右侧截断）
- 修复前后同页同 bbox 对比：A1 内容右边界从 col 1030 → col 1200（恢复完整树形分支），内容贴上下边 → 四周留白
- 新增回归测试 2 个：
  - `test_figure_crop_accounts_for_mediabox_origin_offset`（带偏移页裁剪窗口对准）
  - `test_page_origin_defaults_zero_when_no_cropbox`（常规页面 origin 为 0，行为不变）
- 相关测试套件：**45 passed**（test_vector_pdf.py 全通过）；test_pipeline + test_regulation_preprocess **83 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | 新增 `_page_origin`；`_crop_png` 增加 origin 平移；`_figure_crop_png` 与 table 裁剪自动传入 origin |
| `tests/unit/test_vector_pdf.py` | 新增 2 个 MediaBox 原点偏移回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `8e0e1be8abf54a438c60f81b9733e4fd`）
- 重新解析后应确认：图A1~A16 裁剪图完整（不再右侧/顶部截断），VLM 描述不再是"无实际图形内容"
- 该修复对所有 MediaBox 原点偏移的 PDF 通用；若其他文档出现 figure/table 裁剪贴边，可用 `_page_origin` 逻辑检查

### 2.2 修复：《GB/T 19001-2016》第8页 annex 劫持 + 第28页 TOC 误判（2026-08-07）

> 用户反馈《GB_T19001-2016 质量管理体系要求》第8页以及第28页图片与表格识别有误。job `c1f4204e11be47459692c6db4afc150e`。

#### 问题一：第8页 0.4 条款被误挂到 ['附录'] 下

- 解析前：`0.4 与其他管理体系标准的关系` 及其子节 `0.4.1/0.4.2` 全部挂在 `['附录']` 下
- 根因：pypdf 把正文句"附录 B 给出了 SAC/TC 151 制定…"拆成多行碎片（`附录` / `B` / `给出了` / `SAC/TC 151` / …），孤立的"附录"被 `heading_kind` 判为 `annex` → 清空章节栈 → 后续 `0.4` 全挂到 `['附录']`
- 同时"附录 B 给出了…"整行（若完整出现）也因 annex 正则 `^\s*(?:附录|附件)\s*[A-Za-z0-9…]*` 只匹配前缀而被误判

**修复（`pipeline.py`）**：
1. annex 正则要求"附录/附件"后**必须跟编号**（字母/数字/汉字），孤立"附录"不再判为 annex
2. 新增 `_looks_like_annex_prose`：`heading_kind` 对 annex 结果做后置过滤，排除正文陈述（"附录 B 给出了…"/"附录A 应符合…"），只保留"标签 + 可选（资料性）标记"的真标题
3. `_looks_like_unnumbered_subheading` 增加正文碎片前缀排除（给出/列出/应关系见/对应关系见/本标准采用 等），防止 pypdf 拆行碎片被编号成假子节（`0.4.3 给出了`）

#### 问题二：第28页 附录B 关系表整页被排除

- 解析前：第28页 `route=toc_excluded`、`indexable=False`，整页（含表 B.1）不进索引
- 根因：native 阶段 `numbered_entries=0`（正常），但 vector 解析正确识别表格后，hybrid `_rebuild` 的 **TOC 复查**用 `is_probable_toc_page(cleaned_text)` 判定。表格单元格文本（如 `9.1.2 GB/T 19012 ISO 10002`）被 `clean_page` 合并后，有 8 行匹配 `TOC_NUMBERED_ENTRY_RE`（条款号+标准号+对照编号被当成"编号+标题+页码"）→ `numbered_entries>=6` → 整页误判为 TOC 排除

**修复（`hybrid_pdf.py`）**：
- TOC 复查**跳过已解析为结构化表格的页面**（`page.page_number not in resolved_table_pages`）。表格页不可能同时是目录页；其单元格文本形状容易伪造目录条目，不应再触发 TOC 判定

#### 验证

- **第8页**：修复后 `0.4 与其他管理体系标准的关系` 正确挂 `['0.4 与其他管理体系标准的关系']`，正文段落在其下，不再被附录劫持
- **第28页**：vector preflight `table_pages={28}`，merge 后 `structured_table_pages=[28]`，TOC 复查跳过 → 表格保留
- 新增回归测试 4 个（test_regulation_preprocess.py）：
  - `test_heading_kind_bare_annex_is_not_heading`
  - `test_heading_kind_annex_prose_is_not_heading`
  - `test_heading_kind_annex_with_label_is_heading`
  - `test_unnumbered_subheading_rejects_body_fragments`
- 相关测试套件：**132 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | annex 正则要求编号；新增 `_looks_like_annex_prose`；`_looks_like_unnumbered_subheading` 排除正文碎片 |
| `app/ingestion/parsers/hybrid_pdf.py` | TOC 复查跳过已解析为结构化表格的页面 |
| `tests/unit/test_regulation_preprocess.py` | 新增 4 个回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `c1f4204e11be47459692c6db4afc150e`）
- 重新解析后应确认：第8页 `0.4` 条款不再挂 `['附录']`；第28页 表 B.1 保留进索引
- 已知次要遗留：第9页页眉"质量管理体系要求"被误编号为 `0.4.1`（独立问题，涉及顶层单级条款"1. 范围"的 `1.` 格式识别），本次未覆盖

### 2.3 修复：《GJB 2489A-2023》大量文字页被误判为 figure（2026-08-07）

> 用户反馈《GJB 2489A-2023 航空机载设备履历本及产品合格证编制要求》解析结果较差。job `9182c258c9f04e6ea204fc52f8bfd230`，状态 `needs_review`（`table_caption_alignment` 门禁失败：第38页）。

#### 问题

- 39 页文档中 **20 页被判为 `figure_review` / `page_type=figure`**（第1、3-13、17、20、26-28、31、37、39页），但其中绝大多数是**正文文字页**（前言、范围、引用文件、术语定义等）
- 结果：正文内容没有正常发布为 text chunk，文档检索质量严重下降
- 另有 `table_caption_alignment` 门禁失败（第38页表D.1未结构化）和表格文字乱码

#### 根因

**整页白色背景图导致文字页被误判为 figure**：

- 该 PDF **每一页**都叠了一张覆盖整页的白色 JPEG 背景图（`bbox=(0,0)-(595,842)`、`srcsize≈1241×1755`、stream 仅 16-30KB）
- `preflight_pages` 的 figure 判定只检查 `image_count` 和 `coverage`：这些页 `image_count=1`、`largest_image_coverage=1.0`（覆盖整页），满足 `largest_image_coverage >= 0.12` → 被判为 figure
- 但该背景图解码后 **99.4% 像素为纯白**（亮度均值 253.9/255），是装饰背景而非内容图
- 页面真正的文字是叠加在背景图上的矢量文本

#### 修复（`vector_pdf.py`）

1. **新增 `_is_fullpage_background_image(page, image)`**：检测整页覆盖（面积 ≥90%）+ 编码密度极低（`stream字节数/像素数 < 0.05 B/px`）的背景图。白色 JPEG 压缩后每像素不足 0.05 字节，真实照片/扫描图则更高
2. **新增 `substantive_image_count`**：preflight 中只统计非背景图的图片数
3. **figure 判定改用 `substantive_image_count`**：只有非背景内容图才参与 figure 路由判定

#### 验证

- 修复前 20 页 figure_review → 修复后 **纯文字页全部正确排除**（第1-12、14、16、17、25-27、29、31、35、37、39页走 native 文本）
- 仅保留真图页：第13页（图A.2 履历本验收证明栏样式）、以及有表格+小图标混合的 table_figure 页
- 修复后第3-5页正确生成正文 chunk（"1 范围"、"2 引用文件"、"3.3 合格证"等），修复前这些页无正文 chunk
- 新增回归测试 3 个（test_vector_pdf.py）：
  - `test_is_fullpage_background_image_detects_white_jpeg`
  - `test_is_fullpage_background_image_keeps_real_image`
  - `test_preflight_ignores_fullpage_background_image`
- 相关测试套件：**135 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | 新增 `_is_fullpage_background_image`；figure 判定改用非背景图计数 |
| `tests/unit/test_vector_pdf.py` | 新增 3 个背景图检测回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `9182c258c9f04e6ea204fc52f8bfd230`）
- 重新解析后应确认：正文文字页正常生成 text chunk，不再大量走 figure_review
- **已知遗留**（本次未覆盖，需后续处理）：
  1. 该 PDF 存在 **ToUnicode 映射错乱**（如"编制婓求"应为"编制要求"、"用千"应为"用于"、"单要记巾"应为"主要记录"），文本层提取即乱码，现有假字检测（U+7280 区）无法识别；需建立损坏字符映射表或对该类页走 OCR
  2. 第38页 `table_caption_alignment` 门禁失败：表D.1(续) 表格线不完整（缺顶部/底部闭合水平线，仅 3 条水平线），`_expanded_ruled_tables` 要求 ≥4 条水平线而跳过，导致表D.1 未结构化

### 2.4 修复：《GJB 2489A-2023》第37页无竖线表格未识别（2026-08-07）

> 用户在 2.3 修复后复查发现第37页表格未识别到。

#### 问题

- 第37页（附录D 表D.1 履历本封面和各页的字体和字号）未被识别为表格页：`table_pages=set()`、`asset_ids=[]`，表格内容被发布为零散 text chunk（"6 特殊说明"、"7 为命和可靠性"等）
- 表D.1 是 4 列表格（序号/页别/文字内容/字体和字号），**只有水平分隔线、完全没有垂直线**

#### 根因

- `pdfplumber.find_tables()` 依赖**水平和垂直网格线**组成表格；无竖线表格（仅水平线分隔行）返回 0 个表格
- `_expanded_ruled_tables` 处理无竖线场景时：`vertical_positions` 为空 → `boundaries=[left, right]` 仅 2 个边界 → `len(boundaries)<3` 跳过
- 对比第36页（表C.1/C.2）有 25 条垂直线所以识别成功；第37页 0 条垂直线所以失败

#### 修复（`vector_pdf.py`）

1. **新增 `_text_column_boundaries(page, top, bottom, left, right)`**：当表格无垂直线时，从表格区域内的文本 x 坐标聚类推导列边界（需 ≥3 个有意义的列簇，每簇 ≥2 个词）
2. **`_expanded_ruled_tables`**：当 `vertical_positions` 为空时，用 `_text_column_boundaries` 替代垂直线边界重建网格

#### 验证

- 第37页 `_expanded_ruled_tables` 从 0 个 → **1 个表格**（bbox y=262.6-708.7，14 行 4 列），列边界 x≈100/130/230/436 正确
- `preflight_pages`：第37页进入 `table_pages={37}`，生成 `p0037-table-01` 资产
- `parse_pages`：`table_count=1`，table_title="表D. 1 履历本封面和各页的字体和字号"，14 行结构化
- 新增回归测试 2 个（test_vector_pdf.py）：
  - `test_text_column_boundaries_infers_columns_without_vertical_rules`
  - `test_expanded_ruled_tables_uses_text_columns_for_verticalless_table`
- 相关测试套件：**137 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | 新增 `_text_column_boundaries`；`_expanded_ruled_tables` 无竖线时用文本列聚类重建网格 |
| `tests/unit/test_vector_pdf.py` | 新增 2 个无竖线表格回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `9182c258c9f04e6ea204fc52f8bfd230`）
- 重新解析后应确认：第37页生成结构化 table chunk（含表D.1 的 14 行 4 列）
- 已知遗留：第37页表格内容仍为 ToUnicode 乱码（文本层损坏），表格结构已正确但文字需 OCR 或映射修复

### 2.5 修复：《GJB 2489A-2023》ToUnicode 损坏页面路由到 OCR（2026-08-07）

> 用户在 2.4 修复后指出：第37页表格结构虽已识别，但单元格内容仍是乱码（"为命和可靠性"应为"寿命和可靠性"），内容不算正确。

#### 问题

- GJB 2489A 整个文档（39 页）的文本层存在 **ToUnicode 映射错乱**：同一字体（如 `*SimSun-7594`）、同一 CID 在不同位置被映射到不同 Unicode
- 后果：同一视觉字形提取成不同汉字——"五号宋体"有时正确、有时变"号宋体"/"匹号宋体"/"飞？宋体"；"寿命和可靠性"变"为命和可靠性"
- **无法通过固定字符映射表修复**（映射不稳定），唯一可靠方案是走 OCR（页面渲染正确，文本层损坏）

#### 根因

- PDF 为每个文本运行子集化独立字体（Identity-H 编码 + 各字体独立 ToUnicode CMap），ToUnicode 映射自我矛盾
- 现有文本层损坏检测（`is_glyph_name_garbage` 查 `/GXX` PostScript 名、`is_fake_cjk_garbage`/`has_fake_glyph_corruption` 查 U+7280 区假字）都识别不了这种"普通汉字乱码"

#### 修复（`pipeline.py`）

1. **新增 `has_tounicode_cid_conflict(pdf_page)`**：检查页面所有 Type0 字体的 ToUnicode CMap，若**同一 CID 映射到两个不同 Unicode 码点**则判为损坏。这是可靠的信号——正常嵌入字体每个 CID 只映射一个 Unicode
2. **`parse_pdf` 路由接入**：`text_layer_corrupted` 增加 `has_tounicode_cid_conflict(pdf_page)` → 命中页面走 `text_layer_review` → hybrid 层进入 MinerU OCR

#### 验证

- **GJB 2489A**：全部 39 页检测到 CID 冲突 → 38 个内容页走 `text_layer_review`（OCR 路由），不再依赖损坏文本层
- **正常文档**（GB/T 19001、GB/T 19000）：0 页误报（CID 冲突为 0）
- 新增回归测试 2 个（test_pipeline.py）：
  - `test_tounicode_cid_conflict_detects_contradictory_mapping`
  - `test_tounicode_cid_conflict_clean_mapping_returns_false`
- 相关测试套件：**139 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | 新增 `has_tounicode_cid_conflict`；`parse_pdf` 路由接入 |
| `tests/unit/test_pipeline.py` | 新增 2 个 ToUnicode 冲突检测回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `9182c258c9f04e6ea204fc52f8bfd230`）
- 重新解析后整个文档走 MinerU OCR（39 页），可拿到**正确文字内容**（含表格）；代价是解析时间增加（8G 显存下需分批）
- 若 OCR 使文档解析过慢，可考虑只对 CID 冲突页走 OCR、其余页走原路径

### 2.6 修复：《GJB 2489A-2023》ToUnicode 冲突页被拉回 vector 表格解析（2026-08-07）

> 用户在 2.5 修复后复查发现：第37、38页表格效果依旧很差，第38页 `table_caption_alignment` 门禁仍失败（`Detected table captions were not bound one-to-one to structured tables: [38]`）。

#### 问题

- 2.5 已让 ToUnicode 冲突页走 `text_layer_review`（OCR），但宿主机产物第37、38页 route 仍是 `vector_layout`，表格内容仍乱码
- 第38页 `table_caption_alignment` 门禁失败：table_hints 含 `表D.1(续)` 和 `表D.2` 两个表题，但只生成了 1 个结构化表格（表D.2），表D.1(续) 未被结构化

#### 根因

- **hybrid 层把 text_layer_review 页又拉回了 vector 表格解析**：`hybrid_pdf.py` 第715行 `vector_preflight_targets.update(text_layer_targets)` 是为"文本层损坏但表格线完整可恢复"的页设计的。第37/38页有表格线，`table_candidate_targets` 包含它们 → `vector_targets` 拉回 → `vector_layout`
- 但这个逻辑**对 ToUnicode CID 冲突页是错的**：恢复的表格网格内容仍是乱码（ToUnicode 映射不稳定，无法用映射表修复）
- 第38页表D.1(续) 表格线不完整（缺顶部/底部闭合水平线，仅 3 条宽水平线），`_expanded_ruled_tables` 要求 ≥4 条而跳过，故只有表D.2 被结构化 → 表题与表格数不匹配 → 门禁失败

#### 修复（`pipeline.py` + `hybrid_pdf.py`）

1. **`pipeline.py`**：`text_layer_corruption` 记录 `tounicode_cid_conflict` 标志（区分 ToUnicode 损坏与其他假字）
2. **`hybrid_pdf.py`**：新增 `tounicode_corrupted_targets`（所有 CID 冲突页），`vector_preflight_targets.update(text_layer_targets - tounicode_corrupted_targets)` —— ToUnicode 冲突页**不参与 vector 预检**，强制留在 OCR 目标

#### 验证

- GJB 文档 parse_pdf：全部 38 内容页 `tounicode_cid_conflict=True`，route=`text_layer_review`
- hybrid 模拟：`candidate_ocr=38` 页，`vector_preflight=0` 页，第37/38页不在 vector 表格目标
- 正常文档（GB/T 19001/19000）：0 页 CID 冲突，路由不受影响
- 新增回归测试 1 个（test_pipeline.py）：
  - `test_parse_pdf_records_tounicode_conflict_and_routes_to_ocr`
- 相关测试套件：**140 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `text_layer_corruption` 记录 `tounicode_cid_conflict` |
| `app/ingestion/parsers/hybrid_pdf.py` | ToUnicode 冲突页排除出 vector 预检，强制走 OCR |
| `tests/unit/test_pipeline.py` | 新增 1 个回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `9182c258c9f04e6ea204fc52f8bfd230`）
- 重新解析后第37/38页走 OCR，MinerU 应能识别完整表格（含表D.1 续表），与第37页表D.1 跨页合并，`table_caption_alignment` 门禁应通过
- **重要**：宿主机必须确认加载了最新代码（pipeline.py 的 `tounicode_cid_conflict` 字段）——2.5 修复在宿主机 14:05 重启时可能未完全生效，因为当时代码 13:57 刚改完；本次 2.6 需再次重启 Gateway

### 2.7 修复：《HB 9102-2008》超大图像流触发解压超限导致解析失败（2026-08-07）

> 用户反馈《HB 9102-2008 航空产品首件检验要求》处理失败。job `1dc506a7d7be4b32ad9a0a54c4c16a06`，状态 `failed`，错误 `INGESTION_PROCESSING_FAILED: Limit reached while decompressing. 1841615 bytes remaining.`

#### 问题

- 文档 54MB / 15 页，解析失败（attempts=5）
- 错误 `Limit reached while decompressing` 由 pypdf 抛出的 `LimitReachedError` 导致

#### 根因

- 该文档是**扫描件**：每页一张超大扫描图（~9888×14052 像素，无文本层），所有页应走 OCR
- `extract_page_layout_features` 遍历 `pdf_page.images` 并访问 `image.width`/`image.height`——pypdf 读取图像尺寸需要**解压整个图像流**
- 超大扫描图解压后超过 pypdf 的 `ZLIB_MAX_OUTPUT_LENGTH`（75MB）→ 抛 `LimitReachedError`
- `LimitReachedError` 继承 `PyPdfError`（不是 `ValueError`/`TypeError`/`AttributeError`），现有 `except (AttributeError, TypeError, ValueError)` 捕获不了 → 异常冒泡 → 整个文档解析失败

#### 修复（`pipeline.py`）

- `extract_page_layout_features` 的图像遍历增加 `except Exception` 兜底：单个图像流超限时**当作缺失图像处理**（`image_count` 跳过该图），不让整个文档失败

#### 验证

- `extract_page_layout_features` 对 HB 9102 全部 15 页不再抛异常（此前每页都抛 `LimitReachedError`）
- `parse_pdf` 成功：15 页全部 `ocr_required`（扫描件正确路由到 OCR）
- 新增回归测试 1 个（test_pipeline.py）：
  - `test_extract_page_layout_features_survives_decompression_limit`
- 相关测试套件：**141 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `extract_page_layout_features` 图像遍历增加 `except Exception` 兜底 |
| `tests/unit/test_pipeline.py` | 新增解压超限回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新处理该文档（job `1dc506a7d7be4b32ad9a0a54c4c16a06`）
- 重新处理后该扫描件走 MinerU OCR，能正常解析
- 若 OCR 处理超大扫描图（~14000px）较慢或内存吃紧，可考虑后续对该类文档做降采样

### 2.8 修复：《MH/T 4063.1-2026》公式页 Symbol 字体符号 PUA 乱码（2026-08-07）

> 用户反馈《MH/T 4063.1-2026 民用无人驾驶航空器空域信息数字化技术要求 第1部分：编码及使用》第18-21页的公式表达不理想。

#### 问题

- 第18-21页是附录A/B的网格编码计算公式（经纬度转平面码、高度转高度码）
- 公式文本层提取为 PUA 乱码：`1041919.304−102` 应为 `⌈104°19′19.304″−102°⌉`
- 这些页因文本层乱码被判为 `figure_review`（`page_type=figure`），公式内容不可检索

#### 根因

- PDF 用 **SymbolMT 字体**渲染数学符号，但符号被映射到 **PUA 私有区**（Adobe Symbol 编码 + 0xF000）而非真实 Unicode
- 具体映射：`F0B0`=°、`F0A2`=′、`F0A3`=″、`F0B2`=−、`F0B4`=×、`F0EA/FA`=⌈⌉、`F0EB/FB`=⌊⌋、`F03E`=>、`F044`=Δ
- 现有 `FAKE_GLYPH_TO_ASCII` 只覆盖 CJK Extension-A 假字（U+7280~U+72D8）和 PUA 点号，不含这些数学符号

#### 修复（`pipeline.py`）

- `FAKE_GLYPH_TO_ASCII` 新增 11 个 SymbolMT 数学符号 PUA 映射，`repair_fake_glyphs` 自动还原

#### 验证

- 公式文本 `1041919.304` → `104°19′19.304`；`⌈104°19′19.304″−102°⌉` 完整可读
- **第18-21页从 `figure_review` 转为 `native_text`**（文本层可读后不再误判为 figure），公式内容进入正文 chunk 可检索
- 新增回归测试 1 个（test_pipeline.py）：`test_repair_fake_glyphs_restores_symbol_font_math_symbols`
- 相关测试套件：**142 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `FAKE_GLYPH_TO_ASCII` 新增 SymbolMT 数学符号 PUA 映射 |
| `tests/unit/test_pipeline.py` | 新增公式符号还原回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `2335d1fffb654f848d1d6b88a43d1059`）
- 重新解析后第18-21页公式符号可读（°′″⌈⌉⌊⌋×Δ），不再走 figure_review
- **已知遗留**：公式结构仍碎片化（分式分子分母被拆成多行，如 `c = ⌈...⌉ / ⌊...⌋` 的分子分母分占不同行）；双减号（`−−`）来自 PDF 排版把同一减号用 Symbol/普通字体各渲染一次，共14处。这两项需后续公式结构恢复逻辑处理

### 2.9 修复：《AC-23-AA-2022-01》第28页表格标题误检导致门禁失败（2026-08-07）

> 用户反馈《关于印发〈正常类飞机审定〉咨询通告的通知》解析 `Detected table captions were not bound one-to-one to structured tables: [28]`。job `4d732df3c6c2449cbec78ea33a95310f`。

#### 问题

- 第28页 `table_caption_alignment` 门禁失败：`table_hints` 含 `表 3 替换`、`表 2 400` 两个误检表题，但页面只有 1 个结构化表格（无标题）
- 门禁判定：expected_caption_refs（表3、表2）⊄ actual_caption_refs → fail

#### 根因

- 第28页是跨页 ASTM 标准对照大表格（第27-29页），单元格内引用文字如"ASTM F3230-17 表3 替换…"、"ASTM F3236-17 表2 400至700MHz…"
- 该 PDF 文本层**极度碎片化**（每个词/字拆成独立行：`表`、`3`、`替换`、`ASTM`…）
- `detect_table_hints` 检测到孤立"表"行后拼接后续 2-3 行 → `表`+`3`+`替换`="表 3 替换"（误判为表题）
- 误判表题进入 expected_caption_refs，而页面实际表格无标题 → 门禁失败

#### 修复（`pipeline.py`）

- 新增 `_TABLE_TITLE_CONTENT_RE`：表号后若紧跟**动词/数字/英文**（如"替换"/"400"/"Replace"），判为单元格内容引用而非表题
- `detect_table_hints` 和 `_semantic_table_hints` 都应用该过滤

#### 验证

- 碎片化文本 `表`+`3`+`替换` → 不再生成 hint（此前生成"表 3 替换"）
- 真实表题（`表 3 空域编码规则`、`表 A.1 术语和定义`、`表 1 履历本封面`）仍保留
- 第28页修复后 `table_hints=[]`（误检清除），`preflight_pages` 仍识别为表格页（表格线检测不受影响）
- 新增回归测试 1 个（test_pipeline.py）：`test_detect_table_hints_rejects_fragmented_cell_content`
- 相关测试套件：**143 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | 新增 `_TABLE_TITLE_CONTENT_RE`；`detect_table_hints`/`_semantic_table_hints` 过滤单元格内容误判 |
| `tests/unit/test_pipeline.py` | 新增碎片化单元格内容过滤回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `4d732df3c6c2449cbec78ea33a95310f`）
- 重新解析后第28页 `table_caption_alignment` 门禁应通过（不再有虚假表题）
- 若其他文档出现"表N + 动词/数字"的单元格引用误判，此修复同样适用

### 2.10 修复：《CCAR-23-R3》ToUnicode 冲突误判导致图片页走 OCR 未解析（2026-08-07）

> 用户反馈《CCAR-23-R3 正常类、实用类、特技类和通勤类飞机适航规定》第164-166页图片与公式识别有误，门禁 `OCR pages remain unresolved: [164, 167, 168, 172]`。job `b9201620324a428ebf14914f8c30f9c6`。

#### 问题

- 门禁 `remote_ocr` fail：`OCR pages remain unresolved: [164, 167, 168, 172]`
- `remote_parser` warn：`MinerU result has no page-indexed content list`（第164/167/168/172页）
- 第164/166/167/168/172页是**纯图片页**（图A2/A4/A5/A6 + 图注），但被路由到 OCR，MinerU 对图片页返回空 content list → 图片丢失、门禁失败

#### 根因（两层）

1. **`has_tounicode_cid_conflict` 阈值过敏感**：只要发现 **1 个** CID 冲突就判为文字损坏 → 走 `text_layer_review`（OCR）。但 CCAR-23 是正常文档，仅有个别页的 SimSun 字体有 1 个 CID→空格 编码瑕疵（1166 个 bfchar 中 1 个冲突），被误判为损坏。223 页中 198+ 页被误判走 OCR
2. **图片页走 OCR 返回空**：这些图片页（图A2/A5/A6）MinerU OCR 识别不到文字（只有图和图注），content list 为空 → `unresolved`

#### 修复（`pipeline.py`）

- `has_tounicode_cid_conflict` 从"≥1 冲突"改为**冲突密度阈值**：`冲突数 ≥2 且 冲突率 ≥0.5%`（冲突数/bfchar总数）
- 区分依据：GJB（真损坏）冲突率 0.75%-1.85%，CCAR-23（正常）冲突率 0.09%-0.24%，GB19001（正常）0%

#### 验证

| 文档 | 修复前命中 | 修复后命中 |
|---|---|---|
| CCAR-23（正常） | 198+ 页误判 | **0/223 页** |
| GJB（真损坏） | 39 页 | **38/39 页** |
| GB19001（正常） | 0 页 | **0/29 页** |

- 修复后 CCAR-23 全部走 `native_text`（210页）+ `layout_review`（2页）+ `toc_excluded`（11页），第164-172页不再走 OCR
- 第164/166/167/168/172页图片页走 vector 解析，正确生成 figure 资产
- 新增回归测试 1 个（test_pipeline.py）：`test_tounicode_cid_conflict_single_quirk_returns_false`
- 更新既有测试 2 个（多冲突才判损坏）
- 相关测试套件：**144 passed, 3 failed**（3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `has_tounicode_cid_conflict` 改用冲突密度阈值（≥2 且 ≥0.5%） |
| `tests/unit/test_pipeline.py` | 新增单次冲突不误判测试；更新冲突检测测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `b9201620324a428ebf14914f8c30f9c6`）
- 重新解析后：第164/167/168/172页不再走 OCR（不 unresolved），门禁 `remote_ocr` 应通过；图片页生成 figure 资产
- GJB 2489A 仍走 OCR（真损坏，冲突率高），不受影响

## 3. 验证命令

```powershell
Set-Location D:\internal-rag\source\internal-rag
.\.venv\Scripts\python.exe -B -m pytest tests\unit -q -p no:cacheprovider
.\.venv\Scripts\python.exe -B -m ruff check app tests\unit --no-cache
.\deploy\gateway\stop.ps1
.\deploy\gateway\start.ps1
```

Linux 沙箱兼容验证（Python 3.10，跳过 conftest 的 app.main 导入链）：

```bash
python3 -c "
import datetime
if not hasattr(datetime, 'UTC'): datetime.UTC = datetime.timezone.utc
import pytest
raise SystemExit(pytest.main(['tests/unit/test_vector_pdf.py', 'tests/unit/test_pipeline.py', 'tests/unit/test_regulation_preprocess.py', '-q', '-p', 'no:cacheprovider', '--noconftest', '--basetemp=/tmp/pytest-rag']))
"
```

代码改动后必须重启 Gateway（无热重载）；前端 `Ctrl+F5` 刷新。

## 4. 已知遗留问题（来自 08-04/08-05/08-06 交接，可能作为本会话输入）

1. **封面 chunk 页码 1-5 粘连**（08-06 发现，见 HANDOFF-2026-08-06 2.1）：封面段落与第 5 页公式占位块合并，需拆分或让封面 `indexable=False`
2. **表格+整页图冗余**：`vector_pdf.py` 需区分"纯表格页"vs"表格+插图页"（当页面有真实非表格图片 `image_coverage >= 0.05` 才生成 figure_page）
3. **OCR 英文术语假字**：MinerU 对小号拉丁字符识别精度不足，需 OCR 精度优化或视觉模型复核
4. **TOC 打分阈值未校准**：`toc_score >= 0.85` 目前目录页达不到，需积累真实分布或接入 VLM 复核
5. **8G 显存约束**：MinerU 与 Ollama 不能同时满载，否则 MemoryError
6. **术语定义正文段层级偏深**（08-06 发现，见 HANDOFF-2026-08-06 2.14）：术语定义正文段会被误推断为更深层条款（如 `3.6.1.2 示例…`），内容不丢失但层级略多
7. **编号点号与正文混行**（08-06 发现，见 HANDOFF-2026-08-06 2.8）：第9页 `513`（应为 `5.1.3`）点号与正文 `1s。` 混在同一行，本次未覆盖，后续可继续处理
