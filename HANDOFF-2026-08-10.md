# Internal RAG 解析质量改进交接（2026-08-07 之后）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-07.md` 之后）的工作。项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-07.md`（已覆盖：MediaBox 原点偏移裁剪、附录劫持与 TOC 误判、整页白色背景图误判 figure、无竖线表格识别、ToUnicode CID 冲突检测与 OCR 路由、解压超限兜底、Symbol 字体 PUA 数学符号、碎片化表题误检、ToUnicode 冲突密度阈值）
- **本会话范围**：自 2026-08-08 起的活动。08-10 上午宿主机用最新代码重新解析 `CCAR-23-R3.pdf`，验证 08-07 的 ToUnicode 修复在生产数据上的路由效果（见 2.1）；随后用户抽检发现图片图名与下方正文混合，本会话对 `_caption_near` 做了修复（见 2.2）；排查 `CCAR-27-R2` 大量页面被误判为图片页的问题，新增低质量 OCR 文本层检测，让扫描件正确走 OCR（见 2.3）；因 CCAR-27 扫描件 OCR 吞吐过低做了批大小配置化 + OCR 进度回调（见 2.4）；最后排查 `GB 42590-2023` 第18页等表格页内容为 `/GXX` 乱码的问题，修复 PostScript 字形名页被拉回 vector 表格解析（见 2.5）
- **重要**：2.1 为生产数据复查记录；2.2、2.3、2.4、2.5 为**代码修复/改进**（`vector_pdf.py` / `pipeline.py` / `remote.py` / `hybrid_pdf.py` / `jobs.py` / `registry.py` + `tests`）

## 1. 当前状态快照（2026-08-10）

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`
- 单元测试基线：**176 passed, 3 failed**（3 个既有失败与近期改动无关：TOC 检测、跨页 caption 绑定、figure 去重；本会话 2.2 新增 3 个、2.3 新增 4 个、2.4 新增 2 个、2.5 更新 1 个+新增 1 个回归测试，实测复核）
- Git：仓库仍无 commit，整个工程为未跟踪状态；**禁止** `git reset --hard` / `git clean`
- 服务状态：需在宿主机 PowerShell 运行 `deploy\gateway\status.ps1` 等脚本确认（Linux 沙箱无法执行 Windows 二进制）
- 源码最近改动：`2026-08-10`（`vector_pdf.py` `_caption_near` 图名混合修复见 2.2；`pipeline.py` 低质量 OCR 文本层检测见 2.3；`remote.py`/`hybrid_pdf.py`/`jobs.py` OCR 批大小与进度回调见 2.4；`pipeline.py`/`hybrid_pdf.py` 字形名页排除 vector 见 2.5）

## 2. 改动记录（按日期逐节追加）

### 2.1 验证：CCAR-23-R3.pdf 重新解析（2026-08-10）

> 宿主机在 08-10 凌晨用最新代码重新处理 `CCAR-23-R3.pdf`，核对 08-07 的 ToUnicode CID 冲突密度阈值修复（见 08-07 交接 2.10）在生产文档上的路由效果。job `3afa24fd873e47569263e52115e8f938`。

#### 处理结果

- 文档：`CCAR-23-R3.pdf`（223 页，850 chunks），源路径 `成电RAG项目效果测试要求/标准/规章/CCAR-23-R3.pdf`，sha256 `652dfea0…`，2,074,426 字节
- 状态：`warning`（2 个警告门禁，0 失败）；review 状态 `unreviewed`（"资料已重新处理，需要重新审核"）；`publication` 为 `not_requested`
- 输出目录：`D:\internal-rag\data\ingestion\outputs\3afa24fd873e47569263e52115e8f938\CCAR-23-R3__652dfea0`
- 注意：本 job 处理的是 `CCAR-23-R3.pdf`（sha256 `652dfea0`）；08-07 交接 2.10 对应的 job `b9201620…` 处理的是同规章的另一文件名 `CCAR-23-R3 正常类、实用类、特技类和通勤类飞机适航规定.pdf`（sha256 `8da0a38b`，2,133,435 字节）。两者为同一规章的不同 PDF 变体，解析指标完全一致

#### 路由验证（ToUnicode 修复效果）

| 指标 | 期望（08-07 修复目标） | 08-10 实测 |
|---|---|---|
| `pages_requiring_ocr` | 正常图片页不应被误判走 OCR | `[]`（0 页）|
| `text_layer_corrupted_pages` | 正常文档不应有误报 | `[]`（0 页）|
| OCR unresolved（164/167/168/172） | 应消除 | 无 unresolved 页 |
| `invalid_unicode_pages` | — | `[]`（0 页）|

- route 分布：`native_text 171` / `toc_excluded 11` / `vector_layout 28` / `figure_review 12` / `remote_ocr 1`
- 关键点：第164-172页图片页**不再走 OCR**，由 vector 解析生成 figure 资产；`remote_ocr` 仅剩第165页（公式页，`formula` 类型，附录 A23.13 速度/载荷公式），生成 2 个 `remote-formula` 资产
- 与 08-07 job `b9201620…` 的结果完全一致（route 分布、75 个资产、质量门禁均相同），确认修复在不同 PDF 变体上行为稳定

#### 解析产出概况

- page_type 分布：`text 182`、`table 27`、`figure 12`、`table_figure 1`、`formula 1`
- 表格：`structured_table_count=25`、`cross_page_table_count=7`、`table_fragment_count=40`；28 个 table 候选页全部结构化（`structured_table_pages` 完整），0 个 unresolved 表格页
- 表格系列（12 个，含大跨页表）：`[14]`、`[25]`、`[47,48,49]`、`[58]`、`[70]`、`[84]`、`[91]`、`[138,139,140]`、`[158]`、`[162,163]`、`[171]`、`[212–223]`
- 条款：`article_count=533`、`heading_count=987`、`article_hard_boundary_violations=0`、`unpublished_clause_count=0`
- 资产：75 个（table 40、figure_page 13、figure 19、formula_page 1、formula 2）

#### 质量门禁（2 个警告，均需人工目检）

1. **`table_merge_fidelity`** warn：`Tables contain merged cells and require visual verification: [14, 91, 138, 214, 220, 221, 222]` —— 含合并单元格的表格页需目检
2. **`figure_semantics`** warn：`Figure pages require visual review: [39, 52, 143, 161, 163, 164, 166, 167, 168, 169, 172, 181, 182]` —— 图片语义需目检（`VLM described: []`，本次未做 VLM 描述）

#### 注意

- 该 job review 状态为 `unreviewed`，发布状态 `not_requested`；如需发布到 RAGFlow，需先在接入台人工抽检警告项并确认
- 抽检建议重点：合并单元格表格（第14、91、138、214、220、221、222页）的单元格对齐；图片页（第163、164、166–168、172页）的 figure 裁剪与语义
- 后续继续处理其他文档时，按 08-07 交接各节"注意"项在宿主机重启 Gateway（无热重载）

### 2.2 修复：《CCAR-23-R3》图片图名与下方正文混合（2026-08-10）

> 用户抽检时发现 CCAR-23 解析里有好几张图片的图名与下面的文字混合了。问题根因在 `_caption_near` 的 fallback。

#### 问题

- 多个 figure 资产的 caption 混入了图片下方的正文：第52页 figure block text 为 `图 驾驶员操纵方向舵的最大力 [1993年12月23日第二次修订…] 第23.443条 突风载荷 (a)…`——图名、修订日期、条款正文全部粘连
- 其他受影响页：第39页 caption 变成 `[1990年修订] 第23.335条 设计空速…`（正文当图名）；第143页为 `(b)如果装有飞行机组成员…`；第161页为页脚 `CCAR 23 R3 － 149 －`；第163页混入表格碎片 `同上（D）择大于这些最小值的速度作为…`；第172页混入规章标题

#### 根因（`vector_pdf.py` `_caption_near`）

- `_caption_near` 在图片下方 120pt / 上方 30pt 裁剪带内找图名，只认**带编号**的 `图N 描述`（`FIGURE_CAPTION_RE`）
- 第52页图名 `图 驾驶员操纵方向舵的最大力` **无编号**，带编号正则匹配不到
- 找不到编号图名时，fallback 把**整个裁剪带的多行文字拼接返回**（`text = " ".join(text.split())`），把图名+修订日期+条款正文全当作 caption

#### 修复（`vector_pdf.py`）

1. **新增 `FIGURE_CAPTION_UNNUMBERED_RE`**：匹配无编号图名（`图 驾驶员操纵方向舵的最大力`），要求 `图` 后跟 CJK 描述，避免误配条款引用 `图 23.443条`
2. **新增 `CAPTION_BODY_LINE_RE`**：识别正文行（修订日期 `[…]`、子项标记 `(a)`、条款标题 `第N条`、编号项、页脚破折号/CCAR、表格碎片 `同上/同下`、重复页眉、含 CJK 句读的续行），fallback 跳过这些行
3. **fallback 收紧**：只在裁剪带内**恰好剩一行**短描述（≤80 字符，symbol legend 等）时才返回；拒绝纯 CJK 长句碎片（如 `择大于这些最小值的速度作为`）。无有效行时返回空串，由调用方回退到 `原 PDF 第 N 页图表` 占位，绝不再把整段正文当图名

#### 验证

- **第52页**：caption 从整段正文混排 → `图 驾驶员操纵方向舵的最大力`（无编号图名正确提取）
- **第39/143/161/172页**：caption 从正文/页脚/标题 → 空（走占位），不再污染
- **第163页**：表格碎片 `择大于这些最小值的速度作为` 消除，仅保留 `图A1 在速度V 时确定n 系数的曲线图`
- **图A2/A4/A5/A6/A7 等带编号图名不受影响**，`图1/图2/图3`（水上飞机载荷）正常
- 新增回归测试 3 个（test_vector_pdf.py）：
  - `test_caption_near_unnumbered_caption_not_mixed_with_body`（无编号图名不混正文）
  - `test_caption_near_fallback_rejects_body_text`（修订日期/正文/页脚不返回）
  - `test_caption_near_single_symbol_legend_still_returned`（单个 symbol legend 仍保留）
- 相关测试套件：**147 passed, 3 failed**（修复前 144 passed；+3 为新增回归测试，3 个既有失败与本次无关）；真实 PDF 全 13 个 figure 页复扫 caption 干净

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | 新增 `FIGURE_CAPTION_UNNUMBERED_RE`/`CAPTION_BODY_LINE_RE`；`_caption_near` 无编号图名识别 + fallback 收紧 |
| `tests/unit/test_vector_pdf.py` | 新增 3 个图名混合回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `3afa24fd873e47569263e52115e8f938`）
- 重新解析后应确认：第52页等 figure chunk 的 caption 不再混入正文；`figure_semantics` 门禁仍会提示图片页需目检（本次修复的是 caption 文本，不影响图片裁剪本身）
- 已知次要遗留：第181页 img0（附件I示意图）的图名 `I` 在图片内部，`_caption_near` 提取不到外部文字，走图内文字/占位，需 VLM 描述覆盖

### 2.3 修复：《CCAR-27-R2》扫描件被误判为图片页（2026-08-10）

> 用户反馈 `CCAR-27-R2 正常类旋翼航空器适航规定.pdf` 解析内容较差（第189页正文打散）。排查发现它是"扫描件 + 内嵌 OCR 文本层"，221 个内容页全部被误判为 `figure_review`，正文未作为文本发布。job `f434fa8690374d0aadb2600a0f41ed8e`。

#### 问题

- CCAR-27-R2（236 页）：221 个内容页全部 `figure_review`，0 页走 OCR。第189页正文被拆成 `27.1395条`、`0.300且不大于 540;...` 等碎片 chunk，表格、条款结构全部丢失
- 文本层有大量 OCR 错乱：`交通运榆部`（运输）、`zO17`（2017）、`第 27。 673条`（27.673）、`0。 335`（0.335）、`如呆`（如果）、`軎告`（警告）

#### 根因

1. **`_is_fullpage_background_image` 只过滤"纯白/极低密度"背景图**。CCAR-27 每页的整页 JPEG 密度约 0.004-0.012 B/px（低于 0.05 阈值），但它是**正文扫描图**（有内容），不是装饰背景，所以被当成实质图片 → `substantive_image_count>=1` + `largest_image_coverage>=0.12` → 判为 figure
2. **现有文本层损坏检测全部漏判**：OCR 文本层是合法 Unicode（无 PostScript 字形名、无假字、无 ToUnicode 冲突），只有"错字多 + 逐字空格"特征，现有 `is_glyph_name_garbage` / `is_fake_cjk_garbage` / `has_fake_glyph_corruption` / `has_tounicode_cid_conflict` 都识别不了
3. **`extract_page_layout_features` 图像尺寸读取失效**：pypdf 的 `ImageFile` 没有 `.width/.height` 属性（只有解码后的 `.image`），所有图像尺寸读到 0 → `image_ratio=0`，路由阶段拿不到大图信息

#### 修复（`pipeline.py`）

1. **新增 `has_low_quality_ocr_text(text, image_ratio=0.0)`**：识别"扫描件内嵌低质量 OCR 文本层"，两个信号（满足其一即判）：
   - **数字句号信号**：`\d[。．]\s*\d` 模式（OCR 把小数点点号识别成全角句号），要求**绝对次数 ≥3 且密度 ≥8/万字符**。CCAR-27 每页 3-20 个，正常文档 ≤2 个（如附录编号 `C23．1`）
   - **汉字逐字空格信号**：`汉字 汉字` 相邻对比例 ≥0.15（OCR 每字后插空格），要求文本 ≥100 字符、中文占比 ≥10%、且 `image_ratio ≥ 0.5`（有整页扫描图）。正常文档页眉 `C C A R 23 R 3` 的空格不在两个汉字之间，不会触发
2. **路由接入**：`parse_pdf` 的 `text_layer_corrupted` 增加 `or low_quality_ocr` → 命中页 route 从 `native_text` 改为 `text_layer_review` → hybrid 自动纳入 OCR 目标，并排除出 figure 路径
3. **修复 `extract_page_layout_features` 图像尺寸读取**：优先从 pypdf `ImageFile.image`（PIL）读取尺寸，兜底 `.width/.height`。修复后 CCAR-27 页面 `image_ratio` 从 0 变为 1.0
4. **修复 `_figure_dominant_pages` 误伤封面页**：该函数本意是把"文本短且是纯图表"的文本层损坏页（如 CCAR-23 图A2，只有图名）保留为 figure（OCR 对纯图返回空）。但它对**低质量 OCR 标记的扫描文字页**（封面、分送页等）同样生效——CCAR-27 第1页封面（104字符）被误当图片页拉回 figure。修复：`_figure_dominant_pages` 跳过 `low_quality_ocr=True` 的页（扫描文字页应走 OCR 恢复内容，不是纯图表）

#### 验证

- **CCAR-27**：route 从 221 figure_review / 0 OCR → **214 remote_ocr + 15 toc + 7 figure_review**（宿主机 08-10 11:44 用新代码重跑产物）；低质量 OCR 命中 231/236 页
- **第1页（封面）修复**：`_figure_dominant_pages` 不再把 `low_quality_ocr=True` 的封面页拉回 figure，第1页走 OCR。对照 CCAR-23 图A2（clean 短文本）仍正确保留为 figure
- **CCAR-23（正常）**：0 误判（`low_quality_ocr` 0 命中），route 恢复 native 210 + toc 11 + layout 2，图片页（图A1-A7）不再误判走 OCR
- **CCAR-26 / SAE AS9102C / GJB 2489A**：0 误判（CCAR-26 文本层质量高不触发；SAE 纯英文不触发；GJB 原本走 OCR 不受影响）
- 新增回归测试 4 个（test_pipeline.py）：
  - `test_low_quality_ocr_detects_period_between_digits`（数字句号 ≥3 触发）
  - `test_low_quality_ocr_clean_page_not_detected`（正常文档零散 `C23．1` 不触发）
  - `test_low_quality_ocr_cjk_pair_spacing_requires_scan_image`（汉字逐字空格 + 大图触发；无大图/正常排版不触发）
  - `test_figure_dominant_pages_excludes_low_quality_ocr`（封面页不被当图片保留，图A2 仍保留）
- 相关测试套件：**151 passed, 3 failed**（修复前 144 passed；+7 为本会话新增回归测试，3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | 新增 `has_low_quality_ocr_text`；`parse_pdf` 路由接入；修复 `extract_page_layout_features` 图像尺寸读取（PIL `.image`） |
| `tests/unit/test_pipeline.py` | 新增 3 个低质量 OCR 文本层回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `f434fa8690374d0aadb2600a0f41ed8e`）
- 重新解析后应确认：CCAR-27 正文页走 OCR（MinerU），条款结构/表格恢复；`figure_review` 页数从 221 大幅下降
- 已知遗留：剩余 5 页（HIRF 频率表 20/21/231/232 + 短页 219）因中文占比/长度不足未命中，若 OCR 后仍有问题可单独处理；CCAR-26 走 OCR 是原有行为（扫描件），与新检测无关
- 本修复对"扫描件 + 内嵌 OCR 文本层"的 PDF 通用（不限于 CCAR-27）

### 2.4 改进：MinerU OCR 批大小配置化 + 进度回调（2026-08-10）

> CCAR-27 扫描件 OCR 吞吐过低（约 109 个 OCR 页单位 40 分钟，整份 236 页预计 70-90 分钟）。根因是执行策略过于串行：批大小硬编码 10、每批一个 `/file_parse` 请求顺序等待、进度停留在 15% 无法感知推进。本次先做两个低风险优化，不做并发。

#### 改动一：批大小配置化

- `RemoteParserSettings` 新增 `page_batch_size`（默认 10，范围 1-200）
- `default.yaml` / `.env.example` / `.env` 新增 `MINERU_PAGE_BATCH_SIZE` 环境变量
- `MinerUClient.parse_pages` 的 `batch_size` 参数改为可选，默认取 `self.settings.page_batch_size`，不再硬编码模块常量
- 配置摘要（`public_view`）暴露 `page_batch_size`

#### 改动二：远程解析进度回调

- `MinerUClient.parse_pages` 新增 `progress_callback: Callable[[int, int], None]`，每批结束后上报 `(已完成页, 目标总页)`
- `HybridPdfParser.parse` 新增 `progress_callback`，透传给 MinerU
- `ParserRegistry.parse` 新增 `progress_callback`，透传给 HybridPdfParser
- `process_job` 在 `parsing` 阶段构造回调：每批结束后更新 job 为 `percent=15+67*done/total`、`message="远程 OCR 处理中：done/total 页"`、`indeterminate=False` 并写盘，前端可实时看到推进，不再长期停在 15%

#### 验证

- 批大小 2、目标页 1-5（连续）产生回调 `[(2,5),(4,5),(5,5)]`，分批正确
- 未显式传 `batch_size` 时使用 `settings.page_batch_size`，环境变量 `MINERU_PAGE_BATCH_SIZE=25` 加载生效
- 新增回归测试 2 个（test_remote_parsers.py）：
  - `test_mineru_reports_batch_progress_callback`
  - `test_mineru_batch_size_defaults_to_settings`
- 相关测试套件：**175 passed, 3 failed**（+2 新增；3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/core/config.py` | `RemoteParserSettings.page_batch_size` + 配置摘要暴露 |
| `config/default.yaml` | 新增 `page_batch_size: ${MINERU_PAGE_BATCH_SIZE:-10}` |
| `.env.example` / `.env` | 新增 `MINERU_PAGE_BATCH_SIZE=10` |
| `app/ingestion/parsers/remote.py` | `parse_pages` 批大小默认取 settings + `progress_callback` |
| `app/ingestion/parsers/hybrid_pdf.py` | `parse` 新增 `progress_callback` 透传 |
| `app/ingestion/parsers/registry.py` | `parse` 新增 `progress_callback` 透传 |
| `app/ingestion/jobs.py` | `process_job` 构造 OCR 进度回调更新 job |
| `tests/unit/test_remote_parsers.py` | 新增 2 个回归测试 |

#### 注意

- 宿主机需重启 Gateway 使新配置生效（`stop.ps1` + `start.ps1`）
- 调大 `MINERU_PAGE_BATCH_SIZE` 会提升吞吐，但增加单次请求解压与内存峰值；8G 显存下建议先测 20，再测 30，观察显存与耗时
- **未做并发**：`worker_concurrency` 仍为 1；本批改动只优化单 worker 内的吞吐与可观测性。并发 2 需等本次任务完成后核对 `retried_pages`/`unresolved`/路由再做
- 进度回调只在 `mineru.enabled` 且存在 `ocr_targets` 时生效；纯 native/vector 路径进度仍按原逻辑
- 后续建议（本次未做）：连续 OCR 页合并进更大范围、unresolved 页集中重试、按 SHA+版本+页范围缓存结果

### 2.5 修复：《GB 42590-2023》PostScript 字形名页被拉回 vector 表格解析（2026-08-10）

> 用户反馈 `GB 42590-2023 民用无人驾驶航空器系统安全要求.pdf` 解析结果问题很大（如第18页）。排查发现整份文档文本层是 PostScript 字形名（`/G8E/G47/G6C/...`），15 个表格页被拉回 vector 表格解析，单元格内容全是 `/GXX` 乱码。job `3b31bd75b9a14230bc2aee0c3b9f007e`。

#### 问题

- 38 页全部 `text_layer_corrupted_pages`（字形名乱码），质量门禁 `text_layer_glyph_names` warn
- 但第10/11/13/17/18/19/20/27/28/29/31/32/33/35/36 页（15 页）route=`vector_layout` / `page_type=table_figure`，cleaned_text 全是 `/G8E/G47/...`，无一个汉字
- 第18页 chunk 内容为 `/G8E/G47/G6C/G72/G30/G31/...` 乱码，表格结构"恢复"了但单元格文字不可用

#### 根因（`hybrid_pdf.py`）

- `parse_pdf` 已正确把字形名页 route 设为 `text_layer_review`（`is_glyph_name_garbage` 命中）
- 但 hybrid 层 `vector_preflight_targets.update(text_layer_targets - tounicode_corrupted_targets)` **只排除了 ToUnicode 冲突页**，没有排除 PostScript 字形名页
- 结果：字形名页被拉回 vector 表格预检 → 表格网格恢复 → 单元格文字是 `/GXX` 乱码（与 GJB 2489A 的 ToUnicode 冲突是同类问题，但触发源不同）

#### 修复（`pipeline.py` + `hybrid_pdf.py`）

1. **`pipeline.py`**：`text_layer_corruption["glyph_name_garbage"]` 从"所有损坏标志的 OR"改为**精确的 `is_glyph_name_garbage(raw_text)` 结果**（此前它与 ToUnicode 冲突/低质量 OCR 共用 OR，无法区分具体损坏类型）
2. **`hybrid_pdf.py`**：新增 `glyph_name_corrupted_targets`（所有 `glyph_name_garbage=True` 页），`vector_preflight_targets.update(text_layer_targets - tounicode_corrupted_targets - glyph_name_corrupted_targets)`——字形名页与 ToUnicode 冲突页一样强制走 OCR，不再进 vector 表格预检

#### 验证

- GB 42590 全部 38 页 `glyph_name_garbage=True`，route 均应为 `text_layer_review`；第10/18/19/20/27 页从 vector 表格路径改为走 OCR
- CCAR-23（正常）前40页 `glyph_name_garbage` 0 误报
- 回归测试更新 + 新增：
  - 更新 `test_parse_pdf_records_tounicode_conflict_and_routes_to_ocr`：ToUnicode 冲突页 `glyph_name_garbage` 精确标志应为 False（文本无 /GXX）
  - 新增 `test_glyph_name_pages_excluded_from_vector_preflight_targets`：字形名页排除出 vector 预检，native 页保留
- 相关测试套件：**176 passed, 3 failed**（+1 新增；3 个既有失败与本次无关）；ruff 全部通过

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `glyph_name_garbage` 改为精确的 `is_glyph_name_garbage` 结果 |
| `app/ingestion/parsers/hybrid_pdf.py` | 新增 `glyph_name_corrupted_targets`，从 vector 预检排除字形名页 |
| `tests/unit/test_pipeline.py` | 更新 ToUnicode 测试断言；新增字形名页排除回归测试 |

#### 注意

- 宿主机需重启 Gateway 并重新解析该文档刷新产物（job `3b31bd75b9a14230bc2aee0c3b9f007e`）
- 重新解析后应确认：第18页等表格页走 OCR，单元格文字不再是 `/GXX` 乱码
- 此修复与 GJB 2489A（ToUnicode 冲突）同源：都是"文本层损坏但表格网格完整"的文档，恢复网格只会得到乱码单元格，OCR 是唯一正确路径

## 3. 验证命令

```powershell
Set-Location D:\internal-rag\source\internal-rag
.\.venv\Scripts\python.exe -B -m pytest tests\unit -q -p no:cacheprovider
.\.venv\Scripts\python.exe -B -m ruff check app tests\unit --no-cache
.\deploy\gateway\stop.ps1
.\deploy\gateway\start.ps1
```

Linux 沙箱兼容验证（Python 3.10，跳过 conftest 的 app.main 导入链）：

```bash
python3 -c "
import datetime
if not hasattr(datetime, 'UTC'): datetime.UTC = datetime.timezone.utc
import pytest
raise SystemExit(pytest.main(['tests/unit/test_vector_pdf.py', 'tests/unit/test_pipeline.py', 'tests/unit/test_regulation_preprocess.py', '-q', '-p', 'no:cacheprovider', '--noconftest', '--basetemp=/tmp/pytest-rag']))
"
```

> 本会话实测：**176 passed, 3 failed**（修复前 144 passed；+10 为本会话新增回归测试，3 个既有失败为 TOC 检测、跨页 caption 绑定、figure 去重，与近期改动无关）。

代码改动后必须重启 Gateway（无热重载）；前端 `Ctrl+F5` 刷新。

## 4. 已知遗留问题（来自 08-04/08-05/08-06/08-07 交接，可能作为本会话输入）

1. **封面 chunk 页码 1-5 粘连**（08-06 发现，见 HANDOFF-2026-08-06 2.1）：封面段落与第 5 页公式占位块合并，需拆分或让封面 `indexable=False`
2. **表格+整页图冗余**：`vector_pdf.py` 需区分"纯表格页"vs"表格+插图页"（当页面有真实非表格图片 `image_coverage >= 0.05` 才生成 figure_page）
3. **OCR 英文术语假字**：MinerU 对小号拉丁字符识别精度不足，需 OCR 精度优化或视觉模型复核
4. **TOC 打分阈值未校准**：`toc_score >= 0.85` 目前目录页达不到，需积累真实分布或接入 VLM 复核
5. **8G 显存约束**：MinerU 与 Ollama 不能同时满载，否则 MemoryError
6. **术语定义正文段层级偏深**（08-06 发现，见 HANDOFF-2026-08-06 2.14）：术语定义正文段会被误推断为更深层条款，内容不丢失但层级略多
7. **编号点号与正文混行**（08-06 发现，见 HANDOFF-2026-08-06 2.8）：第9页 `513`（应为 `5.1.3`）点号与正文混行，本次未覆盖
8. **GJB 2489A ToUnicode 乱码**：整个文档（39 页）走 OCR 后解析时间增加，8G 显存下需分批（08-07 2.5 注意）
9. **MH/T 4063.1 公式结构碎片化**：分式分子分母拆多行、双减号（`−−`）共 14 处，需公式结构恢复逻辑（08-07 2.8 注意）
10. **第9页页眉误编号**：页眉"质量管理体系要求"被误编号为 `0.4.1`，涉及顶层单级条款"1. 范围"的 `1.` 格式识别（08-07 2.2 注意）
