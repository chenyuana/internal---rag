# Internal RAG 改动记录（2026-08-21）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-20.md` 之后）的工作。
> 项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。
> 本文档按日期逐节追加改动/验证记录，沿用既有格式：问题 / 根因 / 修复 / 验证 / 改动文件 / 注意。

---

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-20.md`（已覆盖：引用-内容脱钩 `ClaimTopicValidator` 方案 A 实施与在线生效、范围外文档域过滤 `query_scope`、对比题整链修复与确定性矩阵兜底 `comparison_matrix_builder`、智能问答界面与运行时模型切换、复杂对比题 RAGFlow 卡死修复（`keyword=False`）与 `docker desktop restart` 恢复等；末次完整 pytest **401 passed, 3 failed**）。
- **重要时间线补充（08-20 晚新增产出，未记入 08-20 文档）**：`HANDOFF-2026-08-20.md` 于 08-20 16:49 定稿，但**其后 17:17–17:36 又产生了新的文档与评测集产出**，未登记进任何 HANDOFF：
  - `docs/query-translation-decomposition-refactor.md`（17:34 新建，21.6 KB）：查询分解与查询转换重构实施方案（目标架构、QueryPlanV2/EvidenceRecord 数据契约、6 个 Stage 分阶段实施、DeepSeek V4 执行主提示词、最终验收）。
  - `tests/evaluation/`（17:30–17:36 新建）：`planning_cases.jsonl`、`retrieval_cases.jsonl`、`live_rag_cases.jsonl`、`run_live_eval.py`、`README.md`（评测集说明）。
  - `tmp/run_sand_diag.py`、`tmp/sand-debug-20260820.json`、`tmp/sand-chat-20260820.json/.txt`（17:17–17:20 沙箱诊断产物）。
  - 本会话（08-21）负责把这一批"定稿后新增产出"登记进本文档 §2.2，并同步 `README.md` 文档索引。
- **本会话范围**：自 2026-08-21 起。先完成工程熟悉与 08-20 晚产出登记，随后根据 DeepSeek 评审完整实施查询分解/转换架构、EvidenceRecord、确定性矩阵前置、安全 diagnostics 与三级评测闭环；详见 §2.3。
- **重要**：本会话的改动/验证记录在下方第 2 节中按日期逐节追加，不以计划项代替完成结果。

---

## 1. 当前状态快照（2026-08-21）

以下为本次会话实际核对的快照（非引用旧文档）。

### 1.1 环境与代码

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`、`http://127.0.0.1:8080/chat`
- 服务探测（2026-08-21 08:50 实测，HTTP 探活）：

| 服务 | 端口 | 状态 |
|---|---|---|
| Gateway | `127.0.0.1:8080` | ✅ `/health` 200 `{"status":"ok","service":"internal-rag","version":"0.1.0","environment":"development"}` |
| RAGFlow | `127.0.0.1:9380` | ✅ `/api/v1/system/healthz` 200 `{"db":"ok","doc_engine":"ok","redis":"ok","status":"ok","storage":"ok"}` |
| Ollama | `127.0.0.1:11434` | ✅ `/api/tags` 200 |
| Reranker | `127.0.0.1:8003` | ✅ `/health` 200 `{"status":"ok"}` |
| MinerU | `127.0.0.1:8886` | ❌ 关闭（涉及 OCR/远程解析的作业前需先启动 `.\deploy\mineru\start.ps1`） |

- 网关进程核对：改造后已停止旧 PID 17176，最终 8080 真实监听 PID **1540**，与 `runtime/gateway/gateway.pid` 一致；公开配置已确认新 planning/translation/retrieval/generation 开关生效。
- 配置文件核对（`config/default.yaml`）：`prompt_version: answer-generation-v15`、`models.answer.temperature: 0`、`seed: 42`、`json_repair_attempts: 2`、`procedure_rerank_input_k/top_k/final_limit: 22/16/18`、`enforce_declared_scope / enforce_scope_consistency / enforce_claim_grounding / enforce_subject_grounding / enforce_claim_topic / enforce_comparison_completeness` 全部 `true`。

### 1.2 Git 状态

- 仓库基线提交不变（`Initial commit` ×2 + `Merge GitHub repository initialization`，2026-08-17），工作区仍有大量未提交改动（全部来自 08-17～08-20 会话，本会话未新增代码改动）。
- 已修改（tracked）36 个文件 + 未跟踪（untracked）43 项；未跟踪中除既有 HANDOFF（08-18/08-19/08-20）与 `app/services` 新组件外，本会话重点确认：`docs/`（含 08-20 晚新建的 `query-translation-decomposition-refactor.md`）与 `tests/evaluation/`（5 个文件）整目录未跟踪。
- **禁止** `git reset --hard` / `git clean` / `docker compose down -v`。

### 1.3 真实入库数据（D 盘实测统计，2026-08-21 重算）

- `D:\internal-rag\data\ingestion\jobs\`：96 份 `job.json`（UTF-8，读取需 `-Encoding UTF8`）；状态分布 `warning` 74 / `completed` 21 / `needs_review` 1 / `failed` 0；发布状态 96/96 `published`（dataset `b5ecab6a8b2811f1b7c4d904b160df50` 为主要发布目标）。

### 1.4 测试基线

- 08-20 记录（宿主）：完整 pytest **401 passed, 3 failed**（3 个失败均为既有解析基线：noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重，`test_regulation_preprocess.py` 相关），无环境性 error。
- 今日最终宿主完整 pytest：**453 passed, 3 failed**；3 个失败与改造前一致（noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重），无新增失败。`ruff check app tests` 通过，`git diff --check` 通过；对新架构 14 个核心文件跑 mypy，仅剩 `app/services/model_client.py:81` 既有 `no-any-return`。

---

## 2. 改动记录（按日期逐节追加）

### 2.1 会话初始化：熟悉工程与登记 08-20 晚新增文档/评测集（2026-08-21）

#### 内容

- 通读 `README.md`、`docs/agent-handoff.md`（完整交接入口）、`HANDOFF-2026-08-19.md`（08-19 全量改动）、`HANDOFF-2026-08-20.md`（08-20 全量改动）、`docs/query-translation-decomposition-refactor.md`（08-20 晚新建重构方案）、`tests/evaluation/README.md`（评测集说明）。
- 核对今日快照：服务探活（§1.1）、Git 状态（§1.2）、入库任务统计（§1.3）、测试基线（§1.4）。
- **登记 08-20 定稿后新增产出**（详细见 §0 时间线补充与 §2.2）：重构实施方案文档、评测集、沙箱诊断产物，均未写入 08-20 HANDOFF。
- 新建本文档 `HANDOFF-2026-08-21.md`，沿用既有逐日逐节追加格式；并更新 `README.md` 文档索引（加入 `docs/query-translation-decomposition-refactor.md`）。

#### 改动文件

- `HANDOFF-2026-08-21.md`（新增）
- `README.md`（文档索引补充一行）

#### 注意

- 本会话暂无代码改动；后续改动/验证将按日期追加到第 2 节。
- 遗留挂账（来自 08-18/08-19/08-20，尚未处理）：早熟中稻航高侧"单侧缺证据/适用对象约束"、沙地播种量公式 OCR 乱码（需回解析层修公式图像）、生成端对已入证据内容利用不充分、RAGFlow 偶发僵死（ReadTimeout，08-20 已用 `docker desktop restart` 恢复；需按 08-11/08-18 记录处置）、"CLAIM 无法通过校验被省略→输出降级"的可接受性需用户评估、"检索对题面标点/空格敏感（题面归一化）"、`models.answer.timeout_seconds: 60` 建议改 300（08-20 §2.7 挂账）、对比题确定性矩阵重构 Stage 2/3（08-20 §2.10）、查询分解/转换重构 Stage 0–6（见 `docs/query-translation-decomposition-refactor.md` §11）。

---

### 2.2 登记：查询分解与查询转换重构实施方案 + 评测集（2026-08-21 登记）

> 产出时间 08-20 17:17–17:36（在 08-20 HANDOFF 定稿之后），本会话只做登记与核对，未改动内容。

#### 方案文档 `docs/query-translation-decomposition-refactor.md`（17:34，21.6 KB）

- **目标**：不替换 RAGFlow，在 Gateway 新增独立、可测试的查询规划层——查询分解（复杂问题拆成有限可独立检索/验证的单元）、查询转换（仅单元格低召回时生成一条受约束补充查询）、主体文档解析（先文档元数据定范围再检索）、结构化证据（`EvidenceRecord`）、确定性矩阵优先（对比题先建矩阵、模型只做可失败总结）、评测驱动。
- **核心契约**：`app/schemas/planning.py`（`QueryPlanV2`：query_type/cells/synthesis_mode/planner_source，comparison 强制完整 subject×aspect 矩阵）、`app/schemas/evidence.py`（`EvidenceRecord`：evidence_id/cell_id/subject/aspect/document_id/section/requirement/citation/page/score/confidence）；迁移期保留旧 `QueryPlan`，经 `QueryPlanAdapter.v2_to_legacy()` 过渡。
- **分阶段实施（Stage 0–6）**：Stage 0 冻结基线 → Stage 1 契约与 Shadow Planner（新 planner 只 shadow 不驱动检索）→ Stage 2 主体文档解析（停用两字子串兜底）→ Stage 3 自适应 Query Translation（仅低覆盖 cell 一次、每请求最多 4 次）→ Stage 4 EvidenceRecord（表格转键值、标题行不能独立覆盖）→ Stage 5 确定性矩阵前置（summary 最多一次 30s、失败仍 200、删除对比题初答/repair/sanitize 路由）→ Stage 6 切换与清理。
- **配置**：`planning.*`（shadow_mode 等）、`translation.*`、`retrieval.*`（fusion_rrf_k、translated_query_weight 等）、`generation.comparison_matrix_first`。
- **硬约束**：正则只处理"文本长什么样"；不新增面向单题的关键词/语义正则；planner 失败回退 legacy 不让问答失败；不记录 API Key。
- **验收**：规划 Schema 合法率 100%、对比题主体召回 ≥98%、维度召回 ≥95%、跨主体污染 0%、引用存在与归属 100%、模型失败仍返回确定性矩阵 100%、对比题模型调用 ≤1、P95 ≤90s。

#### 评测集 `tests/evaluation/`（17:30–17:36）

- `planning_cases.jsonl`（11.7 KB）：查询类型/主体/维度/cell 数量/转换边界（无服务依赖）。
- `retrieval_cases.jsonl`（7.5 KB）：主体隔离/证据选择/转换触发/诚实缺失（无服务依赖）。
- `live_rag_cases.jsonl`（5.1 KB）：真实 RAGFlow 与回答链路验收（依赖服务，`must_pass=true` 为发布门禁）。
- `run_live_eval.py`：调当前网关执行真实集并输出 JSON 报告；默认只跑 `must_pass=true`。
- `README.md`：使用顺序（合同测试 → 各 Stage 参数化 → 真实门禁）、判断规则、给执行模型的要求。
- 合同校验：`tests/unit/test_evaluation_dataset_contract.py` 沙箱实测 **3 passed**。

#### 沙箱诊断产物（17:17–17:20）

- `tmp/run_sand_diag.py` + `tmp/sand-debug-20260820.json`（73 KB）+ `tmp/sand-chat-20260820.json/.txt`：08-20 收尾时对 8080 网关的沙箱内诊断复测产物（对应 08-20 §2.12 端到端验证的补充快照）。

#### 改动文件

- 无（登记；产出文件本身为 08-20 晚已创建，见 §0）。

#### 注意

- 此处是 08-20 晚登记时的历史状态；2026-08-21 已按 DeepSeek 评审补齐缺口并完成实施，当前生效状态和实测结果以 §2.3 为准。
- `tests/evaluation/live_rag_cases.jsonl` 依赖真实服务与知识库名，跑 `run_live_eval.py` 前需确认 Gateway/RAGFlow 在线且知识库名存在。

---

### 2.3 查询分解/转换架构完整实施（2026-08-21）

#### 背景与评审结论

DeepSeek 对 `docs/query-translation-decomposition-refactor.md` 的 B+ 评审指出三项必补缺口：HTML/表格证据无覆盖、live 基线无 diagnostics 不可复现、对 8GB 单模型驻留环境的 planner 能力假设过于乐观。本次先修订实施规范，再完成 Stage 0–6 所有代码路径，但按本机实测将模型 planner/translator/summary 默认关闭，不将无效模型强行放到主路径。

#### 架构与契约

- 新增 `QueryPlanV2` / `PlannedCell` / `RetrievalQuery` / `SubjectResolution` 与 `EvidenceRecord`；comparison + matrix 强制完整 `subject × aspect`，最多 8 格。
- `QueryPlanningService` 统一 deterministic fast path、结构化模型规划与 legacy adapter；旧 `QueryPlan` 在迁移期保留，不破坏已有路径。
- `SubjectDocumentResolver` 先用请求范围/文档元数据，再用完整标题 bigram 相似度解析主体；不使用两字子串决定文档归属。
- 对比执行路径按主体发现文档一次，再对每个 cell 做 document-scoped retrieval；低覆盖 cell 最多一次受限转换，原始/转换候选用 weighted RRF 融合。`translated_query_weight=0.85` 只影响 RRF 贡献，不与相似度再相乘。
- 原 `_DIMENSION_EXPANSIONS` / `_SUBJECT_EXPANSIONS` 不再被 `cell_evidence` 跨模块引用，迁移到独立 `requirement_taxonomy.py`，仅用于低覆盖的受控补检索。
- 复用现有 `ComparisonMatrixBuilder`，只将数据源演进为 `EvidenceRecord`；矩阵先于模型生成，summary 最多一次且失败仍返回 HTTP 200 确定性矩阵。
- 新增安全 `ChatDiagnostics`：包含 plan 来源、cell 状态、主体文档 ID、EvidenceRecord 数、检索/模型调用数和阶段耗时；不包含 chunk 正文、模型原始输出或密钥。简单题的 cell 状态由 EvidenceAssessment 映射，不再把“有低相关候选”误标为 covered。

#### 表格/证据层修复

- 新增 `table_evidence.py`：识别 HTML `<table>/<caption>/<tr>/<th>/<td>` 和 Markdown 表格，按“表头: 值”转换为可 grounding 键值记录；空表、只有 caption 或原始标签不算实质证据。
- `cell_evidence.py` 先提取表格记录再移除原始表格 markup，过滤冒号结尾引导行和纯标题行，避免“应满足以下要求:”或表格 caption 单独把 cell 标为 covered。

#### 本机模型实测与默认配置

- 使用 verifier `qwen3:4b-instruct-2507-q4_K_M` 对主问题做 shadow planner：512 token 时约 14.9s 且输出截断；放宽到 1536 token 后约 9.9s，JSON 可解析但只生成“一个合并主体 × 三维度”3 格，连续结构检查均不满足 2×3 契约。
- 当前默认：`planning.enabled=true/model_enabled=false/shadow_mode=true`，`translation.enabled=true/model_enabled=false`，`comparison_matrix_first=true`，`comparison_summary_enabled=false`。即架构契约和新执行器已启用，模型路径完整保留但不与 8GB/`OLLAMA_MAX_LOADED_MODELS=1` 环境抢显存。
- taxonomy 回退转换不调模型；合成 A/B 为保留 taxonomy 8/8，移除 taxonomy 5/8，因此本次不删除。

#### 性能和无证据快速路径

- 简单检索新增 `retrieval.min_rerank_score=0.1`；明显低相关候选不再让 9B 模型进入 initial/repair/sanitize 循环。“火星量子雷达”用例由 65.96s/3 次模型调用改为 20.66s/0 次调用，直接返回 `UNANSWERABLE`。
- 章节族扩展限制为每 cell 最多 2 个高排名叶章节（`max_section_expansion_queries_per_cell=2`）。主问题 RAGFlow 调用从 41 降为 15，其中主体发现 2、cell 原始检索 6、转换检索 1、章节扩展 6；覆盖和引用数未退化。

#### 评测与验证

- 规划集 24/24 精确匹配；规划/分析/契约相关测试 **50 passed**。
- HTML/Markdown 表格、冒号引导行、标题行和 EvidenceRecord/矩阵组件相关测试通过。
- live runner 固定 `trust_env=False`，防止企业 HTTP 代理将 `127.0.0.1` 请求送到外网；`--output` 现在保存安全 diagnostics 摘要，不保存完整 chunk/模型原文。
- 最终 live 报告：`D:\internal-rag\temp\live-rag-eval-20260821-final.json`，**2/2 must-pass 通过**：
  - 桥梁/地灾 6 格题：45.052s，`PARTIALLY_ANSWERABLE`，6 cells（5 covered + 1 low_confidence），9 citations，0 model calls，两个主体各只命中自己规程。
  - 无证据题：20.656s，`UNANSWERABLE`，0 citations，0 model calls，diagnostic cell 为 `not_specified`。
- 最终完整 pytest：**453 passed, 3 failed**，无新增失败。`ruff check app tests` 和 `git diff --check` 均通过。新架构 14 个核心文件的 mypy 仅剩既有 `app/services/model_client.py:81 [no-any-return]`。

#### 真实服务恢复记录

- 首次 live 返回 503 不是新架构异常：RAGFlow 容器仍显示 Up，但单 API worker 健康检查/检索全部 ReadTimeout，且 Docker 对该容器 `restart`/`exec` 均失败（无 exit event）。
- 确认当前解析任务已完成后，重启 Docker Desktop 引擎，再执行 `deploy/ragflow/start.ps1`；没有删除容器卷或数据。恢复后 RAGFlow 的 db/doc_engine/redis/storage 均 `ok`，Gateway `/ready` 全依赖 ready。

#### 主要新增/修改文件

- 契约：`app/schemas/planning.py`、`app/schemas/evidence.py`、`app/schemas/chat.py`、`app/schemas/retrieval.py`。
- 规划/转换：`query_plan_adapter.py`、`structured_planner_model.py`、`query_planning_service.py`、`subject_document_resolver.py`、`query_translator.py`、`retrieval_fusion.py`、`requirement_taxonomy.py`。
- 证据/矩阵：`table_evidence.py`、`evidence_record_extractor.py`、`comparison_orchestrator.py`、`comparison_matrix_builder.py`、`cell_evidence.py`。
- 主路径：`app/services/retrieval_service.py`、`app/pipelines/rag_pipeline.py`、`app/core/config.py`、`config/default.yaml`、`app/services/registry.py`、`app/services/answer_generator.py`。
- prompt：`prompts/query_planning.txt`、`prompts/query_translation.txt`、`prompts/matrix_summary.txt`。
- 评测：`tests/evaluation/`、`tests/unit/test_*planning*`、`test_query_translator.py`、`test_retrieval_fusion.py`、`test_subject_document_resolver.py`、`test_table_evidence.py`、`test_evidence_record_extractor.py`、`test_comparison_orchestrator.py`、`test_synthetic_retrieval_evaluation.py`。

#### 注意/后续边界

- 主问题中“长大桥梁 × 像控布设”是 `low_confidence`，系统按主体差异诚实保留，不用地灾规程或第三文档补齐。
- `planning.model_enabled` 只能在新 planner 模型通过 Schema/延迟基线或改用独立远程 API 后开启；不要盲目切回当前 4B。
- taxonomy 删除前必须继续保留有/无 taxonomy A/B，不以“去正则”为由直接删除有实测收益的迁移资产。
- 完整生产日志仍不记录 chunk 正文/模型原文；live 报告只存安全 diagnostics。

---

### 2.4 按环节类型收紧六格证据（2026-08-21 后续）

#### 问题

08-21 首次真实回答虽然已实现六格和主体隔离，但证据行仍可跨环节混入：设备格会吸收前资料/成果记录，验收格会吸收云台、净空和术语定义。“存在相关词”不等于“存在当前环节的标准条款”。

#### 修复

- `requirement_taxonomy.py` 新增按证据行分类的 `requirement_type_for_line()` 和环节允许类型：设备适用 equipment，像控适用 control_point_layout，成果验收仅允许 quality_check/deliverable/acceptance/reporting。
- `cell_evidence.substantive_lines()` 增加按 aspect 的严格过滤：术语定义、TOC 行、飞行净空、安全、前资料、成果精度检查表不再误作设备选型证据。
- `EvidenceRecordExtractor` 改为对每一行分配 requirement_type，不再把整个 chunk 按 cell aspect 统一标注。
- 对比每个 cell 的 RAGFlow 请求增加有界的环节提示（例如“环节要求：像控点 控制点 布设”），不改写用户问题、不创造条款或参数。
- 补充回归：设备格拒绝“成果精度检查记录表”，验收格拒绝云台设备参数，表格/冒号引导行旧测试全部通过。

#### 最终真实回答状态

| 单元格 | 当前状态 | 说明 |
|---|---|---|
| 桥梁设备选型 | covered | 保留云台三轴稳定、相机像素/视频分辨率、触发方式、存储空间条款 |
| 地灾设备选型 | low_confidence | 当前召回主要是前资料/成果表，不冒充成设备参数 |
| 桥梁像控布设 | low_confidence | 当前规程未召回传统摄测像控条款 |
| 地灾像控布设 | covered | 保留平高点、区域网、CH/T 3004、航带法等 7.1.1 要求 |
| 桥梁成果验收 | low_confidence | 当前召回未形成可验收条款 |
| 地灾成果验收 | covered | 仅作为 A.3/A.4 精度检查记录表/成果清单样式，不宣称已获得数值限差 |

真实 live 必测仍为 **2/2 通过**；最终主问题耗时约 **44.22s**、模型调用 **0**，全量 pytest 为 **454 passed, 3 failed**，3 项仍是既有规程预处理基线失败。

### 2.5 完整表格引用与 C 编号展示（2026-08-21 后续）

用户反馈 C6 只显示了“表A.3 …”摘要和原始 `<table>/<td>` 标签。原因不是 RAGFlow 表格解析错误，而是旧 UI 将 `Citation.quote` 截断为 420 字符并全部转义显示；C6 只是当前请求中按最终候选顺序分配的第 6 个引用编号，不代表表格编号。

本次修复：

- `Citation` 增加可选 `table_html`/`table_htmls`/`table_title`，`CitationService` 保留一个 chunk 中的全部 HTML 表格；识别 RAGFlow 某些路径输出的 `\\<table>` 转义标签，但不解码普通正文 HTML。
- 引用侧栏优先以安全白名单表格渲染，不再把 `<tr>/<td>` 当普通文本展示；无表格的引用仍使用原来的摘要预览。
- 回答区新增“回答中引用的完整表格”，仅对回答正文实际出现 `[C#]` 的表格引用展示完整行列与空白表单字段；复制并附引用仍保留原始来源文本。
- 表格完整展示与检索用的“表头: 值”记录分离：后者用于 grounding，前者用于用户核验，避免为检索方便而丢失表格布局。

新增 `test_html_table_blocks_preserve_blank_form_cells_and_escaped_tags` 回归；表A.3/A.4 这类空白记录表仍只能说明“记录表/成果清单样式”，不能被解释为已经填写的实测值或数值限差。

### 2.6 成果验收主证据调整（2026-08-21 后续）

根据用户对《地质灾害调查无人机低空摄影测量技术规程》的核对，成果验收应优先检索正文表 2～表 5 的成果质量/精度要求；A.3/A.4 是附录记录表样式，不能替代限差和合格判定。

- `成果验收`检索词改为通用的成果质量语义（点云密度、点云高程、地面分辨率、限差、中误差、精度检查等），不把表号写成全局规则；当前规程的表 2～表 5 由表题/表头和质量语义自然召回。
- 成果质量表行按 `quality_check` 分类；空白 A.3/A.4 表单不再生成实质性 EvidenceRecord，只有命中表单模板时应降级为 `low_confidence`，并说明“仅找到记录格式”。
- 后续 live 回归应确认：当前规程表 2～表 5 被选入验收格；换成表 7/表 8 或无编号表的规程时仍能按语义工作；不再把 A.3/A.4 单独标为已获得验收限差。

### 2.7 成果验收语义、证据预算与表格精确绑定落地（2026-08-21）

用户复测发现成果验收格仍输出“成果清单（见附录A.5）/测量技术成果报告”，并因该引用 Chunk 同时包含 A.1/A.2 表而在页面展开无关表格。真实诊断确认：表 3 的限差块在 RAGFlow 中存在且简洁查询可排第 1，但旧路径把长关键词包直接拼到 dense query，随后覆盖选择器又无条件保留首候选，导致目录/交付段落挤掉质量表。

本轮修复：

- `requirement_taxonomy.py` 将“成果验收”收紧为 `quality_check/acceptance`，“成果清单、技术报告、巡检报告”独立归入“成果交付”；设备检查/固定、前期资料控制点清单、算法训练 MAP、规范性引用目录均不能再覆盖对应矩阵格。
- 原始 cell query 不再拼接同义词袋；“成果验收”额外执行一次独立的成果质量语义查询，再用 RRF 与原查询融合。这是受限 multi-query，不包含表号，也不会改写主体或生成参数。
- `CoverageSelector` 先选择真正通过该维度语义校验的候选；设备/像控目标最多 2 组、验收目标最多 4 组；验收优先完整表格，并按表题去除重复文档版本，避免同一表的两份副本耗尽预算。
- `ComparisonMatrixBuilder` 按不同 citation 轮转选摘要记录，防止第一张长表的前四行遮住后续质量表；确定性矩阵的引用校验改为使用全部后端已选 Chunk，不再受生成模型证据句全局上限误伤。
- 表格记录在证据行排序中优先于“参照表 X 执行”的说明句；正文中的数值 HTML entity 在进入答案前解码。
- `markdown.js/chat.js` 只在答案中同时出现对应 `[C#]` 和表号/表题时展开该张表；一个引用 Chunk 含多张表时不会再全部展示。引用侧栏对未被答案点名的表回退到正文摘要。
- 真实 RAGFlow 曾出现单 worker ReadTimeout；单容器无法收到 exit event，执行 `docker desktop restart` 后用 `deploy/ragflow/start.ps1` 恢复，未清理任何卷或索引。

回归结果：

- 真实主问题已不再引用成果清单、技术报告、A.1/A.2/A.3/A.4 空白表单；稳定引用表 3“检查点平面位置及高程精度要求”和表 4“不同调查比例尺的点云密度”，回答中含具体限差/密度，页面可完整展开实际被点名的表。
- live 必测：`2/2 passed`，主问题约 `44.212s`，六格完整，`0` 次模型调用；无证据题约 `19.730s`，诚实返回 `UNANSWERABLE`。报告：`D:\internal-rag\temp\live-rag-eval-20260821-final.json`。
- 合成 retrieval 集：启用语义分类 `8/8`，禁用迁移词表 `5/8`；新增“表 2～表 5 质量表可选、成果交付/A.3 表单禁止”门禁。
- 页面 Node 测试 `6/6`；完整 pytest：`473 passed, 3 failed`。3 项仍是既有规程预处理基线失败（噪声 OCR 目录、跨页续表 caption、整页 figure/重复表题），本轮无新增失败；ruff 全通过。

仍需后续处理：RAGFlow 当前索引对表 2/表 5 的直接召回不如表 3/表 4 稳定，即使用表号直查也可能被范围/目录块挤出。本轮没有把“表2～表5”写成硬编码规则；应在重新解析/发布后检查这些表的 Chunk 元数据、表题与父章节关系。另有历史 PDF 硬换行导致个别回答行尾截断（如“条件可适当…”）的问题，宜在证据层做段落重组，不应继续增加词级正则。

---

### 2.8 多文档综合题检索统一走主体文档解析（沙地播种双重要求题，2026-08-21 后续）

#### 问题

用户指出"按题优化低效"，且实测沙地题——"沙地无人机播种治沙作业，结合播种技术规范与无人机通用服务管理要求，完整项目实施需要兼顾技术参数与运营管理哪些双重要求？"——修复前 12 个选中 chunk 混入《水稻丸粒化水直播》《低空旅游服务规范》CCAR-92 等无关文档。根因：`query_planner._cross_source_plan` 拆出的两个子查询 **不带 subject**，而 `retrieval_service._execute_plan` 的多跳路径没有主体发现分支——cell 无 subject 时整条"主体发现 → 文档解析 → document_ids 限定检索"防线被跳过，两个子查询全库混合检索，只靠 rerank/多样性兜底，第三文档自然混入。这与桥梁/地灾题被修复的缺口是**同一个能力缺口（主体→文档解析）在 multi_hop 路径上的第二次暴露**，不是新题特有的问题。

#### 修复（通用能力，非单题补丁）

1. **`app/services/query_planner.py`**：`_cross_source_plan` 的两个子查询补 `subject`（q1=题面主体、q2=第二来源），`subjects` 同步为两个声明主体——"结合 A 与 B…兼顾 X 与 Y"题从此走与 comparison 相同的 cell 驱动执行器。
2. **`app/services/retrieval_service.py`**：`_execute_plan` 入口扩展——`comparison` 或"全部 cell 均带 subject 的 `multi_hop`"都路由到 `_execute_comparison_plan`（主体发现 → `SubjectDocumentResolver` 解析 → 每 cell 按解析出的 document_ids 限定检索）。部分 cell 无 subject 的 multi_hop 面式拆分题保持原路径，避免空 discovery。
3. **`app/services/subject_document_resolver.py`**：新增**检索分数通道**。发现检索（以主体词为 query）的候选按文档聚合 max hybrid：top ≥0.35 且领先第二名 ≥0.015 时，若该文档标题与主体词相似度 ≥0.30（门控防字面撞车误选），直接选定并返回 `source="retrieval"`；否则回退既有 title 通道。阈值来自真实实验：
   - "无人机通用服务管理要求"→《无人机应用服务通用规范》检索分 0.4273 领先（正确）；字面标题更接近《机巢通用管理要求》（0.4069 第二），旧 title 通道会错选——检索通道修复此 case；
   - "沙地无人机播种治沙作业"→《无人机播种治沙技术规程》0.4584 领先第二 0.1077；
   - 无证据题（火星隧道量子巡检）top 仅 0.2619 < 0.35，不触发，诚实返回 none。
4. **`app/schemas/planning.py`**：`SubjectResolution.source` Literal 增加 `"retrieval"`。
5. **顺手修**：`coverage_selector.py` 多跳文档多样性分支的 `current` 变量类型冲突（mypy 既有报错，改名为 `previous`）。

#### 验证

- 单测：`test_subject_document_resolver.py` +3（检索通道领先/无 margin 回退 title/低分不触发）；`test_query_planner.py` 沙地题补 subject 断言；`test_retrieval_service.py` 新增 multi_hop 双声明主体按文档限定检索、高分无关文档被过滤的端到端用例；既有 comparison 单测的 `source=="title"` 断言放宽为 `<= {"retrieval","title"}`（检索分领先时正确走 retrieval 通道，选中文档不变）。
- 评测集：`planning_cases.jsonl` 的 `plan-multihop-sand-project` 期望 subjects 更新为两个声明主体；规划/合同测试 27 passed。
- 端到端（重启网关 pid 36596 后实测）：
  - **debug**：12 选中全部来自两份目标文档（播种治沙规程 7 + 应用服务通用规范 5）；`subject_discovery_calls=2`；水稻水直播/低空旅游/CCAR-92 零出现；覆盖矩阵 2 格全 covered。
  - **chat**：`ANSWERABLE / degraded=False / warnings=[]`；技术参数侧含航高 10~50 m/速度 3~8 m/s、沙障 LY/T 2986、高精度导航+智能控制+专用播撒系统；运营管理侧含 MH/T 1069 合规、运营管理制度、按约定验收；8 条引用全部归属两份主文档，无第三文档。
- 回归：完整 pytest **477 passed, 3 failed**（3 个失败仍为既有解析基线：noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重），无新增失败；`ruff check app tests` 通过；mypy 仅剩既有 `model_client.py:81`；live 必测 **2/2 通过**（桥梁/地灾六格题、无证据诚实题均不受影响）。

#### 改动文件

- `app/services/query_planner.py`、`app/services/retrieval_service.py`、`app/services/subject_document_resolver.py`、`app/schemas/planning.py`、`app/services/coverage_selector.py`
- `tests/unit/test_subject_document_resolver.py`、`tests/unit/test_query_planner.py`、`tests/unit/test_retrieval_service.py`
- `tests/evaluation/planning_cases.jsonl`（沙地题期望）

#### 注意

- 此修复是**能力级修复**：所有"结合 A 与 B…兼顾 X 与 Y 双重要求"式多文档综合题统一走主体文档解析，无需再为同类题逐题补丁；新题应进评测集（required/forbidden documents 断言）而非加代码。
- 检索分数通道阈值（0.35/0.015/0.30）来自当日真实数据校准；若后续发现 title 相似度低于 0.30 但检索分显著领先的正确主体被拒，优先用真实数据重校准而非单题放宽。
- multi_hop 无 subject 的面式拆分题（如 5G 基站×机巢×河湖协同题）仍走原全库路径，其文档多样性依赖 `coverage_selector` 的"每文档至少一个代表"机制；若此类题也出现第三文档污染，下一轮可考虑把"声明来源→文档解析"推广到无 subject 的 multi_hop。

---

### 2.9 跨文档句式覆盖扩展与路由边界收紧（2026-08-21 后续）

#### 问题

用户追问 §2.8 的修复是否只覆盖"结合 A 与 B…兼顾 X 与 Y 双重要求"这一种字面格式。实测 8 个同义变体后发现两个缺口与一个过度匹配风险：

1. **过度匹配（回归风险，已收紧）**：所有 cell 共享同一合并主体的 multi_hop 题（"同时使用 5G 基站与自动机巢…三类要求如何协同""既要…也要…"）此前满足"全 cell 带 subject"条件，会被路由到 cell 驱动执行器——discovery 把单一主体解析到单一文档后，其余来源文档（机巢/河湖等）被 document_ids 过滤掉。§2.8 的路由条件加严为：**multi_hop 必须存在 ≥2 个不同 subject 才走 cell 驱动执行器**。
2. **句式盲区（已补）**：
   - 句首"结合"无前导主语（"结合无人机播种治沙技术规程与无人机应用服务通用规范，…"）此前 subject 为空直接放弃 → 落成 single plan；现在用第一个来源 A 充当 q1 主体。
   - 跨文档引导动词此前只认"结合"；"综合/按照/依据/遵照 A 与 B"是同一语义，现统一覆盖（`query_analyzer._CROSS_DOC_PATTERN` 与 `query_planner._CROSS_DOC_PATTERN` 同步）。
   - 来源短语尾部修饰尾巴（"的要求/的相关要求/执行"等）污染主体词（"无人机通用服务规范的要求"→"无人机通用服务规范"），新增 `_strip_source_tail`。

#### 验证

- 8 个同义句式（原题/句首结合/两方面/既要也要/5G协同/综合…与…/按照…和…/依据…与…）逐一核对 plan：跨文档类全部拆出两个不同 subject 并走主体文档解析；单主体类（既要也要、5G 协同）保持原路径不受影响。
- 端到端（网关 pid 32364）：原题与"按照…和…"变体 debug 各 12 选中、**全部只含两份目标文档**（播种治沙规程 + 应用服务通用规范），零第三文档。
- 评测集：`planning_cases.jsonl` 新增 2 个句式变体用例（26 例），规划/合同测试 29 passed。
- 回归：完整 pytest **478 passed, 3 failed**（3 个仍是既有解析基线）；ruff 全过；live 必测 2/2。

#### 改动文件

- `app/services/query_planner.py`（跨文档动词扩展、句首主语回退、来源尾巴剥离）、`app/services/query_analyzer.py`（跨文档动词扩展）、`app/services/retrieval_service.py`（multi_hop 路由条件加严：≥2 个不同 subject）
- `tests/unit/test_query_planner.py`（句首结合变体用例）
- `tests/evaluation/planning_cases.jsonl`（+2 句式变体）

#### 注意

- 句式识别仍是确定性的（词表+正则）；"同一语义"的覆盖边界是有限的——完全同义改写（如"需要同时满足 A 规范和 B 规范的要求"不带任何引导动词）仍可能漏进 fact/unknown。根治路径是结构化规划模型（4B 实测尚不可用），当前以评测集句式变体为门禁逐步扩覆盖，不追求穷举。
- 路由条件的语义现在是："多个不同声明主体 → 主体文档解析"；单一主体多面题 → 原路径 + 文档多样性兜底。新增句式变体时必须同时检查它不会误把单主体题送进 cell 驱动执行器。

---

### 2.10 multi_hop 确定性矩阵优先（沙地题覆盖完整性，2026-08-21 后续）

#### 问题

§2.8 修好沙地题检索后，chat 输出仍是 9B 自由生成（`model_calls=3`）：技术参数侧只用了 4/8 条证据（漏速度 3~8 m/s、装备要求、种子处理、播种量公式、播期、播幅、作业方式、播前准备、飞行安全、质量检测、播后管理），运营管理侧只用了 5/19 条（漏现场勘察 8.2.2、服务方案 8.4.1、风险告知 8.4.2、合同 8.5、信息管理 9）。根因：`rag_pipeline` 的 `is_matrix_first` 只对 `query_type == "comparison"` 生效，multi_hop 没有确定性兜底——对比题有矩阵、multi_hop 没有，完整性无人把关。

#### 修复（复用现有矩阵组件，不新增第二套实现）

1. **`app/pipelines/rag_pipeline.py`**：`is_matrix_first`、完整性兜底触发、`_comparison_matrix_fallback` 三处 `query_type == "comparison"` 放宽为 `in {"comparison", "multi_hop"}`。
2. **`app/services/comparison_matrix_builder.py`**：新增**对角线布局**支持——comparison 是 subject×aspect 全交叉（2×3=6 格），multi_hop 的"结合 A 与 B…兼顾 X 与 Y"是 A→X、B→Y 一一对应（2 格）。`_is_diagonal_layout`（multi_hop 且每个 subject 只有一个 aspect 时判定对角线）、`_diagonal_pairs`（按 subquery 顺序取 (subject, aspect) 对）、`_render_diagonal_table`（"对比项目|要求"两列表格）。
3. **`app/services/comparison_completeness_validator.py`**：query_type 条件同步放宽（校验"subject：aspect"标签是否被 claim 覆盖，对角线布局标签同样适用）。
4. 矩阵构建、校验链、完整性校验全部复用既有组件，无新增配置。

#### 验证

- 单测：`test_comparison_matrix_builder.py` 新增对角线布局用例（2 格各自有内容/引用正确、无交叉格误报缺失、answerability=ANSWERABLE）；matrix/completeness 相关 8 passed。
- 端到端（网关 pid 29472）：沙地题 chat `model_calls=0`（矩阵优先生效）、`ANSWERABLE / degraded=False / warnings=[]`：
  - 技术参数行：航高 10~50 m [C1]、高精度导航+智能控制+专用播撒系统 [C3]、零星沙地单独设计 [C5]、沙障 LY/T 2986 [C6]；
  - 运营管理行：服务方案 8.4.1（作业指导书/机组编制/应急预案）[C2]、安全管理制度与应急预案 [C8]、按约定验收 8.7.1 [C10]；
  - 引用全部归属两份主文档，无第三文档。
- 回归：完整 pytest **481 passed, 3 failed**（3 个仍是既有解析基线）；ruff 全过；mypy 仅剩既有 `model_client.py:81`；live 必测 2/2。

#### 改动文件

- `app/pipelines/rag_pipeline.py`、`app/services/comparison_matrix_builder.py`、`app/services/comparison_completeness_validator.py`
- `tests/unit/test_comparison_matrix_builder.py`

#### 注意

- 矩阵优先后输出是**提取式**（干燥但完整、逐条附引用），不再是 9B 的叙述式；用户可点击 [Cx] 核对原文。
- 输出仍暴露两个既有证据行质量问题（非本次机制引入，已在 §2.7 挂账）：
  a) **范围句**混入——"本文件规定了无人机应用服务的基本要求、机构要求、人员要求…"（C4）是文档范围句，`substantive_lines` 的标题/表格过滤不覆盖"本文件规定了…"开头；建议 `requirement_taxonomy` 或行过滤补"本文件/本标准规定了…（范围句）"排除；
  b) **PDF 硬换行截断**——"（无人驾驶飞行器）作为载体"被切为"器）作为载体"（原文"飞行\n器）"跨行）；§2.7 已挂账"宜在证据层做段落重组"，本次未处理。

---

### 2.11 证据行质量修复：范围句过滤 + PDF 断行合并 + TOC 误伤条款修复（2026-08-21 后续）

#### 问题

用户指出 §2.10 矩阵输出的运营管理行明显缺内容——"本文件规定了无人机应用服务的基本要求、机构要求、人员要求…"（C4，文档**范围句**）挤占了一个名额，导致现场勘察 8.2.2、风险告知 8.4.2 等实质条款未进入输出。排查还发现两个同源证据行问题：PDF 硬换行把"（无人驾驶飞行\n器）作为载体"切断成"器）作为载体"；TOC 过滤把"8.2.2 应依据作业要求…出具现场勘察报告。"误判为目录行。

#### 修复（`app/services/cell_evidence.py`）

1. **范围句过滤**：新增 `SCOPE_SENTENCE_RE`——"本文件/本标准/本规范/本规程…规定了/适用于/确立了/包含以下"开头是文档引言/范围章节内容（列目录性质），不是任何环节的实质条款，在 `substantive_lines` 中剔除。
2. **PDF 断行合并**：新增 `merge_wrapped_lines`/`_merge_pass`——当前行不以句末标点（。；！？：或右括号）结尾时，与后续正文行合并；编号/标题/元数据行（`_HEADING_OR_NUMBER_START_RE`/`META_LINE_RE`）是独立段落不并入。仅合并到出现句末标点为止，不跨段粘连。
3. **TOC 误伤条款修复**：`TOC_LINE_RE` 原匹配"数字+汉字+句号"把条款句误当目录；新增 `_CLAUSE_VERB_MARKERS`（应/宜/须/不得/按照/执行/要求/采用/包括/为/应依据/应制定），含条款性动词的编号行不是目录。
4. **行长上限放宽**：`MAX_LINE_LENGTH` 过滤针对目录/聚合噪声行（通常无句末标点）；合并后的完整句（以句号/问号结尾）即使较长也是合法条款，不再被误杀。

#### 验证

- 单测（`test_table_evidence.py` +3）：范围句过滤、断行合并（"飞行器）作为载体"接回且不产生"器）作为载体"残留行）、编号条款不被 TOC 误伤（8.2.2 现场勘察保留）。
- 端到端（网关 pid 13332）沙地题 chat：
  - 运营管理行：范围句移除，换入 **8.2.2 现场勘察报告** [C2]、**6.2 人员合规理解** [C12]；保留 8.7.1 按约验收 [C10]、安全管理制度 [C8]；
  - 技术参数行："（无人驾驶飞行器）作为载体"完整（不再"器）作为载体"）；航高 10~50 m **+ 速度 3~8 m/s** 完整 [C1]；
  - `ANSWERABLE / degraded=False / warnings=[]`，引用全部归属两份主文档。
- 回归：完整 pytest **484 passed, 3 failed**（3 个仍是既有解析基线）；ruff 全过；mypy 干净；live 必测 2/2。

#### 改动文件

- `app/services/cell_evidence.py`
- `tests/unit/test_table_evidence.py`

#### 注意

- 矩阵每格输出上限仍为 `_MAX_CELL_LINES=4`；8.4.1 服务方案/8.4.2 风险告知在证据记录中但未进入本行前 4 条。若用户需要单格更多条款，可调 `_MAX_CELL_LINES` 4→5/6（代价是格子更长）。
- 断行合并是保守策略（仅行尾无句末标点时合并），不会改变正常分行语义；新增规则已用真实 chunk 验证。

---

## 3. 工作原则（沿用，不重复展开）

1. 先读 `docs/agent-handoff.md`，再读目标模块和对应测试。
2. 任何解析修复都必须用真实失败文档回归；不写破坏全局的正则。
3. 用户只有一个上传入口，不重新要求用户选择解析器。
4. 物理页码、印刷页码、条款号分开存储；表格文本、二维结构、图片资产三者都保留。
5. `needs_review` 不能绕过门禁发布；发布前优先 dry-run；删除本地任务不删除 RAGFlow 内容。
6. 修改后至少运行目标测试、完整 pytest 和 ruff；页面改动必须浏览器实测。
7. 不输出或提交密钥；不清除 Docker 数据卷；不把 D 盘数据复制回 C 盘。

---

## 4. 相关文档

- `README.md`
- `docs/agent-handoff.md`（最完整交接入口）
- `docs/query-translation-decomposition-refactor.md`（08-20 晚新建重构实施方案；配套评测集 `tests/evaluation/`）
- `docs/pdf-parsing.md`、`docs/ingestion.md`、`docs/milestone-2.md`、`docs/milestone-3.md`
- `HANDOFF-2026-08-20.md`（本会话起点）
