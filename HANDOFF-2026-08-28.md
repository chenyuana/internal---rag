# Internal RAG 改动记录（2026-08-28）

> 用途：记录 2026-08-28 针对"扫描/低质量英文文档（如 FAA Pdf-It/Pdf.Capture 老扫描件）解析效果差"的根因定位与计划改动，供后续开发、验收与回归使用。

## 1. 今日目标

- 解决资料接入台对**扫描型英文 FAA 案卷**清洗效果差的问题：多栏被横向合并、四列/表格结构丢失、表格数字被误识别为 LaTeX 公式乱码（`x^1 21 x^21…`）。
- 让系统在遇到这类"bad text-layer 扫描件"时**自动改走 OCR 结构化解析**（复用现有远程 MinerU OCR 通道），而非直接用 pypdf 抽出的退化文字层。
- 保持对**其它文档类型**的既有路由不变（知识库后续会处理多种多样、质量参差的文档，路由需可扩展）。

## 2. 背景与现象

- 这类 FAA 扫描件为 **Pdf-It version 5.091 / Pdf.Capture version 5.125** 生成（共 43 份），**内嵌一层坐标错乱、词间无空格/连字的退化文字层**。
- 现象：`清洗后文本`呈横向连续文本流（多栏/表格塌陷）；`表格与图片资产`能看到该页（判断确实跑了版面检测 + OCR），但结构化输出把表格数字搅成 `x^`/`$…$` LaTeX 垃圾。
- 对照：命令行 MinerU `--method ocr` 能正确还原四列表（`Values in pounds of force | Pitch | Roll | Yaw`）、无 LaTeX 垃圾；`--method txt`（读文字层）则复现"词连串、结构塌陷"。→ 说明**能力在 OCR，问题在系统没让这些页走 OCR**。

## 3. 根因定位（当前解析路由）

入口：`app/ingestion/parsers/registry.py` 按扩展名分发 → `.pdf` 用 `HybridPdfParser`。

`HybridPdfParser.parse()`（`app/ingestion/parsers/hybrid_pdf.py`）流程：
1. `NativePdfParser` → `parse_pdf()`（`app/ingestion/pipeline.py`）用 **pypdf** 抽文字层并给每页打 `route`：
   - `ocr_required`(0字符) / `low_text_review`(<30字符)
   - `text_layer_review`（判定为文字层损坏：字形垃圾 `glyph_name_garbage`、假CJK `fake_cjk_garbage`、ToUnicode冲突、`low_quality_ocr`）
   - `native_text`（**否则直接用 pypdf 文字层**）
   - `layout_review`（`native_text` 且检测到表标题）
   - `watermark_excluded` / `blank_excluded`
2. `VectorPdfParser.preflight`（`vector_pdf.py`，pdfplumber）对 `vector_preflight_targets` 探测**矢量表格/图/公式**，产出区块与资产。
3. 对 `ocr_targets`（需 OCR 的页）调**远程 `MinerUClient.parse_pages(parse_method="ocr")`**（`remote.py`，httpx 访问 `ingestion.mineru.base_url`）。
4. 图页可走 `FigureVision` VLM 描述；最后 `_rebuild` 做跨页表格合并、清洗、分块、质量门禁。

**关键缺陷**：`parse_pdf` 的 `text_layer_corrupted` 依赖 `has_low_quality_ocr_text`（`pipeline.py` 第 1580 行），而该判断**只针对中文/CCAR 文档**：
- 信号1：`数字。数字`（全角句号夹数字，CCAR 每页 20+ 次）；
- 信号2：要求 **`CJK ≥ 10%`** 且汉字字间带空格（`_OCR_CJK_PAIR_SPACE_*`）。

对**英文** FAA 扫描件：无全角句号夹数字、`cjk/len<0.1` 直接 `False`，也无字形垃圾/假CJK/ToUnicode冲突 → `text_layer_corrupted=False` → 每页路由为 **`native_text`** → **直接用退化的 pypdf 文字层（不送 OCR）**。这才是"四列横排 + 表格变 LaTeX 垃圾"的根因（VectorPdfParser 再把表格数字列误判为公式/图，产生 `x^` 乱码）。

## 4. 计划改动（已完成落地）

### 4.1 路由判定：让"坏文字层英文扫描件"走 OCR（✅ 已实现）
- **`app/ingestion/pipeline.py`**：
  - 新增 `SCAN_PRODUCER_RE`（识别 Pdf-It / Pdf.Capture / Capture / Scanner / Kofax / Nuance / ABBYY / IRIS / Adobe Scan / Nitro Scan 等）与 `is_scan_text_layer(text, *, producer, image_ratio)`；
  - `parse_pdf` 从 `reader.metadata /Producer` 读 `producer`，并调用 `is_scan_text_layer`，其结果并入 `text_layer_corrupted` → 这类页路由为 `text_layer_review`（→ 远程 MinerU OCR）。
  - 信号：① producer 命中扫描/捕获工具（最可靠）；② 兜底：`image_ratio≥0.5` + 英文(CJK<10%) + 合并词/乱码退化标记（`_ENG_SCAN_MERGED_CAP_RE` / `_ENG_SCAN_GARBLED_RE`），避免误伤"图多但文字层干净"的文档。
  - 将 `scan_text_layer` 记入 `PageRecord.text_layer_corruption` 供可观测。
- **`app/ingestion/parsers/hybrid_pdf.py`**：`text_layer_review` 页本就在 `candidate_ocr_targets` → `ocr_targets` 中，且 `_figure_dominant_pages` 不会把文本多的扫描页拉回 figure 路径，故会自动走 `MinerUClient parse_method="ocr"`；无需改动该文件。
- 保持对中文/干净 PDF 的既有判定不变（中文仍走 `has_low_quality_ocr_text`）。

### 4.2 可扩展路由
- 将"文档类型 → 解析策略"归纳为可配置/可扩展的判定（类别：`native_text` / `scanned_ocr` / …），后续新增文档类型只需加探测器，不改主体逻辑。

### 4.3 配置/服务侧
- 核对 `app/core/config.py` 的 `ingestion.mineru`（`base_url` / `backend` / `parse_method` / `page_batch_size`），确保远程 MinerU 用 `parse_method="ocr"` 且后端为结构化（pipeline/正确 OCR）而非产生 LaTeX 的 VLM 路径。
- 视频/代理提醒：本机 Windows 代理 `127.0.0.1:21882` 会让 MinerU 卡死，批处理/请求侧需**清空代理环境变量 + `NO_PROXY=*`**（开发脚本已验证该修复）。

## 5. 涉及文件（已实际改动）

- `app/ingestion/pipeline.py`（新增 `SCAN_PRODUCER_RE`、`_ENG_SCAN_MERGED_CAP_RE`、`_ENG_SCAN_GARBLED_RE`、`is_scan_text_layer`；`parse_pdf` 读取 producer + 接入判定；`text_layer_corruption` 记录 `scan_text_layer`）
- `tests/unit/test_pipeline.py`（新增 `is_scan_text_layer` 4 例 + `test_parse_pdf_routes_scan_producer_page_to_text_layer_review`；`FakePdfReader` 增 `metadata` 参数）
- `HANDOFF-2026-08-28.md`

> 说明：`app/ingestion/parsers/hybrid_pdf.py` 无需改动（`text_layer_review` 页已自动走 OCR 路径）。

## 6. 验证计划（已完成）

- **单元测试**：`tests/unit/test_pipeline.py` **62 passed**；ingestion 相关（`test_vector_pdf` / `test_docx_parser` / `test_remote_parsers` / `test_ingestion_api` / `test_pipeline`）**166 passed**；全量 `tests/unit` 见下方。
- **Ruff**：`app/ingestion/pipeline.py`、`tests/unit/test_pipeline.py` 全部通过。
- **真实样例**：对 `D:\scriptsFAA\faa_test\final_rules` 中的 `Docket1186`（Pdf-It）与 `Docket7012`（Pdf-It）跑 `parse_pdf`：
  - 全部页面 `route == "text_layer_review"`，`text_layer_corruption["scan_text_layer"] = True`（改动前为 `native_text`）；
  - 即这些页将从远程 MinerU OCR 重建版面（修复"四列横排 + LaTeX 垃圾"），与命令行 MinerU `--method ocr` 结果一致。
- **待办**：端到端 `HybridPdfParser` 验证需远程 MinerU 服务在线；代理注意：调用需**清空代理环境变量 + `NO_PROXY=*`**（避免被本机代理 `127.0.0.1:21882` 拦截卡死）。

## 7. 备注

- 本次为目标/方案记录，代码改动按第 4 节落地并在实施后回填"实现细节 / 测试结果 / 全量回归"。
- 相关验证资料：`D:\internal-rag\comparison_out\REPORT_3WAY.md`、`D:\internal-rag\faa_parsed\report.md`、`D:\internal-rag\rasterize_pdf.py`、`D:\internal-rag\doc_router.py`（结构路由原型）。

## 8. 方案调整（2026-08-28 变更）：改为"独立解析器 + 内容路由"

> **第 4/5/6 节的 `is_scan_text_layer`（修改共享 `pipeline.py` 路由）方案已回退**：
> - `app/ingestion/pipeline.py`、`tests/unit/test_pipeline.py` 已 `git checkout` 回 HEAD（无 `is_scan_text_layer`/`SCAN_PRODUCER_RE`/新增用例）。
> - 原因：共享 `HybridPdfParser` + `split_blocks`/`build_chunks` 是为**中文/GB 标准深度调优**的；在这些共享逻辑里加"英文 FAA 标题识别 / 保留 MinerU 结构"**易影响其它文档**；且全局改 `ingestion.mineru.backend` 也影响所有走 OCR 的文档。

### 8.1 新方案（确认采用）：按文档类型路由到**独立解析器**
- **路由**：`app/ingestion/parsers/registry.py` 在 `.pdf` 分支，用**内容判定**决定走现有 `HybridPdfParser` 还是新的 `ScannedRegulatoryPdfParser`；判定需**保守**，不确定时回落到现有解析器。
- **新解析器** `app/ingestion/parsers/scan_regulatory.py`：
  1. **判定** `is_scan_regulatory_pdf(path)`：`producer ∈ {Pdf-It,Pdf.Capture,Capture,Scanner,Kofax,Nuance,ABBYY,IRIS,Adobe Scan,Nitro Scan,…}` 或 图像占比高 + 英文法规关键词（`Title 14`、`PART 23`、`Docket No.`、`Federal Aviation`）；
  2. **OCR + 保留结构**：复用远程 `MinerUClient`（`parse_method="ocr"`），**保留 MinerU 的标题层级/块顺序/表格 HTML/图片引用**（不再压平）；
  3. **噪音剔除**：去顶部手写签名片段（如 `See anrrie t ind`）、去重复页脚/注册号（`No. 246—Pt. I——2…`）；
  4. **英文标题 → chunk 边界**：识别 `PART 23—…`/`Title 14—…`/`§ 23.629`/`[Docket No. …]` → 生成 `section_path`/chunk 标题（现有中文 chunker 做不到）；
  5. 产出兼容的 `ParsedDocument`（复用 `BlockRecord`/`ChunkRecord`/`AssetRecord`）→ 交给现有 RAGFlow publisher。

### 8.2 影响
- **作用域**：仅对"扫描英文法规/案卷"生效；其它文档、现有中文管道**不受影响**。
- **需重索引**：这类文档 chunk 结构/标题改变 → RAGFlow 需重新分块 + 重 embedding（一次性）。
- **可扩展**：后续更多文档类型 = 加一个路由条件 + 一个解析器。

### 8.3 状态（已实现，待端到端）
- [x] `app/ingestion/parsers/scan_regulatory.py`（新解析器 `ScannedRegulatoryPdfParser` + 判定 `is_scan_regulatory_pdf` + 噪音剔除 + 英文标题分块）
- [x] `app/ingestion/parsers/registry.py`（`.pdf` 内容路由：命中→新解析器，否则回落 `HybridPdfParser`）
- [x] `tests/unit/test_scan_regulatory.py`（7 用例：判定 / 噪音剔除 / 英文标题 / 块与 chunk 构建）
- [x] 工程内测试：ingestion 相关 **168 passed**；全量 `tests/unit` **598 passed, 6 failed（均为既有失败，与本改动无关）**；ruff 全过。
- [ ] 端到端 `ScannedRegulatoryPdfParser.parse()` 需远程 MinerU 服务在线；代理注意：调用需**清空代理环境变量 + `NO_PROXY=*`**。
