# 2026-08-30 扫描英文法规解析修复与验收

## 范围与原则

- 用户样本：`D:/scriptsFAA/faa_test/final_rules/1964/1964-09-28__Amdt_23-0__Airworthiness_Standards__Normal,_Utility,_and_Acrobatic_Category_Airplanes_(New)__1918F11D/Docket4080__v0001__1918F11D.pdf`。
- 46 页，Producer 为 Pdf-It 5.091；原件 SHA256：`584f6dda15b8abe37bc5d3388b2e26c311c786a531afd939f4c23b6de658cd16`。
- 先向用户提交方案；用户确认“开始修改并进行测试”后实施。没有覆盖原 PDF、修改 27 日检索功能或发布/重建 RAGFlow 索引。
- 不在 OCR 前擦除手写字迹；解析后保留原始证据、区分正文/页边文字/疑似批注。不能靠删除正常公式或猜测拆分表格数字来通过验收。

## 代码变更

### 专用解析器 `app/ingestion/parsers/scan_regulatory.py`

- 路由同时要求英文法规内容与扫描证据。检查前 3 页，排除中文占比较高的文档；Producer 不再单独决定路由；图像兜底按 PDF 显示面积检查，不再把小 Logo 当整页扫描。
- 删除“异常英文词组正则直接删除”的清洗方式。采用 MinerU 的原始块类型、坐标、标题级别；疑似页首/页边批注留在 `qa.annotations`、`layout_features.ocr_blocks` 和 annotation block 中，不作为正文标题或检索内容。它们是待复核候选，不宣称实现了通用手写识别。
- 保留正式署名、签发信息及普通正文。原始 OCR 文本与清洗后正文分开存储。
- 保存图表、无图注图片及其资产引用；清洗 Markdown 引用本地资产。纯图表页保留图像和明确的未转录提示，分别计算文字覆盖率与内容覆盖率。
- 保留正常公式的可检索文字和 `formula_latex`。无 HTML 的表格保留文字并产生警告。
- 建立 Title/Chapter/Docket/Part/Subpart/Section 层级，保留标题文字；同级标题替换同级及以下路径，不覆盖全部父级。
- 长表按行分块并重复表头，脚注不丢失；完整原始表格 HTML 保留在 block 中。长正文按词边界尽量限制块长度；原始 block ID 仍可追溯。
- 补齐页数、空结果、缺失内容、正文到 chunk 覆盖、资产引用、远程警告、纯图表页、疑似批注、疑似表格行合并等质量检查。没有 OCR 内容时不能假报成功。

### 共享接入修复

- `app/ingestion/parsers/remote.py`：将 `aside_text` 与其它 MinerU 排除块分开保留，不混回正文；保留无图注图片和 chart 类型；记录 `source_type`、`text_level`；分页映射使用 dataclass replacement 保留 LaTeX 等全部字段；缺失图片文件产生警告。
- `app/ingestion/parsers/registry.py`：向专用解析器传递进度回调。
- `app/ingestion/pipeline.py`：输出解析器版本读取 QA 中的版本值，专用解析器为 v2；未修改共享中文分块/目录/跨页合并算法。

## 测试与可复现工具

- `tests/unit/test_scan_regulatory.py`：路由反例、显示图像面积、标题层级、批注隔离、署名保留、正文不按拼写删除、公式/资产/表格脚注保留、空结果、缺页、文字兜底、纯图表页、长表行覆盖等。
- `tests/unit/test_remote_parsers.py`：无图注图片、排除块、标题级别、分页后的 LaTeX 保留。
- `tests/acceptance_scan_regulatory.py`：支持真实本机 MinerU、旧 OCR 回放和本次 API 批次重放。调用真实 ParserRegistry、MinerU ZIP 读取/分页映射、专用解析器及 write_document，不上传索引。
- 旧 OCR 的 `_origin.pdf` 因重新序列化而 SHA256 不同；回放前逐页以 72 dpi 渲染比对，46 页像素全部相同。该校验不等于逐页文字准确性验收。
- 本次 API 批次回放要求每一批上传文件 SHA256 与原件相同，并按请求顺序提供批次目录。

运行示例（仓库根目录，PowerShell）：

```powershell
$sample = Get-ChildItem 'D:/scriptsFAA/faa_test/final_rules/1964/*1918F11D/*.pdf'
$cached = Get-Item 'D:/internal-rag/comparison_out/Docket4080/mineru/*/ocr'
.\.venv\Scripts\python.exe -X utf8 -m tests.acceptance_scan_regulatory $sample.FullName --replay $cached.FullName --output 'D:/internal-rag/deliverables/2026-08-30/docket4080-replay'
# 去掉 --replay 及其参数为本机 MinerU 实时测试。
```

## 验收发现：不能混淆结构完整与 OCR 语义正确

1. 第 1 页顶部手写批注被 OCR 为 `See anrrie t ind`，此前被当作二级标题。现在保留原文/坐标并隔离出正文，提示复核；左侧手写图像也保留。正式 `N. E. HALABY, Administrator.` 仍在正文。
2. 第 6 页四列表有 Pitch/Roll/Yaw 表头，但已有 MinerU OCR 将多行值合并成 `607510 / 30605 / 15020`。对照原页可确认不是一个数值。代码不猜测拆分，保留 HTML/图像并产生 `table_row_alignment` 警告。此项数字准确性**未通过**。
3. 第 38–40 页的图表被 MinerU 作为 chart 图片返回，没有图内文字转录。图像与页码保留，内容覆盖率和文字覆盖率分别报告，并产生 `image_only_pages` 警告。图表内容自动问答能力**未通过**。
4. 识别出图像/文字区域不等于正确识别手写内容。当前规则仅隔离有位置证据的疑似批注，正文中的手写批注仍可能需要人工复核；不得据此声称已清除所有手写内容。

## 最终执行结果

- 第一轮定向：42 passed；随后接入相关回归：179 passed。
- 第一轮全量：612 passed，6 failed。3 个 `test_regulation_preprocess.py` 失败通过在内存中加载 HEAD 的 pipeline 再运行，仍全部复现；没有回退工作树文件。另 3 个 `test_retrieval_service.py` 失败属于未修改的检索路径，与 27/28 日记录的失败相符。
- 最终定向：46 passed。最终全量：**613 passed，6 failed**，仍为下面列出的相同失败；测试报告保存在 `D:/internal-rag/deliverables/2026-08-30/unit-tests.xml`。有一个共享 remote.py 中既有 docstring 的 invalid escape SyntaxWarning。
- Ruff 对本次涉及代码与测试全部通过；`git diff --check` 通过。
- 旧 OCR 全文回放：12 项结构检查全部通过；**484 chunks、16 张表、14 个公式块、53 个资产**。文字覆盖率 43/46（93.48%），内容覆盖率 46/46（100%）。输出 `docket4080-replay/acceptance.json`。
- 本机 `127.0.0.1:8886` 原先未启动，本次使用既有 MinerU 安装、`pipeline` 后端、`ocr` 方法、10 页/批，真实重跑全部 46 页，共 5 批（10:37–10:54，约 17 分钟）。未变更安装、模型或全局配置。
- 实时 OCR 运行期间继续完善了纯图表页处理、长表切块和 Markdown 展示。因此首个长进程使用的是当时已加载的代码，最初将无文字图表页报告为失败；**最终用完成后的代码重放这次真实 API 的 5 批结果**，而不是再次 OCR 或伪装使用旧缓存。每批 uploads 文件 SHA256 都与原件一致，批次目录顺序已记录在最终 acceptance.json 的 `replay_inputs`。
- 最终本次 OCR 结果：**12 项结构检查全部通过，485 chunks、371 个 heading block、16 个 table block、14 个 formula block、53 个资产**；全部资产文件存在。文字覆盖率 43/46，内容覆盖率 46/46。输出 `docket4080-live/acceptance.json`（mode 为 `live_ocr_batch_replay`，明确标示最后的处理方式）。
- 最终仍有 3 类质量警告：`annotation_review`（2 处疑似手写/页边批注）、`image_only_pages`（38/39/40 页图中文字未转录）、`table_row_alignment`（第 6 页多行数字合并）。**结构验收通过不等于所有内容准确性通过；暂不能按全量无人审核发布标准验收。**
- 第 1 页原件、第 6 页原表及提取表格图像、第 38 页图表已进行视觉核对；没有宣称完成 46 页逐字校对。第 6 页问题也在本次实时 OCR 中复现，不是旧缓存或接入器新增的问题。
- 本次临时 OCR 服务在测试后关闭，原先服务未运行的状态恢复；日志保留在 `D:/internal-rag/temp/mineru-review-0830.out.log` 与 `.err.log`。测试完成时尚未提交 Git，没有发布索引。

## 接入台部署注意事项

- 用户随后实测的任务 `3d8298ea1fe44a6aad36877b10f72bb2` 仍由旧进程生成：parser v1、0 个资产、415 chunks、质量门禁为空。页面接口也返回空资产，不是前端隐藏图片。
- 8080 网关进程启动于 2026-08-30 10:30，早于本次修改，未启用自动重载。磁盘代码已为 v2，但必须重启 gateway 才会加载；已有任务结果还需重新解析，刷新浏览器不会重建旧结果。
- `page_type=text` 表示主要内容类型，不表示丢弃该页表格/图片；资产按当前页独立返回。用户未要求代为重启，本次未重启 gateway。

输出目录：`D:/internal-rag/deliverables/2026-08-30/`。其中 `acceptance.json` 是结构验收汇总，文档子目录包含 `baseline.json`、`document_ir.json`、`qa.json`、`cleaned.md`、`chunks.jsonl` 和 `assets/`。

## 全量测试中的既有失败列表

- `test_regulation_preprocess.py::test_detects_noisy_ocr_table_of_contents`
- `test_regulation_preprocess.py::test_cross_page_table_continuation_satisfies_caption_reference`
- `test_regulation_preprocess.py::test_split_blocks_filters_fullpage_figure_and_duplicate_table_title`
- `test_retrieval_service.py::test_comparison_runs_subqueries_and_reports_missing_coverage`
- `test_retrieval_service.py::test_section_family_expansion_does_not_enable_llm_keyword_extraction`
- `test_retrieval_service.py::test_complex_reranker_uses_cell_query_and_rejects_low_scores`
