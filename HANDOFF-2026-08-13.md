# 2026-08-13 改动记录

## 1. 修复：跨文档综合题被误判为单次检索（fact）

### 1.1 问题

现场问答发现，跨文档综合题走错了检索路径，回答质量严重不合格。

复现问题：

> 沙地无人机播种治沙作业，结合播种技术规范与无人机通用服务管理要求，完整项目实施需要兼顾技术参数与运营管理哪些双重要求？

原回答（网关）的三处硬伤：

1. **技术参数半截为空**：回答称"具体参数未在提供的片段中详述"，但《无人机播种治沙技术规程》实际包含 5.3 播种量设计公式（`S = N·W / (E·R(1-A)·G·1000)`，每公顷播种量/出苗株数/千粒重/发芽率等变量）、第 6 章播前准备（按气象预报确定最佳播种时机、空域与飞行计划申请）、5.2 种子处理等明确技术参数，均未被召回。
2. **运营管理引用了错误文档**：用《无人机防治稻纵卷叶螟施药技术规范》的"人员资质要求"（操作证书、熟悉稻纵卷叶螟发生规律）顶替运营管理，属于典型的特定任务要求泛化（模型甚至自述"虽为施药规范但作为人员能力要求的通用表述"）。
3. **真正该引用的《无人机应用服务通用规范》完全未召回**：该规范第 8 章"服务要求"（8.2 现场勘察、8.3 设备匹配、8.4 制定方案、8.6 服务实施、8.7 成果提交）正是问题问的运营管理内容。

### 1.2 根因

两层缺陷叠加：

1. **分类缺陷**：`QueryAnalyzer._classify` 只识别"对比/步骤/总结/分别"等触发词；"结合 X 与 Y……兼顾 A 与 B 双重要求"这类跨文档综合题不含任何触发词，落到末尾的 `endswith("？")` 判断，被误判为 `fact`，走单次检索快速路径。
2. **规划缺陷**：即使判为 `multi_hop`，`QueryPlanner._multi_hop_plan` 也只按"以及/并且/同时/；/;"拆句，拆不出"结合 A 与 B"的跨文档结构，会退回单次检索。

### 1.3 修复

**`app/services/query_analyzer.py`** — 在 `_classify` 中新增两类 multi_hop 信号（位于 fact 判断之前）：

- `_MULTI_DIMENSION_MARKERS`：双重要求、双重、两方面、两个维度、两个层面、兼顾、综合要求；
- `_CROSS_DOC_PATTERN`：`结合[^，,。！？!?]{0,30}[与和]`，识别"结合 A 与 B"跨文档结构。

**`app/services/query_planner.py`** — `_multi_hop_plan` 兜底新增跨文档拆解：

- `_CROSS_DOC_PATTERN`：`结合(?P<a>…)[与和](?P<b>…)`，提取两个来源短语；
- `_DUAL_ASPECT_PATTERN`：`兼顾(.*?)(?:哪些)?(?:双重|双|两方)要求?`，提取两个维度；
- `_cross_source_plan`：以"主体 + 维度1"为一个子查询，"来源2 + 维度2"为另一个子查询，使技术参数与运营管理分别命中各自的来源文档。

对复现问题，规划结果：

- query_type：`multi_hop`；
- 子查询 1：`沙地无人机播种治沙作业 技术参数` → 命中《无人机播种治沙技术规程》；
- 子查询 2：`无人机通用服务管理要求 运营管理` → 命中《无人机应用服务通用规范》第 8 章。

### 1.4 验证

- 沙箱直接 import + 断言验证全部通过：
  - 分类：跨文档双重要求 → `multi_hop`；"兼顾安全与效率两方面" → `multi_hop`；comparison/fact/summary/技术符号 multi_hop 原有用例均不受影响；
  - 规划：跨文档题拆成两个正确子查询；comparison 6 子查询、fact 单路径回归通过。
- 实测 RAGFlow 检索召回（真实知识库 `b5ecab6a…`）：
  - 子查询 `沙地无人机播种治沙 技术参数 播种量 航高` → 前 3 名均为《无人机播种治沙技术规程》；
  - 子查询 `无人机通用服务管理要求 运营管理` → 第 2 名命中《无人机应用服务通用规范》（含"5 机构要求 / 5.4 应建立完善的运营管理制度"）。
- 新增单测 4 个：
  - `tests/unit/test_query_analyzer.py`：`test_cross_document_dual_requirement_is_multi_hop`、`test_dual_requirement_marker_is_multi_hop`；
  - `tests/unit/test_query_planner.py`：`test_cross_document_plan_splits_into_two_source_dimensions`。
- **待宿主机执行**：重启 Gateway 后跑 `python -m pytest tests/unit/test_query_analyzer.py tests/unit/test_query_planner.py -q --noconftest`，
  再用 `/api/v1/chat/completions` 跑复现问题，确认回答同时覆盖技术参数（播种量公式等）与运营管理（服务要求）两侧。

### 1.5 改动文件

- `app/services/query_analyzer.py`（分类新增综合/跨文档信号）；
- `app/services/query_planner.py`（multi_hop 兜底跨文档拆解）；
- `tests/unit/test_query_analyzer.py`（新增 2 用例）；
- `tests/unit/test_query_planner.py`（新增 1 用例）。

### 1.6 注意

- 分类触发词"结合"存在误判风险（如"结合实际操作规范"），已通过 `结合…与/和…` 结构约束 + `{0,30}` 长度限制收窄；
  若后续发现简单事实题被误判为 multi_hop，优先检查 `_CROSS_DOC_PATTERN` 是否过宽。
- 跨文档拆解仅覆盖"结合 A 与 B"句式；"结合 A、B、C 多文档"（≥3 来源）暂未覆盖，需后续扩展。
- 本次修复不改动覆盖选择与 Evidence Judge，仅修正"该走多阶段却走了单次检索"的分类/规划缺陷。

---

## 2. 修复：跨文档综合题技术参数侧仍漏召回（检索链路）

### 2.1 问题

08-13 上午修复"跨文档综合题误判 fact"后，实测该题回答技术参数侧仍不合格：

- 该召回的《无人机播种治沙技术规程》5.3 播种量设计公式（`S = N·W / (E·R(1-A)·G·1000)`）、
  5.5 航高和速度设计、第 6 章播前准备（空域与飞行计划申请）均未召回；
- 反而召回《水稻丸粒化种子无人机水直播技术规程》1 范围（被"技术参数"字面命中顶替）；
- 运营管理侧只召回 5.4 运营管理制度 + CCAR-92 92.9 平台，缺《无人机应用服务通用规范》
  第 8 章"服务要求"（8.2 现场勘察 / 8.3 设备匹配 / 8.4 制定方案 / 8.6 服务实施 / 8.7 成果提交）。

### 2.2 根因（debug 实测定位，不是"没召回"）

`/api/v1/retrieval/debug` 拉覆盖矩阵确认分类/规划已正确（multi_hop，2 子查询，2/2 covered），
根因在检索链路两层：

1. **每子查询候选过少被截断**：`complex_candidates_per_subquery=2`，`_execute_plan` 对每个子查询
   `_execute_single(final_limit=2)` 只保留 hybrid 前 2。治沙规程的技术参数 chunk 其实在候选池
   （q1 raw 3/5/10/18…），被 `below_final_cutoff` 截断，reranker 与 coverage selector 没机会看到。
2. **子查询太泛被无关文档压制**：q1="沙地无人机播种治沙作业 技术参数"，"无人机播种"与
   《水稻丸粒化种子无人机水直播技术规程》标题高度重合，且水稻规程"1 范围 / 7 整地技术参数"字面
   含"技术参数"，hybrid 0.357 略高于治沙 3.2 术语 0.348 排 raw=1。"技术参数"这类高层维度词
   与文档章节具体词汇（"播种量设计""航高"）存在词汇鸿沟。

补充：reranker 服务本身正常（8003 POST /rerank 实测治沙 0.9998 vs 水稻 0.2204）。此前 debug
输出 `rerank_score==hybrid_score` 是因为 per-subquery `_execute_single` 走 `apply_reranker=False`
的 fallback 排序，并非 reranker 未参与——它只是被 top-2 截断"饿死"了。

### 2.3 修复

- `config/default.yaml`：`complex_candidates_per_subquery` 2→6、`complex_rerank_input_k` 12→36
  （配合 6 候选/子查询 × 最多 6 子查询的 comparison 场景）。
- `app/services/query_planner.py`：`_cross_source_plan` 对宽维度做词表扩展，新增
  `_DIMENSION_EXPANSIONS`（技术参数→"播种量 航高 速度 种子处理 整地 作业设计"；运营管理→
  "管理制度 服务要求 现场勘察 设备匹配 制定方案 服务实施 成果提交"）与 `_expand_dimension`，
  只对跨文档综合题、维度词精确匹配生效。

### 2.4 验证

- 沙箱端到端（import app + 直连宿主 RAGFlow `172.16.10.254:9380` + reranker `172.16.10.254:8003`）：
  修复后 `final_selected=8`，技术参数侧选中治沙 5.5 航高速度 / 5.3 播种量设计 / 6 播前准备，
  运营管理侧选中通用规范 5.4 制度 / 8.2 现场勘察 / 8.4 制定方案 / 8.6 服务实施，水稻文档被挤出。
- 沙箱 pytest：`test_query_planner` + `test_query_analyzer` 8 passed（--noconftest）；更新
  `test_cross_document_plan_splits_into_two_source_dimensions` 期望并新增
  `test_cross_document_plan_expands_broad_dimensions_only`（未收录维度不扩展）。
- 行宽 ≤100 手工检查通过；ruff 待宿主机执行。
- **待宿主机**：重启 Gateway 后跑 pytest（含 `test_retrieval_service`，需 Python 3.11）+
  真实 `/api/v1/chat/completions` 跑复现题，确认回答同时覆盖播种量公式与第 8 章服务要求。

### 2.5 改动文件

- `config/default.yaml`（complex_candidates_per_subquery 2→6、complex_rerank_input_k 12→36）；
- `app/services/query_planner.py`（`_DIMENSION_EXPANSIONS` + `_expand_dimension`）；
- `tests/unit/test_query_planner.py`（更新 1 用例、新增 1 用例）。

### 2.6 注意

- `_DIMENSION_EXPANSIONS` 是本语料库（无人机作业/服务类技术规程）的领域词表，只对
  `_cross_source_plan`（"结合 A 与 B"跨文档题）维度词精确匹配生效；新增维度词或改词必须补回归测试，
  避免污染普通单文档查询。
- `complex_candidates_per_subquery=6` 会放大 comparison（最多 12 子查询）的候选规模，已同步把
  `complex_rerank_input_k` 提到 36；rerank 是 CPU（llama.cpp）单请求批量，36 候选内开销可接受。
- 覆盖矩阵 `covered` 仍只代表"有高相关候选"，证据适用范围判定（P0）不受本次影响。
