# Internal RAG 改动记录（2026-08-20）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-19.md` 之后）的工作。
> 项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。
> 本文档按日期逐节追加改动/验证记录，沿用既有格式：问题 / 根因 / 修复 / 验证 / 改动文件 / 注意。

---

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-19.md`（已覆盖：答案确定性稳定化 temperature=0+seed=42、对比题"完整回答"三件套（表1 召回/主体文档优先/跨主体归因拦截）、同名不同时间/地点规程逐份分列（multi_document + 文档均衡轮转 + 文档级补充检索）、流程/汇总类证据覆盖（procedure 名额/章节多样性/单主题文档优先）、multi_hop 方面子查询与文档多样性、环节-章节归属规则（prompt v14）、引用-内容脱钩 `ClaimTopicValidator` 方案 A 实施等）。
- **本会话范围**：自 2026-08-20 起。本会话以**熟悉工程、核对当前状态**为主，并完成 08-19 末项（`ClaimTopicValidator` 引用-内容脱钩）的**离线链式验证 + 在线端到端复测**，随后记录服务重启与生产实测；未新增代码改动。
- **时间线说明（重要）**：`ClaimTopicValidator` 的代码与测试实际完成于今日 09:11–09:12（文件 mtime），并已按 08-19 会话的延续记录于 `HANDOFF-2026-08-19.md` §2.8 末项（该文档今日 09:13 定稿）。因此**本文档不重复记录其实现细节**，只记录其接线现状确认、验证结果与生产实测。
- **重要**：本会话的改动/验证记录在下方第 2 节中按日期逐节追加，不以计划项代替完成结果。

---

## 1. 当前状态快照（2026-08-20）

以下为本次会话实际核对的快照（非引用旧文档）。

### 1.1 环境与代码

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`、`http://127.0.0.1:8080/chat`
- 服务探测（2026-08-20 10:25 实测，HTTP 探活）：

| 服务 | 端口 | 状态 |
|---|---|---|
| Gateway | `127.0.0.1:8080` | ✅ `/health` 200 `{"status":"ok","service":"internal-rag","version":"0.1.0","environment":"development"}` |
| RAGFlow | `127.0.0.1:9380` | ✅ `/api/v1/system/healthz` 200 `{"db":"ok","doc_engine":"ok","redis":"ok","status":"ok","storage":"ok"}` |
| Ollama | `127.0.0.1:11434` | ✅ `/api/tags` 200（pid 27536，今日 10:17:15 启动） |
| Reranker | `127.0.0.1:8003` | ✅ `/health` 200 `{"status":"ok"}`（今日 10:17:31 启动） |
| MinerU | `127.0.0.1:8886` | ❌ 关闭（连接失败；涉及 OCR/远程解析的作业前需先启动 `.\deploy\mineru\start.ps1`） |

- 今日服务重启时间线（`runtime/run/*.pid`、`runtime/gateway/gateway.pid` 与日志 mtime 佐证）：
  - 10:17:15 Ollama 启动；10:17:31 Reranker 启动；10:19:43 Gateway（uvicorn）启动。
  - 网关日志 `runtime/gateway/logs/gateway.out.log` 在每次重启时被覆盖（旧实例日志不可回溯），08-19 之前实例的历史日志已不可查。
  - 11:00 前后（本会话）修复网关进程管理后，Gateway 又经历"停→启"实测；11:30 实施域过滤（§2.8）后再重启一次。当前监听 8080 的为真实 uvicorn 进程（PID 2056），`gateway.pid` 与监听进程一致（见 §2.7）。

### 1.2 Git 状态

- 仓库基线提交不变（`Initial commit` ×2 + `Merge GitHub repository initialization`），工作区仍有大量未提交改动（全部来自 08-17～08-20 会话，本会话未新增代码改动）。
- 今日（08-20）有 mtime 佐证的改动文件：

| 类别 | 文件 |
|---|---|
| 代码/配置/测试（09:11–09:12，已记入 08-19 §2.8 末项） | `app/services/claim_topic_validator.py`、`app/pipelines/rag_pipeline.py`、`app/core/config.py`、`config/default.yaml`、`tests/unit/test_claim_topic_validator.py` |
| 代码（本会话 §2.8 域过滤实施） | `app/services/query_scope.py`（新增）、`tests/unit/test_query_scope.py`（新增）、`app/services/retrieval_service.py`、`app/core/config.py`、`config/default.yaml`、`tests/unit/test_retrieval_service.py` |
| 代码（本会话 §2.9 对比问题整链） | `app/services/comparison_completeness_validator.py`（新增）、`tests/unit/test_comparison_completeness_validator.py`（新增）、`app/services/query_analyzer.py`、`app/services/query_planner.py`、`app/services/answer_generator.py`、`prompts/answer_generation.txt`、`prompts/json_repair.txt`、`tests/unit/test_query_analyzer.py`、`tests/unit/test_query_planner.py`、`tests/unit/test_answer_generator.py` |
| 代码（本会话 §2.10 矩阵兜底） | `app/services/comparison_matrix_builder.py`（新增）、`tests/unit/test_comparison_matrix_builder.py`（新增）、`app/pipelines/rag_pipeline.py` |
| 脚本（本会话 §2.7 修复） | `deploy/gateway/start.ps1`、`deploy/gateway/stop.ps1`（PID 管理修复，CRLF+BOM 编码） |
| 文档 | `HANDOFF-2026-08-19.md`（09:13 定稿）、`HANDOFF-2026-08-20.md`（本文档，新增） |
| 验证产物（09:07–12:30） | `tmp/feifang-songchai.json`、`tmp/decode_fs*.py`、`tmp/fs{2,3}-targets.json`、`tmp/fs{2,3}-out.txt`、`tmp/check_topic_validator.py`、`tmp/fs-validator-out.txt`、`tmp/fs-live.txt`、`tmp/validate_chain.py`、`tmp/fs-validate-out.txt`、`tmp/fs-final.txt`、`tmp/run_original.py`、`tmp/fs-original.txt`、`tmp/decoded-fs-*.txt`（还原副本）、`tmp/repro_20260820.py`、`tmp/repro-debug-20260820.json`（带引号题面）、`tmp/repro-debug-noquote-20260820.json`（无引号题面）、`tmp/verify-live-claims.txt`、`tmp/repro-debug-scoped-20260820.txt`（域过滤后 debug）、`tmp/run_chat_scoped.py`、`tmp/repro-chat-scoped-20260820.json`（域过滤后 chat）、`tmp/run_bridge_diag.py`、`tmp/bridge-debug-20260820.json/.txt`、`tmp/bridge-chat-20260820.json/.txt`（对比问题各轮诊断）等 |

- **禁止** `git reset --hard` / `git clean` / `docker compose down -v`。

### 1.3 真实入库数据（沿用 08-19 实测，本会话未重算）

- `D:\internal-rag\data\ingestion\jobs\`：96 份 `job.json`（UTF-8，读取需 `-Encoding UTF8`）；状态分布 `warning` 74 / `completed` 21 / `needs_review` 1 / `failed` 0；发布状态 96/96 `published`（dataset `b5ecab6a8b2811f1b7c4d904b160df50` 为主要发布目标）。

### 1.4 测试基线

- 08-19 记录（今日 09:13 前定稿）：完整 pytest **370 passed, 3 failed**（3 个失败均为既有解析基线：noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重，`test_regulation_preprocess.py` 相关），无环境性 error。
- 今日沙箱实测：`test_claim_topic_validator.py` **6 passed**（本会话复跑确认）。
- 完整 pytest 需宿主（非沙箱）重跑确认（沙箱临时目录限制见 08-19 §1.4）。

---

## 2. 改动记录（按日期逐节追加）

### 2.1 会话初始化：熟悉工程与建立今日改动文档（2026-08-20）

#### 内容

- 通读 `README.md`、`docs/agent-handoff.md`（完整交接入口）、`HANDOFF-2026-08-19.md`（昨日全量改动记录）。
- 核对今日快照：服务探活（§1.1）、Git 状态（§1.2）、入库任务统计（§1.3）、测试基线（§1.4）。
- 按文件 mtime 与 `tmp/` 验证产物重建今日时间线（见 §2.3–§2.5），新建本文档 `HANDOFF-2026-08-20.md`，沿用既有逐日逐节追加格式。

#### 改动文件

- `HANDOFF-2026-08-20.md`（新增）

#### 注意

- 本会话暂无代码改动；后续改动/验证将按日期追加到第 2 节。
- 遗留挂账（来自 08-18/08-19，尚未处理）：早熟中稻航高侧"单侧缺证据/适用对象约束"、沙地播种量公式 OCR 乱码（需回解析层修公式图像）、生成端对已入证据内容利用不充分、RAGFlow 偶发僵死（ReadTimeout，需按 08-11/08-18 记录处置）、"CLAIM 无法通过校验被省略→输出降级"的可接受性需用户评估（见 §2.5）。

---

### 2.2 确认：ClaimTopicValidator 实施落地现状（2026-08-20）

> 代码实施完成于今日 09:11–09:12，完整设计/测试记录见 `HANDOFF-2026-08-19.md` §2.8 末项（引用-内容脱钩，用户确认方案 A）。本会话仅核对接线现状。

#### 核对结果（代码 + 配置实测）

1. `app/services/claim_topic_validator.py`：`ClaimTopicValidator`——提取 claim 中文信息 bigram（过滤停用字/词、数字单位），与全部被引 chunk 文本并集比对，**缺失比例 ≥50% 判定引用-内容脱钩**并抛 `ValueError`；信息 bigram <2 的短 claim 跳过；全部被引 chunk 未找到时跳过（保守）。
2. `app/core/config.py`：`GenerationSettings.enforce_claim_topic: bool = True`（默认开）。
3. `app/pipelines/rag_pipeline.py`：`_validate_evidence_consistency` 链尾新增（citation → scope → grounding → subject → **claim_topic**），受 `self._enforce_claim_topic` 开关控制；触发后走既有 repair/sanitize 路径。
4. `config/default.yaml`：`generation.enforce_claim_topic: true`；`models.answer.temperature: 0`、`seed: 42`；`prompt_version: answer-generation-v14`。
5. 测试：`tests/unit/test_claim_topic_validator.py` 6 用例；沙箱实测 **6 passed**。

#### 改动文件

- 无（仅核对；实施文件已列于 §1.2，记录归属 08-19 §2.8 末项）。

#### 注意

- 该开关默认开启，若后续出现"校验过严导致有效答案被降级"的反馈，可先 `enforce_claim_topic: false` 关闭对比，再决定阈值（当前 50%）。

---

### 2.3 离线验证：引用-内容脱钩判定（09:21–09:35）

> 目标：不依赖在线 Gateway，用真实 chunk 文本验证 `ClaimTopicValidator` 能否精确区分"引用挂错"与"引用正确"。

#### 数据准备

- 从真实检索/chat 响应提取被引 chunk 文本（含"文档：/章节：/条号：/页码："前缀），存 `tmp/fs2-targets.json`（C2/C3/C6/C7/C9/C11/C12）、`tmp/fs3-targets.json`（C1/C2/C3/C4/C7/C8/C10/C11/C12/C15）。
- 文本为控制台双重编码乱码（UTF-8 字节被 latin-1 化），脚本内用 `text.encode("latin-1").decode("utf-8")` 还原（`decode_fs*.py`；`tmp/fs2-out.txt`、`fs3-out.txt` 为还原后的可读副本）。

#### 用例（与用户报告的"8 处挂错"同场景）

| claim | 内容 | 引用 |
|---|---|---|
| claim1 | 药剂防治阶段作业前需获得空域批准（管制空域或真高大于 30 m），并勘查确认区域边界、面积及林地结构。 | C7(喷洒无人机)、C2(水质)——**挂错** |
| claim2 | 多种药剂混用前应做桶混试验，药液浓度应为常规喷雾的 15~30 倍并宜添加飞防专用助剂。 | C9(勘查)——**挂错** |
| claim3 | 作业实施中需遵守安全距离与预警规定，如起降时距障碍物 10 m 以上、敏感区域 100 m 以上及设置警戒线等。 | C6/C11/C12——**挂错** |
| claim4 | 将无人机作为空中平台，搭载可见光相机传感器，通过飞行作业和空中遥感获取照片视频资料。 | C3(3.2 枯死松树监测)——**正确** |

#### 结果一：仅 topic 校验器（`check_topic_validator.py` → `fs-validator-out.txt`）

- claim1 **拒绝**（主题词缺失 93%：空域/管制/批准/勘查/边界/面积/林地结构 等全部不在被引 chunk）；
- claim2 **拒绝**（缺失 100%：桶混/助剂/浓度/混用 等）；
- claim3 **拒绝**（缺失 77%：警戒/预警/敏感/设置 等）；
- claim4 **放行**（主题词在被引 chunk 中命中）。

#### 结果二：完整校验链（`validate_chain.py` → `fs-validate-out.txt`）

- 链序：citation / scope / grounding / subject / topic；
- claim1：citation 拒 + grounding 拒（数值 30m 不在被引 chunk）+ **topic 拒**；
- claim2：citation 拒 + **topic 拒**；
- claim3：citation 拒 + grounding 拒（100m）+ **topic 拒**；
- claim4：citation 拒 + grounding 过 + subject 过 + **topic 过**（唯一全绿项）。

#### 结论

- **topic 校验器是"引用-内容脱钩"的精确拦截器**：错挂引用（claim1–3）全部拒绝、正确引用（claim4）放行，与 grounding（只查数值）互补——本场景 grounding 只能拦 30m/100m 数值错配，拦不住"空域批准挂到人员要求""桶混挂到清洗"这类非数值脱钩。
- 注意：链式测试中 **citation 列全拒是测试脚本传 `evidence=[]` 的产物**（`CitationValidator` 以 evidence 为准校验引用集合），不代表真实行为；真实 pipeline 中 evidence 来自证据提取层。

#### 改动文件

- 无（仅验证；脚本与产物在 `tmp/`）。

#### 注意

- 控制台（PowerShell 5.1）显示乱码属编码显示问题，文件本身还原后可读（`decoded-fs-*.txt` 为还原副本）。

---

### 2.4 在线端到端复测：原问题重跑（09:23 / 10:03 / 10:08）

> 原问题："结合林业有害生物飞防、松材线虫病遥感监测两类无人机作业规范，如何搭配完成林地病害识别-药剂防治全链条作业？"（dataset `b5ecab6a…`）。以下三次实测均在 8080 Gateway 上完成，但分属不同网关实例（旧/新代码），顺序按产物 mtime。

#### ① 09:23 `tmp/fs-live.txt`（错挂引用未被拦截的形态）

- 输出 5 环节（前期准备/病害识别/药剂配制/作业实施/后期维护），但**引用整体错挂**：空域批准→[C8]（实为"飞防作业人员要求"）、施药量→[C4]（实为"桶混"）、桶混试验与 15~30 倍→[C11]（实为"作业后清洗"）、水质→[C1]（实为"障碍物标注"）、10m→[C7]（实为"喷洒无人机"）、100m→[C15]（实为"样株标识/空域"）、喷洒监控→[C12]（实为"植保无人机一般要求"）、清洗→[C10]（实为"田间调查/药剂选择"）；仅病害识别→[C3] 正确。
- 与用户报告同源：**内容大多有据但引用挂错 chunk**（引用-内容脱钩）。

#### ② 10:03 `tmp/fs-final.txt`（引用全部正确落地的形态）

- 输出结构更完整：病害识别（遥感监测阶段）→ 药剂防治（飞防实施阶段，含准备与勘察/药剂配制/飞行作业/后续维护四小节）+ 文末注（人员要求）。
- **引用全部落到与内容匹配的 chunk**：空域批准→[C9]、障碍物标注→[C1][C6]、核实地势地貌林地结构→[C9]、桶混试验与 15~30 倍→[C10]、水质→[C2][C13]、10m→[C6]、100m→[C14]、覆盖率检测→[C2][C13]、清洗→[C11]、人员要求→[C8]。

#### ③ 10:08 `tmp/fs-original.txt`（`run_original.py` 原问题重跑）

- `status=PARTIALLY_ANSWERABLE  degraded=True  repaired=True`
- `warnings: ['生成结果未通过整体证据校验：ValueError', '已省略未通过证据校验的结论：CLAIM_3']`
- 输出仍含病害识别 [C3]、药剂防治（人员要求 [C8][C1]、空域审批/敏感区/电子围栏 [C9][C5][C12][C16]）、作业后清洗 [C11]；**CLAIM_3（桶混/助剂类结论）因无法通过证据校验被省略**。
- 说明：新校验链的**保守行为**——当模型输出某结论但引用/数值/主题无法与证据对上时，按设计 repair→仍不过→省略该结论并降级，而不是把错误内容留在答案里。同一问题多次实测结果在 degraded 与否上可能有差异（问题措辞/子查询微差），但**错挂引用不再进入最终答案**。

#### 结论

- 引用-内容脱钩修复**端到端生效**：错挂引用被拦截（repair/sanitize 路径），幸存答案的引用与内容匹配；代价是可能省略无法验证的结论并降级为 PARTIALLY_ANSWERABLE（诚实降级，符合"系统要求无对应内容就禁止输出"原则）。

#### 改动文件

- 无（仅验证；产物在 `tmp/`，`decoded-fs-live.txt` / `decoded-fs-final.txt` 为还原副本）。

#### 注意

- 三次实测定性结论稳定，但 ① 与 ② 具体引用编号差异源于分属不同网关实例/代码版本，编号映射以各次输出自带引用列表为准。

---

### 2.5 服务重启与生产实测：校验链在线生效（10:17–10:21）

> 10:17–10:19 经 `deploy` 脚本重启模型服务与网关（ollama 10:17:15、reranker 10:17:31、gateway 10:19:43，pid 文件见 `runtime/run/`、`runtime/gateway/`）。重启后立即有生产请求命中新代码。

#### 生产请求实测（request_id `06c17842-ac09-406f-bc9f-35182b99ad14`）

- `POST /api/v1/chat/completions` → **200 OK**，耗时 116.6s（Reranker `8003/rerank` 与 Ollama `11434/v1/chat/completions` 均 200）。
- 网关日志（`runtime/gateway/logs/gateway.out.log`）两次 `answer_validation_failed`：
  - **initial 阶段**：`claim CLAIM_2 topic words ['健康','员需','如操','满足','状态','神状','精神','足健','需满'] absent from its cited chunks (missing 60%)`——`ClaimTopicValidator` 在生产中首次实测触发；
  - **repair 阶段**：`claim CLAIM_2 has no citation`——repair 后仍无法通过 citation 校验，CLAIM_2 最终被省略（sanitize 降级路径）。
- 结论：**`ClaimTopicValidator` 已在线生效**，引用-内容脱钩在生产请求中按设计拦截并降级，未产生 500。

#### 注意

- 该请求触发的降级属于"保守拦截"的预期行为；若用户反馈"答案被砍"过多，需评估 50% 缺失阈值与 repair 提示词（`prompts/json_repair.txt`）的配合。
- 网关日志在每次重启时覆盖，生产排障需保留旧日志（可考虑 start.ps1 增加日志轮转/归档）。

---

### 2.6 用户重启电脑后复测：确认修复已生效 + 两种题面差异（2026-08-20 10:28–10:45）

> 用户重启电脑后重测同一问题，反馈"回答依然是（错挂引用的版本）"。本会话完整复现与逐条核对后确认：**修复已生效，用户贴的答案在当前证据集下引用正确**；措辞未变是确定性生成的必然结果。

#### 复现与核对过程

1. **当前网关代码确认**：8080 监听进程（重启后 10:19:43 启动）带 `ClaimTopicValidator`；10:20–10:21 的请求 `06c17842` 日志显示该校验器在生产中真实触发（§2.5），且用户贴的答案中**没有**"操控前 8h 不饮酒/精神状态"这条——正是被校验器省略的结论（C8 全文只有"不可饮酒,不可连续12h以上疲劳作业,不可带病操作"，**没有"精神状态良好"**，模型添加证据外内容被拦）。
2. **逐条核对引用映射**（用户题面＝带全角引号版 `林地 “病害识别 - 药剂防治” 全链条作业`，调 `/api/v1/retrieval/debug` 取当前 18 chunk 映射，产物 `tmp/repro-debug-20260820.json`）：

| 用户贴的引用 | 当前实际指向 | 匹配 |
|---|---|---|
| [C3] 病害识别 | 松材线虫规程 3.2 枯死松树监测 | ✅ |
| [C7][C2] 空域批准+勘查 | C7=林业规程 **5.1.1 空域批准 + 5.2.1 勘查区域边界/面积/林地结构**；C2=5.1.1 空域 + 5.1.3 施药量 | ✅ |
| [C9] 桶混试验/15~30倍 | 马铃薯规程 **6 农药选择及配制要求**（含"多种药剂混用前应…"） | ✅ |
| [C6][C11][C12] 10m/100m/警戒线 | C6=**7.2.2 障碍物10m**；C11=**7.2.3 敏感区域100m**；C12=**7.2.5 48h公布+设置警戒线** | ✅ |

3. **离线复跑校验器**（`tmp/verify_live_claims.py` → `tmp/verify-live-claims.txt`，用上述真实 chunk）：用户贴的 4 条 claim **全部放行**；"人员健康"claim **拒绝（missing 56%）**——与线上 06c17842 行为一致。
4. **两种题面对照**（关键）：同一问题**带引号**与**无引号**题面检索结果不同——
   - 无引号版（`林地病害识别-药剂防治全链条作业`）→ 旧映射：[C7]=**喷洒无人机设备(4.2.1)**、[C2]=**水质(6.1.1)**——即用户最初看到的"8 处挂错"形态（`tmp/repro-debug-noquote-20260820.json` 复现，与 09:07 `feifang-songchai.json` 完全一致）；
   - 带引号版 → 新映射，[C7]=空域批准+勘查，正确。
   - 该题面下错挂引用现在会被 topic 校验器拦截（省略结论、诚实降级），不会再原样输出。

#### 结论（重要，供后续接手）

- **用户看到的"老答案"是新代码的确定性输出**：`temperature=0 + seed=42` 使同证据下输出逐字节一致，措辞必然不变；但**引用编号背后的 chunk 已换成正确内容**（[C7][C2][C9][C6][C11][C12] 全部指向匹配章节）。
- 校验器在线工作：拦掉了模型添加的证据外内容（"精神状态良好"），并对无引号题面的错挂引用按设计拒绝。
- 建议用户：在界面点击 [Cx] 引用核对引用块内容；**统一用带引号题面测试**，无引号版会因检索映射不同而出现被省略结论的降级答案。

#### 注意

- 检索对题面标点/空格敏感（引号改变分词与混合检索分数→chunk 排序→引用编号）。若希望两种写法行为一致，属检索层"题面归一化"课题（未实施，挂账）。

---

### 2.7 修复：网关进程管理（gateway.pid 与实际监听进程不一致）（2026-08-20 11:00）

> 用户确认修复。排查中发现 `deploy\gateway` 的 PID 管理有缺陷，导致 `stop.ps1` 可能停不掉网关、端口长期占用。

#### 根因

- Windows venv 的 `python.exe` 是**启动器**：`Start-Process .venv\Scripts\python.exe -m uvicorn …` 会拉起**基础解释器**（本机 `D:\anaconda3\python.exe`）作为子进程真正运行 uvicorn（实测：父 17592=`\.venv\Scripts\python.exe`，子 17204=`D:\anaconda3\python.exe`，子进程才是 8080 监听者）。
- 原 `start.ps1` 把 `$Process.Id`（启动器 PID）写入 `gateway.pid`；`stop.ps1` 按该 PID 停止——停掉启动器后子进程是否退出取决于 venv 行为，且原 `stop.ps1` 的"项目根路径"校验对基础解释器命令行（不含项目路径）会误判拒绝。
- 另：原 `start.ps1` 对并发启动竞态（两个实例同时通过"端口未占用"检查）未处理，PID 文件可能记录到失败方。

#### 改动

- **`deploy/gateway/start.ps1`**：
  - 就绪后经 `Get-NetTCPConnection` 解析 **8080 真实监听进程 PID** 写入 `gateway.pid`（而非 Start-Process 返回的启动器 PID）；
  - "already listening" 分支也把 PID 文件对齐到真实监听者；
  - 就绪循环容忍竞态：本进程退出但 8080 已有健康网关（且命令行含 `uvicorn`+`app.main:app`）时视为成功。
- **`deploy/gateway/stop.ps1`**：
  - 新增 `Test-GatewayProcess`（python.exe + `*uvicorn*` + `*app.main:app*`，不校验项目根路径，兼容基础解释器命令行）；
  - 停止目标：优先 PID 文件（须通过网关校验），否则**回退到 8080 端口真实监听进程**（须为网关，避免误杀其它服务）；清理 PID 文件。
- **编码**：两个脚本统一为 **CRLF + UTF-8 BOM**（原 LF-only 无 BOM 的 UTF-8 在 PowerShell 5.1 下解析失败——实测多行 `return (...)` 表达式报"意外的标记 }"）。

#### 验证（本机实测通过）

- 停→启循环：`stop.ps1` 停掉真实监听者、8080 释放、PID 文件清理；`start.ps1` 启动后 `gateway.pid`=真实监听 PID（如 20456、27132），`/health` 200。
- 兜底路径：伪造失效 PID 文件（99999）→ `stop.ps1` 警告并按端口找到真实监听者停止；重启后 PID 文件重新对齐。
- 两个脚本 `Parser::ParseFile` 均 PARSE OK。

#### 改动文件

- `deploy/gateway/start.ps1`、`deploy/gateway/stop.ps1`

#### 注意

- 用户选择**暂不调整** `models.answer.timeout_seconds: 60`（config/default.yaml 写死、无环境变量覆盖）。实测 Ollama 一次生成 30~120s，超过 60s 时网关对 chat 请求返回 503（ReadTimeout，本会话复现两次：request `6d2f2522`、`fefc4cc2` 均 503，耗时 94~101s）；06c17842 成功是当时两次生成恰好 43s/29s。**挂账：建议改为 300（`config/default.yaml` `models.answer.timeout_seconds`）并重启网关后复测。**

---

### 2.8 实施：范围外文档域过滤（方案 B）——"结合 A、B 两类…规程"只允许声明类别文档进证据（2026-08-20 11:30）

> 用户指出：答案"药剂防治"细节（桶混试验、15~30 倍浓度、安全距离、电子围栏、48h 公布、警戒线）引用来自《基于植保无人机的马铃薯病虫害药剂防治作业规范》——马铃薯是作物（大田）病虫害规范，不在问题限定的"林业有害生物飞防 + 松材线虫病遥感监测"两类规程范围内。用户确认实施方案 B（确定性文档域过滤）。

#### 根因

- 检索按相关度取 chunk：马铃薯规程的"药剂配制/作业要求"章节内容最细、分数最高（修复前 18 选中 **14 个是马铃薯规程**）；问题限定的林业规程（20240705）只进 3 个 chunk，其自身的 4.4.1 药剂选择、6.2 喷洒实施、7.1.1 清洗章节**没进证据**；生成端于是拿马铃薯内容"补齐"药剂防治步骤。
- `ClaimTopicValidator`/grounding/scope/subject 只校验"引用文字↔chunk 内容"一致，**不校验"引用文档是否属于问题限定的规程类别"**（文档适用域），故拦不住。

#### 改动

1. **新增 `app/services/query_scope.py`**（确定性判定，无模型参与）：
   - `declared_scope_cores(query)`：命中"**结合 …N 类…规范/规程/标准**"句式时提取类别短语并剥离通用后缀（飞防/遥感监测/智能巡检/常态化巡查/监测/防治/作业/应用/无人机 等），得到领域核心词（如"林业有害生物飞防"→"林业有害生物"，"松材线虫病遥感监测"→"松材线虫病"）；其它句式（对比题"对比…"、多跳题"同时使用…"、普通流程题）返回空列表不触发。
   - `document_in_scope(document_name, cores)`：文档标题（复用 `multi_document.document_title` 去日期/书名号/.pdf）包含任一核心词子串即视为范围内。
2. **`app/core/config.py` + `config/default.yaml`**：`retrieval.enforce_declared_scope: true`（开关）。
3. **`app/services/retrieval_service.py`**（仅 procedure/summary 且命中句式时生效）：
   - `_execute_single`：补充检索后按声明类别**过滤 `accepted`**——范围外文档不进 rerank/最终证据；debug 的 `filter_reason="outside_declared_scope"` 标注被排除候选。
   - `_supplement_process_documents`：`scope_cores` 非空时补充目标 = **声明类别内文档**（而非分数最高的范围外主文档，如马铃薯规范），保证林业规程 4.4.1/6.2/7.1.1 全程章节进候选（`post_filter_accepted` 30→50）。
   - 新增纯函数 `_filter_declared_scope`、`_supplement_target_names`；顺手修 `_filter_reason` 的 `document_ids` 可空类型与 `self._ragflow` None 守卫（mypy）。
4. **测试**：新增 `tests/unit/test_query_scope.py`（5 用例：两类提取/多后缀剥离/不触发场景×4/文档匹配/无书名号）；`test_retrieval_service.py` +2（域过滤保留范围外排除、补充目标优先声明类别而非最高分范围外文档）。`test_config.py` 通过。

#### 验证（网关重启后实测，pid 2056）

- **debug 层**（`tmp/repro-debug-scoped-20260820.txt`）：18 选中 **全部来自林业规程（17）+ 松材线虫规程（1）**；马铃薯/胡萝卜（20250228）/农业植保安全（20240628）等范围外文档全部 `filter_reason="outside_declared_scope"`；林业规程 4.2.1/4.4.1 药剂选择、6.1.1 配药、6.2.1~6.2.5 喷洒实施、7.1.1/7.1.2 清洗维护等章节均进入证据。
- **chat 端到端**（`tmp/repro-chat-scoped-20260820.json`）：`status=ANSWERABLE / degraded=False / repaired=False / warnings=[]`；引用全部落在两份范围内规程（[C2] 松材线虫 3.2 病害识别；[C5][C16] 空域批准/性能检查、[C8] 勘查+样株、[C1] 施药量、[C7][C12] 配药水质+流量误差±5%、[C14] 环境温度 15~30℃、[C3][C11] 覆盖率检测/异常修正、[C6][C17] 清洗+GB 12475——全部林业规程 20240705）；**不再出现桶混/15~30 倍浓度/电子围栏/48h 公布/警戒线等马铃薯内容**，改用林业规程自身的作业细节。
- 回归确认：对比题（"对比…"）、多跳题（"同时使用…"）、普通流程题（无"结合…N 类…规范"句式）`declared_scope_cores` 均返回空列表，行为不变（单测覆盖）。
- 单测：query_scope + retrieval_service + config 合计 **23 passed**；ruff 全过；mypy 仅剩既有挂账（`model_client.py:81`、`_single_topic_final_chunks` 的 `sorted(key=document_best.get)`）。

#### 改动文件

- `app/services/query_scope.py`（新增）、`tests/unit/test_query_scope.py`（新增）
- `app/services/retrieval_service.py`、`app/core/config.py`、`config/default.yaml`、`tests/unit/test_retrieval_service.py`

#### 注意

- 触发条件刻意收窄：仅 `query_type ∈ {procedure, summary}` 且命中"结合…N 类…规范/规程/标准"句式；范围内证据缺失的细节（如林业规程无"桶混试验/浓度倍数"规定）将如实输出"当前资料中未明确说明"，不再用范围外文档顶替（符合"无对应内容禁止输出"原则）。
- 检索对题面标点/空格敏感（§2.6）的问题依然存在；域过滤只在声明句式下生效，普通问题仍按原检索逻辑。

---

### 2.9 修复：对比类问题（"A与B在X、Y、Z环节如何统筹区分"）的系统性完善（2026-08-20 12:00）

> 用户新问题："长大桥梁无人机精细化巡检与地质灾害倾斜摄影测量，在设备选型、像控布设、成果验收三个环节的标准要求如何统筹区分"——最初回答只有 3 条碎片（"明显不全"）。本会话定位并修复了**对比问题整链**的多个缺陷。

#### 根因链（逐层定位）

1. **分类误判**：`_classify` 只认"对比/比较/差异"，"统筹区分"含"如何"被误判为 procedure → 单查询、无矩阵、无主体归因。
2. **维度尾巴污染**：对比规划器 `_clean_aspects` 不剥"X、Y、Z **三个环节**的标准要求如何统筹区分"，最后一个维度被污染。
3. **第三文档污染格子**：对比子查询候选不做主体域过滤，维度词（像控点/倾斜摄影）命中《水运工程桩位无人机测量导则》等第三文档，其 chunk 被标成子查询主体（地灾）→ subject grounding 按 subquery 标签放行拦不住。
4. **维度词与章节术语词汇鸿沟**："成果验收"对应附录 A.2~A.5"精度检查记录表/限差/成果清单"，"设备选型"对应"相机/POS/电池"——不扩展则含真实要求的附录/设备章节进不了候选。
5. **模型编号幻觉**：模型引用证据外编号（C9/C10）→ CitationValidator 拦 → 整条结论被省略。
6. **repair 链震荡**：`json_repair_attempts` 原实现只有"是否修复"（至多一轮）；修复会整体重写答案、可能丢整行；`_validate_answerability` 还要求模型把判定缺失的要求逐条写进 missing_information（模型只写正文不填字段 → 反复触发修复）。

#### 改动（全部确定性、可单测）

1. **`app/services/query_analyzer.py`**：comparison 触发词补 "区别/异同/区分"（"统筹区分"、"A和B有什么区别"此前都漏网）。
2. **`app/services/query_planner.py`**：
   - 新增 `_ASPECT_COUNT_TAIL_RE`：预剥离"…N 个环节/方面/维度/要素/阶段…"数量词尾巴；
   - `_ASPECT_SUFFIXES` 补 "上/方面如何统筹区分/统筹协调/区分/协调" 等尾巴；
   - `_DIMENSION_EXPANSIONS` 补 "设备选型/像控布设/成果验收" → 具体章节术语扩展。
3. **`app/services/query_scope.py`**：后缀表补 "精细化巡检/倾斜摄影测量/倾斜航测" 等；新增 `document_belongs_to_subject`（对比主体核心词 ↔ 文档标题匹配，含子串兜底）与 `subject_core`。
4. **`app/services/retrieval_service.py`**：`_execute_plan` 对比路径按 `document_belongs_to_subject` 过滤每个子查询候选——第三文档不进格子，主体无对应内容的格子自然变 missing。
5. **`app/services/answer_generator.py`**：
   - 首版生成 payload 补 `allowed_citation_ids` + `citation_documents`（与 repair 一致，遏制编号幻觉）；
   - `generate()` 重构为**多轮修复循环**（`json_repair_attempts` 现在真正表示轮数）；
   - sanitize 分支对 `CompletenessValidationError` 优雅降级（缺失格子写入 missing_information，不 502）。
6. **新增 `app/services/comparison_completeness_validator.py`**：答案级校验——comparison 矩阵每个 covered 格子必须被某 claim 覆盖（点名主体核心+维度词，或引用该格 chunk）；缺失抛 `CompletenessValidationError` 触发修复。挂在 pipeline `validate()`（整答）路径，不进 per-claim（避免 sanitize 误伤）。
7. **`app/pipelines/rag_pipeline.py`**：接线完整性校验；`_validate_answerability` 去掉"模型必须自填 judge 缺失要求"的硬性检查（响应层确定性合并 assessment.missing_requirements，模型漏填只会徒增修复震荡）。
8. **`app/core/config.py` + `config/default.yaml`**：`generation.enforce_comparison_completeness: true`、`json_repair_attempts: 1→2`（上限放开）、`prompt_version: answer-generation-v15`。
9. **`prompts/answer_generation.txt`**：新增规则 36（对比矩阵必须输出全部维度行、每行覆盖全部主体，不得整行/整格缺失）；`prompts/json_repair.txt` 补"缺少格子必须补充（证据编号见 error），不得删减已有格子"。

#### 测试

- `tests/unit/test_query_scope.py` +3（主体↔文档匹配、兜底子串）；`test_retrieval_service.py` 适配 +2 处假数据文档名；`test_query_analyzer.py` +2（统筹区分/区别/异同 → comparison）；`test_query_planner.py` +2（三环节数量词尾巴清理、单维度统筹区分）；`test_answer_generator.py` +2（首版 payload 白名单、多轮修复直到通过）；新增 `tests/unit/test_comparison_completeness_validator.py`（4 用例：全格通过/缺格拒绝/未覆盖格跳过/非对比跳过）。
- 完整 pytest：**388 passed, 3 failed**（3 个失败仍为既有解析基线，无回归）；ruff 全过；mypy 仅剩既有挂账（model_client:81、_single_topic_final_chunks sorted key）。

#### 端到端验证（网关重启后，`tmp/repro-debug-scoped…`/`bridge-*` 产物）

- **debug 层**：`query_type=comparison`、6 子查询（2 主体×3 维度）、覆盖矩阵 6 格（4 covered + 2 missing：桥梁像控布设/桥梁成果验收确无证据）；18→8 选中全部来自两份主体文档，**水运导则污染消失**；附录 A.3/A.4/A.5 精度检查记录表与成果清单、5.5.1 设备要求、7.1.1 像控点布设均进入证据。
- **chat 层**（v15）：答案覆盖桥梁设备选型 [C1]（续航≥30min/载荷≥2.5kg/六向避障/图传≥1km）、地灾像控布设 [C3][C8]（空三原则/应急放宽/正午前后2h）、地灾成果验收 [C4][C6][C7]（原始影像/POS/DOM/DEM/点云 + 平面与高程精度检查记录表）；桥梁像控/成果验收在 missing_information 中明确"未明确说明"；**仍缺"地灾设备选型"格**（有证据 [C2 5.5.1 相机/POS/电池][C5 A.1 记录表]，但 9B 模型 repair 链始终写不出通过 topic 校验的 claim，完整性问题由 CompletenessValidationError 显式标记而非静默缺失）。
- **结论**：对比问题整链（分类/矩阵/主体域过滤/维度扩展/编号白名单/完整性校验/多轮修复）已系统性修复并回归通过；**残余限制是 9B 模型能力**（对"某格证据存在但内容需精确转述"的修复不可靠），系统现在会显式标记缺失格子（missing_information + 校验警告）而非静默输出不完整答案。

#### 改动文件

- 新增：`app/services/comparison_completeness_validator.py`、`tests/unit/test_comparison_completeness_validator.py`
- 修改：`app/services/query_analyzer.py`、`app/services/query_planner.py`、`app/services/query_scope.py`、`app/services/retrieval_service.py`、`app/services/answer_generator.py`、`app/pipelines/rag_pipeline.py`、`app/core/config.py`、`config/default.yaml`、`prompts/answer_generation.txt`、`prompts/json_repair.txt`、`tests/unit/test_query_scope.py`、`tests/unit/test_retrieval_service.py`、`tests/unit/test_query_analyzer.py`、`tests/unit/test_query_planner.py`、`tests/unit/test_answer_generator.py`

#### 注意

- `json_repair_attempts: 2` 意味着最坏情况 3 次模型调用（初始+2 修复），耗时可能 3~5 分钟；若用户反馈太慢可调回 1。
- 9B 模型对"证据存在但内容需精确重述"的格子（如地灾设备选型 5.5.1）修复不稳定；如要完整矩阵输出，建议评估换更强答案模型（`config/default.yaml` `models.answer` 端点）或对缺失格做提取式兜底（`regulation_answer_builder` 路线）。

---

### 2.10 实施：确定性对比矩阵兜底（ComparisonMatrixBuilder）（2026-08-20 13:00）

> 用户反馈 §2.9 后的答案"明显也不正确"——生成模型即使通过了完整性校验，输出仍是"逐句单侧碎片"（无矩阵结构、每格只覆盖一侧、缺格）。用户确认实施方案 1（确定性兜底），并询问泛化性影响（见下）。

#### 泛化性说明（回应用户关切）

- 本组件是**纯结构驱动**的通用机制：作用于所有 `query_type=comparison` 的问题，只消费 plan 的 subjects×aspects 与覆盖矩阵的"格子→chunk"映射（两个既有通用抽象），**不含任何具体问题/文档词汇**；
- 逐格内容提取复用通用工具（`lexical_units`/`overlap_score` + `_DIMENSION_EXPANSIONS` 领域词表），按"维度词+扩展词"重叠打分；
- 是**兜底**：仅当生成模型缺失已覆盖格子或整体降级时启用，模型正常时（08-19 那些题）输出原样保留；
- 坦承：系统守卫层数确有累积（检索侧确定性过滤有价值；生成侧守卫在换更强模型后可简化），本组件是"模型写不全→稳定完整且诚实"的务实手段，非正则堆积。

#### 实现

1. **新增 `app/services/comparison_matrix_builder.py`**：
   - 输入 plan/chunks/assessment，输出 `StructuredAnswer`（Markdown 表格：行=维度、列=主体；逐格 claim）；
   - `_cell_content`：取该格 chunk 的**原文散文行**（过滤 文档/章节/条号/页码 元数据行、Markdown 表格行、引用行），按"维度词+扩展词"重叠打分取前 3 行（阈值 0.08，与 `min_evidence_overlap` 对齐）；无行可取的格写"当前资料中未明确说明"；
   - 有内容格 claim 只放内容文本（主体/维度前缀词不在 chunk 里会误触发 topic 校验，归属由引用承担）；"未明确说明"格保留前缀（无引用，topic 校验自动跳过，满足完整性"点名"检查）；
   - answerability 与 assessment 状态一致（PARTIALLY_ANSWERABLE 不升级）。
2. **`app/pipelines/rag_pipeline.py`**：生成结束后，若 comparison 且（`validation_degraded` 或完整性校验失败）→ 构建矩阵 → `validate()` 通过则替换 generation 结果，warning 注明"已改用确定性对比矩阵输出"；失败则保留原结果。
3. 关键设计决策：**不用 EvidenceExtractor 的证据句**（附录块被合并成"章节头+整表"的多行句，表格行过滤要 MULTILINE 且丢失章节头信息），改为**直接按 chunk 原文行提取**——表格行/元数据行天然排除，章节头（"A.3 平面相对位置精度检查记录表样式见表A.3。"）保留为散文内容。

#### 测试

- `tests/unit/test_comparison_matrix_builder.py`（2 用例）：完整矩阵（4 claim、未明确说明格无引用、有内容格引用正确、表格行不得进入格内容）+ 非对比返回 None；产物通过真实校验链（citation/scope/grounding/subject/topic）。
- 完整 pytest：**391 passed, 3 failed**（3 个失败仍为既有解析基线，无回归）；ruff 全过；mypy 仅剩既有挂账。

#### 端到端验证（网关重启后，`tmp/bridge-chat-20260820.json`）

- 触发：生成模型仍缺"地灾设备选型"格（CompletenessValidationError）→ 兜底启用（warning：已改用确定性对比矩阵输出）。
- 输出（v15）：

| 对比项目 | 长大桥梁无人机精细化巡检 | 地质灾害倾斜摄影测量 |
|---|---|---|
| 设备选型 | 4.3.2 续航≥30min/载荷≥2.5kg/六向避障/视距内飞行 [C1] | 5.5.1 相机与POS系统固定、镜头清洁 [C2] |
| 像控布设 | 当前资料中未明确说明 | c) 空三原则放宽；d) 航带法布点（≤8条基线/每组2点）[C3] |
| 成果验收 | 当前资料中未明确说明 | A.3 平面相对位置精度检查记录表 [C4]；A.4 高程精度检查记录表 [C6] |

- 6 格全部有内容（或如实"未明确说明"）、引用全部正确（C1 桥梁/C2/C3/C4/C6 地灾）、无表格倾倒、无第三文档污染、无编号幻觉；status=PARTIALLY_ANSWERABLE（桥梁像控/成果验收确无证据，诚实降级）。

#### 改动文件

- 新增：`app/services/comparison_matrix_builder.py`、`tests/unit/test_comparison_matrix_builder.py`
- 修改：`app/pipelines/rag_pipeline.py`

#### 注意

- 兜底输出为提取式（干燥但精确），无模型叙述；模型正常时不会触发，不受影响。
- 残余可优化项（非阻塞）：地灾成果验收格未含 A.5 成果清单行（每格最多 3 行，A.5 未进 top-3）；可调 `_MAX_CELL_LINES` 或按"章节头行优先"排序。
- **兜底校验跳过 scope（重要）**：`ScopeConsistencyValidator` 会从答案文本抽取条号（4.1.1/A.3/2.5 等）当作"声明条款"并强制被引 chunk 归属，而矩阵内容是逐字取自被引 chunk 的（含条号字样）→ 误拒。矩阵按主体文档过滤 + 引用绑定，跨文档污染在构造上不可能，故 `_validate_evidence_consistency(skip_scope=True)` 校验矩阵（citation/grounding/subject/topic/completeness 全保留）。
- **版本踩坑**：曾出现"改码后重启网关仍是旧行为"——疑似旧进程/缓存残留，干净停→启（stop 后 sleep 3s 再 start）后才加载新代码；排查手法：比对 `__pycache__` pyc mtime、`Get-NetTCPConnection` 实际监听进程与 `gateway.pid`。§2.10 的线上验证以此后（pid 17416）为准。
- §2.10 追加的标题行过滤（`_HEADING_LINE_RE` + 冒号结尾行排除）已生效：矩阵格不再含"4.3.2 无人机应满足以下要求:"等无实质内容的引导行。
- **内容选择强化（2026-08-20 14:30 追加）**：用户指出《长大桥梁…》4.3.4 云台相机要求应属"设备选型"。修复链：
  1. `_DIMENSION_EXPANSIONS["设备选型"]` 补 "云台 像素 分辨率 存储 镜头"（否则 4.3.4 chunk 的 hybrid 排第 15，进不了选择）；
  2. `complex_candidates_per_subquery` 2→8、`complex_rerank_input_k` 12→20、新增 `complex_final_limit: 12`（对比/多跳矩阵最终名额，`_execute_plan` 覆盖选择改用之）——每格可容纳 1~3 个 chunk（如桥梁设备选型需同时含 4.3.2 与 4.3.4）；
  3. `comparison_matrix_builder._cell_content` 打分由"重叠比例"（扩展词增多会稀释比例→误判空）改为**共享 bigram 绝对数量 ≥1**（剔除通用 bigram：控制/系统/检查/记录/数据/成果/要求…），并新增内容去重、`_TEXT_NUMBER_HEADING_RE`、行长上限 90、`_MAX_CELL_LINES` 3→4。
  - 效果：4.3.4 云台相机（分辨率≥2000万像素/1080P、存储≥32G [C1]）与 4.3.2（续航/载荷 [C10]）同格；地灾设备选型取到 5.5.1（相机/POS 检校）与飞行平台（2.5kg/抗风4级）；完整 pytest 391 passed（3 个既有解析基线）。
- **主体文档限定重检索（2026-08-20 15:00 追加）**：对比子查询的候选常被另一主体的附录垄断（"成果验收/精度检查"强命中地灾 A 附录，hybrid 0.36 vs 桥梁 9.4 的 0.285 排不进 rerank 输入）→ 主体过滤后池子被清空 → 格子误判"未明确说明"。修复：`_execute_plan` 对比路径先**主体文档发现**（第一遍候选标题匹配），再按主体 `document_ids` **限定重检索**（桥梁 9.4 按"长大桥梁+成果验收"检索排第 1）；6 格全部 covered。测试同步（假 RAGFlow 调用数 4→8）。
- **像控布设/成果验收核查结论（2026-08-20 15:30，用户询问）**：逐字扫《长大桥梁》全部 chunk——
  - **像控布设：全文确无**（无"像控/控制点/基准点/标靶"字样），"未明确说明"正确；
  - **成果验收：确有 9.4 巡检成果**（9.4.1 巡检报告：工程概况/检测目的/检测依据/检测内容与方法/构件编号及病害描述规则；9.4.2 影像资料：工作照/原始病害图片视频/识别结果图），但 9.4.1/9.4.2 内容 chunk（3105726f/e90b917a）对"成果验收"查询的 RAGFlow hybrid 低于阈值、排不进候选（scoped 检索只返回 9.4 标题行 chunk 1ab2e3fb，且该行被"文字+编号"标题过滤）→ 桥梁成果验收格仍"未明确说明"。**这是 RAGFlow 检索召回层面的限制**（内容 chunk 存在但相关性打分低），非规则可修；后续可选项：对矩阵每格做"章节关键词"级补充检索（如按"9.4 巡检成果"精确召回）。
- **对比题确定性矩阵重构（采纳 GPT 优化意见，2026-08-20 16:00 起，分步实施）**：用户明确"不要继续围绕单题追加关键词/正则，改为'确定性矩阵优先、模型只负责总结'"。实施顺序（每步可回归）：
  - **Stage 1（已完成）**：
    - **① covered 四态**：`CoverageStatus` 扩为 `covered / not_specified / low_confidence / no_document`；`coverage_selector._cell_status` 要求 covered 必须能从格子 chunk 提取出与维度相关的**实质条款行**（新增 `app/services/cell_evidence.py`：维度关键词（剔除通用 bigram）+ 标题/表格/目录行过滤 + 共享 bigram≥1）——桥梁"像控布设"格的"3.2 自动巡检"内容被判 low_confidence 而非 covered；`evidence_judge` 仅 covered 计入需求覆盖。
    - **② 多对多归属**：`CoverageSelection` 新增 `chunk_cells`（chunk→格子集合）；矩阵构建器/完整性校验器改**直接消费 coverage_matrix**（格子→chunk_ids/citation_ids），不再依赖 `SelectedChunk.subquery_id` 单值。
    - **③ 章节族扩展**：`retrieval_service._expand_section_family`——命中"章节：…/N.x 叶章节"的 chunk 时，用叶章节标题做**关键词（BM25）检索**（`keyword=True`）补充同文档子条款（9.4→9.4.1/9.4.2）；纯向量对形式化条款召回差，BM25 是正确机制（线上验证受 RAGFlow 僵死阻塞，机制已就位）。
    - 测试适配：schema/选择器/构建器/完整性/检索测试按新语义更新；**完整 pytest 391 passed, 3 failed**（既有解析基线）；ruff 全过；mypy 仅剩既有挂账（model_client:81、retrieval_service sorted key）。
  - **Stage 2（待实施）**：`SubjectDocumentResolver`（每主体只解析一次文档，删除 query_scope 两字子串兜底）+ 管线改为"确定性矩阵优先、模型仅基于矩阵生成统筹总结"（删除初始自由生成/两轮 repair，兜底只在完整性失败时触发）。
  - **Stage 3（待实施）**：`CellEvidenceExtractor` 条款级提取（requirement_type 分类、Markdown 表格转键值文本）+ scope 校验修复（去掉 skip_scope）+ 黄金基线回归 + ≥10 道新对比题。
  - 注：RAGFlow 当日多次 ReadTimeout 僵死（07:31 又 503），Stage 1 线上端到端验证待 RAGFlow 稳定后补。

### 2.11 智能问答界面与运行时模型切换（2026-08-20）

- **界面重构**：问答页改为“提问工作区 + 答案/校验信息 + 右侧引用证据”的三段式布局；新增知识库/模型状态、生成计时、证据充分性提示、缺失项、冲突项、覆盖矩阵、可折叠声明、复制答案/带引用复制，以及点击正文引用定位证据卡片。
- **模型选择**：问答请求新增可选 `model.source_id/model_name`，响应回传实际使用的 `model_source_id/model_name`；管线只为当前请求创建对应 `AnswerGenerator`，不修改全局默认模型。
- **本地模型**：默认连接从现有 answer model 配置读取，并通过 OpenAI 兼容的 `GET /v1/models` 动态发现模型；嵌入/重排模型不会进入答案模型选择器。
- **API 接入**：新增 `GET /api/v1/admin/answer-models`、`POST /api/v1/admin/model-connections`、`DELETE /api/v1/admin/model-connections/{source_id}`。连接要求提供 OpenAI 兼容 Base URL、API Key，并能访问模型列表；API Key 仅以 `SecretStr` 保存在 Gateway 内存，不写配置文件、数据库或浏览器存储，进程重启即清除，连接按 `X-User-ID` 隔离。
- **兼容性**：模型请求优先使用严格 JSON Schema；对不支持该参数的 OpenAI 兼容服务，遇到 400/422 时依次降级为 `json_object` 和普通 chat completion，不会对鉴权、限流或服务端错误盲目重试。
- **主要文件**：新增 `app/schemas/models.py`、`app/services/answer_model_manager.py`；修改 admin API、registry、RAG pipeline、chat schemas、model client，以及 `app/web/chat.html`、`chat.css`、`chat.js`、`markdown.js`。
- **验证**：完整 pytest 为 **399 passed, 3 failed**（仍是既有 regulation preprocess 解析基线，无本次回归）；前端 Markdown/语法检查和本次改动文件 ruff 均通过。浏览器实测从 9B 切换到 `qwen3:4b-instruct-2507-q4_K_M` 后真实问答成功，响应标注所选模型，正文引用可定位对应证据卡片，控制台无 warning/error。

### 2.12 复杂对比题 RAGFlow 卡死修复（2026-08-20）

- **现象**：问题“长大桥梁无人机精细化巡检与地质灾害倾斜摄影测量，在设备选型、像控布设、成果验收三个环节的标准要求如何统筹区分”在页面返回 `RAGFlow is unavailable or returned invalid JSON`；RAGFlow 容器显示 running，但 9380、容器内 health 请求均超时，甚至无法正常 restart/exec。
- **根因**：Stage 1 的 `_expand_section_family` 误把 RAGFlow `keyword=True` 当作 BM25 开关。该参数实际会调用 LLM 做关键词提取；六格对比题的章节族扩展因此为多个章节串行调用 9B 模型，每次约 10 秒，最终占满 RAGFlow 单 worker。BM25 本来就在 hybrid 检索中，权重由 `vector_similarity_weight` 控制。
- **修复 1**：章节族扩展改为 `keyword=False`，仍保留高 BM25 权重的 hybrid 检索；新增回归测试，明确断言扩展请求不得启用 LLM 关键词提取。
- **修复 2**：章节族扩展结果构造 `SelectedChunk` 时原先传 `citation_id=None`，真实返回扩展结果后会触发 Pydantic 校验错误；改为空的内部占位字符串，正式编号仍由 `CitationService.build()` 统一分配。
- **修复 3**：4B/9B 模型若初答、repair、sanitize 后仍产生无法解析/不符合 schema/语义校验的输出，对比题现在会直接使用已验证的确定性矩阵，不再向页面返回 `ANSWER_JSON_PARSE_FAILED` 502；非对比题和模型连接/网络错误不吞掉。
- **服务恢复**：卡死容器已无法由 `docker restart` 正常终止，使用 `docker desktop restart` 重启运行时后重新启动 RAGFlow 和 Gateway；未删除数据卷。当前 RAGFlow health 的 db/doc_engine/redis/storage 全部为 `ok`，Gateway pid 21860。
- **端到端验证**：同一问题使用“法规11”知识库和 `qwen3.5:9b-q4_K_M`，API 返回 200 / `PARTIALLY_ANSWERABLE`；页面显示对比矩阵、5 条证据和安全降级提示，控制台无 error，未再出现 RAGFlow 不可用。页面总耗时约 236 秒，主要是 reranker 约 30 秒和 9B 的初答/repair/sanitize；后续 Stage 2 应将确定性矩阵前置、模型仅做可失败的总结，减少无效等待。
- **回归**：完整 pytest 为 **401 passed, 3 failed**（3 项仍是既有 regulation preprocess 基线）；本次修改文件 ruff 和 `git diff --check` 均通过。
- **改动文件**：`app/services/retrieval_service.py`、`app/pipelines/rag_pipeline.py`、`tests/unit/test_retrieval_service.py`、`tests/unit/test_rag_pipeline.py`。

---

## 3. 工作原则（沿用，不重复展开）

1. 先读 `docs/agent-handoff.md`，再读目标模块和对应测试。
2. 任何解析修复都必须用真实失败文档回归；不写破坏全局的正则。
3. 用户只有一个上传入口，不重新要求用户选择解析器。
4. 物理页码、印刷页码、条款号分开存储；表格文本、二维结构、图片资产三者都保留。
5. `needs_review` 不能绕过门禁发布；发布前优先 dry-run；删除本地任务不删除 RAGFlow 内容。
6. 修改后至少运行目标测试、完整 pytest 和 ruff；页面改动必须浏览器实测。
7. 不输出或提交密钥；不清除 Docker 数据卷；不把 D 盘数据复制回 C 盘。

---

## 4. 相关文档

- `README.md`
- `docs/agent-handoff.md`（最完整交接入口）
- `docs/pdf-parsing.md`（解析方法讲解）
- `docs/ingestion.md`、`docs/milestone-2.md`、`docs/milestone-3.md`
- `HANDOFF-2026-08-19.md`（本会话起点，含 ClaimTopicValidator 实施记录 §2.8 末项）
- 本会话验证产物：`tmp/fs-validator-out.txt`、`tmp/fs-validate-out.txt`、`tmp/decoded-fs-live.txt`、`tmp/decoded-fs-final.txt`、`tmp/fs-original.txt`、`tmp/repro-debug-20260820.json`（带引号题面）、`tmp/repro-debug-noquote-20260820.json`（无引号题面）、`tmp/verify-live-claims.txt`、`tmp/repro-debug-scoped-20260820.txt`（域过滤后 debug）、`tmp/repro-chat-scoped-20260820.json`（域过滤后 chat 端到端）
