# Internal RAG 改动记录（2026-08-18）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-17.md` 之后）的工作。
> 项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。
> 本文档按日期逐节追加改动/验证记录，沿用既有格式：问题 / 根因 / 修复 / 验证 / 改动文件 / 注意。

---

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-17.md`（已覆盖：修订决定/修订说明页混入检索索引的修复 `is_probable_revision_page()`、CCAR-27-R2 回归验证、Reranker `ubatch` 过小导致 fallback 的附带发现）。
- **本会话范围**：自 2026-08-18 起的活动。本会话开始以熟悉工程、核对当前状态为主，尚未产生代码改动；本交接文档自本次起按日期逐节追加后续改动/验证记录。
- **重要**：本会话的改动/验证记录在下方第 2 节中按日期逐节追加，不以计划项代替完成结果。

---

## 1. 当前状态快照（2026-08-18）

以下为本次会话实际核对的快照（非引用旧文档）。

### 1.1 环境与代码

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`、`http://127.0.0.1:8080/chat`
- 服务探测（2026-08-18 实测，沙箱 HTTP 探活）：

| 服务 | 端口 | 状态 |
|---|---|---|
| Gateway | `127.0.0.1:8080` | ✅ `/health` 200 `{"status":"ok",...}` |
| RAGFlow | `127.0.0.1:9380` | ✅ `/api/v1/system/healthz` 200 `{"db":"ok","doc_engine":"ok","redis":"ok","storage":"ok"}` |
| Ollama | `127.0.0.1:11434` | ✅ 端口开放 |
| Reranker | `127.0.0.1:8003` | ✅ 端口开放（注意：08-17 发现其 `ubatch` 过小导致实际 fallback，见 08-17 交接） |
| MinerU | `127.0.0.1:8886` | ❌ 关闭 |

- 结论：宿主机主要服务在线；MinerU 关闭，涉及 OCR/远程解析的作业前需先启动 `.\deploy\mineru\start.ps1`。

### 1.2 Git 状态

- 仓库已于 08-17 建立基线提交（`git log` 有 3 个 commit：`Initial commit` ×2 + `Merge GitHub repository initialization`），不再是未跟踪状态。
- 工作区仍有未提交改动（截至 2026-08-18）：

| 文件 | 内容 |
|---|---|
| `app/ingestion/pipeline.py` | 修订页排除 `is_probable_revision_page()`（08-17 改动） |
| `tests/unit/test_pipeline.py` | 修订页检测新增 9 个用例 |
| `README.md` | 08-17 重写精简 |
| `HANDOFF-2026-08-17.md` | 08-17 交接内容补充 |

- **禁止** `git reset --hard` / `git clean` / `docker compose down -v`。

### 1.3 真实入库数据（D 盘实测统计）

- `data\ingestion\jobs\`：96 份 `job.json`（每份为独立目录）。
- 任务状态分布：

| 状态 | 数量 |
|---|---|
| `warning` | 74 |
| `completed` | 21 |
| `needs_review` | 1 |
| `failed` | 0 |

- 发布状态：全部 `published`（未逐一复核，沿用 08-17 统计口径）。

### 1.4 测试基线

- 08-17 实测：完整 pytest `286 passed, 3 failed`；3 个失败仍为既有解析问题（noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重），与近期改动无关。
- 08-18 本会话尚未重跑 pytest；重跑命令：
  ```powershell
  Set-Location D:\internal-rag\source\internal-rag
  .\.venv\Scripts\python.exe -m pytest
  .\.venv\Scripts\python.exe -m ruff check app tests
  .\.venv\Scripts\python.exe -m mypy app
  ```

---

## 2. 改动记录（按日期逐节追加）

### 2.1 复现并定位：跨规章语义混淆——"25.981 燃油箱点燃防护"回答串入 CCAR-26 内容（2026-08-18）

#### 问题

对问题"对于运输类飞机燃油箱点燃防护的要求是什么？"的智能问答回答如下（引用 [C4][C5][C6]）：

> 根据《运输类飞机适航标准》第 25.981 条，对于所有其他燃油箱（即非设计成通常为空且位于机身轮廓线以内的），FRM 必须满足附录 M 除 M25.1 之外的所有要求 [C4]。同时，机队平均可燃性暴露水平不得超过 7% [C4]。对于设计成通常为空的且任一部分位于机身轮廓线以内的燃油箱，其必须满足《运输类飞机适航标准》附录 M 的可燃性暴露水平标准 [C4]。此外，评估的燃油箱必须进行可燃性暴露分析，以确定所评估飞机和燃油类型的机队平均可燃性暴露 [C5]。该分析需依据附录 N 的要求执行 [C6]。

用户指出正确出处应在 CCAR-25-R4《运输类飞机适航标准》PDF 第 123 页第 25.981 条。

#### 核验（本会话完成，非引用旧文档）

- **PDF 第 123 页第 25.981 条原文**（`source.pdf` f517bf12… 第 123 页，pypdf 提取）：
  - (a) 在可能因燃油或其蒸气点燃导致灾难性失效的燃油箱/系统内任一点不得有点火源存在，须通过三种方式表明（最高温度低于最低自燃温度有裕度 / 各部位温度验证 / 单点失效及失效组合分析）。
  - (b) 每一燃油箱机队平均可燃性暴露时间不得超过附录 N 定义 FEET 的 **3%**（或所评估机型机翼油箱可燃性暴露时间，取大者）；(b)(1) 按附录 N 确定；(b)(2) 除主燃油箱以外任何部分位于机身轮廓线以内的燃油箱须满足附录 M 可燃性暴露标准。
  - (c) (b) 不适用于采用减轻燃油蒸气点燃影响措施(IMM)的燃油箱；(d) 须建立 CDCCL。
- **回答内容实际来自 CCAR-26《运输类飞机的持续适航和安全改进规定》第 26.33 条(c)款**（已从 CCAR-26 入库 chunk 核实原文）：
  - "(c) 设计更改 对于机队平均可燃性暴露水平**超过7%**的燃油箱，必须采用以下设计更改之一：(1) FRM… (i) 设计成通常为空的且任一部分位于机身轮廓线以内的燃油箱必须满足《运输类飞机适航标准》附录M的可燃性暴露水平标准。(ii) 对于所有其它燃油箱，FRM必须满足《运输类飞机适航标准》附录M**除M25.1之外**的所有要求，并且使得机队平均可燃性暴露水平不得超过7%。"
- **"除 M25.1 之外"在 CCAR-25-R4 全文 0 命中**（document_ir 全文扫描）；"7%"在 CCAR-25-R4 中只出现在附录 M 第 259 页 Monte Carlo 表格（"按满足 CCAR-26 的 7% 的要求"），不是 25.981 的标准。
- **检索复现**（沙箱 → 网关 `172.16.10.254:8080/api/v1/retrieval/debug`，dataset `b5ecab6a…`，同题）：30 候选 / 选中 6；候选同时含 CCAR-25-R4 25.981（页码123-124）与 **CCAR-26 26.33**（页码6-8）与附录 N N25.3（页码256-257）；`reranker_fallback=True`（ubatch 问题仍在）。

#### 根因

**跨规章语义混淆，属于已挂账的 P0"证据适用范围判定"（task_type/applicable_subject/normative_level）缺失**：
1. 检索端：CCAR-25 25.981 与 CCAR-26 26.33 都命中"燃油箱可燃性"，被同时召回（本属正常，二者确实相关）。
2. 生成端：模型把 CCAR-26 26.33(c) 的 FRM/附录M除M25.1/7%/"通常为空"内容抄入答案，并**错误归因到"第 25.981 条"**；且把 7% 表述为 25.981 的机队平均可燃性暴露标准（正确值是 3%）。
3. 校验端：`CitationValidator` 只校验引用编号存在/合法（citation_ids 非空、存在），**不校验"被引 chunk 的规范适用范围是否与主张所指规章一致"**；`EvidenceJudge` 无 scope 一致性信号。
4. 附带：Reranker `ubatch` 过小仍致 fallback（沿用 08-17 发现的遗留项，非本次根因）。

#### 修复方案（已按分层实施，见 §2.2）

方案 A：规范性范围感知（normative scope awareness）三层改造；用户确认"分层推进"后已实施第一、二、三层，记录见 §2.2。

#### 验证记录要点

- 出处在 PDF 第 123 页 25.981（用户定位正确）；回答前两句是 26.33(c)(1) 的改写，最后一句（可燃性暴露分析依据附录 N）符合 25.981(b)(1)/附录 N N25.1，正确。
- 逐句核对结论：核心数值错（7% vs 3%）、出处张冠李戴（26.33→25.981）、遗漏(a)款点火源根本要求、适用范围限定（"通常为空"）是 CCAR-26 提法非 25.981。

#### 注意

- 本条目为**复现/定位记录**；修复实施见 §2.2。
- 可作为后续"适用范围判定"P0 的真实回归样例入库。

---

### 2.2 实施"规范性范围感知"三层改造（2026-08-18）

> 针对 §2.1 跨规章语义混淆，按用户确认的"分层推进"方式实施。核心设计原则：**把"归因一致性"做成通用机制，而非针对 CCAR-25/26 的特殊规则**——只在问题或回答显式点名条号时收紧，且仅约束"引用内容必须与主张所指规范一致"。

#### 第一层：证据携带出处信息

- `app/schemas/chat.py`：`EvidenceSentence` 增加 `document_name`、`version` 字段（可选，默认 None）。
- `app/services/evidence_extractor.py`：`EvidenceExtractor.extract()` 从 chunk metadata 填充上述字段。
- 效果：模型在生成时能看到每条证据属于哪份文件/规章，具备做归因判断的信息基础。
- 测试：`tests/unit/test_evidence_extractor.py` 新增 2 个用例（出处字段透传、缺失时为 None）。

#### 第二层：提示词通用归因规则

- `prompts/answer_generation.txt`：新增第 25 条——"若问题明确指定条款、条号或文件，回答必须围绕该条款/文件展开；不得把其他文件/规章的内容归因到问题所指的条款。引用其他文件内容时，必须如实标明其真实出处（如写出该文件/规章名称），不得混称。"（通用表述，不含具体规章名）。
- `app/core/config.py`：`prompt_version` bump 至 `answer-generation-v2`。

#### 第三层：确定性 scope 一致性校验器（核心安全网）

- 新增 `app/services/scope_validator.py`：`ScopeConsistencyValidator`。
  - **触发条件**：`query.original_query` 或 `answer.answer` 中提取到显式条号才启用；无条号点名时完全不做约束（保障开放式/对比/跨文档题不受影响）。
  - **判定规则**：主条号 = 查询声明 ∪ 回答正文声明。每个被引 chunk 必须满足其一：① chunk 头部 `条号：` 行（权威条号）属于主条号；② chunk 头部 `文档：` 行属于"包含主条号的文档"（由权威条号==主条号的 chunk 确定 primary document）；③ claim 文本显式点名 chunk 的权威条号（诚实交叉引用）；④ 无头部信号的遗留格式 chunk 文本含主条号 alias（兜底）。
  - **保留合法场景**：同文档内的附录/子条款（文档归属一致）；诚实交叉引用（claim 显式点名另一条号）。无任何头部信号且无法判定的 chunk 保守放行（不误报）。
  - 违反时抛 `ValueError`，经生成器的一次 repair 让模型自行修正归因。
- `app/pipelines/rag_pipeline.py`：接入 `validate()` 链（`_validate_answerability` → `CitationValidator` → `ScopeConsistencyValidator`）。
- `app/core/config.py`：新增 `enforce_scope_consistency: bool = True` 开关，可应急关闭。

#### 补充修复（08-18 实际调用发现，见 §2.3 记录）

- **M25.1 绕过漏洞**：初版以"chunk 文本是否含声明条号"判定文档归属，被跨规章共有的附录条号（如 M25.1 同时出现在 CCAR-26 26.33 原文）绕过，导致 CCAR-26 内容仍被归因到 25.981。已重写为以 chunk 头部 `条号：`/`文档：` 权威条号判定（`app/services/scope_validator.py`）。
- **repair 通道增强**：repair 的 user payload 增加 `citation_documents`（citation_id → document_name/version），`prompts/json_repair.txt` 增加按出处修正归因的指引，避免 scope 拦截后 repair 失败导致 `ANSWER_MODEL_INVALID_JSON`（前端显示为 "ValueError"）。
- **config 同步**：`config/default.yaml` 的 `generation.prompt_version` 同步为 `answer-generation-v2`（此前 yaml 覆盖使 v2 未生效）。

#### 验证

- 单测（沙箱 Python 3.10 可运行部分）：`test_scope_validator.py` 8 个用例全过（含 M25.1 绕过回归）；`test_evidence_extractor.py`、`test_citation_validator.py`、`test_answer_generator.py`（含 repair payload 出处断言）全过；ruff 全过。
- 管道集成用例 `tests/unit/test_rag_pipeline.py::test_pipeline_repairs_cross_regulation_scope_violation`（构造 25.981 主张 + CCAR-26 chunk 引用，断言经 repair 后只保留 CCAR-25 chunk）——需在 Windows 宿主 Python 3.11+ 环境运行完整 pytest 确认。
- **注意**：沙箱仅 Python 3.10（项目要求 ≥3.11），无法在沙箱跑完整 pytest/mypy；完整基线 `286 passed, 3 failed` 需在宿主重跑：
  ```powershell
  Set-Location D:\internal-rag\source\internal-rag
  .\.venv\Scripts\python.exe -m pytest
  .\.venv\Scripts\python.exe -m ruff check app tests
  .\.venv\Scripts\python.exe -m mypy app
  ```

#### 待办 / 遗留

- **宿主服务需重启**以加载重写后的 scope_validator、repair 增强与 v2 提示词（`http://127.0.0.1:8080` 网关 + RAGFlow，见 §2.3）。
- 用 §2.1 真实问题（"对于运输类飞机燃油箱点燃防护的要求是什么？"）在宿主端到端复测：断言答案中不再出现 7%、无 26.33 归因、保留 3% 与 (a) 款点火源要求。
- 沙箱 mypy 报 INTERNAL ERROR（Python 3.10 环境问题），宿主上需跑 mypy 确认新模块类型无误。

---

### 2.3 实际调用暴露的问题与修复（2026-08-18）

> 宿主 `http://127.0.0.1:8080/chat` 用户报"回答请求失败：ValueError"，经沙箱复现定位以下问题并修复。

#### 问题一：M25.1 绕过漏洞（已修复）

- **现象**：复现真实问题时返回 200，但 claim 3 仍把 CCAR-26 26.33 的"7%/FRM/通常为空"内容归因到 25.981 语境，scope 校验未拦截。
- **根因**：初版 `ScopeConsistencyValidator` 用"chunk 文本是否含声明条号"判定文档归属。claim 文本顺带提到"M25.1"，而 CCAR-26 26.33(c) 原文也含"M25.1"，于是 CCAR-26 的 chunk 被误判为"属于 M25.1 的文档"而放行。
- **修复**：改为以 chunk 头部 `条号：`/`文档：` 行的**权威条号**判定文档归属；`条号：` 为 26.33 的 chunk 不再因顺带提到 M25.1 而放行。保留"claim 显式点名 chunk 权威条号"的诚实交叉引用通道。
- **验证**：`tests/unit/test_scope_validator.py::test_rejects_cross_regulation_content_attributed_to_named_article` 覆盖该场景（claim 提到 M25.1 仍拒绝 CCAR-26 引用）。

#### 问题二：scope 拦截后 repair 失败导致 "ValueError"（已缓解）

- **现象**：scope validator 抛 `ValueError` 后，生成器走一次 repair；repair 的 user payload 只有 `allowed_citation_ids`，模型看不到每个 citation 的出处，无法正确修正归因，repair 仍失败 → `AppError(ANSWER_MODEL_INVALID_JSON)`，`details.error_type` 为 `ValueError`，前端显示"回答请求失败：ValueError"。
- **修复**：`app/services/answer_generator.py` 的 repair user payload 增加 `citation_documents`（citation_id → document_name/version）；`prompts/json_repair.txt` 增加"依据 citation_documents 修正归因，无法正确归因的引用必须删除"的指引。

#### 问题三：RAGFlow 检索链路僵死/超时（待宿主处理）

- **现象**：沙箱复现时 `RAGFlow is unavailable ... ReadTimeout`；直接探测 `172.16.10.254:9380` 也超时（healthz、datasets 均超时），判定 RAGFlow 服务僵死（类似 08-11 事故）。
- **影响**：检索阶段 10s 超时返回 503，所有依赖检索的 chat/retrieval 请求不可用。与问题一/二的 ValueError 叠加，用户侧表现为各类请求失败。
- **处理**：需在宿主重启 RAGFlow 容器（参考 08-11 恢复记录），再重启网关服务加载修复代码。

#### 问题四：模型绕过条号点名、改为文件级归因（已修复）

- **现象**：宿主重启后重测，回答开头只声明"根据《运输类飞机适航标准》（CCAR-25-R4）"，未点名任何条号；随后 claim 2 仍把 CCAR-26 26.33 的"7%/FRM/通常为空"内容列为 CCAR-25-R4 的要求（引用 [C4]，chunk 文档行 `文档：CCAR-26...` 条号 26.33）。用户判定"不是正确答案"。
- **根因**：scope 校验器只在"查询或回答显式点名条号"时触发。模型这次**不点名条号、只声明文件**，`declared_articles` 为空 → 条款级校验放行，且当时无文件级校验。
- **修复**：`app/services/scope_validator.py` 新增**文件级归因一致性**：
  - 从 `query.original_query` + `answer.answer` 提取 CCAR 文件标记（如 `CCAR-25-R4`、`CCAR-26`）作为 `declared_files`；从 chunk `文档：` 头（兜底 `document_name`）提取 chunk 文件标记。
  - claim 引用 chunk 的文件标记不在 `declared_files` 且 claim 未点名该文件 → 拦截。
  - 兼容：对比题 query 声明多文件（如 CCAR-25 与 CCAR-26）→ 都放行；claim 显式点名 chunk 文件（诚实交叉引用）→ 放行；chunk 无文件信号 → 保守放行。
  - 条款级与文件级**解耦**：条款级因"证据池无对应权威条号"无法判定时不再整体跳过文件级（修复了一个 early-return 缺陷）。
- **验证**：`tests/unit/test_scope_validator.py` 新增 3 个用例（11 个全过）；用用户真实三 claim 场景（C3=25.963 / C4=26.33 / C6=N25.3）复测：C4 被拦截、C3/C6 放行。

#### 问题五：25.981 的 3% 标准未进入证据（提取配额，已修复）

- **现象**：宿主重测回答仍漏 25.981 的 (a) 点火源防护与 (b) 3% 标准，且把附录 N 表 5 转述的"CCAR-26 的 7%"误当 25.981 标准。
- **核查**（沙箱读 `chunks.jsonl`）：25.981 被切成两个 chunk——第 344 行含 (a)(b)（3%），第 345 行含 (iii)(c)(d)。回答引用的 [C1] 是第 345 行，天然不含 (a)(b)。
  - 用真实 chunk 跑 `EvidenceExtractor`：`max_sentences_per_citation=3` 时 3% 那句被"标题句+(a)款+(a)(2)款"挤出（标题句在两 chunk 重复各占配额）；提到 5 后 3% 与 (b) 款均进入 evidence。
  - (a) 款本身能进 evidence，但模型未使用（**生成端忠实性问题**，未在本项修复范围内）。
  - 7% 确认来源：仅附录 N `N25.4 表 5` 的"按满足 CCAR-26 的 7% 的要求"列，模型把它误引为 25.981 标准（**数值级误归因**，需要内容正确性校验，未在本项修复范围内）。
- **修复**：`config/default.yaml` 与 `app/core/config.py` 的 `max_sentences_per_citation` 3→5（用户确认"A: 提升句子配额"）。验证：真实 chunk 模拟下 evidence 从 9 句增至 14 句，3% 与 (a) 款均在场；24 个沙箱单测全过。
- **决策（用户确认"保持现状"）**：分块策略不改。25.981 等长条款按字符预算（MAX 1400）被拆成多 chunk、可能在款中间切断，属设计权衡，接受现状；依靠提取层补偿（`max_sentences_per_citation=5`）把同一 `条号` 的多 chunk 句子带进 evidence。不改为"按款切分"，不重新解析/重新发布。
- **遗留**：生成端忠实性（(a) 款进了但模型没用）与数值级误归因（7%）未处理，建议后续按 §2.2 待办评估。

#### 问题六：7% 挂到 C1 引用——"引用-内容脱钩"（引用忠实性校验，已修复）

- **现象**：宿主重启生效全部改动（`max_sentences_per_citation=5`、scope 校验、v2 提示词）后重测，回答已含 (a) 点火源防护与 (c) IMM、引用全来自 CCAR-25-R4，但"7%"仍出现、"3%"仍缺失。
- **核查**（宿主实时检索 debug）：6 个选中 chunk 中 C2 含 3%（25.981 (a)(b)），**C4 是 CCAR-26 26.33 且含 7%**。回答引用 [C1]（25.981 (iii)(c)(d)，无 7%），但 claim 里写"不超过 7%"——模型从 C4 读到 7%，却把引用挂到 C1。这是**引用-内容脱钩**：
  - `ScopeConsistencyValidator` 只校验引用的 chunk 是否在范围内（C1 合法，不拦）；
  - `CitationValidator` 只校验编号合法性（不拦）。
- **修复**：新增 `app/services/grounding_validator.py` `ClaimGroundingValidator`——**claim 中出现的数值（百分比/带单位）必须能在其引用的 chunk 文本中找到**，否则拒绝触发 repair。通用机制，不依赖规章字典；复用 `evidence_text` 数值提取思路，保守（只校验明确数值标记，允许数值出现在任一被引 chunk）。`app/core/config.py` 新增 `enforce_claim_grounding: bool = True`，接入 `RagPipeline` validate() 链。
- **验证**：`tests/unit/test_grounding_validator.py` 7 个用例全过（含全角%/大小写单位归一化、多 chunk 任一命中、未知 citation 跳过）；用真实 chunk 复测用户 4-claim 回答：claim 2 的"7%"被拦截，其余通过。沙箱 31 个单测全过、ruff 全过。
- **残留风险**：若模型把 7% 挂到附录 N `N25.4 表 5` chunk（同时含 3%/7% 列）则数值校验通过——但该 chunk 当前未被选中进 evidence，实际风险低；彻底解决需"关键条款→权威数值"字典，未实施。

#### 问题七：门禁拦截后 repair 无据可改导致 ValueError（已修复）

- **现象**：用户再次报"请求失败：ValueError"。根因链：门禁（scope/grounding）拦截后走一次 repair，但 repair 的 user payload **只有 `allowed_citation_ids` + `citation_documents`（文档名），没有 evidence 原文文本**。模型在 repair 时看不到 C2 里的"3%"正文，无从把错误的"7% 挂 C1"改成"3% 挂 C2"，repair 必然失败 → `ANSWER_MODEL_INVALID_JSON`（`details.error_type=ValueError`）→ 前端显示"ValueError"。
- **修复**：`app/services/answer_generator.py` 的 repair user payload 增加 `evidence`（每条含 `citation_id/chunk_id/text/document_name/version`）；`prompts/json_repair.txt` 增加指示——依据 evidence 原文找出支撑数值的正确 citation，找不到支撑的数值/事实必须删除。
- **验证**：真实 chunk 模拟完整链路——首次 7% 挂 C1 被 grounding 拦 → repair 拿到 evidence 原文 → 修正为 3% 挂 C2 → 通过，`repaired=True`；`test_answer_generator.py` 断言 repair payload 含 evidence；沙箱 22 个相关单测全过、ruff 全过。
- **注意**：真实模型能否在 repair 时正确利用 evidence 文本取决于模型能力；本修复保证通道**有据可改**，若仍频繁失败需另查（如模型过弱、evidence 里本就不含正确数值）。

#### 遗留

- 宿主重启后按 §2.2 待办端到端复测。
- RAGFlow 若反复僵死，建议排查其资源占用/日志（见 `logs/ragflow/` 与 08-11 交接记录）。


### 2.4 修复法规问答 ValueError，并增加严格安全降级与原文回答（2026-08-18）

#### 用户问题与最终结果

- 用户要求修复智能问答中“运输类飞机燃油箱点燃防护的要求是什么？”返回 `请求失败：ValueError`，并要求在保证答案正确的情况下正常输出。
- 最终宿主端到端请求：`f50bccc0-8a82-447b-aecc-98d34cb27027`。
- 最终结果：HTTP 200、`ANSWERABLE`、约 35.93 秒；答案直接按 CCAR-25-R4 第 25.981 条原文列出 (a)～(d)，包含正确的 `3%`，不含 `7%`，引用仅为 CCAR-25-R4 的 C1/C2；`json_repaired=false`、`validation_degraded=false`。
- 同题检索 debug：约 35.94 秒，`reranker_used=true`、`reranker_fallback=false`、选中 6 个 chunk。

#### 根因复核

1. 生成模型会把 CCAR-26 第 26.33 条的 `7%/FRM` 内容混入 CCAR-25 第 25.981 条答案；scope/grounding 门禁拒绝后，模型的单次 repair 仍可能失败，旧逻辑将其统一包装为 `ANSWER_MODEL_INVALID_JSON`，前端仅显示 `ValueError`。
2. Reranker 使用 llama.cpp 默认物理批次 `ubatch=512`，法规长 chunk 输入达到 707 tokens 时返回 500；提高批次后，30 个候选在 CPU 上又会超过原 30 秒超时，继续触发 fallback。
3. 即使移除跨规章证据，模型仍可能扩大逻辑限定（真实复测曾把“本条 (b) 不适用”错误扩写为“点火源和温度要求不适用”），因此仅依赖提示词和模型改写无法保证法规条款忠实性。

#### 实施改动

1. **逐 Claim 确定性安全降级**（`app/services/answer_generator.py`、`app/pipelines/rag_pipeline.py`）：
   - 初次生成和一次模型 repair 都未通过语义校验时，不再立即返回 502。
   - 后端逐 Claim 执行引用、有效状态、条款/文件 scope、数值 grounding 校验；失败 Claim 删除，通过 Claim 机械拼接为 `PARTIALLY_ANSWERABLE`。
   - 若所有 Claim 均失败，返回 HTTP 200 的安全 `UNANSWERABLE`，不输出未经验证的结论。
   - 逐 Claim 校验继承原答案中声明的条号/文件范围，修复“条号只写在总答案、Claim 不重复条号”导致的 scope 绕过。
   - 响应新增 `validation_degraded`、`validation_warnings`；前端显示“已安全降级”。

2. **错误分类与可观察性**（`app/services/answer_generator.py`、`app/web/static/chat.js`）：
   - 错误细分为 `ANSWER_JSON_PARSE_FAILED`、`ANSWER_SCHEMA_VALIDATION_FAILED`、`ANSWER_SEMANTIC_VALIDATION_FAILED`。
   - 错误详情保留 `validation_error`；初次/repair 失败写入结构化日志，含阶段、异常类型和具体门禁原因。
   - 前端优先显示具体 `validation_error`，不再只显示异常类名。

3. **精确法规条款证据锚定**（新增 `app/services/evidence_scope_selector.py`）：
   - 查询显式指定条号，或查询精确包含结构化章节标题（本例“燃油箱点燃防护”）时，生成证据限定为同一文档、同一条号的全部 chunks。
   - 比较/对比/差异类问题保留跨文档证据，不进行单条款收窄。
   - 这使 CCAR-26 候选仍可用于检索诊断，但不会进入本题生成上下文。

4. **法规要求类问题使用原文抽取**（新增 `app/services/regulation_answer_builder.py`）：
   - 当问题精确锚定一个法规条款并询问“要求/规定/有哪些/如何”时，后端直接从结构化 chunk 提取顶层 `(a)～(d)` 款，按原文组装答案并绑定原 chunk 引用，不调用模型改写条文逻辑。
   - 保留各款的子项、数字和限定词；自动去除修订说明后的尾部噪声。
   - 不精确命中条款或非要求类问题仍走原有生成链路。

5. **Reranker 修复**（`deploy/models/start-reranker.ps1`、`config/default.yaml`）：
   - llama.cpp 启动参数增加 `--batch-size 2048 --ubatch-size 2048`，消除 707 tokens > 512 的 500 错误。
   - 普通/复杂问题送入重排的候选上限调整为 12，Reranker 超时 30→60 秒，适配 CPU 吞吐并避免 30 个长法规 chunk 超时 fallback。
   - `deploy/models/README.md` 已记录该运行约束。

6. **服务恢复**：实施期间 RAGFlow 容器出现运行时僵死（health 超时、`docker exec` 失败、普通 `docker restart` 无法获得 exit event）；重启 Docker Desktop 后按部署脚本恢复全部容器，未删除数据卷。网关与 Reranker 均已重启加载新代码/配置。

#### 新增/更新测试

- 新增 `tests/unit/test_evidence_scope_selector.py`：精确标题收窄、显式条号收窄、对比题保留跨文档证据。
- 新增 `tests/unit/test_regulation_answer_builder.py`：跨 chunk 原文合并、(a)～(d) 顺序、3%/限定词保留、修订尾噪去除、非要求题不触发。
- 更新 `tests/unit/test_answer_generator.py`：语义 repair 再失败后的安全 sanitizer、细分错误码和详细错误。
- 更新 `tests/unit/test_rag_pipeline.py`：部分 Claim 删除、全部 Claim 失败返回安全 UNANSWERABLE、总答案声明 scope 但 Claim 省略条号的真实绕过回归。

#### 验证

- 相关测试：`33 passed`。
- 完整 pytest：`316 passed, 3 failed, 1 warning`；3 个失败仍是既有解析基线（noisy OCR TOC、跨页表格 continuation caption、full-page figure/table 标题去重），与本次问答改动无关。
- 本次涉及文件 Ruff：通过。
- `node --check app/web/static/chat.js`：通过。
- PowerShell AST 解析 `deploy/models/start-reranker.ps1`：通过。
- 全量 Ruff/Mypy 仍有既有问题：Ruff 3 项（admin 未使用 import、citation_validator 长行、query_planner 测试长行）；Mypy 27 项，定向 Mypy仅剩既有 `app/services/model_client.py:81 no-any-return`。

#### 改动文件

- `app/core/config.py`
- `app/pipelines/rag_pipeline.py`
- `app/schemas/chat.py`
- `app/services/answer_generator.py`
- `app/services/evidence_scope_selector.py`（新增）
- `app/services/regulation_answer_builder.py`（新增）
- `app/web/static/chat.js`
- `config/default.yaml`
- `deploy/models/start-reranker.ps1`
- `deploy/models/README.md`
- `tests/unit/test_answer_generator.py`
- `tests/unit/test_evidence_scope_selector.py`（新增）
- `tests/unit/test_rag_pipeline.py`
- `tests/unit/test_regulation_answer_builder.py`（新增）


---

### 2.5 对比题"检索不到答案"三层修复（2026-08-18）

> 用户问"对比林地病虫害喷洒无人机、水稻植保无人机，二者亩均施药量、作业高度有哪些明显差异？差异形成的原因是什么？"检索不到答案。经复现定位完整因果链：**对比未拆分 → 整句检索偏定义章节 → 参数章节未进证据 → 覆盖判定误判证据充分 → 模型只能答"没找到数据"**。GPT 复查补出关键一环：`evidence_judge._is_covered` 只要有词汇重合(>=0.08)就判 covered，导致 ANSWERABLE 与"未找到数据"矛盾。按"先修覆盖判定→再修句式拆分→最后数值加权"实施三层。

#### 第一层：扩展对比句式拆解（`app/services/query_planner.py`）

- 原 `_comparison_plan` 只靠"在/从/就"标记定位主体与维度边界，无法拆解"对比A、B，二者X、Y有哪些差异？原因？"。
- 新增 `_TWO_SIDES_RE`（"二者/两者"分隔主体与维度）、`_REASON_TAIL_RE`（句末"差异形成的原因是什么"作为独立子查询）、`_build_comparison_matrix`、`_extract_reason`。
- `_ASPECT_SUFFIXES` 按长度降序补充"有哪些明显差异""有何差异"等后缀。
- 效果：该查询拆成 2主体×2参数 + "差异形成原因" = 5 个子查询矩阵（`synthesis_mode=matrix`）；原"在…上有什么不同"句式与"比较A和B，两者X、Y"句式均回归通过。

#### 第三层（最高优先级）：覆盖判定检查数值证据（`app/services/evidence_text.py`、`app/services/evidence_judge.py`）

- 新增 `has_parameter_value(text)`：数字+物理单位（m/L/667m²/%、m/s 等）才算数值参数，排除"3.1 作业高度"这类条款号与纯定义句。
- 新增 `is_parametric_text(text)`：判断需求是否参数类（含"量/高度/速度/…"）。
- `_is_covered`（单查询路径）与 `_assess_coverage_matrix`（矩阵路径）对参数类需求/参数 cell 增加数值检查：无数值证据判未覆盖/缺失，非参数需求保持原规则。
- 效果：参数 cell 只有"定义句"时不再误判 covered，"原因"无直接证据时返回部分可回答。

#### 第二层：参数章节数值信号加权（`app/services/coverage_selector.py`）

- `select` round one 对参数类 aspect 优先选含数值 chunk（`_prefer_parameter_value`），避免定义句占满前排。
- `_score` 对含数值 chunk +0.1。

#### 验证

- 单测：`test_query_planner.py` +2（二者句式/无原因纯矩阵）、`test_evidence_judge.py` +4（参数无数值→未覆盖/有数值→覆盖/非参数不受影响/矩阵 cell 无数值→missing）；沙箱相关 46 个单测全过、ruff 全过。
- 端到端模拟（真实 chunk 文本）：round one 对参数 cell 正确选中含数值 chunk（f2/r2/f4/r4），evidence_judge 判参数 cell covered、缺失正确。
- **残留问题**："差异形成原因"cell 因非参数类仍走 legacy 规则（有 chunk 即 covered），未做"因果信号"检测；规范文档通常无"为什么"直接证据，若要严格判"部分可回答"需另做因果词检测（如"因为/由于/取决于/受…影响"）。未实施，留待后续。

#### 改动文件

- `app/services/query_planner.py`
- `app/services/evidence_text.py`
- `app/services/evidence_judge.py`
- `app/services/coverage_selector.py`
- `tests/unit/test_query_planner.py`
- `tests/unit/test_evidence_judge.py`

#### 注意

- 沙箱仅 Python 3.10，完整 pytest 需宿主重跑（`.\deploy\gateway\start.ps1` 后 `python -m pytest`）。
- 本次改动未动检索/重排主链路，仅改对比拆解与证据选择/判定，风险面窄。


---

### 2.6 对比题主体词同义扩展与表格输出（2026-08-18）

> 承接 §2.5。用户期望上述对比题能输出类似表格：水稻侧引用《无人机防治稻纵卷叶螟施药技术规范》的“亩均施药量 2 L~3 L/作物冠层上方 2 m~3 m”，林业侧引用《无人机喷洒防治林业有害生物技术规程》的“施药量按林地状况/防治要求/防治对象/为害程度确定、平原 2 m~5 m/山地坡地 2 m~10 m”。经核对，这两份规范均已入库发布，问题出在**口语化主体与规范标题术语存在词汇鸿沟**，子查询检索不到对应文档。

#### 问题

- 子查询“水稻植保无人机 亩均施药量”与《无人机防治稻纵卷叶螟施药技术规范》字面重合度低（文档正文几乎不用“植保无人机”而用“无人机防治稻纵卷叶螟”），RAGFlow 检索容易漏召回。
- 子查询“林地病虫害喷洒无人机 亩均施药量”与《无人机喷洒防治林业有害生物技术规程》同样存在词汇鸿沟（“林地病虫害喷洒”≠“林业有害生物喷洒”）。
- 即使检索命中，生成端也未被要求用表格呈现对比结果。

#### 根因

- `query_planner._build_comparison_matrix` 直接把 `subject + aspect` 作为子查询，未对口语化 subject 做领域同义扩展。
- `prompts/answer_generation.txt` 仅要求“逐维度检查”，未要求对比问题优先使用 Markdown 表格。

#### 修复

1. **`app/services/query_planner.py` 新增 `_SUBJECT_EXPANSIONS` 与 `_expand_subject()`**
   - 仅对矩阵子查询的 `query` 字段生效，`subject`/`aspect` 保持不变（不影响 coverage 矩阵与前端展示）。
   - 当前词表（受控、精确匹配）：
     - `水稻植保无人机` → `水稻 病虫害 施药 稻纵卷叶螟`
     - `林地病虫害喷洒无人机` → `林业 有害生物 喷洒 防治`
   - 扩展逻辑与既有的 `_DIMENSION_EXPANSIONS` 一致，避免污染普通查询。

2. **`prompts/answer_generation.txt` 增加规则 26**
   - 对比问题优先使用 Markdown 表格：表头为“对比项目”与各比较对象，行按维度组织；单元格内事实仍需 `[Cx]` 标记；缺少同口径证据时写“当前资料中未明确说明”。表格写在 JSON 的 `answer` 字段内，JSON 本身仍只输出合法 JSON。
   - `app/core/config.py` 与 `config/default.yaml` 的 `prompt_version` 同步由 `answer-generation-v2` bump 至 `answer-generation-v3`，避免 yaml 覆盖导致 prompt 不生效。

3. **`tests/unit/test_query_planner.py` 更新**
   - 在 `test_comparison_two_sides_plan_splits_subjects_aspects_and_reason` 中追加断言，验证主体扩展词已进入 q1/q2 query，且合并原因子查询 q5 不做扩展。

#### 改动文件

- `app/services/query_planner.py`
- `app/core/config.py`
- `prompts/answer_generation.txt`
- `config/default.yaml`
- `tests/unit/test_query_planner.py`

#### 验证

- 目标单测 `test_query_planner.py` 需在宿主 Python 3.11+ 环境跑通；沙箱无依赖无法运行 pytest。
- 端到端验证建议：宿主机重启 Gateway 后，在 `/chat` 重新提问该对比题，检查：
  - 检索 debug 中 q2/q4 能命中《无人机防治稻纵卷叶螟施药技术规范》chunk 23/24；
  - q1/q3 能命中《无人机喷洒防治林业有害生物技术规程》chunk 13/20；
  - 答案输出 Markdown 表格，引用 `[Cx]` 正确；
  - 若某侧证据仍缺失，对应单元格显示“当前资料中未明确说明”而非编造差异。

#### 注意

- 主体扩展词表是领域受控词表，新增其他口语化主体（如“小麦植保无人机”“果园喷洒无人机”）时需同步补充映射并补回归测试。
- RAGFlow 若仍僵死，需先按 §2.4 方式重启容器再测。

---

### 2.7 复现定位：对比题"沙地无人机播种 vs 早熟中稻无人机直播"回答"未明确说明"（2026-08-18）

#### 问题

问题"对比沙地无人机播种、早熟中稻无人机直播，二者作业航高、播种量计算逻辑存在哪些区别？"，回答"当前资料中未明确说明"二者航高数值与播种量公式/区间，而两份规程均已入库且含对应条款（沙地规程 5.3 播种量公式、5.5 航高 10~50 m/3~8 m/s；早熟中稻规程 5.3 播量区间、5.2 无人机选型）。

#### 根因（真实检索复现，网关 `/api/v1/retrieval/debug`，dataset b5ecab6a…）

**规划错位是根本原因：`QueryPlanner._first_marker_index` 把"存在哪些区别"中的"在"（位置>0）误判为比较分隔标记，`_TWO_SIDES_RE`（"二者/两者"分支）永不执行。**

1. **规划层**：`_comparison_plan` 先查 `_first_marker_index(text)`（找"在/从/就"），命中"存在"里的"在" → `subject_text="沙地无人机播种、早熟中稻无人机直播，二者作业航高、播种量计算逻辑存"`、`aspect_text="哪些区别？"` → subjects=[沙地无人机播种, 早熟中稻无人机直播, 二者作业航高, 播种量计算逻辑存]、aspects=[哪些区别]，4 个垃圾子查询（q1~q4 均以"哪些区别"结尾，维度词未作为 aspect 进入子查询）。真实 debug 确认 query_plan 与上述一致。
2. **召回失败**：q3"二者作业航高 哪些区别" 前 30 候选里沙地 doc 仅 1 块（3.4 航高定义），**含"航高10 m~50 m"的 5.5 数值块（RAGFlow chunk f3e73367236bb0f6）完全未进候选池**，其余多为 CCAR 飞机适航无关内容；q4"播种量计算逻辑存 哪些区别" 召回沙地 5.3 公式（67b8a59c7af7bb5b）与式中（ca14ab85233d2661）为 raw=3/4，但均未进入最终选中。
3. **证据选择**：`CoverageSelector` round one 每子查询取 1 块，aspect="哪些区别" 非参数类 → `_prefer_parameter_value` 数值优先不启用 → q3 选中定义块而非数值块；round two 按分数补足 8 名额时沙地 5.3 公式（rerank 0.308）被其他块挤出。最终选中 8 块以术语定义/范围为主（沙地 3.2 播区、早熟中稻 3.1/3.3 定义、沙地 3.4 航高定义、油菜飞播定义、飞播造林定义、沙地 6 作业、早熟中稻 5.3 播量区间 C4）。
4. **覆盖判定**：coverage_matrix 4 cell 全 "covered"（每子查询有候选被选中）→ ANSWERABLE，但证据缺沙地侧参数块。
5. **生成端**：基于定义/范围证据如实写"未明确说明"，对沙地侧航高数值与播种量公式**属实（证据里确实没有）**；但忽略了证据中实际存在的 C4（早熟中稻 5.3 播量区间），"均未包含播种量参数差异"表述不准确 → 生成端对已有证据利用不充分（次要问题）。

#### 关键证据（真实 RAGFlow 数据）

- 沙地规程发布 42 块、早熟中稻 29 块（与本地 chunks.jsonl 数一致）；5.5 航高数值块 `f3e73367236bb0f6` 在库中（"5.5 航高和速度设计 … 航高10 m~50 m,速度3 m/s~8 m/s"），q3 未召回。
- 沙地 5.3 播种量设计：公式块 `67b8a59c7af7bb5b`、式中定义块 `ca14ab85233d2661`；早熟中稻 5.3 播量区间块 `f75e289cfab7ef16`（被选中为 C4）。
- stage_counts：4 子查询 / 120 候选 / 8 选中 / 4 覆盖。

#### 修复建议（待确认，未实施）

1. **`_comparison_plan` 调整判定顺序**：先查 `_TWO_SIDES_RE`（"二者/两者"），命中则直接走该分支，不再先走 `_first_marker_index`（避免"存在/不在/实在"里的"在"误判）。模拟验证：`_TWO_SIDES_RE` 优先 → subjects=[沙地无人机播种, 早熟中稻无人机直播]、aspects=[作业航高, 播种量计算逻辑存在哪些区别]，q1"沙地无人机播种 作业航高"等子查询带上维度词，召回应显著改善。
2. **`_ASPECT_SUFFIXES` 补充后缀**："存在哪些区别/存在什么区别/存在哪些差异"等，清掉 aspect 残留尾巴（"播种量计算逻辑存在哪些区别" → "播种量计算逻辑"）。
3. **生成端**：C4 已在证据中却未用于回答，可后续评估（对照 08-18 已实施的安全降级/逐 Claim 校验，属利用不充分而非编造）。

#### 改动文件

- 本条目为**复现/定位记录**，未改代码。

#### 注意

- 修复合入后需在宿主重启 Gateway，重新在 `/chat` 提问该题，用检索 debug 断言 q1"沙地无人机播种 作业航高"命中 5.5 数值块、q2/q4 命中早熟中稻 5.3 播量。
- 可把本问题作为"对比句式规划错位"回归样例加入 `tests/unit/test_query_planner.py`。

---

### 2.8 修复：对比句式"存在哪些区别"里的"在"误判为比较标记（2026-08-18）

> 承接 §2.7 定位。实施对 `QueryPlanner` 的两处修改 + 回归用例。

#### 问题

"对比沙地无人机播种、早熟中稻无人机直播，二者作业航高、播种量计算逻辑存在哪些区别？" 被规划成 4 个垃圾子查询（subjects 含"二者作业航高""播种量计算逻辑存"，aspects=["哪些区别"]），导致沙地 5.5 航高数值块（RAGFlow f3e73367…）未进候选、5.3 播种量公式块未进证据，回答"当前资料中未明确说明"。

#### 根因

`_comparison_plan` 先走 `_first_marker_index`（找"在/从/就"），命中"存在哪些区别"里的"在" → 主体/维度边界切错；`_TWO_SIDES_RE`（"二者/两者"分支）永不执行。

#### 修复（`app/services/query_planner.py`）

1. **`_comparison_plan` 调整判定顺序**：先查 `_TWO_SIDES_RE`，命中则直接用"二者/两者"定位主体与维度边界；否则才回退 `_first_marker_index`。避免"存在/不在/实在"里的"在"误判。
2. **`_ASPECT_SUFFIXES` 补充后缀**："存在哪些区别/存在什么区别/存在哪些差异/存在什么差异"，把 aspect 残留尾巴清干净。
3. **`_clean_aspects` 剥离开头比较标记**：`re.sub(r"^(?:在|从|就)", "", text)` 只剥一次，处理"对比A、B，二者在X、Y上…"残留的"在"前缀。
4. **`_DIMENSION_EXPANSIONS` 增加对比题参数维度 + `_build_comparison_matrix` 子查询应用维度扩展**：`query=self._expand_dimension(self._expand_subject(subject), aspect)`，新增 `"作业航高": "航高 速度"`、`"播种量计算逻辑": "播种量 播量"`。实测（真实 RAGFlow）"沙地无人机播种 作业航高" 时 5.5 航高数值块（含"航高10 m~50 m"）仅排第 15、进不了 `complex_candidates_per_subquery=6` 候选池；加入"速度"词后 5.5 块升到第 1。该扩展只对词表内维度生效，词表外 aspect 保持原样。

#### 验证

- 沙箱（importlib 绕过 conftest，Python 3.10 无法跑完整 pytest）：
  - `tests/unit/test_query_planner.py` **8 passed**（含新增 2 用例）；
  - 下游相关 `test_query_analyzer.py`/`test_coverage_selector.py`/`test_evidence_judge.py`/`test_query_planner.py` **20 passed**；
  - ruff 两文件通过。
- 端到端检索预验证（真实 RAGFlow，修复后 4 个子查询各取前 6）：q1 沙地 5.5 航高数值块排第 1；q3 沙地 5.3 播种量公式/式中排第 2/3；q4 早熟中稻 5.3 播量区间排第 2。目标参数块均能进入候选池。
- 手工断言：用户问题 → subjects=[沙地无人机播种, 早熟中稻无人机直播]、aspects=[作业航高, 播种量计算逻辑]，q1"沙地无人机播种 作业航高"；"二者在…上"句式 aspect 无"在"前缀；"在…上"（无二者）句式行为不变。
- **注意：宿主网关进程未重启，`/api/v1/retrieval/debug` 实测仍是旧 plan**（subjects 含"二者作业航高"/aspects=["哪些区别"]）。需宿主重启 Gateway 后复测。
- **附带修正一处既有测试笔误**：`test_comparison_two_sides_plan_splits_subjects_aspects_and_reason` 中 q5 断言期望顿号 `"…、… 差异形成原因"`，实现实际用空格 `' '.join(subjects)`（§2.5 既有实现）→ 断言改为空格形式。该断言 §2.6 新增后未在宿主实际跑通。

#### 改动文件

- `app/services/query_planner.py`
- `tests/unit/test_query_planner.py`（+2 用例；+1 断言修正）

#### 注意

- 宿主验证路径：重启 Gateway（`.\deploy\gateway\start.ps1`）→ 在 `/chat` 重问该题 → 检索 debug 断言 q1"沙地无人机播种 作业航高 航高 速度"命中 5.5 数值块（RAGFlow f3e73367…，应排第 1）、q3 命中沙地 5.3 播种量公式块（67b8a59c…/ca14ab85…）、q4 命中早熟中稻 5.3 播量区间（f75e289c…）；答案应给出"航高 10~50 m、速度 3~8 m/s"与"沙地按公式 S=E×R×(1-A)×G×1000/(N×W) 动态计算、早熟中稻按稻种类型直接给播量区间"的差异；早熟中稻侧航高无规定应明写（其 5.2 播种方式仅要求无人机符合 GB/T 35018、GB/T 38931）。
- 生成端遗留：早熟中稻 5.3 播量区间（C4 f75e289c…）此前已进证据但模型未用于作答，属利用不充分，待宿主复测观察。

---

### 2.9 第二轮：播种量侧仍"未明确说明"——扩展词顶替 + 公式块未识别 + 生成端（2026-08-18）

> 宿主重启后实测：航高侧已答对（沙地 10~50 m [C1]），但"播种量计算逻辑"两侧仍写"当前资料中未明确说明"。经真实检索 debug + CoverageSelector 模拟定位两处检索层根因 + 一处生成端根因。

#### 根因

1. **沙地 5.3 播种量公式/式中块进了候选但没进最终证据**：q3"沙地无人机播种 播种量计算逻辑 播种量 播量"候选中沙地 5.3 排 raw=2/3，被《水稻丸粒化种子无人机水直播技术规程》9.3 压到第 1。① 扩展词"播量"把水稻丸粒化规程顶高（实测去掉"播量"后沙地 5.3 升到第 1）；② 沙地 5.3 公式块（`arrayrs=(N.W)/(E.R(1-A).G.1000)`）与式中块（"单位为克每公顷 (g/hm^2)" 等）**无数值+单位组合**，`has_parameter_value` 判 False → round-one 参数类数值优先跳过它们，选中水稻丸粒化 9.3（含数值）→ round-two 沙地 5.3 分数低被挤出 8 名额。
2. **早熟中稻 5.3 播量区间（C4 f75e289c…）已进证据但模型未用**：生成端对"计算逻辑"维度只认"公式"，把"无公式、只有按类型给区间"写成"未明确说明"。

#### 修复

1. **`app/services/query_planner.py`**：`_DIMENSION_EXPANSIONS` 的 `"播种量计算逻辑"` 扩展词 `"播种量 播量"` → `"播种量"`（去掉"播量"）。实测 q3/q4 目标块升到第 1。
2. **`app/services/evidence_text.py`**：`has_parameter_value` 增加公式/量纲定义识别——`_PARAM_DIMENSION_RE`（`单位(?:为|是)…（…）` 与 `依据公式（\d）`）。沙地 5.3 公式/式中块判为参数块。误伤检查：普通定义句仍 False。
3. **`prompts/answer_generation.txt` 规则 27 + `prompt_version` v3→v4**（`app/core/config.py`、`config/default.yaml`）：对比"计算逻辑/方式"类维度时，一方公式、另一方固定区间/查表均属有效证据，须如实列两侧确定方式，不得因无公式就写"未明确说明"。

#### 验证

- 沙箱：`test_query_planner.py` 8 + `test_evidence_judge.py` 7 + `test_coverage_selector.py` 1 + `test_query_analyzer.py` 4 + `test_evidence_scope_selector.py` 3 + `test_grounding_validator.py` 7 + `test_scope_validator.py` 11 = **41 passed**（query_planner 断言已同步扩展词）；ruff 通过。
- 真实 RAGFlow + CoverageSelector 模拟（修复后 4 子查询）：round-one 选中 沙地 5.5 航高 / 沙地 5.3 播种量 / 早熟中稻 5.3 播量区间（+ 早熟中稻航高侧常绿果树无关块）；播种量两个参数块均进最终证据。
- 沙箱 importlib 无法跑需 pytest fixture 的 `test_config.py`/`test_rag_pipeline.py`（Python 3.10 + 无 fixture），宿主需重跑完整 pytest。

#### 改动文件

- `app/services/query_planner.py`
- `app/services/evidence_text.py`
- `prompts/answer_generation.txt`、`app/core/config.py`、`config/default.yaml`
- `tests/unit/test_query_planner.py`

#### 注意 / 遗留

- **宿主重启后验证**：播种量侧应给出"沙地用公式 S=E×R×(1-A)×G×1000/(N×W)（含 S/N/W/E/R/A/G 定义）动态计算"与"早熟中稻按稻种类型直接给区间（常规籼稻 2.0~3.5、常规粳稻 4.0~6.0、杂交稻 1.8~2.0 kg/667m²）"的对比。
- 沙地公式块文本为 OCR 乱码（`arrayrs=(N.W)/(E.R(1-A).G.1000)`），式中块无公式本体；模型可能只能据参数定义推断公式，若宿主实测公式表达仍不理想，需回解析层修公式图像（不在本会话范围）。
- **遗留：早熟中稻航高侧（q2）仍召回常绿果树等无关块**（其规程本身无航高数值，属"单侧缺证据"，§2.5 残留的领域约束未覆盖）。模型当前能正确写"未明确说明"，但无关块占证据名额，待后续按 task_type/适用对象约束处理。

---

### 2.10 第三轮：证据在、模型也答得对，但宿主仍答"未说明"——证据提取层把正文句挤掉了（2026-08-18）

> 前两轮修复后，宿主 debug 确认 C3（沙地 5.3 播种量式中）、C4（早熟中稻 5.3 播量区间）都在**选中 chunk** 里，且用同样 evidence 直接调模型（v4 prompt + json_schema + reasoning_effort:none）模型能正确作答。但宿主完整链路仍答"播种量未明确说明"。定位到**证据提取层**。

#### 根因

`EvidenceExtractor.extract` 用 `split_sentences` + `overlap_score` 选句。chunk 文本的**头部元数据行**（"文档：20241224：《早熟中稻无人机直播栽培技术规程》.pdf"、"章节：5 播种 / 5.3 播种量"、"条号：5.3 页码：6"）与查询**词汇重叠度极高**（含"早熟中稻/无人机/直播/播种量"全部主体词），被选为 top 证据；而真正的参数正文句（"常规籼稻播种量为每2.0 kg~3.5 kg/667 m²…"）因是长句、含大量查询外 bigram，overlap 比例低，被挤出 `max_evidence_sentences=16` 名额。

实测宿主链路给模型的 16 条 evidence 中：C4 只剩"文档：…"头部行，C3 只剩"章节：5.3 播种量设计"标题碎片——**模型根本看不到播量数值和 S/N/W 参数定义**，只能写"未明确说明"。这也解释了为什么此前各轮"证据在选中块里却答不出"。

#### 修复（`app/services/evidence_extractor.py`）

1. **过滤 chunk 头部元数据行**：`_META_PREFIX = re.compile(r"^(?:文档|章节|条号|页码|图表)[:：]")`，证据句跳过这些行（无正文时回退保留）。
2. **参数类查询给含数值/量纲/公式的句子加分**：`_sentence_score` 中 `if is_parametric_text(query.normalized_query) and has_parameter_value(sentence): score += 1.0`，让数值正文句胜过短标题行（如"5.3 播种量"）。

#### 验证

- 真实 chunk + 修复后 EvidenceExtractor：C3 的 "S—每公顷播种量，单位为克每公顷 (g/hm^2)" / "N—每公顷设计出苗株数" / "W—处理后种子千粒重"、C4 的 "常规籼稻播种量为每2.0 kg~3.5 kg/667 m²…"、C1 的 "航高10 m~50 m" 全部进入 evidence（此前只有标题碎片）。
- 真实模型端到端（该 evidence + v4 + json_schema）：播种量侧正确输出"沙地 S=(W×N)/P 结合千粒重、出苗株数设计 [C3]；早熟中稻直接规定固定区间：常规籼稻 2.0~3.5、粳稻 4.0~6、杂交稻 1.8~2.0 kg/667m² [C4]"。
- 沙箱单测：`test_evidence_extractor.py` 4（+1 新用例"元数据行过滤+参数值优先"）、`test_evidence_judge.py` 7、`test_query_planner.py` 8、`test_coverage_selector.py` 1、`test_grounding_validator.py` 7 = **26 passed**；ruff 通过。

#### 改动文件

- `app/services/evidence_extractor.py`
- `tests/unit/test_evidence_extractor.py`

#### 注意 / 遗留

- **宿主重启 Gateway 后**在 `/chat` 重问该题：播种量侧应给出"沙地用公式（含 S/N/W/E/R/A/G 参数）动态计算"与"早熟中稻按稻种类型直接给播量区间"的对比。
- 遗留①：作业航高沙地侧模型会引用常绿果树块 [C2]（"不超过冠层 100m"）作参考——常绿果树块因含数值被参数加分选中进入证据（q2 早熟中稻航高侧检索召回），主答案 10~50 m 正确，但引入无关引用噪音。属"单侧缺证据/适用对象约束"（§2.5 残留），待后续按 task_type/适用对象过滤。
- 遗留②：沙地播种量公式块文本是 OCR 乱码（`arrayrs=(N.W)/(E.R(1-A).G.1000)`），模型只能据 S/N/W/E/R/A/G 参数定义推断公式结构，无法给出规范原文的完整公式 S=E×R×(1-A)×G×1000/(N×W)。若需精确公式，回解析层修公式图像（不在本会话范围）。


---

### 2.11 智能问答 Markdown 表格正确渲染（2026-08-18）

> 用户希望模型在 `answer` 中输出 GitHub 风格 Markdown 表格时，智能问答台能够显示为真正的表格，而不是原样展示 `| ... |`、`| :--- |` 文本。

#### 根因

`app/web/static/chat.js` 原先对 `data.answer` 只执行 `escapeHtml` 和引用标记高亮，然后直接写入 `answerBody.innerHTML`；前端没有 Markdown 解析步骤。提示词规则 26 已要求对比题优先输出 Markdown 表格，因此生成格式正确，但展示层把 Markdown 当成了普通文本。

#### 修复

1. 新增 `app/web/static/markdown.js`：实现受限、安全的 Markdown 渲染，支持：
   - GitHub 风格表格表头、对齐分隔行和数据行；
   - 单元格内 `**粗体**`、行内代码与 `[C1]` 引用高亮；
   - 普通段落与无序列表；
   - `\|` 转义管道；
   - 所有原始内容先 HTML 转义，避免回答内容注入任意标签或脚本。
2. `app/web/static/chat.js` 改用 `ChatMarkdown.renderMarkdown` 渲染回答；脚本未加载时仍回退到原来的安全纯文本展示。
3. `app/web/static/chat.css` 增加表格边框、表头底色、斑马纹、顶部对齐、对齐列样式及横向滚动容器；窄屏下表格保持可横向滚动，回答卡片减小内边距。
4. `app/web/chat.html` 加载 `markdown.js`，并更新 CSS/JS 缓存版本为 `20260818-2`。
5. 新增 `tests/web/markdown.test.cjs`，覆盖示例三列表格、粗体、引用标记、XSS 转义、转义管道、段落、列表和行内代码。

#### 验证

- `node --check app/web/static/markdown.js`：通过。
- `node --check app/web/static/chat.js`：通过。
- `node --test tests/web/markdown.test.cjs`：**4 passed**。
- 完整 Python 回归：**325 passed，3 failed**；3 个失败均为已有的 OCR 乱码/表题测试（`test_regulation_preprocess.py`），与本次前端改动无关。
- 浏览器本地验收：`/chat` 已加载 `chat.css?v=20260818-2`、`markdown.js?v=20260818-2`、`chat.js?v=20260818-2`；实际问答生成测试受 RAGFlow `ReadTimeout` 阻断，页面按预期显示“请求失败”，待 RAGFlow 恢复后用本节示例问题复测真实回答表格。

#### 改动文件

- `app/web/static/markdown.js`（新增）
- `app/web/static/chat.js`
- `app/web/static/chat.css`
- `app/web/chat.html`
- `tests/web/markdown.test.cjs`（新增）

---

### 2.12 对比题表格数据不输出："地面分辨率不低于表1"只有引用无内容（2026-08-18）

> 问题"对比地质灾害倾斜航测、河湖智能巡检无人机，在航向/旁向重叠度、地面分辨率要求上有什么差异？"，回答地面分辨率侧为"不低于表1中的要求（注：表中具体内容未在证据文本提供）"。但表1 数据在库中 chunk 里完整存在。

#### 根因

1. **表1 数据与引用正文分属两个 chunk**：地质灾害规程 6.1 被切成正文 chunk（"…地面分辨率不低于表1中的要求"，chunk_id 8ba23453…）与表格 chunk（`content_type="table"`，text 字段含完整 Markdown 表格，chunk_id 9183471e…，parent 指向正文 chunk）。
2. **表格数据行在证据提取时被丢弃**：`EvidenceExtractor` 按句子切分 + `overlap_score` 排序。表格行"| 1:500 | 地裂缝 | 5 |"与查询词汇重叠率极低（数字+短词），被挤出 `max_evidence_sentences` 名额 → 模型只见"不低于表1"正文，看不到表1 内容。
3. **`is_parametric_text` 术语表缺"分辨率"**："地面分辨率"未被识别为参数类，证据选择/覆盖判定不启用数值优先，表格块也无参数加分。

#### 修复

1. **`app/services/evidence_extractor.py`**：新增 `_merge_table_rows`——把连续 Markdown 表格行合并为一条证据；`_sentence_score` 对表格块用**块内最高行 overlap** 打分（避免整块含大量数字稀释比例），表格块被保留为完整证据。
2. **`app/services/coverage_selector.py`**：`_prefer_parameter_value` 在参数类 aspect 下无数值块时，优先选中含 Markdown 表格的块；`_score` 给表格块 +0.05，提升进入最终证据的概率。
3. **`app/services/evidence_text.py`**：`has_parameter_value` 识别含数字的 Markdown 表格块（`_TABLE_VALUE_RE` + `\d`）；`_PARAMETER_TERMS` 增加"分辨率"。
4. **`tests/unit/test_evidence_extractor.py`**：新增用例 `test_evidence_extractor_keeps_markdown_table_as_one_evidence`。

#### 验证

- **沙箱 bash/VM 自 2026-08-18 下午持续 "Workspace still starting" 不可用（约 15+ 分钟），未能运行单测**；三处代码已人工审查语法与逻辑。
- 逻辑推演：修复后表格 chunk 进入 evidence 时，"| 调查比例尺 | 地质灾害类型 | 地面分辨率值（cm） |\n| 1:500 | 地裂缝 | 5 |…"整块保留；模型可见表1 数据（1:500 地裂缝 5cm、崩塌/滑坡/泥石流 5cm、1:1000 10cm、1:2000 20cm）。
- **宿主验证路径**：重启 Gateway → `/chat` 重问该题 → 地面分辨率侧应输出表1 具体数值；河湖侧仍应为"未明确"（该规程确无重叠度/地面分辨率规定，用户回答 [C2][C4] 正确）；重叠度侧 [C1] 已正确。

#### 改动文件

- `app/services/evidence_extractor.py`
- `app/services/coverage_selector.py`
- `app/services/evidence_text.py`
- `tests/unit/test_evidence_extractor.py`

#### 注意

- 表格块作为一条证据可能较长（多数据行），但 `max_evidence_sentences=16` 下可接受；若个别文档表格超大需评估。
- `has_parameter_value` 放宽到"含数字的表格块"会使含表格 chunk 在参数类任务中被视为参数证据，属预期；需宿主回归确认无副作用。

---

### 2.13 观察：重叠度侧输出内容减少——全局证据截断 + 适用对象混淆（2026-08-18）

> 用户在 §2.12 修复后重测该对比题：地面分辨率侧已正确输出表1 数值（1:500 地裂缝/崩塌 5cm、1:2000 20cm），但**重叠度侧内容较上一轮减少**：缺少"航向重叠度 65%~85%、最小 60%、复杂区 70%~85%"，且把"丘陵山地地区航向旁向重叠度 50%~80%"（实为 c 款机载激光雷达的规定）输出成了地质灾害倾斜航测的通用值。

#### 核对

- 库中 6.3.2 像片重叠度 chunk（chunk_id 7a4209aec0e643867921，纯文本无表格）内容完整，含三款：
  - a) 垂直影像：旁向 40%~80%/最小 30%；航向 65%~85%/最小 60%；复杂/密集区航向 70%~85%
  - b) 倾斜摄影：满足垂直影像重叠度后航向旁向可不再重新设计
  - c) 机载激光雷达：旁向不小于 30%；丘陵山地地区航向旁向 50%~80%
- **§2.12 表格改动不涉及纯文本重叠度 chunk，不是本次内容减少的原因**。
- 推断原因：① `EvidenceExtractor` 全局 `max_evidence_sentences=16` 截断——a) 款次段（"航向 65%~85%…"）与 a) 首段、c) 尾段 overlap 同为 0.25，按句序排在中间，处于 16 条边界附近，可能被挤出；② 模型对同一 chunk 内 a/b/c 三款（垂直影像/倾斜摄影/机载激光雷达）的适用对象区分不足，把 c 款"丘陵山地"当通用值输出（prompt 规则 23 未约束住）。

#### 建议（待 bash 恢复后实施/验证）

1. `max_evidence_sentences` 16→24，降低对比题参数证据被截断概率（注意与 `max_output_tokens=800` 的预算平衡）。
2. `EvidenceExtractor` 对含具体数值的句子在参数类之外也轻微加分（如 +0.3），确保数值句稳定入选、不被同分短句挤出。
3. prompt 强化适用对象区分：重叠度/分辨率等参数若同一 clause 内按"垂直影像/倾斜摄影/机载激光雷达"分项，模型须标明适用对象，不得泛化为该作业类别的通用值。

#### 验证状态

- **沙箱 bash/VM 持续不可用（"Workspace still starting"自本会话下午起 30+ 分钟）**，未能跑单测/复现真实 chat evidence。以上为基于库内 chunk 与代码逻辑的分析，需宿主验证。
- 宿主验证路径：重启 Gateway → 连续问该题 2~3 次 → 确认重叠度侧是否稳定缺失"航向 65~85%/60%、复杂区 70~85%"；若稳定缺失则按建议 1/2 调整，若时有时无则为模型转述波动。

---

## 3. 工作原则（沿用，不重复展开）

1. 先读 `docs/agent-handoff.md`，再读目标模块和对应测试。
2. 任何解析修复都必须用真实失败文档回归；不写破坏全局的正则。
3. 用户只有一个上传入口，不重新要求用户选择解析器。
4. 物理页码、印刷页码、条款号分开存储；表格文本、二维结构、图片资产三者都保留。
5. `needs_review` 不能绕过门禁发布；发布前优先 dry-run；删除本地任务不删除 RAGFlow 内容。
6. 修改后至少运行目标测试、完整 pytest 和 ruff；页面改动必须浏览器实测。
7. 不输出或提交密钥；不清除 Docker 数据卷；不把 D 盘数据复制回 C 盘。

---

## 4. 相关文档

- `README.md`
- `docs/agent-handoff.md`（最完整交接入口）
- `docs/pdf-parsing.md`（解析方法讲解）
- `docs/ingestion.md`、`docs/milestone-2.md`、`docs/milestone-3.md`
- `HANDOFF-2026-08-17.md`（本会话起点）
