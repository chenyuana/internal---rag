# HANDOFF 2026-09-03

## 目标
在企业知识库"答案必须出自证据、禁止模型自行补充"前提下，接入 DeepSeek V4 API 并优化回答质量。

## 一、模型接入 API
- `.env`：`ANSWER_MODEL_*` / `VERIFIER_MODEL_*` 切到 DeepSeek `deepseek-v4-flash`（base_url=`https://api.deepseek.com/v1`，API key 见 .env）。
- embedding / reranker / figure_vlm 保持本地（Ollama / llama.cpp）。
- RAGAS 评测本就走 DeepSeek API（另一 key），未动。
- 原 `.env` 备份于 `.env.bak-api-test`。

## 二、用 API 自动开启模型角色开关
- `app/core/config.py`：`Settings._auto_enable_remote_model_stages`（model_validator）：answer/verifier 为远程 API 时，自动置 `planning.model_enabled` / `translation.model_enabled` / `generation.comparison_summary_enabled`=True。
- 显式覆盖 env：`PLANNING_MODEL_ENABLED` / `TRANSLATION_MODEL_ENABLED` / `COMPARISON_SUMMARY_ENABLED`（.env 里 `COMPARISON_SUMMARY_ENABLED=false` 已关闭对比"统筹说明"）。

## 三、thinking（推理）做成开关
- `app/core/config.py`：`ModelEndpointSettings.thinking: bool = False`；`public_view` 暴露 `thinking` / `max_output_tokens`。
- `config/default.yaml`：answer/verifier 各加 `thinking: ${..._THINKING:-false}`。
- `.env`：`ANSWER_MODEL_THINKING=true`。
- `app/services/model_client.py`：`_uses_deepseek_v4_thinking` → `_is_deepseek_v4`；新增 `_enable_thinking`；DeepSeek V4 用 `thinking:{type:enabled|disabled}`，其它端点用 `reasoning_effort`。
- UI：`app/schemas/models.py` `AnswerModelSelection.thinking`；`app/services/answer_model_manager.py` resolve 覆盖；`app/web/chat.html` `#thinkingToggle` 复选框；`app/web/static/chat.js` 随请求发送。

## 四、输出 token 上调
- `config/default.yaml`：answer.max_output_tokens 2048→8192；verifier 1536→4096。

## 五、答案生成模式（answer_mode）
- `app/core/config.py` + `config/default.yaml`：`generation.answer_mode` = `deterministic` / `llm` / `hybrid`（默认 deterministic）。
- `app/pipelines/rag_pipeline.py`：deterministic=矩阵/提取优先（现状）；llm=直接模型生成；hybrid=LLM 为主 + 确定性补全（试验性，未采用）。
- `.env`：`ANSWER_MODE=deterministic`。

## 六、通俗讲解（explain）
- `prompts/answer_explanation.txt`（新建）：只转述、不新增、引用原样。
- `app/services/answer_generator.py`：`AnswerGenerator.explain()`。
- `app/pipelines/rag_pipeline.py`：deterministic 模式下对矩阵答案追加"通俗讲解"；**仅 `matrix_first_applied`（对比/多跳）触发，fact/流程不触发**。
- `.env`：`ANSWER_EXPLAIN_ENABLED=true`。

## 七、fact 题纯确定性（不调 LLM）
- `app/services/regulation_answer_builder.py`：fact 分支从"单一条文"改为"**按来源（文档+条号）分组、逐条 verbatim 提取**"。
- 保持 `_CLAUSE_RE` 只认字母分项 `([a-d])`，`sorted(clauses)` 排序——数字 `(1)(2)(3)`、罗马 `(i)(ii)` 保留在所属字母条款内部（嵌套结构正确，未扁平化）。
- 效果：fact 题 `model_calls=0`，LLM 不参与正文；按来源分组 + 逐条原文 + 引用。

## 八、对比总结校验修复
- `app/services/comparison_orchestrator.py`：`NUMBER_RE` → `NUMBER_CORE_RE`；`_validate_summary` 数字从"严格字符串相等"改为"按数值集合子集"校验（容忍空格/单位写法差异，禁止新数值）；引用/受保护词仍精确匹配。

## 九、answerability 守卫降级
- `app/pipelines/rag_pipeline.py`：`_validate_answerability` 由"部分证据 + 模型报完整可答 → 500"改为"降级为 PARTIALLY_ANSWERABLE"。

## 十、前端引用面板增强
- `app/web/static/chat.js`：`compactAnswer` 保留 `table_htmls`；新增 `sanitizeTable`（去 script/事件属性）；`renderCitations` 显示完整原文（去掉 520 截断）+ 渲染表格。
- `app/web/static/chat.css`：`.cite-tables` 表格样式。

## 十一、引用页码
- `app/services/ragflow_client.py`：`_normalize_chunk` 从 RAGFlow `positions`（`[[page,x1,x2,y1,y2],…]`）解析 `page_number` → 侧栏显示"第 N 页"。
- 注：`positions` 只对带图片/表格的 chunk 有值；纯文本条款 chunk（如 CCAR-25、FAA final rule）为空 → 无页码。

## 十二、定位到原文（尝试后回退）
- 曾新增：`ragflow_client.download_document`、`documents.py` `/document/{dataset_id}/{document_id}/file`、`chat.js` 点 `[C1]` 打开 PDF#page=N。
- 因纯文本文档无页码，点 `[C1]` 无法定位，**已回退前端点击处理**（恢复为"定位到右侧引用面板"）。
- 遗留孤儿（无调用、无害、可删）：`download_document` 方法 + `/document/.../file` 端点。

## 当前生效状态
- 默认：deterministic + explain（仅对比）+ thinking（answer）+ fact 纯确定性。
- .env 关键项：`ANSWER_MODE=deterministic`、`ANSWER_EXPLAIN_ENABLED=true`、`ANSWER_MODEL_THINKING=true`、`COMPARISON_SUMMARY_ENABLED=false`。

## 回滚
- 整体回退本地：覆盖 `.env.bak-api-test` 并重启。
- 关 thinking：`ANSWER_MODEL_THINKING=false`。
- 关 explain：`ANSWER_EXPLAIN_ENABLED=false`。
- answer_mode 切换：`ANSWER_MODE=deterministic|llm|hybrid`。
