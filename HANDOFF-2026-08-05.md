# Internal RAG 解析质量改进交接（2026-08-04 之后）

> 用途：供后续智能体快速接手本会话（自 `HANDOFF-2026-08-04-rag-improvements.md` 之后）完成的改动。项目运行环境为 Windows，所有项目、运行时、模型与数据均位于 D 盘。不要将部署方案改为 WSL。

## 0. 交接起点与范围

- **起点**：`HANDOFF-2026-08-04-rag-improvements.md`（已覆盖假字还原、表格 Markdown、目录打分、跨页续表、插图识别、一页多图、顶层条款、分式公式、单元格重建、流程图聚合、同级去重、空白记录表）
- **本会话范围**：自 2026-08-05 起的改动，随工作推进逐节追加
- **重要**：`CHANGELOG-rag-improvements.md` 是较早的累积记录（含 08-03 之前内容），本会话新增内容单独记录在本文档，避免混淆

## 1. 当前状态快照（2026-08-05）

- 代码目录：`D:\internal-rag\source\internal-rag`
- 入口：`http://127.0.0.1:8080/ingestion`
- 单元测试基线：**191 passed, 3 failed**（3 个既有失败与近期改动无关：TOC 检测、跨页 caption 绑定、figure 去重）
- Git：仓库仍无 commit，整个工程为未跟踪状态；**禁止** `git reset --hard` / `git clean`
- 服务状态：需在宿主机 PowerShell 运行 `deploy\gateway\status.ps1` 等脚本确认（Linux 沙箱无法执行 Windows 二进制）

## 2. 改动记录（按日期逐节追加）

### 2.1 空白记录表（表单）统一处理（2026-08-05）

> 针对《无人机应用于化学中毒现场采样检测技术规范 发布稿》第 10-12 页（A.1/A.2/A.3 记录表）的解析质量问题，实现"表单"语义的统一处理。用户确认采用**混合方案**：保留结构化表格文本（字段可检索）+ 附加整页原图（视觉保真）。

#### 问题

- 第 11 页（A.2 空气采样记录表）含 `CambriaMath` 数学字体（表底公式注），被 `_has_math_font()` 误判为公式页 → `route=formula_review`、整页走 OCR，表格结构被破坏。
- 一张横向记录表被 pdfplumber 拆成上下两个网格（上部字段区 2 列 + 下部数据区 10 列），生成两个割裂的 table chunk。
- 页眉残渣 `DB4401/T`、孤立破折号 `—`、占位符 `采样任务编号：第页，共页` 成为独立噪声 chunk。
- 第 10 页 table-03（纯表头 + 空行）被 `_valid_table`/`_split_embedded_tables` 判 invalid，触发 `table_structure` 门禁 fail。

#### 修复

**① 表格优先于公式误判（`vector_pdf.py`）**
- `preflight_pages`：`_has_math_font` 命中时，若页面已有可恢复的矢量表格（`_extract_best_tables` 非空），不再判为 `formula_pages`（表格信号优先）。
- `parse_pages`：页面同时在 table_pages 和 formula_pages 时，`page_type` 设为 `table` 而非 `formula`，避免生成整页 `formula_page` 资产覆盖表格内容。

**② 同页相邻表格合并（`pipeline.py`）**
- `merge_cross_page_tables` 新增同页合并分支：同页两个表格块 bbox 上下相邻（左右边界一致、间距 ≤40pt）且表题均为空时，合并为一个表格块（窄表右填充到宽表列宽保持矩形）。空白记录表不再拆成两个割裂 chunk。

**③ 表单噪声过滤（`pipeline.py`）**
- 新增 `FORM_STANDARD_NUMBER_RE`/`FORM_DASH_ONLY_RE`/`FORM_PAGE_PLACEHOLDER_RE`，在 `split_blocks` 中过滤标准编号残渣（`DB4401/T`）、孤立破折号、`第页，共页` 占位符。

**④ 纯表头空白记录表保留（`pipeline.py`）**
- `_split_embedded_tables` 和 `merge_cross_page_tables` 接受"只有表头行 + 全空数据行"的空白记录表（单行表头、宽度≥2），不再判 invalid。

**⑤ 混合方案：附加整页原图（`vector_pdf.py` + `hybrid_pdf.py`）**
- 页面文本含"记录表/登记表/申报表"标题时，生成整页 `form_page` 资产（`p00NN-form-01`），挂到表格块 `asset_ids` 首位。
- `VectorBlock` 新增 `asset_ids` 字段；`hybrid_pdf.py` rich_block 透传。
- 发布时 `form_page` 作为 chunk 的 `image_base64`（整页原图），表格局部图作为 `positions` 定位。

#### 效果（真实文档）

| 页面 | 修复前 | 修复后 |
|---|---|---|
| 第11页 A.2 | route=formula_review，8 个碎片 chunk（含噪声），2 个割裂 table | route=table，干净 4 块（导语/表题/合并表单/落款），table chunk 带 `p0011-form-01` 整页图 |
| 第10页 A.1 | 3 个 table + `第11页公式原版图像` 泄漏 | 3 个 table + `p0010-form-01` 整页图，无公式泄漏 |
| 第12页 A.3 | 2 个 table + Wingdings2 乱码 | 2 个 table + `p0012-form-01` 整页图 |

#### 测试

- 新增 5 个回归测试：
  - `test_merges_same_page_adjacent_blank_title_tables_into_form`
  - `test_same_page_merge_requires_adjacent_bbox`
  - `test_split_blocks_filters_form_placeholder_noise`
  - `test_preflight_table_page_with_math_font_is_not_formula`
  - `test_parse_pages_table_wins_over_math_font`
- 完整单元测试：**171 passed, 3 failed**（3 个既有失败与本次无关）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | 表格优先路由、`_looks_like_record_form`、`form_page` 资产、`VectorBlock.asset_ids` |
| `app/ingestion/parsers/hybrid_pdf.py` | rich_block 透传 `asset_ids` |
| `app/ingestion/pipeline.py` | 同页表格合并、表单噪声过滤、纯表头记录表保留 |
| `tests/unit/test_regulation_preprocess.py` | 新增 3 个测试 |
| `tests/unit/test_vector_pdf.py` | 新增 2 个测试 + 更新 1 个断言 |

#### 已知限制

- 表底 CambriaMath 公式注仍乱序（如 `V0=Vt×293×...273+101.3`），是公式结构还原问题（08-04 已知待办），非本次范围。所有关键符号保留，检索可命中。

### 2.2 修复：A.2/A.3 记录表跨页误合并（2026-08-05）

> 用户核验发布内容时发现：《化学中毒》第 11 页 A.2 和第 12 页 A.3 被错误合并成一个 chunk（页码 11-12），A.3 的字段区内容混入 A.2。

#### 问题

发布内容出现：
```
章节：附录 A / A.2 空气中化学毒物采样记录表  页码：11-12
| 现场地址 | ... | 检测仪器，编号 | ... | 气象条件 | ...
```
A.2（第 11 页）与 A.3（第 12 页）是两个**独立的空白记录表**，但被当作跨页续表合并。

#### 根因

- 同页合并把 A.2 的字段区（2 列）+ 数据区（10 列）合并成一个表格块，但合并时 `previous["rows"].extend(...)` **未右填充矩形化**，`previous["rows"][0]` 仍是 2 列（字段区第一行）。
- 跨页合并分支判断 `same_width = len(previous["rows"][0]) == len(fragment["rows"][0])`（2 vs 2 = True），且两表 `title_key` 都为空 → 误判为续表，把第 12 页 A.3 字段区（2 列）合并进 A.2。

#### 修复（`pipeline.py`）

- 合并后若 `previous["rows"]` 行宽不一致（`len({len(row) for row in rows}) > 1`），立即 `_rectangular_rows` 右填充矩形化，使后续跨页宽度判断使用真实合并宽度（10 vs 2 → 不合并）。
- `row_pages` 同步补齐。

#### 验证

- 真实文档端到端：A.2 page=11-11（8 行 10 列）、A.3 page=12-12（6 行 8 列，含"注：检测区域分为隔离区（热区）..."），互不混入；`cross_page_table_count=0`。
- 新增回归测试 `test_same_page_merged_form_not_cross_merged_with_next_page`。
- 完整单元测试：**172 passed, 3 failed**（既有失败）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | 合并后矩形化行宽，防止跨页宽度误判 |
| `tests/unit/test_regulation_preprocess.py` | 新增跨页误合并回归测试 |

### 2.3 修复：图题误标"表A.1"导致表格门禁失败（2026-08-05）

> 用户报告《无人机遥感法鉴定高水效小麦品种技术规程》解析失败（status=needs_review，table_structure 门禁 fail）。

#### 问题

`qa.json` 报：
```
[fail] table_structure: Table candidates remain unstructured and publication is blocked: [8]
[fail] table_caption_alignment: Detected table captions were not bound one-to-one to structured tables: [8]
```

第 8 页被列为表格候选但未结构化 → unresolved。

#### 根因

第 8 页正文是"附录 A 产量预测模型 MultimodalNet 使用方法"，包含一张**流程图**，但图题文字被 PDF 排版误标为 `表 A.1 小麦产量预测模型构建流程`（正文写"构建流程见图A.1"）。native 层 `detect_table_hints` 检测到"表 A.1"→ 页面 route=`layout_review` → 进入 `layout_targets` → 进入 `table_candidate_targets`。但 preflight 的 `_extract_best_tables` 拒绝该页（无真实表格线框，附录标题网格被 `_valid_table` 过滤）→ 不在 `preflight.table_pages` → `parse_pages` 不处理 → unresolved，门禁失败。

矛盾点：`table_candidate_targets = layout_targets | preflight.table_pages`，native 的表题误判无法被 preflight 纠正。

#### 修复（`hybrid_pdf.py`）

`table_candidate_targets` 不再从 `layout_targets` 初始化，**只以 `preflight.table_pages` 为准**。layout_review 页会被 preflight 探测（`vector_preflight_targets` 包含 layout_targets），若确有矢量表格线框会进入 `preflight.table_pages`；仅有图题/附录标题而无表格的页被排除。

#### 验证

- 真实文档端到端：`table_candidate_pages` 从 `[7,8]` 变 `[7]`，`table_series` 从 `[[7,8]]` 变 `[[7]]`，第 8 页不再 unresolved；`table_structure`/`table_caption_alignment` 门禁通过。
- 新增回归测试 `test_preflight_keeps_figure_caption_page_out_of_table_pages`。
- 完整单元测试：**173 passed, 3 failed**（既有失败）。ruff 全部通过。
- 注意：旧产物（Aug 5 10:18）仍是失败状态，需重新解析刷新。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/hybrid_pdf.py` | `table_candidate_targets` 只以 preflight.table_pages 为准，排除无表格线框的图题页 |
| `tests/unit/test_vector_pdf.py` | 新增图题页排除回归测试 |

### 2.4 修复：《小麦》文档 5 个解析质量问题（2026-08-05）

> 用户检查《无人机遥感法鉴定高水效小麦品种技术规程》解析结果，发现"问题很大"。逐一修复 5 个内容质量问题。

#### 问题 1：图 1（技术流程图）章节归属错误

- **现象**：第 5 页顶部图 1 被归到 `5.3.5.3 气象条件` 章节。
- **根因**：figure 页正文走 native cleaned_text（无 bbox），figure 块（有 bbox）被 `split_blocks` 追加到页面文本末尾，未按坐标排序。
- **修复**：
  - `vector_pdf.py`：figure 页（`page_type == "figure"`）额外用 `_line_blocks` 生成带 bbox 的正文 paragraph 块，使整页内容都带坐标。
  - `pipeline.py`：`split_blocks` 的 `page_parts` 按 bbox top 排序（当 rich_blocks 带坐标时）。
  - `pipeline.py`：figure caption 去重——`_line_blocks` 重新提取的图题行不再重复（保留 figure 块承载图题）。
- **效果**：图 1 figure 块归属 `['4 技术流程图']`，正文段落按 5/5.3.x 正确分节。

#### 问题 2：公式编号污染 `WERI=Y_T/Y_CK·1`

- **现象**：第 6 页公式 `WERI=Y_T/Y_CK·1`，末尾 `·1` 是公式编号 `(1)` 的 OCR 误识别。
- **修复**（`remote.py`）：`latex_to_text` 剥离公式末尾的 `·<1-2位数字>`（仅当公式只有一个 `·`，化学式点乘 `MgSO4·7H2O` 不受影响）。
- **效果**：`WERI=Y_T/Y_CK·1` → `WERI=Y_T/Y_CK`；`(MgSO4·7H2O):0.7g;` 保留。

#### 问题 3：`5.3.6 数据处理` 空标题 chunk

- **现象**：`5.3.6 数据处理` 生成只有标题无正文的独立 chunk。
- **根因**：旧代码下该行 `heading_kind` 未识别为 clause。
- **状态**：当前代码下 `heading_kind("5.3.6 数据处理") == "clause"`，`build_chunks` 将其作为父章节 context，子章节 chunk 前缀带 `5.3.6 数据处理`。**已自然修复**（旧产物是旧代码差异）。

#### 问题 4：附录 A 标题与正文粘连

- **现象**：`(资料性)产量预测模型 MultimodalNet 使用方法遥感产量预测模型...` 粘连成一行。
- **根因**：`clean_page` 按标点合并段落，`（资料性）` 和附录标题行无句末标点，与正文合并。
- **修复**（`pipeline.py`）：
  - 新增 `ANNEX_MARKER_RE` 匹配 `(资料性)/(规范性)/(附录性)`。
  - `clean_page` 让 `(资料性)` 单独成段，且紧跟其后的附录标题行也单独成段。
- **效果**：`（资料性）`、标题行、正文各自独立。

#### 问题 5：第 8 页装饰线误识别为 figure

- **现象**：第 8 页底部一条水平装饰线（`[246,742,363,767]`，117×25pt）被当作独立 figure，VLM 判为"一条黑色水平直线"。
- **修复**（`vector_pdf.py`）：figure 生成时过滤高度 <35pt 且 高度/宽度 <0.3 的细长水平装饰元素。
- **效果**：第 8 页只剩 1 个 figure（真流程图），装饰线不再成 chunk。

#### 测试

- 新增 4 个回归测试：
  - `test_clean_page_keeps_annex_marker_and_title_on_own_lines`
  - `test_latex_to_text_strips_trailing_equation_number`
  - `test_figure_page_paragraphs_carry_bboxes_for_reading_order`
  - `test_figure_page_caption_not_duplicated_in_paragraph`
- 完整单元测试：**177 passed, 3 failed**（既有失败）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | figure 页正文 `_line_blocks`；过滤细长装饰图 |
| `app/ingestion/parsers/remote.py` | `latex_to_text` 剥离公式末尾编号残留 `·N` |
| `app/ingestion/pipeline.py` | `split_blocks` bbox 排序；figure caption 去重；`ANNEX_MARKER_RE` + 附录标题独立成段 |
| `tests/unit/test_regulation_preprocess.py` | 新增 4 个回归测试 |

## 3. 验证命令

```powershell
Set-Location D:\internal-rag\source\internal-rag
.\.venv\Scripts\python.exe -B -m pytest tests\unit -q -p no:cacheprovider
.\.venv\Scripts\python.exe -B -m ruff check app tests\unit --no-cache
.\deploy\gateway\stop.ps1
.\deploy\gateway\start.ps1
```

代码改动后必须重启 Gateway（无热重载）；前端 `Ctrl+F5` 刷新。

## 4. 已知遗留问题（来自 08-04 交接，可能作为本会话输入）

1. **表格+整页图冗余**：`vector_pdf.py` 需区分"纯表格页"vs"表格+插图页"（当页面有真实非表格图片 `image_coverage >= 0.05` 才生成 figure_page）
2. **OCR 英文术语假字**：MinerU 对小号拉丁字符识别精度不足，需 OCR 精度优化或视觉模型复核
3. **TOC 打分阈值未校准**：`toc_score >= 0.85` 目前目录页达不到，需积累真实分布或接入 VLM 复核
4. **8G 显存约束**：MinerU 与 Ollama 不能同时满载，否则 MemoryError

### 2.5 修复：《小麦》第 5 页子节编号缺失（2026-08-05）

> 用户反馈：第 5 页从 `5 高水效小麦品种鉴定` 直接跳到 `5.3.1`，中间 `5.1/5.2/5.3` 缺失。

#### 问题

PDF 原文第 5 页：
```
5 高水效小麦品种鉴定
基本要求          ← 实际是 5.1（编号未渲染）
种子要求          ← 实际是 5.2
试验要求          ← 实际是 5.3
5.3.1 田间布置
```
子节标题的编号在 PDF 排版中缺失，解析把 `基本要求/种子要求/试验要求` 当普通段落，`5.3.1` 直接以 `5` 为父级，section_path 缺 `5.3 试验要求`。

#### 修复（`pipeline.py`）

- 新增 `_looks_like_unnumbered_subheading`：识别无编号子节标题（纯中文名词短语、3-20 字、无标点、无英文、非列表项、非表题/图题/公式引导词）。
- `split_blocks` 中，当无编号子节标题出现在顶层条款（编号无点号，如 `5`）下时，按顺序推断编号：`基本要求→5.1`、`种子要求→5.2`、`试验要求→5.3`，合成 `5.x 标题` 作为 active clause。后续 `5.3.1` 正确嵌套在 `5.3 试验要求` 下。
- `clean_page` 让无编号子节标题行独立成段（不被合并进正文）。
- **上下文保护**：只有候选标题后跟正文段落（或更深编号子节）时才提升，避免表格单元格（如 `大豆苗情等级` 后跟 `好(1级)…`）和术语定义项被误判。
- 保守条件：只有 `clause_stack[0]` 是顶层条款（无点号编号）时才推断。

#### 效果

- 《小麦》第 5 页：`5.1 基本要求` / `5.2 种子要求` / `5.3 试验要求` 全部生成，`5.3.1` 的 section_path = `['5 高水效小麦品种鉴定', '5.3 试验要求', '5.3.1 田间布置']`（三级）。
- 《大豆》第 6 页：`5.1 无人机影像数据获取及预处理` / `5.2 归一化差值植被指数计算`、`6.1 数据存储` / `6.2 关联分析` / `6.3 构建遥感监测模型` / `6.4 生成大豆苗情遥感长势指数分布图` 全部推断；第 7 页仅真实 `7.1`/`7.2`，表头 `大豆苗情等级` 不误判。
- 《大豆》第 5 页："3 术语和定义"下术语项编号为 `3.1 太阳反射波段多光谱遥感 multispectral remote sensing` / `3.2 苗情长势 growth conditions` / `3.3 归一化差值植被指数 normalized difference vegetation index;NDVI`（新增术语项识别，见下）。
- 化学中毒文档 A.1/A.2/A.3 表格无回归。
- 完整单元测试：**183 passed, 3 failed**（既有失败）。ruff 全部通过。

#### 术语项编号（2026-08-05 追加）

- 问题：GB/T 1.1 术语章节（`3 术语和定义`）的术语项（术语名+英文名）在 PDF 中无编号，且被 clean_page 粘连进定义正文。
- 修复：
  - 新增 `_TERM_HEADING_RE` + `_looks_like_term_heading`：识别"中文术语名 + 英文术语名"行（如 `太阳反射波段多光谱遥感 multispectral remote sensing`）。
  - `clean_page` 让术语项行独立成段（不粘连定义正文）。
  - `split_blocks` 把术语项按顺序编号为 `3.1/3.2/3.3`（在 `3 术语和定义` 条款下）。
- 回归测试：`test_term_definitions_get_inferred_numbers_in_terminology_clause`。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `_looks_like_unnumbered_subheading` + `clean_page` 独立成段 + `split_blocks` 编号推断（含上下文保护） |
| `tests/unit/test_regulation_preprocess.py` | 新增推断/长标题/表头不误判回归测试 |

### 2.6 修复：图题误标"表A.1"触发 caption-alignment 门禁失败（2026-08-05）

> 用户反馈 `Detected table captions were not bound one-to-one to structured tables: [8]`。

#### 问题

《小麦》第 8 页的图题被 PDF 误标为 `表 A.1 小麦产量预测模型构建流程`（正文写"见图A.1"）。`page.table_hints` 含该表题 → `merge_cross_page_tables` 的 `expected_caption_refs` 计入 `表a1`，但第 8 页没有真实结构化表格（是流程图）→ `actual_caption_refs` 为空 → `table_caption_mismatch_pages: [8]` → `table_caption_alignment` 门禁 fail。

#### 修复

- `merge_cross_page_tables` 新增可选参数 `table_candidate_pages`。
- 传入后，只有真实表格候选页（`table_candidate_pages`）的表题提示才计入 `expected_caption_refs`；非候选页（图题误标、附录标题等）的表题不计入，不再触发 mismatch。
- `hybrid_pdf._rebuild` 传入 `table_candidate_pages`（即 preflight 确认的真实表格页）。
- 不传参数时行为完全兼容（默认 None，所有页表题都计入，保持旧行为）。

#### 验证

- 真实文档模拟：修复前 `table_caption_mismatch_pages: [8]`，修复后 `[]`。
- 新增回归测试 `test_caption_mismatch_ignores_figure_page_with_mislabeled_table_caption`。
- 完整单元测试：**180 passed, 3 failed**（既有失败，其中 `test_cross_page_table_continuation_satisfies_caption_reference` 与本改动无关）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `merge_cross_page_tables` 新增 `table_candidate_pages` 过滤 expected 表题 |
| `app/ingestion/parsers/hybrid_pdf.py` | `_rebuild` 传入 `table_candidate_pages` |
| `tests/unit/test_regulation_preprocess.py` | 新增图题误标不触发 mismatch 回归测试 |

### 2.7 修复：解析卡 15% 的 VLM 阻塞（2026-08-05）

> 用户反馈《无人机监测松材线虫病致死松树技术规程》《无人机飞播造林技术规程》解析卡在 15%。

#### 根因 1：table_figure 页无条件触发 VLM

- 《飞播造林》第 17 页是 `table_figure`（表格 + 1 张覆盖率 0.58% 的小图）。`_merge_vector_pages` 旧逻辑把 `table_figure` 页**无条件加入 `figure_review_pages`** → 触发 Ollama VLM 图片描述。
- Ollama `qwen3.5:9b` 处理图片请求时卡死不返回（`sock.recv` 阻塞 300s 超时），导致 job 卡在 parsing 15%。
- 小图（覆盖率 ~0.5%）是装饰元素，本不需要 VLM 描述。

#### 修复（`hybrid_pdf.py`）

`table_figure` 页**只有实际生成了独立 `figure` 资产**（真实插图）时才加入 `figure_review_pages`；只有整页 `figure_page` + 表格的页（小图为装饰）不再触发 VLM。

```python
elif result.page_type == "table_figure":
    has_figure_asset = any(asset.asset_type == "figure" for asset in result.assets)
    if has_figure_asset:
        figure_review_pages.add(page_number)
```

#### 根因 2：Ollama VLM 服务卡死（环境问题）

- 《松材线虫》卡在 VLM（figure 页 [10,11,13] 触发图片描述），Ollama 处理图片时挂起，连接失败。
- 日志证据：`connect_tcp.started port=11434 timeout=300` → `connect_tcp.failed All connection attempts failed`。
- 处理：重启 Ollama 后恢复正常。8G 显存约束下，`qwen3.5:9b` 图片推理可能崩溃。

#### 验证

- 《飞播造林》修复后 `figure_review_pages: []`，完整解析 4.5s。
- 完整单元测试：**184 passed, 3 failed**（既有失败）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/hybrid_pdf.py` | `table_figure` 页仅在有独立 figure 资产时触发 VLM |

### 2.8 修复：2 字术语名被合并（2026-08-05）

> 用户反馈《无人机飞播造林技术规程》第 5、6 页 3.3 区块被合并，标题移位。

#### 问题

- 《飞播造林》术语名（无人机飞播造林/小播区群/区块/宜播地/播幅/有效苗/复播/临时起降场/单程式/穿梭式）在 PDF 中无编号，纯中文。
- `_UNNUMBERED_SUBHEADING_MIN=3` 导致 2 字术语名（`区块`、`播幅`、`复播`）不被识别为术语标题，被并入上一术语的正文（如"区块"并入 `3.2 小播区群`）。

#### 修复（`pipeline.py`）

`_UNNUMBERED_SUBHEADING_MIN` 从 3 降到 2，允许 2 字术语名。`好`（1 字）仍被排除。

#### 验证

- 《飞播造林》术语正确编号：3.1 无人机飞播造林 / 3.2 小播区群 / 3.3 区块 / 3.4 宜播地 / 3.5 播幅 / 3.6 有效苗 / 3.7 复播 / 3.8 临时起降场 / 3.9 单程式。
- 《大豆》第 7 页表头 `大豆苗情等级` 不误判，第 5 页 3.1/3.2/3.3 正常；《小麦》5.1/5.2/5.3 正常。
- 新增回归测试 `test_two_char_unnumbered_subheadings_get_inferred_numbers`。
- 完整单元测试：**184 passed, 3 failed**（既有失败）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | `_UNNUMBERED_SUBHEADING_MIN` 3→2 |
| `tests/unit/test_regulation_preprocess.py` | 新增 2 字术语编号回归测试 |

### 2.9 修复：跨页子节标题导致编号位移（2026-08-05）

> 用户反馈《无人机飞播造林技术规程》第 7 页漏了 6.3（种子处理），标题移位。

#### 问题

- 《飞播造林》第 6 页页尾是子节标题 `种子质量`（2 字，无编号），正文（"种子调拨按GB/T 15776规定执行…"）在第 7 页。
- `split_blocks` 的 `next_is_body` 只检查**同页内下一个 block**。`种子质量` 是第 6 页最后一个 block，无下一个 → 判断失败，未编号为 6.1。
- 后续 `种子使用` 被编号为 6.1、`种子处理` 为 6.2，整体位移，真正的 6.3 缺失。

#### 修复（`pipeline.py`）

无编号子节标题推断时，`is_last_block_on_page`（候选是页内最后一块）也视为可提升——正文在下一页继续。

```python
is_last_block_on_page = local_index + 1 >= len(page_parts)
if (
    base_number and "." not in base_number
    and (next_is_body or next_is_deeper_clause or is_last_block_on_page)
):
    ...
```

#### 验证

- 《飞播造林》第 7 页：`6.1 种子质量`（页尾标题）/ `6.2 种子使用` / `6.3 种子处理` 编号正确。
- 《大豆》《小麦》无回归。
- 新增回归测试 `test_cross_page_unnumbered_subheading_is_inferred`。
- 完整单元测试：**185 passed, 3 failed**（既有失败）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/pipeline.py` | 无编号子节推断支持"页尾标题"（正文跨页） |
| `tests/unit/test_regulation_preprocess.py` | 新增跨页子节标题回归测试 |

### 2.10 修复：纯矢量流程图未识别为 figure（2026-08-05）

> 用户反馈《低空数字航空摄影测量外业规范》第 8 页的图片未能正确识别。

#### 问题

- 第 8 页"图1 工作流程图"是**纯矢量流程图**：14 个 rect 框 + 10 条 curve 箭头 + 框内文字，**没有任何图片对象**（`images=0`）。
- `_flowchart_regions` 要求 `image_count > 0`，对纯矢量流程图无效。
- preflight 的 figure 判定 `if image_count and (...)` 也因 `image_count=0` 跳过，即使页面有"图1 工作流程图"图题。
- 结果第 8 页未被识别为 figure 页，流程图缺失。

#### 修复（`vector_pdf.py`）

1. `_flowchart_regions` 支持**纯矢量流程图**：`image_count == 0` 但 `rects >= 6` + `curves >= 4`（大量框 + 箭头）时也识别。
2. preflight figure 判定：`has_figure_caption and pure_vector_flow`（有图题 + 纯矢量流程图）时判为 figure 页，即使无图片。
3. `is_tile_flow` 的 `small_image_ratio` 对 `image_count == 0` 安全（除零保护）。

#### 验证

- 《外业规范》第 8 页：figure_pages 从不含 8 → 含 8；生成 `p0008-figure-01`（bbox `[185.2, 170.7, 437.5, 373.7]`，caption "图1 工作流程图"）。
- 荒漠第 7 页（表格+多图）：从多小图合并为 1 个流程图 figure，VLM 调用次数减少。
- 飞播第 13 页（154 rect 但无 curve）：不误判为流程图。
- 新增回归测试 `test_flowchart_regions_detect_pure_vector_flow_chart`、`test_preflight_flags_pure_vector_flow_chart_with_caption`。
- 完整单元测试：**189 passed, 3 failed**（既有失败）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | `_flowchart_regions` 支持纯矢量流程图；preflight figure 判定加纯矢量流程图信号 |
| `tests/unit/test_vector_pdf.py` | 新增纯矢量流程图识别回归测试 |

### 2.11 修复：标准封面页被误判为表格（2026-08-05）

> 用户反馈《基于系留无人机的应急通信空中基站技术要求》第 1 页解析不对。

#### 问题

- 第 1 页是标准封面（ICS/CCS 分类号 + 标准号 YD/T + 发布/实施）。封面有**装饰性框线**（9 个 rect）。
- pdfplumber 把封面标题"中华人民共和国通信行业标准"和"YD/T××××—××××"误判为表格网格，`_valid_table` 判定通过 → 第 1 页被列为 table_pages → 封面文字被拆成表格 chunk（结构错误）。
- 且封面 chunk 的 page 范围变成 1-5（与第 5 页公式页粘连）。

#### 修复（`vector_pdf.py`）

- 新增 `_looks_like_cover_page`：识别标准封面页（`ICS/CCS` 分类号 + 标准号如 `YD/T`/`DB41/T` + `发布/实施` 标记）。
- `preflight_pages` 中，封面页**跳过表格检测**（其框线是装饰，不是真实表格）。

#### 验证

- 《应急通信空中基站》第 1 页不再判为 table_pages，封面标题不再被拆成表格 chunk。
- 大豆/小麦/化学中毒/飞播等标准封面页均被识别为 cover（不误判正文页）。
- 新增回归测试 `test_cover_page_not_treated_as_table`、`test_preflight_excludes_cover_page_from_table_pages`。
- 完整单元测试：**191 passed, 3 failed**（既有失败）。ruff 全部通过。

#### 改动文件

| 文件 | 改动 |
|---|---|
| `app/ingestion/parsers/vector_pdf.py` | `_looks_like_cover_page` + preflight 封面页跳过表格检测 |
| `tests/unit/test_vector_pdf.py` | 新增封面页排除回归测试 |
