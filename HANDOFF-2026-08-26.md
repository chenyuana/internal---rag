# Internal RAG 改动记录（2026-08-26）

> 用途：记录 2026-08-26 的问题分析、改造设计与后续实施结果，供后续智能体快速续接。
> 项目运行环境为 Windows，代码、运行时、模型与数据统一位于 `D:\internal-rag`；不得改为 WSL 部署。
> 本文沿用既有格式，按日期持续追加“问题 / 根因 / 修复 / 验证 / 改动文件 / 注意”。

---

## 0. 交接起点与本日范围

- **交接起点**：`HANDOFF-2026-08-25.md`。
- **本日问题**：用户在智能问答窗口上传《文档格式要求_测试框架 (1).docx》后，要求系统“生成无人机适航要求及符合性验证文档，内容详细、有数据支撑、由知识库文件支撑，附件只作为文档格式要求”。系统却输出“编制指南/格式要求摘要”；追问“将内容完整表达出来”后，仍只扩写附件格式说明。
- **本日任务边界**：先完成根因核对与改造方案记录，不直接修改业务源码、配置、提示词和测试；后续实施结果继续追加到第 3 节。
- **本日新增文件**：仅本文档。

---

## 1. 现象与正确预期

### 1.1 实际现象

首次回答将任务主题改写成“无人机适航要求及符合性验证文档编制指南”，正文主要说明：

- 持续适航文件的编制依据；
- 上传附件中的页面设置、字号、标题层级；
- 少量知识库条款摘录。

第二次追问后，回答进一步收缩为附件中的“基础结构与章节组织、页面与排版格式”，没有继续完成适航要求、符合性方法、验证数据和证据追溯矩阵。

### 1.2 正确预期

用户请求包含两个语义角色，必须明确分离：

1. **知识库文件是事实证据源**：用于支持适航要求、条款、指标、验证方法、判据和引用；
2. **上传附件是格式/模板源**：只控制章节层级、版式、字体、表格样式和交付形态，不应成为适航事实证据，更不能抢占回答主题。

系统应先形成完整文档大纲，再按章节检索和撰写，至少覆盖：目的与范围、适用法规/标准、审定基础、系统级要求、结构与材料、动力/能源、飞控与导航、指挥控制链路、地面站与数据链、软件与复杂电子硬件、环境适应性、电磁兼容、安全性/失效处置、运行限制、持续适航，以及“要求—证据—符合性方法—试验条件—判据—结果记录”的符合性验证矩阵。知识库缺少项目实测值时必须标注待补，不能把示例值写成已验证结果。

---

## 2. 根因核对

### 2.1 P0：同一个附件对象同时承担“格式参考”和“事实证据”

当前 `ReferenceDocument` 的注释即为“auxiliary evidence and presentation context”。`RagPipeline.run()` 在知识库检索完成后调用 `_attach_auxiliary_document()`，把格式附件转换成 `SelectedChunk` 和 `Citation`，直接并入知识库证据、引用和覆盖矩阵。

`build_auxiliary_evidence()` 又给附件 Chunk 固定设置 `hybrid_score/vector_score/keyword_score/rerank_score = 1.0`。对于 summary/procedure 类型，还直接取附件前 8 个 Chunk，不按“格式参考”角色过滤。这使附件中的“页面设置、标题层级、正文格式”等内容成为高权重事实证据，足以改变 Evidence Judge 的覆盖判断和最终生成焦点。

**结论**：本次错误的首要原因不是模型能力不足，而是后端把格式模板错误注入了事实证据通道。

### 2.2 P0：系统提示词与“详细完整文档”要求正面冲突

`prompts/answer_generation.txt` 第 18 条要求“回答控制在500字以内”。该约束适合普通问答，不可能承载一份详细的适航要求及符合性验证文档。用户要求越详细，模型越需要在“服从用户长度要求”和“服从系统 500 字限制”之间取舍，系统约束必然优先。

当前 `StructuredAnswer` 也只描述单次问答的 `answer + claims + missing_information`，没有文档章节、要求条目、符合性方法、验证矩阵和附件产物等长文档结构。

### 2.3 P0：没有“文档生成”意图和专用生成链路

`QueryAnalyzer` 只支持 comparison、multi_hop、procedure、summary、fact 和 unknown。“生成/编制/撰写……文档”不会进入专用任务类型；本次语句没有“总结/概述/流程/如何”等稳定标志，通常落入 unknown 的单查询直答路径。

这会造成：

- 只用一条宽泛检索问题召回少量高相似 Chunk；
- 没有先规划文档章节，也没有逐章节补检索；
- 没有对“适航要求”和“符合性验证”建立覆盖矩阵；
- 普通问答生成器试图一次性在 500 字内完成长文档。

### 2.4 P0：追问实际上没有服务端会话上下文

前端把历史消息保存在浏览器 `localStorage`，每次请求只发送当前 `query`、知识库 ID、`conversation_id`、模型和附件。后端仅原样返回或新建 `conversation_id`，没有读取此前消息，也没有把上一轮问题/答案加入 planner、retrieval 或 answer generator。

因此，“将内容完整表达出来”会被当成一个新的独立问题。它没有“无人机适航要求及符合性验证文档”的主题信息，却仍附带格式附件；检索和生成自然进一步向格式说明偏移。

### 2.5 P1：DOCX 格式附件解析后丢失真正的版式信息

`parse_reference_document()` 对 DOCX 仅提取段落文本和用 ` | ` 拼接的表格文字，没有保留：

- 页面尺寸、方向和页边距；
- 段落样式、字体、字号、行距、缩进；
- 标题级别与自动编号；
- 页眉页脚、封面、目录、分节与分页；
- 表格宽度、边框、底纹、单元格合并；
- 图片、题注和交叉引用。

同时，答案模型实际只接收 `presentation_reference.text[:4000]`。因此当前系统既把附件误当证据，又没有能力真正复用附件的 Word 格式。

### 2.6 P1：证据预算与输出形态不支持系统级技术文档

普通生成默认最多选择 8 个最终 Chunk、16 条 evidence sentence；这对单点问答合理，但不足以覆盖适航审定多个专业域。前端也只有 Markdown 回答和“复制回答”，没有 DOCX 生成、下载、章节续写或任务进度机制。

---

## 3. 今日改动记录

### 3.1 诊断：格式附件导致长文档生成任务偏题

#### 内容

- 核对智能问答前端的附件上传、消息发送和历史会话实现。
- 核对 `ReferenceDocument`、附件解析、辅助证据构建、覆盖矩阵增强和答案生成链路。
- 核对查询分类、检索/证据预算、结构化答案 Schema 与系统提示词。
- 确认问题由任务建模、附件角色、长度约束和会话上下文四层共同导致，不能只靠修改用户提示词解决。

#### 验证

- `app/web/static/chat.js`：历史消息只保存在浏览器；请求不携带历史消息。
- `app/pipelines/rag_pipeline.py`：格式附件被合并进 `selected_chunks/citations/coverage_matrix`。
- `app/services/reference_document.py`：附件 Chunk 固定满分，DOCX 只抽取纯文本。
- `prompts/answer_generation.txt`：存在 500 字硬限制。
- `app/services/query_analyzer.py`：不存在 document_generation 任务类型。
- 本节为只读诊断，未运行真实问答、未重启服务、未修改业务文件。

#### 改动文件

- 新增：`HANDOFF-2026-08-26.md`。

---

### 3.2 新增：独立“文档编制”工作台首版

#### 内容

- 新增 `/documents` 独立页面，并在资料接入台、智能问答、文档编制和评测结果之间建立统一导航。
- 工作台采用四栏结构：文档项目、大纲、章节编辑、引用与缺失检查；窄屏时自动收起次要面板。
- 文档项目和章节草稿保存在当前浏览器，支持新建/切换/删除项目、自动保存、章节新增/删除、编制状态与完成进度。
- 项目设置支持文档名称、编制目标、使用对象、目标篇幅和知识库范围。
- 将上传入口明确拆分为：
  - **格式模板**：只控制版式和章节呈现，不参与事实引用；
  - **补充证据**：参与事实生成与引用，不改变版式。
- 复用现有安全的请求级文档解析接口读取附件；浏览器草稿只保存有界文本预览，避免保存整个源文件。
- 新增适航/符合性任务的 15 节确定性初始大纲，覆盖适航审定基础、系统安全、结构、动力、飞控、链路、软件、环境、运行限制、持续适航、符合性方法和验证矩阵；其它主题使用通用 8 节大纲。
- 章节编辑器实时统计字数和 `[C1]` 形式的引用数；右侧质量门禁检查任务信息、知识库、大纲、引用和章节完成度。
- “在智能问答中检索本节证据”会生成包含文档主题、章节要求、数据/知识库支撑要求的完整查询，并通过 `/chat?q=...` 带入问答输入框，避免省略上下文。
- 支持把当前项目、大纲、正文和资料角色信息导出为 Markdown 草稿。

#### 当前边界

- 本版是可操作的编制工作台与本地草稿闭环，未把旧的 500 字问答生成器伪装成长文档生成器。
- AI 逐章节检索/生成、服务端项目持久化、引用回填和 DOCX 模板套版仍需按第 4 节方案实现独立后端。
- `/documents` 路由需要重启当前 Gateway 后才会在正式 8080 进程生效；本日浏览器验证使用独立 8091 测试进程，未清除现有 Gateway 内存中的临时答案模型连接。

#### 验证

- 页面/静态资源/现有页面导航回归：**4 passed**。
- ruff（`app/main.py`、新增页面测试）：**All checks passed**。
- `node --check`（`documents.js`、`chat.js`）：通过。
- `git diff --check`：通过，仅有仓库既有 LF/CRLF 提示。
- 浏览器实测：创建“无人机适航要求及符合性验证文档”，成功生成 15 节大纲；填写含 `[C1]` 的章节正文并标记完成后，进度显示 **1/15**、本节与全文均显示 **65 字 / 1 条引用**，质量分同步变化。
- 浏览器实测：章节检索链接能在智能问答输入框预填完整问题；两个页面均无 console error。
- 浏览器视觉检查：四栏布局、滚动区域、编辑画布和质量门禁无重叠、截断或乱码。

#### 改动文件

- `app/main.py`
- `app/web/documents.html`
- `app/web/static/documents.css`
- `app/web/static/documents.js`
- `app/web/chat.html`
- `app/web/static/chat.js`
- `app/web/ingestion.html`
- `app/web/evaluation.html`
- `tests/unit/test_document_workbench.py`
- `HANDOFF-2026-08-26.md`

---

### 3.3 改进：取消智能问答跳转，工作台内独立完成章节生成

#### 问题

首版工作台的“在智能问答中检索本节证据”仍会跳转到普通问答页面，继续使用短问答提示词和 500 字约束。实测“编制说明”仍被少量 UHS.1529 持续适航证据带偏，擅自把《无人机适航》缩窄为中高风险无人直升机持续适航文件；同时缺少真正的数据、术语、证据控制原则，并输出“本回答已遵循要求”等元话语。

#### 修复

- 删除工作台到 `/chat` 的章节检索跳转；章节编辑区改为“检索证据并生成本节”按钮。
- 新增 `POST /api/v1/documents/sections/generate`，在工作台内完成：
  1. 按文档标题、章节标题和章节要求构造受控检索问题；
  2. 从选定知识库检索，并合并明确标记为“补充证据”的请求级文件；
  3. 使用独立的 24 条证据句预算；
  4. 调用文档章节专用提示词生成；
  5. 执行引用白名单、有效版本、数值 grounding、主题 grounding 和文件/条款范围校验；
  6. 将正文、引用原文和待补信息直接回填工作台。
- 新增文档章节请求/响应 Schema，支持文档目标、使用对象、章节要求、知识库、运行时模型和最多 8 份补充证据；格式模板字段不在生成请求中，因此不会进入事实证据。
- 新增 `prompts/document_section_generation.txt`，明确：
  - 不复用普通问答的 500 字限制，按证据充分程度生成约 800～1600 个中文字符；
  - 不得擅自缩窄整份文档的适用对象；窄范围证据只能标明其自身适用范围；
  - 文件名/标准号不等于数据，缺少数值、条件或判据时必须列为待补；
  - 区分法规要求、验证安排与已取得的试验结果；
  - “编制说明”必须覆盖目的、对象、边界、术语和证据原则；
  - 禁止输出“本回答已遵循要求”等元话语。
- 工作台项目设置新增章节生成模型选择；生成期间显示状态，完成后保存正文、claims、引用和 missing information。
- 右侧新增“当前章节引用”，展示引用文件、原文摘要和待补信息，用户不离开工作台即可核对。

#### 验证

- 新增服务级测试：专用提示词和章节上下文进入模型、引用正确回传、证据文档/句子统计正确；无证据时不调用模型并安全返回 UNANSWERABLE。
- 工作台回归确认页内生成接口存在，且页面不再包含“在智能问答中检索本节证据”。
- 相关目标测试（新章节服务、工作台、配置、AnswerGenerator、RAG pipeline、附件解析）：**32 passed**。
- ruff：**All checks passed**；`node --check documents.js`：通过；`git diff --check`：通过，仅有既有 LF/CRLF 提示。
- 浏览器实测：选择章节后页内生成按钮可见；不再存在 `researchSection`/智能问答跳转；缺少知识库时在当前章节底部显示“请先选择知识库”，无页面跳转、无 console error。
- 浏览器测试使用独立 8091 进程并关闭 RAGFlow/答案模型，成功生成路径由服务级 FakeRetrieval/FakeModel 测试覆盖；尚未使用正式知识库和 9B 模型做在线内容质量复测。

#### 改动文件

- `app/api/router.py`
- `app/api/v1/documents.py`
- `app/core/config.py`
- `app/schemas/documents.py`
- `app/services/document_section_generator.py`
- `config/default.yaml`
- `prompts/document_section_generation.txt`
- `app/web/documents.html`
- `app/web/static/documents.css`
- `app/web/static/documents.js`
- `tests/unit/test_document_section_generator.py`
- `tests/unit/test_document_workbench.py`
- `HANDOFF-2026-08-26.md`

#### 当前边界

- 正式 8080 Gateway 仍需重启才能加载新 API 和静态资源；为保留用户当前进程内模型连接，本轮未自动重启。
- 首次正式在线复测应使用用户当前失败用例“《无人机适航》—编制说明”，重点检查是否仍错误缩窄为无人直升机持续适航、是否出现元话语、引用是否逐项支持正文。
- 当前为逐章节生成；整份文档一键排队、跨章节一致性检查和 DOCX 套版仍属后续阶段。

---

### 3.4 新增：运行监控页（实时性能监控）

#### 需求

用户希望在修改系统后**实时查看问答性能**（延迟、成功率、错误分布），形成“改代码 → 重启 → 智能问答实测 → 监控页看变化”的迭代闭环；确认与“评测结果”页的批量评测是两条独立链路（评测页只读展示 rag-evaluation 的 SQLite 结果，与智能问答无实时联动）。

#### 内容

- 新增 `/monitor` 页面（导航第 5 个标签“运行监控”，四个旧页面同步加链接）：
  - 每 3 秒自动轮询 `GET /api/v1/monitor/stats`，展示请求数、成功率、平均耗时、**P50/P90/P95/P99 延迟**、平均模型/检索调用数、网关运行时长；
  - 阶段耗时条形图（来自响应 diagnostics 的 `latencies_ms`，成功请求平均）；
  - 错误分布与回答状态分布；
  - 最近 20 条请求表（时间/结果/问题/错误/耗时/模型调用/检索）；
  - 服务健康状态（复用 `ServiceRegistry.probe_all()`，每 15 秒轮询）。
- 新增 `app/services/performance_monitor.py`：进程内内存滑动窗口（上限 1000 条），记录每次 `/api/v1/chat/completions` 的请求状态、错误类型、总耗时、分阶段耗时、模型/检索调用数；`stats(minutes)` 提供分位数/成功率/错误分布/最近请求聚合；**不持久化，网关重启即清空**。
- `app/api/v1/chat.py` 的 `/completions` 包装计时与成败记录：成功记录 answer + 引用 quote（供 RAGAS 使用，不进 `/stats` 载荷）；失败记录 `AppError.code` 或异常类名后原样抛出，不影响既有行为。
- 新增 `GET /api/v1/monitor/stats`、`GET /api/v1/monitor/services`。

#### 验证

- 相关测试（健康、评测 API、流水线、监控、工作台）：**75 passed**；新增 `tests/unit/test_monitor.py` **6 passed**（聚合/分位数/错误分布/窗口过滤/API schema）。
- 全量测试：**522 passed**，6 个失败经 `git stash` 对比确认为**仓库存量问题**（`test_regulation_preprocess`×3、`test_retrieval_service`×3，与本次改动无关）。
- ruff / mypy（本次改动文件）：**All checks passed** / 通过（仓库存量 mypy 错误 26 个均在 `app/ingestion/parsers` 旧文件）。
- 重启 8080 网关（旧进程 27924/29312 → 新进程 4716/36052）后实测：`/monitor` 页 200、`/stats` schema 正常、`/services` 5 个依赖全部 ready。
- 真实问答实测（PowerShell 发送中文时发生 GBK 乱码，改用 Python UTF-8 发送验证）：监控正确记录失败请求 `RAGFLOW_UNAVAILABLE`（ReadTimeout，约 10s）；**顺带发现正式链路检索 RAGFlow 实际超时**（探测正常但真实检索 ReadTimeout），是监控上线后捕捉到的第一个真实信号。

#### 改动文件

- 新增：`app/services/performance_monitor.py`、`app/api/v1/monitor.py`、`app/web/monitor.html`、`app/web/static/monitor.css`、`app/web/static/monitor.js`、`tests/unit/test_monitor.py`
- 修改：`app/api/v1/chat.py`、`app/api/router.py`、`app/main.py`、`app/web/chat.html`、`app/web/ingestion.html`、`app/web/evaluation.html`、`app/web/documents.html`（导航加“运行监控”）

#### 当前边界

- 监控数据仅存内存（1000 条滑动窗口），网关重启即清空；正式 8080 网关已在本节完成后重启并在线生效。
- 网关以无 `--reload` 方式启动，改代码后需手动重启。
- 进程健康探测含网络调用（RAGFlow ~1s），页面 15 秒轮询一次，可接受。

---

### 3.5 新增：RAGAS 校准评测（集成进运行监控页）

#### 需求

用户提出引入外部 RAG 评测框架作为自研评测的**第三方校准**。经对比（RAGAS / DeepEval / TruLens / RAGChecker / Phoenix / LangSmith / MLflow），结论：**只接入 RAGAS**——它提供行业标准四指标（faithfulness / answer_relevancy / context_precision / context_recall）、可完全离线（本地 Ollama）、且项目 `rag_eval` 早已预留 `JudgeBackend` 接口；DeepEval/TruLens/Phoenix 与自研 CLI/监控高度重叠、LangSmith 偏云、MLflow 无生态基础，均不引入。用户选择把 RAGAS 接口直接放进**运行监控页**。

#### 内容

- 监控页新增“RAGAS 校准评测”卡片（始终可见，不随 dashboard 隐藏）：
  - 评测对象二选一：**最近真实请求**（智能问答里问过的问题 + 回答 + 引用 quote 作为 contexts，无 ground_truth，跑 3 指标）或**基准集**（`benchmark.jsonl` 的 question/evidence_texts/reference_answer + 最新一次 `rag-eval run` 在 runs.db 中的答案，跑 4 指标）；
  - 条数上限 3/5/10/20，逐题进度条，可“取消”（题间生效），同一时间只允许一个任务；
  - 结果展示 4 指标均值卡 + 逐题分数表；分数低于 0.4 标红、≥0.7 标绿；
  - 4B judge 单条约 10–30s；context_recall 需更强分类模型，4B 下可能为 null（页面已注明）。
- 新增 `app/services/ragas_evaluator.py`：
  - **懒加载 ragas**（未安装时网关照常启动，接口返回安装提示）；
  - 运行时适配 `ragas.evaluate` 调用约定（0.2.x 传 `llm/embeddings` kwargs，0.3+ 用 `GlobalConfig`）；
  - 逐题评测隔离失败（`raise_exceptions=False` + 单行 Dataset），单题异常不拖垮整批；
  - `RagasTaskManager`：任务状态机 queued/running/done/error/cancelled，进度回调，`asyncio.to_thread` 执行阻塞评测；
  - 内网安全：`RAGAS_DO_NOT_TRACK=true`，不向外发送任何遥测；LLM=本地 `qwen3:4b-instruct-2507-q4_K_M`，Embedding=本地 `qwen3-embedding:0.6b`。
- `performance_monitor.RequestRecord` 扩展 `answer`/`contexts` 字段（仅成功请求，供 RAGAS 取用），`/stats` 载荷不含答案文本。
- 新增 API：`GET /api/v1/monitor/ragas/status`、`GET /ragas/cases`、`POST /ragas/run`、`GET /ragas/tasks/{id}`、`POST /ragas/cancel`；`RagasRunRequest`（Pydantic body，mode + limit 校验）。
- 配置：`RagasSettings`（judge/embedding 模型、Ollama 地址、benchmark_path、max_cases）+ `config/default.yaml` 的 `ragas:` 段。
- **依赖安装与兼容性处理**：
  - 安装 `ragas==0.2.15` + `langchain-ollama==0.3.10`；
  - ragas 0.2.15 与 langchain 1.x 不兼容（`langchain_community.chat_models.vertexai` 在 0.4 被移除），将 langchain 全家降至 0.3.x（langchain 0.3.30 / langchain-community 0.3.31 / langchain-core 0.3.86 / langchain-openai 0.3.35 / openai 2.54.0），卸载 langchain 1.x 系（langgraph / langchain-classic / langchain-protocol 等）；
  - numpy 2.5.2 的 stubs 使用 Python 3.12 `type` 语法，破坏 mypy 3.11 目标，降级 `numpy==2.1.3`；
  - `pyproject.toml` 增加 mypy overrides（ragas/datasets/langchain 系 `ignore_missing_imports`，numpy `ignore_errors`）。
- 测试基建：`tests/conftest.py` 静默 ragas 0.2.x 无条件启动的分析线程（`RAGAS_DO_NOT_TRACK` + 实例级 flush/shutdown 置空 + `ragas` 日志器级别 CRITICAL），避免 pytest 退出时“I/O operation on closed file”噪音。

#### 验证

- 新增 `tests/unit/test_ragas.py` **13 passed**（recent/benchmark 用例构建、limit 截断、NaN 归一、聚合、任务管理器拒绝路径、API schema/404/取消）。
- 全量测试：**535 passed**，6 个失败仍为仓库存量（同 3.4）。
- ruff：**All checks passed**；mypy 本次改动文件通过（仅仓库存量 26 错误）。
- 真实端到端（本地 Ollama）：
  - 探针单题 22.9s 跑完 4 指标：faithfulness 0.667 / answer_relevancy 0.563 / context_precision 1.0 / **context_recall null**（4B 解析分类 JSON 失败，被 `raise_exceptions=False` 降级，不崩溃）；
  - 通过 API 跑基准集 2 题（145.4s）：**faithfulness 0.214 / answer_relevancy 0.852 / context_precision 0.75 / context_recall 0.333（仅 1 题出值）**；
  - 该结果**独立印证自研评测发现**：RAGAS faithfulness 0.21 ≈ 自研关键点覆盖 0.18，确认答案与证据一致性是系统当前真实短板，非单指标偏差。

#### 改动文件

- 新增：`app/services/ragas_evaluator.py`、`tests/unit/test_ragas.py`
- 修改：`app/services/performance_monitor.py`（answer/contexts）、`app/api/v1/monitor.py`（ragas 端点）、`app/api/v1/chat.py`、`app/core/config.py`（RagasSettings）、`config/default.yaml`（ragas 段）、`app/web/monitor.html` / `static/monitor.css` / `static/monitor.js`（RAGAS 面板）、`app/main.py`（ragas_manager 挂载）、`tests/conftest.py`、`pyproject.toml`（mypy overrides）

#### 当前边界

- RAGAS 结果存内存（`RagasTaskManager._last_result`），网关重启即丢；如需历史校准对比应后续持久化到 `rag-eval-reports`。
- 4B judge 是任何 LLM-judge 类外部框架的瓶颈：context_recall 大概率 null、faithfulness 偏严；按既有 9B 抽样校准策略使用，不作为批量门禁。
- benchmark 模式依赖 runs.db 中已存储的答案（最新 run 的已评测题），未跑过 `rag-eval run` 时提示先运行。
- 未接入 `rag_eval` 的 JudgeBackend（RAGAS 在网关侧独立运行）；如需 CLI 侧复用可后续打通。

---

### 3.6 改进：RAGAS 判题后端支持 OpenAI 兼容 API（替代本地 4B）

#### 需求

本地 4B 模型做 LLM judge 偏弱（context_recall 频繁解析失败、faithfulness 偏严）。用户要求 RAGAS 改用 **API** 判题。

#### 内容

- `RagasSettings` 新增 API 配置（全部可经环境变量注入，默认留空 = 本地 Ollama）：
  - `api_base_url` / `api_key` / `api_model`：配置 `api_model` 后判题 LLM 用 OpenAI 兼容 API（`ChatOpenAI`）；
  - `api_embedding_model`：可选，配置后 embedding 也走 API（`OpenAIEmbeddings`）；**不配则 embedding 保持本地 Ollama**（DeepSeek 等无 embedding 端点的 API 也能用：API 判题 + 本地 embedding 混用）；
  - 新增 `RagasSettings.llm_backend` 属性（api/local）。
- `config/default.yaml` 新增 `ragas.api_*` 四项，环境变量：`RAGAS_API_BASE_URL` / `RAGAS_API_KEY` / `RAGAS_API_MODEL` / `RAGAS_API_EMBEDDING_MODEL`（可写入 `source/internal-rag/.env` 后重启网关生效）。
- `_make_llm` / `_make_embeddings` 改为按 settings 选择后端；`status` 接口与 `last_result` 返回 `backend`、`api_model`、`api_key_configured`（**永不返回 api_key**）；`public_view()` 增加 ragas 段（脱敏，不含密钥）。
- 前端：状态 pill 显示“API · <model>”或“本地 Ollama · <model>”；结果注脚显示后端；API 模式缺 key 时页面给出 `RAGAS_API_KEY` 提示。
- **踩坑记录（重要）**：langchain-openai 0.3.35 的 `OpenAIEmbeddings` 默认 `check_embedding_ctx_length=True`，会用 tiktoken 把文本**先 tokenize 再发 token ID**（实测请求体为 `{"input":[[82805]],"encoding_format":"base64"}`），Ollama 等 OpenAI 兼容端点返回 400 `invalid input type`；构造时置 `check_embedding_ctx_length=False` 解决。另 0.3.x 的真实字段名是 `openai_api_key` / `openai_api_base`（`api_key`/`base_url` 会被 pydantic 当 extra 静默吞掉，不生效）。

#### 验证

- 新增 4 项后端选择测试（默认本地、api_model 切换 ChatOpenAI、api_embedding_model 切换、`public_view` 不泄露 key）：`tests/unit/test_ragas.py` **17 passed**。
- ruff：**All checks passed**；mypy 本次文件通过（仓库存量 27 错误均在 ingestion/parsers 与 document_section_generator，与本次无关）。
- **API 路径端到端实测**（把 API 指向 Ollama 的 OpenAI 兼容端点 `http://127.0.0.1:11434/v1`，验证 ChatOpenAI/OpenAIEmbeddings 代码路径，无需外部凭据）：chat 调用正常；embedding 在 `check_embedding_ctx_length=False` 后正常（1024 维）；完整跑 1 题 RAGAS 15.3s：faithfulness 1.0 / answer_relevancy 0.678 / context_precision 1.0（context_recall null 因探测仍用 4B，换强模型即出值）。
- **正式接入 DeepSeek 实测**（用户提供 key，写入 `source/internal-rag/.env`，网关重启后 `status.backend=api`、`api_model=deepseek-chat`）：
  - 基准集 2 题（23.9s）：**faithfulness 0.142 / answer_relevancy 0.940 / context_precision 1.0 / context_recall 0.667（2 题全部出值）**；
  - **context_recall 恢复出值**（4B 时代 null）——这正是换 API 的核心收益；
  - 对比结论：检索相关（ctxP=1.0）但答案不忠实于证据（f=0.14），与自研关键点覆盖 0.18 再次互证“答案生成是短板”；
  - 修复一个显示问题：任务结果补充 `model`/`backend` 字段（此前页面注脚会回退显示本地模型名）。
- 网关重启后 `status.backend` 已切换为 `api`，`public_view` ragas 段无密钥。

#### 改动文件

- `app/core/config.py`（RagasSettings 扩展 + public_view）
- `config/default.yaml`（ragas.api_*）
- `app/services/ragas_evaluator.py`（双后端 _make_llm/_make_embeddings、status/last_result/任务结果后端信息）
- `app/web/monitor.html` / `app/web/static/monitor.js`（状态与注脚显示后端、缺 key 提示）
- `tests/unit/test_ragas.py`（新增 TestBackendSelection）
- `source/internal-rag/.env`（**已配置**：`RAGAS_API_BASE_URL=https://api.deepseek.com`、`RAGAS_API_KEY=<sk-…>`、`RAGAS_API_MODEL=deepseek-chat`；gitignore 保护，不入库）

#### 当前边界

- **数据边界**：RAGAS 评测会把基准题/回答/证据文本发送到 DeepSeek API（公网）；用户已确认使用，但后续若评测内容涉密需评估。
- embedding 仍用本地 Ollama（DeepSeek 无 embedding 端点）；若改用带 embedding 的网关可配 `RAGAS_API_EMBEDDING_MODEL`。
- RAGAS 结果仍为内存态；持久化需求未变（见 3.5）。
- 修改 `.env` 后需重启网关生效。

---

### 3.7 新增：监控页内热切换 RAGAS 判题模型 / API（仿智能问答形式）

#### 需求

用户要求在运行监控页内直接切换 RAGAS 判题模型或 API，不必改 `.env` 重启网关。交互形式参考智能问答页（工具栏下拉选模型 + 「接入 API」弹窗 + 移除连接）。

#### 内容

- **后端（运行时 profile，热生效）**：
  - `RagasSettings` 新增 `active_backend: Literal["api","local"] | None`（运行时覆盖，不持久化）；`llm_backend` 优先取它、否则按 `api_model` 推导——修复“切本地但 api_model 仍在 → 实际仍走 API”的缺陷；
  - `RagasTaskManager.update_profile()`：切换后端/模型，api_key 语义（缺省=保留、空串=清除、非空=替换）、`clear_api` 整体移除、`persist` 可选写 `.env`；`profile()` 返回脱敏 profile（`mask_api_key` 掩码：`sk-****5678` 形式）；
  - `persist_profile()`：安全重写 `.env` 中 `RAGAS_*` 七行（replace-or-append，不动其它行）；**按生效状态写**——活跃后端为 local 时 API 行写空，重启后复现本地选择；
  - `_make_llm` / `_make_embeddings` 改为尊重 `active_backend`（embedding 也跟随：本地后端 → 本地 embedding）。
- **API**：
  - `GET /api/v1/monitor/ragas/profile`：有效 profile（key 掩码）+ 实时拉取 Ollama 已装模型列表（`/api/tags`，best-effort）；
  - `PUT /api/v1/monitor/ragas/profile`（`RagasProfileUpdate`：backend/judge_model/embedding_model/api_base_url/api_key/api_model/api_embedding_model/clear_api/persist）。
- **前端（仿 chat）**：RAGAS 卡内新增「判题模型」下拉（本地 Ollama 组 + API 连接组）+「接入 API」按钮（dialog：Base URL / Key / 模型 / 可选 Embedding）+「移除 API 连接」按钮 + 「保存到 .env」勾选；下拉切换即 PUT 生效并刷新状态；key 输入留空=不更换。
- `config/default.yaml`：`judge_model`/`embedding_model`/`ollama_base_url` 也改为 `${VAR:-default}` 注入，保证持久化后重启一致生效。

#### 验证

- 新增 14 项测试（profile 切换/保留/清除 key/api 必填校验/clear_api/持久化写入与去重/掩码/API 端点）：`tests/unit/test_ragas.py` **31 passed**；含 monitor/config 共 **40 passed**。
- ruff：**All checks passed**；mypy 本次文件通过（仓库存量 26 错误不变）。
- **热切换实测（网关不重启）**：`PUT backend=local` → status 变 local（judge=qwen3:4b，API 凭据保留）；`PUT backend=api` → 立即切回 deepseek-chat，key 仍生效；profile GET 返回 Ollama 三模型列表、key 掩码 `sk-****1b3b`。
- 全量测试结果见 3.7 底部（535+31-已合并统计）。

#### 改动文件

- `app/core/config.py`（`active_backend` 字段 + llm_backend 属性）
- `app/services/ragas_evaluator.py`（update_profile/persist_profile/profile/mask_api_key、_make_llm/_make_embeddings 尊重后端）
- `app/api/v1/monitor.py`（profile GET/PUT、_ollama_models）
- `app/web/monitor.html`（模型栏 + 接入 API dialog）
- `app/web/static/monitor.css`（模型栏/dialog 样式）
- `app/web/static/monitor.js`（loadRagasProfile/切换/连接/移除逻辑）
- `config/default.yaml`（模型字段环境变量化）
- `tests/unit/test_ragas.py`（TestRagasProfile + API profile 测试）

#### 当前边界

- 活跃后端选择仅存内存（`active_backend`）；勾选「保存到 .env」才持久化（写的是**生效状态**，切本地时持久化会把 API 行写空）。
- 下拉的本地模型列表来自 Ollama `/api/tags`（Ollama 未启动时显示配置的 judge_model 兜底）。
- 数据边界提醒不变（3.6）：RAGAS 评测内容会发往所连 API。

---

### 3.8 修复：RAGAS 整题无分数时静默（用户实测反馈）

#### 问题

用户接入 API 后跑基准集评测，桥题（demo-bridge-disaster-equipment）整行四个分数全部显示「—」且无任何错误提示。排查结论：

- **非配置问题**：当前 profile 为 DeepSeek/本地 embedding，同题重跑出全部指标（faithfulness 0.032 / answer_relevancy 0.953 / context_precision 0.867 / context_recall 0.333），判断用户那次为**瞬时失败**（DeepSeek 限流/超时等）；
- **前端 bug**：`renderRagasTask` 里 `errorCell` 变量计算了但**从未渲染**——失败原因对用户完全不可见；
- **后端盲区**：`raise_exceptions=False` 会把单指标 API 失败吞成 NaN（不抛异常），整题可静默地全为 null，`error` 也为空。

#### 修复

- **后端重试**：`_call_evaluate` 传 `RunConfig(max_retries=3, max_wait=60)`（ragas 0.2.15 支持），限流/5xx/超时自动重试；
- **后端诊断**：`evaluate_cases` 逐题在“无分数或全 None”时写入明确错误：`全部指标未产出分数：底层 LLM/Embedding 调用失败（检查 API 地址/Key/模型名/额度，或服务限流）。`
- **前端**：逐题 `error` 渲染在问题单元格下方（红字 + title 完整错误）；聚合四指标全空时注脚给出同一诊断提示。

#### 验证

- 新增 `test_all_nan_scores_surface_diagnostic`（mock `_call_evaluate` 返回全 NaN，断言 scores 全 None 且 error 含诊断文案）：`tests/unit/test_ragas.py` **32 passed**。
- ruff / mypy / `node --check monitor.js`：全部通过。
- 网关重启（`backend=api · deepseek-chat · sk-****1b3b`）后重跑桥题正常出分，无回归。

#### 改动文件

- `app/services/ragas_evaluator.py`（RunConfig 重试 + 全空诊断）
- `app/web/static/monitor.js`（渲染逐题 error + 聚合全空提示）
- `tests/unit/test_ragas.py`（诊断分支测试）

#### 当前边界

- 若重试 3 次后仍失败（如 Key 失效、额度耗尽），整题仍显示「—」但**现在会带原因**，可据此排查。
- 重试会放大耗时：每指标最多 3 次尝试；DeepSeek 稳定时几乎无感知。

---

### 3.9 重构：RAGAS 模型选择改为「多 API 连接 + 混合下拉」（仿智能问答，用户确认）

#### 需求

用户两次反馈收敛为明确要求：不想要「本地/API 二选一」的后端选择，而是**像智能问答一样**——连接 API 后（可多个连接），本地 Ollama 与所有连接 API 的模型全部出现在一个下拉框里自由选择。

#### 内容

- **Manager 重构为多连接模型源**：
  - 新增 `ApiConnection`（source_id/name/base_url/api_key/models/embedding_model），`RagasTaskManager` 持有 `_connections` 字典；启动时若 `.env` 配置了 `RAGAS_API_MODEL`，自动建 `default` 连接并设为活跃；
  - 活跃选择 = `{active_source, active_model}`；`_effective_settings()` 按活跃选择合成 `RagasSettings` 视图供 `_make_llm`/`_make_embeddings` 使用（本地源用 Ollama，API 源用该连接的 base_url/key/model）；
  - `select_model(source_id, model, persist)` / `add_connection(...)`（自动选中首个模型）/ `remove_connection(...)`（活跃回退本地）替代原 `update_profile`；
  - `profile()` 返回 `{active, sources[]}`（本地源 + 各 API 源，key 掩码），`status()` 基于有效设置。
- **API**：
  - `GET /ragas/profile`：合并实时 Ollama 模型到本地源；
  - `PUT /ragas/profile`：`{source_id, model, persist}` 选择活跃模型；
  - `POST /ragas/connections`：`{name, base_url, api_key, models?, embedding_model?}`，models 留空时自动 `GET {base_url}/models`（/v1/models 兜底）发现模型，失败提示手动填写；
  - `DELETE /ragas/connections/{source_id}`：移除连接，活跃回退本地。
- **前端**：模型下拉按来源分组（本地 Ollama / API · 连接名，与 chat `loadModels` 同构）；「接入 API」弹窗加**连接名称**与**模型列表（可选，逗号分隔，留空自动获取）**字段；移除按钮改为「移除当前连接」（仅 API 连接显示）；下拉切换即 PUT。
- 持久化语义不变：写**当前生效状态**（API 活跃写连接字段，本地活跃清空 API 行）。

#### 验证

- 测试重构：`TestRagasProfile` 改为多源选择语义（.env 种子连接/本地选择/API 源选择/未知源拒绝/添加自动选中/移除回退/持久化写入与清空），`TestRagasApi` 增加连接 CRUD 与 /models 失败路径：`tests/unit/test_ragas.py` **39 passed**。
- ruff / mypy / `node --check`：全部通过（mypy 仅仓库存量 26 错误）。
- **真实端到端（网关不重启）**：
  - PUT 选本地 `qwen3:4b` → active=local；PUT 切回 `default/deepseek-chat` → active=api，即时生效；
  - POST 新连接 DeepSeek → 自动从 `GET /models` 拉到 `deepseek-v4-flash / deepseek-v4-flash-vision-exp / deepseek-v4-pro` 并自动选中 v4-flash；
  - DELETE 该连接 → 活跃回退本地、源列表恢复；
  - PUT persist=true → `.env` 正确写入 RAGAS_API_* 与 judge/embedding 行。

#### 改动文件

- `app/services/ragas_evaluator.py`（ApiConnection、多源 manager、_effective_settings、select/add/remove/persist）
- `app/api/v1/monitor.py`（profile/select/connections 端点、_api_models）
- `app/web/monitor.html`（连接名称/模型列表字段）
- `app/web/static/monitor.js`（多源分组下拉、连接 CRUD 逻辑）
- `tests/unit/test_ragas.py`（多源语义测试）

#### 当前边界

- 连接仅存进程内存（重启后只剩 .env 持久化的 default 连接及其单模型）；多连接如需跨重启保留，后续可扩展 .env 多连接存储。
- `GET /models` 发现失败时需手动填模型列表（弹窗已支持）。
- 本地源模型列表来自 Ollama `/api/tags`；Ollama 未启动时下拉兜底显示当前活跃本地模型。

---

### 3.10 修复：RAGAS「最近真实请求」模式整题报列缺失（用户实测反馈）

#### 问题

用户用「最近真实请求」模式跑桥题（在智能问答中真实问过、监控有记录），整行四格全「—」，行内错误被截断显示为 `The metric [context_precision] that is used requires the following additional co…`。

#### 根因（两处）

1. **指标列要求判断错误**：本仓库安装的 ragas 0.2.15 中 `context_precision` 的 `required_columns` 为 `{'reference', 'user_input', 'retrieved_contexts'}`——**它也要求标准答案列（reference）**（此前按旧版 ragas 记忆认为只需要 question/answer/contexts）。「最近真实请求」模式无 ground_truth，行内仅有 question/answer/contexts；`evaluate()` 内部校验发现缺 `reference` → 抛 `ValueError("requires the following additional columns")` → 整题 scores 全空。
2. **错误被截断**：前端行内错误只显示前 80 字符，用户看不到缺失列名。

#### 修复

- `NO_GROUND_TRUTH_METRICS` 改为 `["faithfulness", "answer_relevancy"]`（抽出 `_metric_names(has_ground_truth)` 便于测试）——无标准答案时只跑这两项，context_precision/context_recall 正确跳过；
- 前端：任务完成且 `mode=recent` 时注脚明确说明「仅评测 faithfulness / answer_relevancy（context_precision / context_recall 需要标准答案，显示为 —）」。
- 复现验证路径：Python 直连 `evaluate_cases`（无 ground_truth 用例 + DeepSeek）：faithfulness 0.4 / answer_relevancy 0.687，context_precision/context_recall 为 null 且**无列错误**——与用户失败场景完全对应，修复生效。

#### 验证

- 新增 `test_metric_selection_without_ground_truth`：`_metric_names(False)` 不含 context_precision：`tests/unit/test_ragas.py` **40 passed**。
- ruff / mypy / `node --check`：全部通过（mypy 仅仓库存量 26 错误）。
- 网关已重启加载修复。

#### 改动文件

- `app/services/ragas_evaluator.py`（NO_GROUND_TRUTH_METRICS + `_metric_names`）
- `app/web/static/monitor.js`（recent 模式注脚提示）
- `tests/unit/test_ragas.py`（指标选择测试）

#### 当前边界

- 「最近真实请求」模式本就无标准答案，只产出 faithfulness / answer_relevancy 属预期；需要完整四指标请用「基准集」模式。
- ragas 指标列要求随版本可能变化；后续升级 ragas 时需复核 `required_columns`。

---

### 3.11 重构：RAGAS 评测对象改为「指定问题 / 基准集单题」（用户确认）

#### 需求

用户要求评测对象能**直接指定某个具体问题**，不要「最近真实请求」这类模糊集合。

#### 内容

- **评测对象改为两种明确方式**（移除「最近真实请求」）：
  1. **指定问题**（默认）：输入框填问题 + 可选标准答案 + 知识库选择 → 后端先走**智能问答链路**（`rag_pipeline.run`）获取回答与引用证据（quotes 作 contexts）→ 单用例跑 RAGAS；有标准答案 4 指标、无则 2 指标；
  2. **基准集**：新增题目下拉（`GET /ragas/benchmark-questions` 列出 benchmark.jsonl 全部 12 题含 id/优先级），可选**具体某道题**（单选）或「全部（前 N 道）」批量。
- 后端：
  - `RagasRunRequest` 扩展：`mode: question|benchmark`、`question`/`ground_truth`/`knowledge_base_ids`（问题模式）、`question_id`（基准集单题，空=批量前 limit 道）；
  - `build_question_case()` 纯函数（回答+引用 quotes → 单用例，无回答/无证据报错）；
  - `build_benchmark_cases(question_id=...)` 支持单选；
  - `manager.start(mode, ..., cases=None, question_id=None)` 支持外部用例与单题；
  - 移除 `/ragas/cases` 预览端点与 recent 分支。
- 前端：模式切换面板（问题输入/标准答案/知识库下拉 ↔ 题目下拉/条数上限）；结果注脚按指标是否出分提示「未提供标准答案仅评测两项」。

#### 验证

- 测试：`build_question_case`（含无回答/无证据/空标准答案）、`build_benchmark_cases` 单题选择、API 问题模式缺问题/缺知识库 400、benchmark-questions 端点、start 新 mode 拒绝路径：`tests/unit/test_ragas.py` **47 passed**。
- ruff / mypy / `node --check`：全部通过（mypy 仅仓库存量 26 错误）。
- **真实端到端**：benchmark-questions 返回 12 题；问题模式缺知识库 → 400「请先选择知识库」；`question_id=demo-bridge-disaster-equipment` 单题评测**恰好 1 道**（faithfulness 0.0 / answer_relevancy 0.953 / context_precision 0.867 / context_recall 0.333）——修复了初版「指定单题却跑 3 道」的传参遗漏。
- 问题模式完整链路依赖智能问答（RAGFlow 当前不稳定，实测 502），失败时返回清晰错误「智能问答链路失败：…」。

#### 改动文件

- `app/services/ragas_evaluator.py`（`build_question_case`、`build_benchmark_cases(question_id)`、`start(cases/question_id)`、移除 recent）
- `app/api/v1/monitor.py`（run 新 payload、`benchmark-questions` 端点、`_build_question_case`、移除 cases 端点）
- `app/web/monitor.html` / `static/monitor.js`（模式面板、知识库/题目下拉、run payload）
- `app/web/static/monitor.css`（面板样式）
- `tests/unit/test_ragas.py`

#### 当前边界

- 「指定问题」模式依赖智能问答链路（RAGFlow 检索），RAGFlow 不稳定时评测会失败并提示——这是系统侧问题，非评测功能问题。
- 基准集单题要求该题在最新 `rag-eval run` 中已有存储答案；无答案的题在下拉里仍显示但评测会提示无可用用例。
- 「最近真实请求」模式已移除；如需对实时问答做评测，用「指定问题」输入同样的问题即可。

---

### 3.12 简化：RAGAS 评测区界面精简（用户反馈「太复杂」）

#### 需求

用户反馈评测区界面复杂：看不懂「评测对象」选择器的作用；题目选择、条数上限都不需要。要求最简：一个问题输入框 + 开始评测。

#### 内容

- **移除**：「评测对象」模式选择器、基准集题目下拉、条数上限下拉、面板切换逻辑；
- **保留最简表单**：问题输入框（textarea）+ 标准答案（可选）+ 知识库下拉 + 「开始评测」按钮（同排）；
- 前端 `runRagas` 固定走 `mode="question"`；删除 `loadRagasBenchmarkQuestions`/`toggleRagasPanels` 等死代码；
- **后端不动**：`benchmark` 模式与 `benchmark-questions` 端点保留（测试覆盖，供 CLI/后续需要），UI 不再暴露。

#### 验证

- `node --check monitor.js` 通过；`tests/unit/test_ragas.py` **47 passed**（后端未动）。
- 网关重启后页面确认：`ragasMode`/`ragasBenchmarkSelect`/`ragasLimit` 均已不存在于 HTML；`ragasQuestion` 输入框在（精确 grep 确认，先前子串误报为 `ragasModelSelect`）。

#### 改动文件

- `app/web/monitor.html`（评测表单精简）
- `app/web/static/monitor.js`（runRagas 简化、删除基准集 UI 逻辑）
- `app/web/static/monitor.css`（subrow 布局）

#### 当前边界

- 基准集批量/单题评测目前只能通过 API（`mode=benchmark` + `question_id`）或后续恢复 UI 使用；如有需要可再加回一个入口。
- 「指定问题」仍依赖知识库选择（问答链路必需），该项保留。

---

### 3.13 新增：RAGAS「指定问题」的回答模型可选（用户确认）

#### 需求

用户询问「指定问题」里负责回答的模型到底是 RAGAS 判题 API 还是智能问答的回答模型。排查结论：**都不是**——`_build_question_case` 调用 `pipeline.run` 时未带 `model` 字段且用独立用户身份，回落为**系统配置的默认回答模型**（`ANSWER_MODEL_NAME=qwen3.5:9b` 本地）。用户确认在监控页加「回答模型」下拉。

#### 内容

- 后端：
  - `RagasRunRequest` 新增 `answer_model: {source_id, model_name} | None`（复用智能问答的 `AnswerModelSelection`）；
  - `_chat_request()` 纯函数：带 `answer_model` 时构造带 `model` 的 `ChatCompletionRequest`；
  - `_build_question_case` 使用 `RAGAS_ANSWER_USER_ID = "dev"`（与智能问答同用户，保证按用户存储的运行时模型连接可解析）——此前误用 `"ragas-monitor"` 导致永远拿不到用户连接；
- 前端：模型栏新增「回答模型」下拉，从 `GET /api/v1/admin/answer-models`（X-User-ID: dev，与智能问答完全同源）加载，默认系统配置模型；`runRagas` 把选择随 payload 提交。
- **语义澄清**：判题模型下拉控制 RAGAS 打分用的模型（DeepSeek）；回答模型下拉控制问题由哪个模型回答（走智能问答链路，默认 qwen3.5:9b，可切本地/用户已连 API）。

#### 验证

- 新增 `test_chat_request_carries_answer_model`、`test_run_accepts_answer_model_payload`：`tests/unit/test_ragas.py` **49 passed**。
- ruff / mypy / `node --check`：全部通过（mypy 仅仓库存量 26 错误）。
- 网关重启后：页面含「回答模型」下拉；`/api/v1/admin/answer-models`（dev）返回默认 `configured-answer/qwen3.5:9b`，源列表含本地 qwen3:4b 与 qwen3.5:9b（用户在智能问答接入的 API 连接会同步出现在此下拉）。

#### 改动文件

- `app/api/v1/monitor.py`（`answer_model` payload、`_chat_request`、用户 id 修正）
- `app/web/monitor.html` / `static/monitor.js`（回答模型下拉、`loadRagasAnswerModels`、payload）
- `tests/unit/test_ragas.py`

#### 当前边界

- 回答模型下拉与智能问答同源（同一内存目录）；智能问答在**本网关进程内**接入的 API 连接才会出现，重启后需重新接入（与 chat 行为一致）。
- 回答走智能问答链路，RAGFlow 不稳定时仍会失败并提示。

---

### 3.14 打通：RAGAS 接入的 API 同时成为回答模型（用户反馈「回答模型应有 API 模型」）

#### 需求

用户指出：在 RAGAS 卡接入的 API，其模型也应出现在「回答模型」下拉中。根因是**两套连接存储未打通**——RAGAS 判题连接存 `RagasTaskManager._connections`，「回答模型」下拉读 `answer_model_manager`（智能问答目录），互不相通。

#### 内容

- **接入即同步**：`POST /ragas/connections` 在注册 RAGAS 判题连接后，再调用 `answer_model_manager.connect(..., user_id="dev")` 注册为回答模型；`ApiConnection` 新增 `answer_source_id` 记录映射（`set_answer_source`/`connection_answer_source`）；
- **删除即清理**：`DELETE /ragas/connections/{id}` 同步 `answer_model_manager.disconnect`（best-effort）；
- 效果：同一 API 连接 → 判题模型下拉 + 回答模型下拉 + 智能问答页都能用；接入失败（如 /models 探测失败）返回 `warning` 字段提示（判题仍可用）；
- 前端：连接成功/移除后刷新「回答模型」下拉；有 warning 时在弹窗内展示而非自动关闭。
- **踩坑**：`answer_model_manager.connect()` 返回的是 Pydantic 模型 `AnswerModelSource` 而非 dict——首版用 `answer_source["source_id"]` 抛 `not subscriptable`，导致「注册成功但映射未存 + 误报 warning」；改属性访问修复。

#### 验证

- 测试：连接 CRUD 测试 monkeypatch `answer_model_manager.connect/disconnect`（避免网络探测）：`tests/unit/test_ragas.py` **49 passed**。
- ruff / mypy / `node --check`：全部通过（mypy 仅仓库存量 26 错误）。
- **真实端到端（网关重启后）**：接入 DeepSeek → 无 warning；`/api/v1/admin/answer-models`（dev）同时出现 本地(qwen3:4b/qwen3.5:9b) + DeepSeek(deepseek-v4-flash/pro/vision-exp)；删除 RAGAS 连接 → 回答模型目录回落仅本地。
- 全量测试见底部统计。

#### 改动文件

- `app/services/ragas_evaluator.py`（`ApiConnection.answer_source_id`、`set_answer_source`/`connection_answer_source`）
- `app/api/v1/monitor.py`（POST/DELETE 连接同步 answer_model_manager、warning）
- `app/web/static/monitor.js`（连接/移除后刷新回答模型下拉、warning 展示）
- `tests/unit/test_ragas.py`（monkeypatch connect/disconnect）

#### 当前边界

- 回答模型同步以 user "dev" 注册；与智能问答同一内存目录，重启后需重新接入（一致行为）。
- 若 /models 探测失败（手动提供模型列表的接入），回答模型注册也会失败（warning 提示），判题不受影响。

---

## 4. 建议改造方案

### 4.1 第一阶段（P0）：先纠正附件角色，阻止格式文件污染事实证据

将请求模型拆为明确角色，例如：

```text
reference_document:
  purpose: presentation | evidence | both
  name: ...
  text/structure: ...
```

推荐默认规则：

- 用户说“格式要求、格式模板、按此格式、附件为格式参考”时，自动设为 `presentation`；
- 用户说“依据附件内容、分析附件、附件也是资料来源”时，设为 `evidence` 或 `both`；
- UI 提供清晰选择：“格式模板（不参与事实引用）”与“补充资料（参与回答和引用）”，默认前者；
- `presentation` 绝不调用 `_attach_auxiliary_document()`，不进入 Evidence Judge、coverage、citation、scope/grounding validator；
- `evidence` 才进入辅助证据通道，且必须按相关性重排，不能固定满分。

这一步完成后，本次附件不会再生成 `[C7][presentation_reference]` 这类混合语义，也不会把页面设置当成适航内容。

### 4.2 第一阶段（P0）：增加文档生成意图与独立提示词

新增 `document_generation`（或 request `task_mode=document`），识别“生成/编制/撰写/形成 + 文档/报告/方案/规范/说明书”等组合。该模式使用独立 Schema 和提示词，不复用普通问答的 500 字限制。

建议结构：

```text
DocumentPlan
  title
  purpose
  audience
  sections[]
    section_id
    title
    retrieval_queries[]
    required_evidence_types[]

DocumentSection
  section_id
  content
  claims[]
  citations[]
  missing_information[]

ComplianceItem
  requirement_id
  requirement
  source_clause
  means_of_compliance
  verification_input
  acceptance_criteria
  result_record
  status
```

普通问答继续使用短答案 prompt；长文档模式按章节设置预算，例如总字数目标、每节最小覆盖数和最大输出 token，并允许分批生成。

### 4.3 第一阶段（P0）：先规划、再逐节检索、最后合成

推荐链路：

1. 从用户任务生成文档大纲和必需章节；
2. 将每个章节拆成独立检索问题，分别召回知识库证据；
3. 建立章节覆盖矩阵，缺证据的章节明确标记；
4. 每节独立生成并执行引用、范围、数值和主体归属校验；
5. 汇总符合性验证矩阵；
6. 做跨章节术语、标准号、数值、引用和缺失项一致性检查；
7. 最后应用格式模板并生成交付物。

“有数据支撑”应解释为：优先保留知识库中的数值、单位、公式、试验条件和判据；如果资料只有法规要求而没有项目实测数据，应写“待试验/待提供”，不得伪造结果。

### 4.4 第一阶段（P0）：补上真实追问上下文

短期方案：前端随请求发送最近若干轮去敏后的 `conversation_context`，后端在查询规划前解析“完整表达、继续、按上面、第二点”等省略表达，生成一条独立完整查询。

长期方案：服务端按用户和 `conversation_id` 存储有界会话状态，至少保存：原始任务、已确认的附件角色、当前文档计划、已生成章节、待补信息和上一轮答案摘要。历史内容不能直接作为事实证据；事实仍必须重新绑定本轮知识库引用。

### 4.5 第二阶段（P1）：真正解析并应用 DOCX 模板

格式模板不能只传纯文本。建议抽取结构化 `PresentationProfile`：

- section/page：纸张、方向、页边距、页眉页脚距离；
- styles：Normal、Title、Heading 1~3、Caption、Table 等样式参数；
- numbering：标题与列表编号定义；
- front matter：封面字段、修订记录、目录；
- tables：表头、列宽、边框、底纹和对齐；
- headers/footers：页眉、页脚、页码和文档编号位置。

最终输出应由专用 DOCX 生成器完成，并执行“生成 DOCX → 渲染为页面图片 → 逐页检查 → 修正”的视觉验收；聊天窗口可显示摘要、引用和下载链接，但不应把完整 Word 版式降格为 Markdown 文本。

### 4.6 第二阶段（P1）：增强可观测性

diagnostics 增加但不泄露正文的字段：

- `task_mode`、`attachment_purpose`；
- 知识库证据数与模板证据数（presentation 应始终为 0）；
- 文档计划章节数、已覆盖章节数、缺失章节；
- 每节检索调用数、证据数、模型调用数；
- 输出字数/章节数、是否因 token 限制截断；
- 是否使用会话补全后的查询。

前端应显示“正在规划大纲 / 检索第 N 节 / 校验引用 / 生成 DOCX”，避免长任务被误认为卡死。

---

## 5. 建议实施顺序与验收标准

### 5.1 实施顺序

1. **附件角色分离**：presentation 不进入证据和引用；同步修改 UI 文案。
2. **文档意图识别**：新增 task mode、独立 Schema 和独立 prompt，移除 500 字限制的误用。
3. **多章节检索与覆盖门禁**：先支持聊天内长文档，再扩展 DOCX。
4. **追问上下文**：保证“继续/完整表达”能恢复原始任务，但不把历史回答当证据。
5. **DOCX 模板解析和产物生成**：保留真实版式并做渲染验收。
6. **评测集与回归门禁**：稳定后再上线。

### 5.2 必须新增的回归用例

1. 上传“格式要求.docx”，提问本次无人机适航文档：附件不出现在 citations，回答主题不得变成“格式说明”。
2. 上传“补充适航试验数据.docx”并选择“参与证据”：相关数据可被引用，但不相关段落不能固定满分进入答案。
3. 同时上传格式模板和补充证据：两者角色、引用和 diagnostics 清晰分离。
4. 长文档请求：输出超过 500 字，章节数和必需专业域达到计划要求，无中途截断。
5. 数值要求：每个数值保留单位、适用对象、试验条件和来源；无实测数据时不得生成虚假通过结论。
6. 追问“将内容完整表达出来”：后端恢复原始“无人机适航要求及符合性验证文档”任务，不回答成格式摘要。
7. 普通事实问答：仍维持短答案预算，不因长文档能力引入冗长退化。
8. DOCX 模板：页面、页边距、标题级别、正文样式、表格和页眉页脚与模板结构一致；生成后渲染无溢出、错页和乱码。

### 5.3 本场景验收口径

- 文档标题和主体始终是“无人机适航要求及符合性验证”，不是“编制指南”或“格式要求摘要”；
- 格式附件只影响排版，不产生事实 Claim 和引用；
- 所有适航条款、数据、标准号和验证判据均可回溯到知识库引用；
- 符合性验证矩阵至少包含要求、来源、符合性方法、验证条件/输入、验收判据、结果记录和状态；
- 知识库没有的数据明确列入“待补信息/验证计划”，不泛泛而谈，也不虚构；
- 追问可继续同一文档任务；
- 最终可下载按模板排版并经过视觉检查的 DOCX。

---

## 6. 临时使用建议（代码修复前）

在现有版本中，用户提示词无法彻底绕过后端设计，但可降低偏题概率：

1. 不在首轮附加格式文件，先要求系统按指定章节生成知识库支撑的正文；
2. 明确写“不要解释如何编制，不要总结格式要求，直接输出文档正文”；
3. 将任务拆成若干专业章节逐节提问，每节要求列出条款、数据、符合性方法和引用；
4. 最后在系统外将各节合并并套用 Word 模板；
5. 追问不要只写“完整表达”，应重复主题，例如“继续完成《无人机适航要求及符合性验证文档》尚未输出的章节，保持知识库引用”。

这只是绕行方案。由于系统 prompt 仍限制 500 字，且没有真正会话上下文，不能作为最终产品方案。

---

## 7. 注意

- 不应只删除 prompt 中“500字以内”就宣称修复；附件污染、单查询检索、无会话状态和无 DOCX 生成仍会存在。
- 不应把格式模板整体发布进业务知识库；否则即使请求级附件分流修复，模板仍可能被常规检索召回。
- 不应把“符合性验证方法”自动写成“已验证通过”；验证计划、验收判据与实际试验结果必须分层建模。
- 实施时保留现有普通问答链路，文档模式应独立开关、独立预算、独立评测，避免影响已经通过的事实/对比/流程题。
